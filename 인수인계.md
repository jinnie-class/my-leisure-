# 나의 여가 — 인수인계

특수교육대상학생이 자신의 여가생활을 **계획하고 · 기록하고 · 알아보고 · 모아 전시**하는
마무리 단계 교육용 반응형 웹앱입니다.

- 만든 곳 : 서울경인특수학급교사연구회
- 마지막 정리 : 2026-08-17
- 앱 판(version) : `js/app.js` 의 `App.VERSION` — 선생님 설정 → 데이터 → 저장 상태에서 확인

---

## 1. 실행과 배포

| 방법 | 파일 | 설명 |
|---|---|---|
| 폴더째 | `index.html` | **권장.** 그림(PNG)이 모두 나옵니다 |
| 한 파일 | `single-file.html` | USB·공유용. 표지·벽지·여가지도바탕·단추 그림이 담기고 활동/캐릭터는 기본 SVG |

- **서버·로그인·깃허브가 필요 없습니다.** 파일을 열면 바로 실행됩니다.
- 인터넷 연결이 없어도 동작합니다. React 는 `vendor/` 에 함께 넣어 두었습니다.
- 한 파일 판을 다시 만들려면:

```bash
powershell -ExecutionPolicy Bypass -File .\build-single-file.ps1
```

### 바뀐 내용이 화면에 안 보일 때
브라우저가 예전 `css/js` 를 재사용해서 그렇습니다. **Ctrl + F5**(강력 새로고침)를 누르세요.
`index.html` 의 파일 주소에 `?v=20260819j` 을 붙여 두어 보통은 자동으로 갱신됩니다.
**소스를 고쳤으면 이 숫자를 꼭 올려 주세요.** (안 올리면 선생님 컴퓨터에는 예전 화면이 그대로 뜹니다)

---

## 2. 화면 흐름

```
표지  ──[시작하기]──▶  홈  ──▶ 계획하GO! / 알아보GO! / 기록하GO! / 모아보GO!
 │                     │                              └─▶ 그림일기 (A4 인쇄)
 └─ 오른쪽 위:         └─ 왼쪽 위: 홈(표지로) · 오른쪽 위: 소리 · 학생 이름표
    소리끄기 · 전체화면 · 선생님 설정
```

**핵심 순환** — 이 연결이 앱의 뼈대입니다. 끊지 마세요.

```
계획하GO! 에서 계획 저장
      ↓
홈에 '오늘의 여가 계획' 카드가 뜸  ← 계획이 있는 날에만 나옵니다
      ↓ [활동을 했어요]
기록하GO! 이 열리며 날짜·활동·사람·장소가 자동으로 채워짐
      ↓ 일기 저장
① 해당 활동에 '해봤어요' 발자국 자동 등록
② "이 활동을 좋아하나요?"      → 하트
③ "또 하거나 도전하고 싶나요?" → 별
④ 포트폴리오에 자동 수집
⑤ "전시하고 싶어요?"           → 별표
⑥ [그림일기 보기] → A4 한 장짜리 그림일기 · 인쇄
```

> 홈의 계획 카드를 없애면 ①~⑤ 로 가는 입구가 사라지고, 학생이 기록하GO! 에서
> 활동을 처음부터 다시 골라야 합니다. 계획과 기록이 서로 연결되지 않습니다.

---

## 2-1. 일기 단계 (기록하GO!)

| 단계 | 어떻게 쓰나 | 그림일기 글자칸 |
|---|---|---|
| **1단계** | 그림으로 골라 쓰기 — 언제·누구와·무엇을·어디서·기분·또하기 | 원고지 칸 |
| **2단계** | 문장 틀 채우기 — **한 번에 하나씩** 묻고 넘어갑니다 | 원고지 칸 |
| **3단계** | 자유롭게 쓰기 (키보드 필요) | **밑줄** (글이 길어도 다 담깁니다) |

**2단계 순서** — `누구와 → 기분 → 기억 → 다음에 → 제목 → 그림 → 확인`
- **계획에서 온 일기는 `누구와` 를 건너뛰고 `기분` 부터** 시작합니다.
  계획에 이미 사람이 정해져 있어서, 빈 질문을 다시 묻지 않습니다.
  고른 내용은 노란 문장 띠에 `계획에서 가져왔어요` 표시와 함께 그대로 보이고,
  바꾸고 싶으면 `앞 질문으로` 를 누르면 됩니다.
- 고르면 **0.45초 뒤 다음 질문**으로 넘어갑니다 (규칙 2)
- 낱말은 **활동 카드처럼 큰 카드에 그림을 붙여** 고릅니다. 작은 글자 단추는 쓰지 않습니다
- 만들어지는 문장을 **노란 띠로 질문 위에** 늘 보여 줍니다 (`.frame-line`)
- 고른 기분은 '오늘의 기분' 으로도 함께 저장되어 **같은 것을 두 번 묻지 않습니다** (규칙 7)
- 마지막 **확인**에서 노란 칸을 눌러 문장을 직접 고칠 수 있습니다

---

## 2-2. 그림일기 3단계 구성 원칙 ★고정 규칙★

단계가 올라갈수록 **글의 양**이 아니라 **학생이 스스로 하는 정도**가 늘어납니다.

```
보고 쓰기  →  따라 쓰거나 스스로 쓰기  →  자유롭게 일기 쓰기
```

| 단계 | 글쓰기 양식 | 글 제공 정도 | 학생이 하는 일 |
|---|---|---|---|
| **1단계** 보고 써요 | **칸** | 완성된 글 **전체** 제공 | 완성된 글을 보며 그림일기를 경험 |
| **2단계** 따라 쓰거나 스스로 써요 | **칸** | 선택적으로 제공 | 따라 쓰기 **또는** 스스로 쓰기 |
| **3단계** 나의 일기를 써요 | **줄** | 완성 문장 **주지 않음** | 자기 경험을 문장으로 자유롭게 |

### ⛔ 절대 바꾸지 말 것

**1·2단계의 칸을 줄로 바꾸거나, 3단계를 칸으로 바꾸지 마세요.**
`js/screens/picdiary.js` 의 `useLines = (lv === 3)` 한 줄이 이 구분을 지킵니다.

### 인쇄 모양은 단계마다 다릅니다 (`MODES_BY_LEVEL`)

```
1단계  [ 글자 ] [ 따라 쓰기 ]            ← 완성된 글이 칸에 모두 (읽기 / 덧쓰기)
2단계  [ 따라 쓰기 ] [ 스스로 쓰기 ]      ← 두 가지
3단계  [ 내가 쓴 글 ] [ 빈 줄 ]          ← 줄 양식
```

- **1단계에도 `따라 쓰기` 가 있습니다.** 따라쓰기는 완성된 글을 연한 글씨로
  **그대로** 보여 주는 것이라 `1단계 = 완성된 글 전체 제공` 과 어긋나지 않습니다.
  단계 이름이 **`보고 써요`** 인 만큼 오히려 가장 잘 맞습니다
- **1·2단계가 `따라 쓰기` 를 함께 씁니다.** 다른 점은 2단계에만 `스스로 쓰기`
  (빈 칸 + 도움말)가 있다는 것입니다 — 거기서 학생의 독립성이 늘어납니다

- 속으로는 `t-text` · `t-trace` · `t-empty` 세 가지를 **같이 씁니다.**
  단계에 맞는 것만 내놓고 이름만 그 단계의 말로 바꾼 것입니다
- 처음 열 때는 **그 단계의 첫 번째 모양**으로 시작합니다 (`defaultModeFor`)
- 단계에 없는 모양이 넘어오면 **그 단계의 기본 모양으로 되돌립니다.**
  그래서 포트폴리오의 `일기장 인쇄 · 따라쓰기 판 · 빈칸 판` 으로 여러 학생을
  한꺼번에 뽑아도 **1단계 학생은 늘 완성된 글**로 나옵니다
- `첫글자만`(`t-first`) 은 **없앴습니다.** 2단계를 두 가지로 못박은 이 규칙과
  어긋나서입니다. `.pd-ch.lead` 표시도 함께 지웠습니다
- 위쪽 줄에 **`2단계 · 따라 쓰거나 스스로 써요`** 알약이 나와, 선생님이 인쇄 전에
  단계를 확인할 수 있습니다 (`.pd-lv`)

### 마지막 화면은 「완성된 그림일기」를 보여 줍니다

1·2단계의 마지막 단계 이름을 `확인` → **`완성`** 으로 바꾸고,
노란 문장 칸 아래에 **완성된 그림일기를 작게 그대로** 보여 줍니다
(`C.DiaryPreview`, 눌러서 크게 보기).

- 예전에는 문장 칸만 있어서 학생이 **자기 일기가 어떻게 됐는지 모르고 저장**했습니다.
  완성품은 저장 뒤 창을 네 번 지나서야 나왔고, 그 사이에
  `글 → 그림 → 완성` 흐름이 끊겼습니다
- 1·2단계가 **같은 함수(`confirmStep`)** 를 씁니다. 2단계는 문장 만드는 방식만
  달라서 그 문장을 넘겨 줍니다
- 아직 저장 안 된 `draft` 를 그대로 `C.PicDiarySheet` 에 넣습니다 — 모양이 같습니다
- 배율은 `--dv` 하나로 정합니다 (작게 .30 · 크게 .62 · 낮은 화면에서는 .24/.50).
  종이는 794×1123 로 크기가 고정이라 `transform:scale` 로 줄여 담습니다

### 그림칸의 자리 나누기

```
┌─────────────────────────────────────┐
│ [활동]                              │  ← 왼쪽 위 96px (무슨 활동인가 표시)
│                                     │
│    [사람] [기분] [기분] [장소]        │  ← 가운데 150px (그림일기의 그림)
└─────────────────────────────────────┘
```

- 활동 그림은 '무슨 활동인가' 를 알려 주는 표시라 작아도 됩니다.
  학생이 고른 사람·기분·장소가 그림일기의 그림 노릇을 하니 크게 보여 줍니다
- **네 장까지 한 줄**에 들어갑니다 : 4×150 + 3×26 = 678 ≤ 766
- 활동 그림은 절대자리(왼쪽 위)라 좌우 여백을 비울 필요가 없습니다 —
  위아래로 떨어져 있어 겹치지 않습니다 (겹침 0 확인)

### 생각 도움말 — 스스로 쓰는 모양에서만 나옵니다

빈 칸만 주면 학생이 막막해집니다. 그래서 **`스스로 쓰기` · `빈 줄`** 일 때만
칸 바로 위에 도움말 한 줄(`.pd-hint`)을 냅니다.

| 모양 | 도움말 |
|---|---|
| 2단계 `스스로 쓰기` | **학생이 고른 내용** — 시간대 · 누구와 · 무엇을 · 어디에서 · 기분 |
| 3단계 `빈 줄` | **생각 질문 7개** — 언제? 누구와? 어디에? 무엇을? 기억에 남는 것? 기분? 왜? |

- `글자` · `따라 쓰기` 에서는 글이 이미 칸에 있으니 **내지 않습니다**
- 도움말이 나오면 종이 맨 아래 칩은 **감춥니다** (같은 내용이라 겹칩니다)
- 장소가 활동 이름에 이미 들어 있으면 두 번 쓰지 않습니다 (`공원 가기` + `공원`)
- 시간대는 일기에 없어서 **계획에서 온 일기면 그 계획의 시간대**를 가져옵니다

### 3단계도 **한 단계에 한 가지**씩 묻습니다 (`L3`)

```
뼈대 → 일기 쓰기 → 기분 → 또 하고 싶나 → 제목 → 그림 → 완성
```

- 예전에는 이 모든 것을 **한 장**에 담아서, 1050×700 화면에서 **5쪽으로 갈라졌고**
  가장 중요한 글쓰기가 뒷쪽으로 밀려 1쪽에는 제목·그림만 보였습니다
- **`뼈대` 는 육하원칙**입니다 (`SIX`). 언제·누구와·어디에·무엇을·어떤 기분·왜 를
  한 줄씩 짧게 답하면 일기의 뼈대가 됩니다.
  빈 칸에 바로 긴 글을 쓰라고 하면 3단계 학생도 막막해집니다
- 다음 단계에 **내가 만든 뼈대** 띠가 나오고, `글쓰기 칸에 넣기` 로 옮겨
  살을 붙입니다. 뼈대는 `draft.six` 에 남아 다시 볼 수 있습니다

### ⚠️ 낮은 화면(1050×700)에서 쪽이 갈라지던 문제

이 화면 크기에서 **내용 자리가 391px(56%)** 밖에 없었습니다.
테두리 요소(위쪽 줄 · 앞 질문으로 · 다음 단추)가 309px 을 먹었습니다.

고친 것 (`@media (max-height: 780px)`)
- 테두리 요소를 조금씩 줄여 내용 자리를 **391 → 415px** 로
- 활동 고르기·제목 낱말을 `cols-6` 으로 (낮은 화면 한 줄, 높은 화면 두 줄)
- 3단계 글쓰기 칸을 8.5rem 으로, 문장 칸에 `max-height`
- **완성 화면은 좌우 두 칸**으로 (`.confirm-2col`) — 넓고 낮은 화면에서는
  문장과 그림일기를 위아래로 쌓지 않고 옆으로 놓습니다
- 미리보기 그림 높이 제한 (`.hw-shot` `.my-draw` `--dv`)

**⛔ 미리보기 높이 규칙은 기본 규칙보다 `뒤`에 두어야 합니다.**
앞에 두면 기본 규칙(34vh)이 더 뒤에 있어서 이겨 버립니다.
한 번 그렇게 두어 손글씨 그림이 238px 로 남아 3쪽으로 갈라졌습니다.

결과 : 1050×700 · 1920×1080 에서 **1·2·3단계 모든 화면이 1쪽** (쓰기방법 3가지 포함)

### 3단계는 쓰는 방법을 셋 중에 고릅니다 (`WRITE_WAYS`)

학생마다 쓰기 수단이 다릅니다. **키보드를 못 치는 학생도 3단계일 수 있습니다.**

| 방법 | 어떻게 | 저장되는 곳 |
|---|---|---|
| **키보드로 쓰기** (기본) | 글자판으로 씁니다 | `text` |
| **손글씨로 쓰기** | 전자칠판·태블릿에 손가락·펜으로 | `writePhotoId` (IndexedDB) |
| **종이에 쓰기** | `빈 줄` 로 인쇄해 연필로 | 앱에는 저장 안 됨 |

- 손글씨 판은 `C.DrawPad` 에 `w=1400 h=800 ruled=true` 를 준 것입니다.
  **줄을 바탕에 함께 그려** 두어서 저장한 그림에도 줄이 남습니다 — 인쇄하면
  학생 글씨가 줄 위에 앉은 것처럼 보입니다
- 그림일기에서는 손글씨가 있으면 **그 손글씨를 그대로** 넣습니다 (`.pd-hw`).
  이때 배경 줄무늬는 끕니다 — 줄이 두 겹으로 보입니다
- **`빈 줄` 로 인쇄할 때는 손글씨를 빼고 줄만** 냅니다 (종이에 쓰는 길)
- `종이에 쓰기` 는 새로 만든 것이 없습니다. `빈 줄` 인쇄가 그 길입니다

### 3단계의 줄은 글이 없어도 나옵니다

줄은 `<p>` 가 아니라 **`.pd-lines` 의 배경 무늬**로 그립니다.
`min-height:calc(var(--lh) * 7)` 이 있어 **글이 하나도 없어도 7줄**이 그려집니다.
빈 줄 인쇄가 되는 까닭입니다. `--lh` 와 `background-position` 을 따로 고치면
글자가 줄에서 뜹니다 — 반드시 함께 고치세요.

### 공통 내용 구조 — 세 단계가 같이 씁니다

`① 언제  ② 누구와  ③ 어디에서  ④ 무엇을 했는지  ⑤ 어떤 기분이었는지`

단계가 올라갈수록 **같은 내용**을 학생이 점점 더 스스로 글로 표현합니다.
세 단계가 서로 다른 활동처럼 느껴지지 않게 하는 것이 중요합니다.

> 이 활동의 목적은 **맞춤법 시험이나 글씨 평가가 아닙니다.**
> 학생이 "내가 언제·누구와·어디에서 무엇을 했고 어떤 기분이 들었는지" 를
> 그림과 글로 표현하는 것이 가장 중요합니다.
> 글쓰기가 어려운 학생은 그림·도움 기능으로 충분히 표현하게 하고,
> 글쓰기가 되는 학생에게는 완성 문장을 지나치게 주지 말고 스스로 쓸 기회를 주세요.

---

## 2-3. ⛔ html`` 템플릿 안에 **백틱을 쓰지 마세요**

주석 안이라도 백틱 하나가 템플릿을 **그 자리에서 끊습니다.**
화면이 아예 안 뜨고 콘솔에 `Unexpected identifier` 만 남아, 원인을 찾기 어렵습니다.

```js
/* ✗ 이렇게 쓰면 앱이 안 뜹니다 */
return html`<div>
  <!-- cols-8 은 ... -->     ← 여기 백틱을 쓰면 끝
</div>`;

/* ✓ 백틱 없이 그냥 씁니다 */
  <!-- cols-8 은 ... -->
```

- `/* 주석 */` 도 마찬가지로 쓰면 안 됩니다 (아래 8. 함정 참고)
- 설명에 코드 이름을 넣고 싶으면 **백틱 없이** 쓰거나,
  템플릿 **바깥**에 주석을 두세요

---

## 2-4. 나의 여가 모아보기 (`mymap` — 지도 다음 화면)

지도는 30장을 한눈에 보여 주지만, **내가 어떻게 표시했는지**는 카드마다
눌러 봐야 알 수 있었습니다. 그래서 지도 아래 `내가 표시한 활동 모아보기` 로
넘어가는 화면을 두었습니다.

```
┌──────────────┬──────────────┐
│ 👣 해봤어요   │ ♥ 좋아해요    │
│    13가지    │    2가지     │
├──────────────┼──────────────┤
│ ★ 도전하고   │ ? 아직 잘    │
│   싶어요 2가지│  모르겠어요 1 │
└──────────────┴──────────────┘
```

- 칸을 누르면 그 표시를 한 활동만 **번호·그림·이름·날짜**로 모아 보여 줍니다
- **★ 아직 아무것도 없을 때 빈 창으로 두지 않습니다.**
  `아직 해본 활동이 없어요. / 활동을 열심히 시작해 보아요!` 처럼
  다음에 할 일을 알려 줍니다 (`EMPTY_WORD` 에 네 가지 문구)
- **다 보고 나면 일기가 아니라 홈으로** 갑니다 (`다 봤어요 · 나의 여가로`).
  여기는 확인하는 곳이고, 일기는 활동을 마친 뒤에 씁니다

---

## 2-5. 일기는 **기록하GO! 한 곳에서** 끝냅니다 ★고정 규칙★

여러 코너를 왔다 갔다 하면 선생님도 헷갈리고 학생에게는 혼란입니다.
그래서 **쓰기 → 그림 → 완성 → 고치기 → 인쇄 → 모으기** 를 한 줄기로 묶었습니다.

```
기록하GO!  단계별로 쓰기
     ↓ 일기 저장하기
칭찬  →  전시할까요?  →  모두 마쳤어요
                              ↓ 완성한 그림일기 보기
                        그림일기 (A4 인쇄)
                              ↓ 일기 고치기
                        일기 고치기 (좌우 2단)
                              ↓ A4로 인쇄하고 일기 모음에 담기
                        나의 일기 모음  →  책으로 인쇄
                              ↓
                        나의 여가로 돌아가기 (홈)
```

### 저장 뒤 창이 5개에서 3개로

`이 활동을 좋아하나요?` `또 하거나 도전하고 싶나요?` 를 **없앴습니다.**
일기 안에서 이미 `또 하고 싶나요?` 를 물었으니 같은 것을 두 번 묻는
것이었습니다 (규칙 7). 지도 표시는 **저장할 때 그 답에서 바로** 만듭니다.

| 또 하고 싶나요? 답 | 여가 지도에 |
|---|---|
| 또 하고 싶어요 | 해봤어요 👣 + 좋아해요 ♥ + 도전하고 싶어요 ★ |
| 다른 것도 하고 싶어요 | 해봤어요 👣 만 (싫다는 뜻이 아니라서 지우지 않습니다) |
| 잘 모르겠어요 | 해봤어요 👣 + 아직 잘 모르겠어요 ? |

`전시하고 싶어요?` 만 남겼습니다 — 일기를 쓰고 나서 정하는 **새** 질문입니다.

### 일기 고치기 (`fixdiary` — 좌우 2단)

```
┌──────────────────────┬──────────────┐
│ 선생님이 도와주세요 (1단계) │  완성된      │
│ [ 그림 수정하기 ]        │  그림일기     │
│ [ 일기 내용 수정하기 ]    │  (바로 반영)  │
│   └ 글칸 + 되돌리기      │              │
│ [ 나의 일기 모음 보기 ]   │              │
│ [ 나의 여가로 돌아가기 ]   │              │
└──────────────────────┴──────────────┘
      [ A4로 인쇄하고 일기 모음에 담기 ]
```

- **왼쪽에서 고치면 오른쪽에 바로 보입니다** (타이핑하는 즉시)
- 예전 `일기 고치기` 는 기록하GO! **처음으로 되돌아가** 언제·누구와부터
  다시 골라야 했습니다. 고칠 것은 그림과 문장 둘뿐이라 그 둘만 고칩니다
- 1단계에는 **`선생님이 도와주세요`** 안내를 띄웁니다
- 좁은 화면(860px 미만)에서는 위아래로 쌓입니다

### 나의 일기 모음 (`journal`)

- 쓴 일기가 **날짜가 이른 것부터** 카드로 쌓입니다 (번호 · 작은 종이 · 날짜 · 제목)
- 카드를 누르면 크게 보고 **이 일기만 인쇄 / 고치기** 를 할 수 있습니다
- 맨 아래 **`일기 모음을 책으로 인쇄하기`** — 모인 일기를 A4 여러 쪽으로 한 번에
- **따로 저장하지 않습니다.** 일기에 이미 날짜가 있어 그것을 그대로 씁니다.
  인쇄한 일기는 `inJournal` 로 표시해 두지만, 표시가 없어도 쓴 일기는 모두
  보여 줍니다 — 인쇄를 안 했다고 빼면 학생이 서운합니다
- 아직 없을 때는 빈 창으로 두지 않고 `여가 일기를 쓰면 여기에 한 장씩 쌓여요!`

### 그림은 한 장을 그리고 확인·수정

```
[내가 그리기] 누르면 → 그림판이 바로 열림
       ↓ 그림 다 그렸어요
  그림이 완성되었어요!  [이 그림으로 할래요] [다시 그릴래요]
       ↓
  그림 고르기 화면에 그 그림이 바로 보임
```

- **여러 장 그려 고르게 하지 않습니다.** 그림일기에는 한 장만 들어가서
  학생이 '왜 여러 개 그렸는데 하나만?' 하고 헷갈립니다
- `다시 그릴래요` 는 그린 것을 **안고** 판으로 돌아갑니다 (처음부터 아님)
- 그림판 단추(`다 그렸어요` · `그만두기`)는 **판 안 맨 윗줄**에 있습니다.
  아래에 두면 팝업에 스크롤이 생겼습니다

---

## 3. 학생 화면을 쉽게 만들기 위해 정한 규칙

여러 차례 다듬으며 세운 원칙입니다. 새로 고칠 때에도 지켜 주세요.

1. **한 화면에 주요 질문 하나**, 선택지는 4~6개
2. **고르면 바로 다음 질문으로 넘어갑니다** (계획하GO!)
   - 시간이 지나 저절로 넘어가는 것이 아니라, 학생이 고른 결과로 0.45초 뒤 넘어갑니다
2-1. **되돌아가기(`앞 질문으로`)는 흰 칸 안 맨 위**에 둡니다 (`C.Stage top=`).
   예전에는 홈 단추 바로 옆에 있었는데, **홈 단추와 하는 일이 달라서** 헷갈렸습니다.
   　홈 = 화면을 나감 · 앞 질문으로 = 지금 하던 것을 이어서 한 칸 되돌림
   - 첫 질문에서는 **내보내지 않습니다.** 돌아갈 앞 질문이 없기 때문입니다.
     (예전 `그만두기` 는 홈 단추와 똑같이 홈으로 가서 없앴습니다 — 규칙 7)
   - 되돌아가기 단추는 모두 **파스텔 노랑**으로 같게 합니다.
3. **'지금 할 일' 단추는 흰 칸 맨 아래 파란 바 하나**
   - 단추마다 **파스텔 색**으로 하는 일을 구별합니다 (`.btn.pastel-yellow/blue/red`)
     　노랑 = 돌아가기 · 파랑 = 사진 넣기 · 빨강 = 이대로 정하고 가기
4. **읽어주기는 화면마다 하나**, 소리 그림만 (글자 없음)
4-1. **위쪽 줄이 좁으면 두 줄로 나눕니다.**
   `C.TopBar` 에 `below=` 를 주면 아랫줄이 생깁니다.
   지금 두 줄로 쓰는 화면 : **여가 일기**(단계 표시) · **그림일기**(단계 알약 + 인쇄 모양).
   한 줄에 몰아 두면 서로 밀려 제목이 `기록하…` 처럼 잘립니다.
4-2. **화면 제목은 `여가 계획하기` 처럼 '무엇을 하는 곳인가' 로만 씁니다.**
   `…GO!` 는 **홈 카드에만** 남아 있고, 각 창의 위쪽 줄에서는 뺐습니다.
   - 홈 카드 : `여가 계획하기`(크게, 위) + `계획하GO!`(작게, 아래)
   - 각 창 위쪽 줄 : `여가 계획하기` 만 — **한 줄뿐이라 그만큼 크게** 씁니다
     (1920×1080 에서 30px, 1024×768 에서 24px. `.topbar .title` 의 clamp)
   학생이 먼저 읽어야 하는 것은 **무엇을 하는 곳인가**이기 때문입니다.
4-3. **학생 이름표는 늘 오른쪽 끝**입니다. 화면마다 위쪽 줄의 마지막에 두고,
   `.topbar > .topbar-row{ flex:1 1 auto }` 로 줄이 폭을 꽉 채워야
   안쪽 `.spacer` 가 이름표를 오른쪽으로 밀어냅니다.
5. **아래 바(`C.BottomBar`)는 쓰지 않습니다.**
   가는 단추는 위쪽 줄, 할 일 단추는 흰 칸 맨 아래(`C.Stage action=`) 하나.
   아래쪽 구석에 두면 벽지와 겹쳐 학생 눈에 잘 안 들어옵니다.
6. **홈 단추는 화면마다 딱 하나, 왼쪽 위 집 그림**으로 통일합니다.
   (`C.IconBtn uiKey="home"`) 아래·오른쪽에 `홈` `홈으로` 를 또 두지 않습니다.
   예외는 팝업 안 마지막 화면 — 팝업에는 위쪽 줄이 없어서 팝업 단추로 나갑니다.
7. **중복된 말을 넣지 않습니다** — 같은 곳으로 가는 단추를 두 개 두지 않기
8. **선생님 도구는 학생 화면에서 감춥니다** (아래 4번 참고)
9. 색은 흰 바탕 + 연한 회색 선. 색은 *고른 것*과 *지금 할 일* 에만
10. 세로 스크롤 없음. 넘치면 좌우 쪽으로 나뉘고 **← →** 방향키로 넘김
10-1. **선택지는 한 쪽에 다 들어가야 합니다.**
    `.pick-grid` 는 **통째로** 움직입니다(반씩 나뉘지 않음). 흰 칸에 안 들어가면
    그리드 전체가 다음 쪽으로 밀려 **1쪽이 텅 빕니다.**
    그래서 사람(7)·기분(8) 은 화면 높이에 따라 줄 수를 바꿉니다.
    　**950px 이상** → 두 줄(4칸씩, 카드 238px) · **그보다 낮으면** → 한 줄
    (`cols-6/7/8` + `@media (min-height:950px)`. 낮은 화면에서는 카드마다 붙던
    　읽어주기도 감춥니다 — 읽어주기는 화면마다 하나면 됩니다)
11. **여가 지도는 한 번에 8장** — 맨 위 가운데에 정사각형 '나',
    그 아래 **왼쪽에 실내 섬 2×2 · 오른쪽에 실외 섬 2×2**.
    - **가운데 점선은 두지 않습니다.** 바탕 그림에 이미 물길·다리가 있어 겹쳤습니다
    - 섬 이름은 **활동 카드 첫 줄 바로 위** 에 알약 모양 바로 놓습니다
      (`.island-head` / `.island-label`, 바탕은 **파스텔 노랑**).
      바탕 그림의 **집·나무 아래**에 앉아야 그림을 가리지 않습니다.
      ★ 기준은 섬 칸 맨 위(`top`)가 아니라 **놓인 카드 자리(`placed`)** 입니다.
      　 카드는 남는 자리에 따라 위아래로 조금씩 움직이므로, 섬 칸 맨 위에 고정하면
      　 큰 화면에서 이름 바만 덩그러니 떠 보입니다
      ★ **`labelTop` 은 이름 바의 '아랫변' 자리**입니다.
      　 `.island-head` 의 높이를 44px 로 못박지 않고 `transform:translateY(-100%)` 로
      　 위로 매답니다. 글자 크기가 화면 폭에 따라 커지기 때문입니다
      　 (1024 에서 20px·42px 높이 → 1920 에서 26px·53px 높이).
      　 높이를 고정해 두면 큰 화면에서 알약이 자라 **카드를 파고듭니다**
      ★ **위아래로 옮기려면 `js/screens/map.js` 의 `LABEL_NUDGE` 숫자(px)만** 고치세요
      　 (음수면 더 위로. 지금은 **0** = 카드 위 11px)
      ★ **좌우로는 바깥쪽으로 밀어 둡니다** — 실내는 왼쪽, 실외는 오른쪽.
      　 바탕 그림의 **발자국 길이 두 섬 안쪽(다리 쪽)** 에 있어서, 한가운데 두면
      　 발자국을 덮습니다. `LABEL_OUT = 0.14` (섬 폭의 14%. 0 이면 한가운데)
      　 px 가 아니라 **비율**이라 화면이 커져도 같은 자리에 옵니다.
      　 이름 바 폭을 줄여 가운데로 모으는 방식이라 섬 칸 밖으로 나가지 않습니다
      　 (1024·1920 둘 다 카드 중심에서 68px 바깥, 화면 가장자리까지 82px 이상 남음)
    - 이름 글자가 **잘 안 보이던 문제**는 글씨가 아니라 **알약 자체**가 문제였습니다.
      지도 바탕이 아주 밝아서(평균 밝기 223) 연노랑 알약이 묻혔습니다.
      　**흰 글씨는 답이 아닙니다** — 연노랑 위 흰 글씨는 대비가 1.1 밖에 안 됩니다
      　**흰 알약도 답이 아닙니다** — 배경과 밝기가 같아 통째로 사라집니다
      → 색은 그대로 두고 ①글씨 `#6b5410`→`#4a3608`(대비 6.4→10)
      　 ②테두리 `#efdb92 2px`→`#e6bf4a 3px` ③알약 바깥에 **흰 테두리 3px** 한 겹.
      　 그래도 교실 뒤에서 안 보이면 `css/app.css` 의 `.no-map-wrap` 두 줄에서
      　 맨 앞 `.no-` 를 지우세요. 진한 갈색/초록 바탕 + 흰 글씨로 바뀝니다
      ※ **`LABEL_H` 는 52 로 그대로 둡니다.**
      　 0 으로 줄이면 그 자리를 카드가 먹어 175 → 231px 로 커집니다 (지금 크기가 맞습니다)
    - **나의 여가 도장판** (`.stampcard`) — 위쪽에 도장 6칸이 놓입니다.
      　★ 예전에는 진행 막대 하나뿐이라 **30곳을 다 가야** 뭔가 되는 구조였습니다.
      　　너무 멀어서 학생이 목표를 느끼지 못했습니다.
      　　이제 **5곳마다 도장 하나** (30곳 ÷ 5 = 6칸). 가까운 목표가 계속 보입니다
      　- 그래서 활동을 **30개로 맞췄습니다** (실내 15 · 실외 15).
      　　`음악 듣기` 를 더한 까닭입니다 — 5로 나누어떨어져야 칸이 딱 맞습니다
      　- 도장은 문구마켓 `참잘했어요` 처럼 **살짝 기울어 찍히고 날짜가 남습니다**
      　- 도장을 누르면 **그때까지 해본 활동 5개와 날짜**가 나옵니다.
      　　날짜는 그 활동으로 쓴 일기 가운데 **가장 이른 날**입니다
      　　(지도 표시만 눌렀고 일기가 없으면 날짜를 비워 둡니다)
      　- 아직 안 받은 칸은 발자국을 아주 연하게 두어 **자리를 미리 보여 줍니다**.
      　　지금 채우는 칸은 파란 테두리(`.stamp.next`)로 다음 목표를 알려 줍니다
    - **`일기 쓰기` 단추를 두지 않습니다.** 여기는 지도를 보는 곳이고, 일기는
      홈의 계획 카드나 기록하GO! 에서 씁니다 (규칙 7 — 중복 금지)
    - **`자세히` 단추도 없앴습니다.** 위쪽 `지도 도움말` 과 똑같은 창을 열어
      같은 곳으로 가는 단추가 두 개였습니다
    - **지도 위에 발자국을 뿌리지 않습니다.** 예전에는 '나' 에서 섬으로 발자국이
      이어졌는데, 바탕 그림이 들어오면서 지저분해져 없앴습니다.
      발자국은 **맨 위 진행 막대(`.mprog`)와 카드의 '해봤어요' 배지**에만 씁니다
    - **위아래로 쌓지 않습니다.** 화면이 세로로 길어도 두 섬은 늘 좌우입니다
      (`js/screens/map.js` 의 `layoutOf()`. 예전에는 `w/h >= 1.15` 일 때만 좌우로 놓아서,
      세로로 긴 화면에서 위아래로 바뀌어 보였습니다)
    - 섬마다 아래에 `◀ 이전 · 1/4 쪽 · 다음 ▶` 이 따로 있어 **각각 넘깁니다.**
      **← →** 방향키는 양쪽을 함께 넘깁니다
    - 한꺼번에 28장을 보여 주면 카드가 90px·글자 12px 로 작아져 읽기 어렵습니다
      (지금은 1280×800 에서 175px·21.9px, 1024×768 에서 163px·20.4px)
12. **모든 화면의 흰 칸 폭은 1060px 로 같습니다.**
    지도 패널에 `flex:1 1 auto` 를 주면 **가로로도 늘어나** 다른 화면보다 넓어집니다.
    높이만 늘리려면 `align-self:stretch` 만 쓰세요.
13. **낱말을 고르면 조사가 저절로 맞춰집니다.**
    `스티커북 만들기` + `을/를` → `…를`. 화면에 `을 했어요` 처럼 굳혀 쓰지 마세요
    (`js/screens/diary.js` 의 `josaOf`, `js/lib/korean.js` 의 `App.josa`)
14. **기분 낱말은 끝맺은 꼴(`재미있었어요`)로 저장합니다.**
    예전에는 줄기(`재미있`)만 저장하고 뒤에 `했어요` 를 붙여서
    `재미있했어요` · `화났했어요` 같은 문장이 나왔습니다.
    `App.moodWord()` 가 예전 기록도 알아서 고쳐 읽습니다

---

## 4. 선생님만 보는 것 / 학생도 보는 것

**선생님 설정**은 표지 오른쪽 위 톱니바퀴를 누르면 **바로** 열립니다. (잠금·탭 없음)
**한 페이지에 번호 순서대로** 모여 있고 아래로 내리며 설정합니다.

```
1. 우리 반 학생        [그림] [이름] [캐릭터] [여자/남자] [지우기]  + 학생 추가
2. 어느 학생의 설정인가요?   ← 3~11번은 여기서 고른 학생에게만 적용
3. 일기 쓰기 수준          4. 계획 방식
5. 음성 안내 · 사진 첨부    6. 학생 화면에 도구 보이기
7. 함께하는 사람 선택지     8. 기분 선택지
9. 학생에게 보여줄 활동     10. 포트폴리오 기간
   (＋ 우리 반 활동 더하기)
11. 그 학생의 기록 (고치기 · 지우기)
12. 백업 · 불러오기        13. 예시 자료 · 초기화
```

맨 위 **설정 완료** 를 누르면 `누구의 여가생활일까요?` 화면으로 넘어갑니다.

**★ 캐릭터는 학생 줄마다 있는 `캐릭터` 단추로 각각 고릅니다.**
예전에는 학생 목록 **아래에 단추가 하나뿐**이었습니다. 그 단추는 `App.store.current()`
(＝지금 고른 학생)를 보는데, `＋ 학생 추가` 가 새 학생을 곧바로 current 로 바꾸기 때문에
**6명을 먼저 다 넣으면 맨 마지막 학생의 캐릭터만** 계속 골라졌습니다.
이제 줄마다 단추가 있고 그 줄의 `s.id` 를 넘기므로, 누른 학생의 캐릭터가 정해집니다.
- 아직 안 고른 학생 → **빨간 점선** 단추 (눈에 띔)
- 다 고른 학생 → **회색 실선** 단추 + 고른 캐릭터 그림이 단추 안에 보임 (`.tchoice.avatar.done`)
- 캐릭터를 고르고 나면 **선생님 설정으로 되돌아옵니다** (`from:'teacher'`)

### 학생이 시작하는 흐름

```
표지 ─[시작하기]→ 누구의 여가생활일까요?  (이름 앞 그림 + 이름 칸)
        └ 이름을 누르면 ─→ 나의 여가 탐험 캐릭터를 골라요
                            (왼쪽 미리보기 · 오른쪽 목록 · 아래 큰 단추)
                              └→ 홈
```

캐릭터를 이미 고른 학생은 이름을 누르면 곧바로 홈으로 갑니다.

**학생 화면에서 기본으로 숨긴 도구** (선생님 설정 → 일기·음성·사진 에서 켤 수 있음)

- `mapTools` — 지도의 찾아보기(필터)·확대·도움말
- `folioTools` — 포트폴리오의 기간 표시·전시판형·책자형·인쇄

기본값은 **꺼짐**입니다. 학생에게는 지도와 활동카드만, 포트폴리오는 "전시할 기록 고르기"만 보입니다.

**하나만 기본이 켜져 있습니다** — `addTools` (활동 고르는 화면의 `＋ 활동 더하기`).
수업 중에 선생님이 바로 활동을 더할 수 있어야 해서 켜 두었습니다. 위 4-1 참고.

---

## 5. 파일 구조

```
index.html              화면 뼈대 + 불러올 파일 목록 (?v= 로 캐시 갱신)
single-file.html        한 파일 판 (build-single-file.ps1 이 만듦)
build-single-file.ps1   한 파일로 묶는 스크립트

css/app.css             모든 화면 스타일
css/print.css           인쇄·PDF 저장용

js/data/activities.js   ★ 활동 목록 · 그림 경로(App.IMAGE_BASE) — 그림 교체는 여기
js/data/options.js      함께하는 사람 · 기분 · 캐릭터 · 코너 · 문장 틀
js/data/icons.js        기본 SVG 그림 + 조작용 선 아이콘(App.uiIcon)

js/lib/korean.js        조사(은/는·이/가·을/를) 처리, 날짜, 문장 조립
js/lib/store.js         학생·계획·일기·지도 저장(localStorage) + 백업/불러오기
js/lib/photos.js        사진 저장(IndexedDB, 실패 시 대체) + 1000px 축소
js/lib/speech.js        브라우저 음성 읽어주기

js/components/common.js 버튼·읽어주기·활동카드·무대(좌우 쪽)·확인창 …
js/screens/cover.js     표지
js/screens/home.js      홈
js/screens/plan.js      계획하GO!
js/screens/map.js       알아보GO!
js/screens/diary.js     기록하GO!
js/screens/picdiary.js  그림일기 (A4 한 장 · 원고지 · 따라쓰기 · 인쇄)
js/screens/portfolio.js 모아보GO!
js/screens/profiles.js  학생 선택 + 캐릭터 고르기
js/screens/teacher.js   선생님 설정
js/app.js               화면 이동 · 인쇄 · 시작
```

---

## 4-1. 우리 반 활동 더하기 (2026-08-17 새로 넣음)

학급 특성에 맞는 활동을 **그 자리에서 바로** 더할 수 있습니다.

**더하는 곳 (둘 다 같은 창)**
1. 계획하GO! · 기록하GO! 의 **활동 고르는 화면 맨 끝 `＋ 활동 더하기` 카드**
2. 선생님 설정 **9. 학생에게 보여줄 활동** 의 `＋ 실내/실외 활동 더하기`

**적는 것** : 활동 이름 · 실내/실외 · 장소(선택) · 그림 고르기
계획·일기 문장은 이름에서 **조사까지 맞춰 저절로** 만들어집니다.
　`텃밭 가꾸기` → "나는 오늘 텃밭 가꾸기**를** 할 거예요 / 했어요"

- 더한 활동은 **우리 반 전체**에 적용되고 `localStorage` 에 함께 저장됩니다
  (`store.customActivities` · `App.setCustomActivities()`)
- **지우기**는 선생님 설정에서만 합니다. 이미 쓴 계획·일기는 그대로 남습니다
- `images/activities/<활동이름>.png` 를 넣어 두면 고른 그림 대신 그 그림이 나옵니다
- `＋` 카드를 학생 화면에서 숨기려면 선생님 설정 **6. 학생 화면에 도구 보이기** 에서 끕니다
  (`addTools` — 다른 도구와 달리 **기본이 켜짐**입니다)

---

## 5-1. 그림일기 (2026-08-17 새로 넣음)

일기 하나를 **보통의 그림일기 서식**으로 A4 한 장에 담아 인쇄합니다.

```
┌──────────────────────────────────────┐
│  2026 년 8 월 17 일 월 요일   날씨 ☀ ☁ ☂ ⛄ │  ← 날씨는 인쇄해서 동그라미
├──────────────────┬───────────────────┤
│ 일어난 시간:      │ 잠드는 시간:       │  ← 빈칸 (학생이 직접)
├──────────────────┴───────────────────┤
│                                      │
│        사진 또는 내가 그린 그림        │  ← 한 장만. 잘라내지 않음
│                                      │
├──────────────────────────────────────┤
│ 제 목:                               │
├──┬──┬──┬──┬──┬──┬──┬──┬──┬──┐        │
│  │나│는│  │친│구│와│  │함│께│        │  ← 원고지 : 한 칸에 한 글자
└──┴──┴──┴──┴──┴──┴──┴──┴──┴──┘        │
```

**가는 길** — 일기 저장 후 마지막 창의 `그림일기 보기`,
또는 모아보GO! 의 각 기록에 있는 `그림일기` 단추.

- **인쇄 모양 4가지** — 오른쪽으로 갈수록 쓰기가 어려워집니다. 같은 일기로 여러 수준을 인쇄할 수 있어요.

  | 모양 | 어떻게 나오나 |
  |---|---|
  | **글자** | 완성된 글자를 그대로 읽어요 |
  | **첫글자만** | 줄마다 첫 글자만 진하고 나머지는 연해요 |
  | **따라쓰기** | 모두 연한 회색 — 그 위에 그대로 따라 써요 |
  | **빈칸** | 칸(줄)만 나와요 — 보고 옮겨 써요 |

- **글자칸은 1·2단계 원고지, 3단계 밑줄** — 3단계는 글이 길어 칸에 다 안 들어갑니다.
  밑줄은 `줄 간격 = 글자 줄 높이` 로 맞춰 두어, **한 문장이 줄바꿈되어도 다음 줄에 얹힙니다**
  (`.pd-lines` 의 `--lh` 와 `background-position`. 이 둘을 따로 고치면 글자가 줄에서 뜹니다)
- **원고지는 최소 5줄 · 최대 8줄** (`MIN_ROWS` · `MAX_ROWS`)
  - 예전에는 최대 7줄이어서, 7줄을 넘으면 다음 칸수로 떨어져 **칸이 오히려 작아졌습니다.**
    8줄까지 허용하니 같은 글이 **12칸(66px) → 10칸(79px)** 로 커졌습니다
  - 8줄일 때 원고지 632px, 그림칸 277px 남습니다 (그림칸 최소 180px). **더 올리면 그림이 찌그러집니다**
- **A4 세로 예산** — 이 셈을 알아야 칸·줄·그림을 고칠 수 있습니다
  ```
  1123px  A4 높이
   −  64  날짜·날씨 줄        }
   −  46  일어난·잠드는 시간   } 고정 214px
   −  52  제목 줄            }
   −  52  아래 기분·사람·장소 칩 }
   ─────
     909px  ← 그림칸(flex, 최소 180) + 원고지(칸크기 × 줄수) 가 나눠 씁니다
  ```
  **칸을 키우면 그림칸이 그만큼 줄어듭니다.** 글자 수가 정해져 있으니 **줄 수는
  고르는 것이 아니라 결과**입니다 — 칸이 커지면 줄이 저절로 늘어납니다.
  8칸(99px)으로 해 보면 어떤 일기는 그림칸이 **18px** 까지 찌그러집니다
- **칸 안 글자는 칸의 70%** (`GLYPH_FILL`). 예전 58% → **70%**.
  칸 크기(79px)는 그대로인데 글자만 46 → **55px** 로 커집니다.
  **그림칸을 한 픽셀도 안 줄이고** 시원해 보이는 방법입니다.
  0.75 를 넘으면 받침 있는 글자(`쌓`)가 칸 선에 닿습니다
  - 글자+마침표가 한 칸에 든 칸(원고지 규칙 4)은 `.pd-ch.two{ font-size:.8em }` 로
    조금 줄입니다. 안 줄이면 두 글자가 칸 선을 넘습니다
- **줄이 넘쳐도 자르지 않습니다.** 예전에는 `rows.slice(0, MAX_ROWS)` 로 잘라서
  **학생이 쓴 글이 사라졌습니다** (원고지 규칙 13 위반). 지금은 콘솔에 알리고 그대로 둡니다
- **원고지 맞춤법** → 아래 **5-1-1** 을 꼭 읽어 주세요 (앱의 고정 규칙입니다)
- **칸 수는 저절로 정해집니다** — 기본 10칸, 글이 길면 12·14·16·20칸으로 늘려
  종이 한 장(6줄 이내)에 들어가게 맞춥니다 (`COL_OPTIONS`, `MAX_ROWS`)
- **그림은 한 장만** 들어갑니다. 여러 장 넣었으면 기록하GO! 의
  `사진 · 내가 그린 그림` 에서 **하나를 눌러 고릅니다** (`mainPhotoId`)
- 인쇄하면 **186 × 273 mm** (A4 - 여백 12mm) 실치수로 나옵니다.
  화면에서는 `transform:scale` 로 줄여 보여 주고, 인쇄 때에는 `css/print.css` 가 축소를 풉니다

### 그림칸에 넣을 것 — 세 가지 (기록하GO! 의 `그림일기에 넣을 그림`)

| 고르기 | 무엇이 들어가나 |
|---|---|
| **내가 고른 그림** (기본) | 고른 **활동 그림 + 함께한 사람 + 기분 + 장소**. 아무것도 더 안 해도 그림일기가 채워집니다 |
| **사진 넣기** | 이 기기에 있는 사진. 여러 장 넣었으면 **한 장을 눌러 고릅니다** |
| **내가 그리기** | 앱 안에서 직접 그립니다 |

- **직접 그리기**(`C.DrawPad`) 는 색 8가지 · 굵기 3단계 · 지우개 · 다 지우기.
  `PointerEvent` 와 `touch-action:none` 을 써서 **전자칠판·태블릿·아이패드의 손가락과 펜**,
  마우스가 모두 같은 방법으로 동작합니다. 그린 그림은 사진과 같은 곳(IndexedDB)에 저장됩니다
- 셋 가운데 **하나만** 그림일기에 들어갑니다 (`picKind`)

---

## 5-1-1. 「나의 여가 일기 — 원고지 작성 규칙」 ★고정 규칙★

> 학생이 쓴 글을 원고지에 표시할 때는 **항상** 이 규칙을 적용합니다.
> 구현은 `js/screens/picdiary.js` 의 `gridRows()` 하나에 모여 있습니다.

### ⛔ 절대 하지 말 것

**글자를 한 글자씩 잘라 칸에 순서대로 넣는 방식으로 만들지 마세요.**
`text.split('')` 로 칸을 채우면 아래 네 가지가 **모두** 틀립니다.

```
① 문단 첫 칸 비우기        ② 자동 줄바꿈은 첫 칸부터
③ 줄 첫 칸에 마침표 금지    ④ 줄바꿈으로 생긴 띄어쓰기 버리기
```

그래서 **칸 번호 · 줄 번호 · 문단 시작 여부 · 문장부호 종류 · 줄 첫 칸이 되는지**를
하나하나 판단하며 놓는 함수로 만들어 두었습니다.

### 13개 조항

| # | 규칙 |
|---|---|
| 1 | 한 칸에 한 글자. 한글·숫자·알파벳 모두 한 칸. 왼→오, 위→아래 |
| 2 | **새 문단**은 첫 칸을 비우고 **둘째 칸부터**. 일기 첫 문장도 새 문단입니다 |
| 3 | 낱말 사이 띄어쓰기는 **빈 칸 1칸**. 단, **줄 첫 칸은 띄어쓰기로 비우지 않습니다** |
| 4 | 마침표·쉼표는 한 칸 차지. 줄 마지막 칸에 글자가 있고 바로 뒤에 와야 하면 **같은 칸에 함께** |
| 5 | `?` `!` 는 각각 한 칸. 뒤에 문장이 이어지면 **한 칸 비우고** 이어 씁니다 |
| 6 | 여는·닫는 따옴표도 각각 한 칸. **닫는 따옴표가 줄 첫 칸에 혼자 남지 않게** |
| 7 | **자동 줄바꿈 → 다음 줄 첫 칸부터** / **새 문단 → 첫 칸 비우고 둘째 칸부터**. 이 둘을 반드시 구분 |
| 8 | **제목은 원고지 칸에 넣지 않습니다.** 별도 제목 칸(`.pd-titlebar`). 제목 뒤에 마침표 없음 |
| 9 | **날짜도 원고지 칸에 넣지 않습니다.** 별도 날짜 칸(`.pd-datebar`) |
| 10 | 숫자·영어(`3시` `2번` `TV` `K-pop`)를 지우거나 한글로 바꾸지 않습니다. 읽기 쉬운 배치와 일관성 우선 |
| 11 | 처리 순서 : 원문 → 문단 구분 → 띄어쓰기 분석 → 칸 수에 맞춰 배치 → 자동줄바꿈/새문단 구분 → 문장부호 검사 → 표시 |
| 12 | 위 판단 정보를 다루는 **별도 배치 함수**로 구현 (단순 문자열 자르기 금지) |
| 13 | 내놓기 전 **스스로 검사** — 아래 검사 항목 |

### 규칙 13 검사 항목

- [ ] 새 문단의 첫 칸이 비어 있는가?
- [ ] 자동 줄바꿈에서는 첫 칸부터 글자가 시작되는가?
- [ ] 낱말 사이 띄어쓰기가 한 칸으로 표현되는가?
- [ ] 줄이 바뀌면서 생긴 띄어쓰기 때문에 첫 칸이 비어 있지 **않은가**?
- [ ] 마침표·쉼표가 다음 줄 첫 칸에 혼자 있지 않은가?
- [ ] 글자가 빠지거나 겹치지 않았는가? 문장 순서가 바뀌지 않았는가?

마지막 항목은 코드가 **저절로** 검사합니다 (`verifyGrid`). 어긋나면 브라우저 콘솔에
`[원고지] 글자가 어긋났습니다` 경고가 뜹니다. 공백을 뺀 글자로 견줍니다 —
띄어쓰기는 줄바꿈이 대신하기도 해서 개수가 달라질 수 있기 때문입니다.

### 콘솔에서 바로 확인하기

```js
App.manuscriptRows(['오늘은 공원에 갔다.'], 10)   // 원고지 배치를 배열로 돌려줍니다
App.verifyManuscript(문단배열, 결과)              // 글자가 어긋나지 않았는지 검사
```

실제로 확인된 보기 (10칸, `□` 는 빈 칸):

```
□오늘은 공원에 갔      ← ② 문단 첫 칸 비움
다.□□□□□□□□        ← ⑦ 자동 줄바꿈은 첫 칸부터

□나는 친구와 놀이
터에서 그네를 탔다.     ← ④ 마지막 칸에 '다' + '.' 함께

□정말 신났다!□그       ← ⑤ ! 뒤에 한 칸 비움
래서 좋았다.□□□

□엄마가 "재미있었
니?"라고 물었다.       ← ⑥ 닫는 따옴표가 줄 첫 칸에 혼자 오지 않음
```

### 적용되지 않는 곳

**3단계(자유쓰기) 일기는 원고지 칸이 아니라 밑줄**을 씁니다 (`useLines`).
글이 길어 칸에 다 안 들어가기 때문입니다. 이 규칙의 칸 배치는 1·2단계에만 씁니다.

---

## 5-2. 재미있게 참여하게 하는 것들 (2026-08-17 새로 넣음)

**① 모은 만큼 보이기 — 여가 지도 맨 위**
`발자국 7 / 29곳` 과 막대. 섬 한 쪽(4장)을 모두 '해봤어요' 로 채우면 **🏅 도장**이 찍힙니다.
(`js/screens/map.js` 의 `triedCount` · `islandDone`)

**② 나의 여가 일기장 — 모아보GO!**
기간 안의 그림일기를 **A4 여러 쪽으로 한꺼번에** 인쇄합니다. 묶으면 그대로 한 권.
`일기장 인쇄하기` · `따라쓰기 판으로` · `빈칸 판으로` 세 가지로 뽑을 수 있어요.
(선생님 도구 — `folioTools` 를 켠 학생에게만 보입니다)

**④ 오늘의 도전 — 홈**
아직 '해봤어요' 발자국이 없는 활동 하나를 권합니다.
**날짜를 씨앗으로 골라 하루에 하나만** 바뀝니다 (누를 때마다 바뀌면 고르기 어려워서요).
`이거 할래요` 를 누르면 계획하GO! 로 가되 **활동은 이미 골라진 채** '누구와' 부터 물어봅니다.
모든 활동을 한 번씩 해 본 학생에게는 칭찬 문구가 나옵니다.

**⑤ 칭찬 반응 — 일기를 저장한 순간**
캐릭터가 폴짝 뛰며 `잘했어요!` 하고, 도-미-솔-도 소리가 짧게 울립니다.
소리는 **파일 없이 브라우저가 직접 만듭니다** (`App.sound.cheer` · `js/lib/speech.js`).
음성 안내를 끈 학생에게는 울리지 않습니다 (`App.cheerFor`).

**⑥ 교실 TV 전시 — 모아보GO!**
전시하기로 고른 기록을 **한 장씩 크게 띄우고 8초마다 저절로 넘깁니다.**
**← →** 로 넘기고, **스페이스**로 멈췄다 다시 시작, **Esc** 로 나갑니다.
교실 TV·전자칠판에 띄워 두면 친구들 기록을 함께 보게 됩니다. (`C.ShowMode`)

---

## 6. 그림 교체

`images/README.md` 에 파일 이름표가 모두 있습니다. 요약하면:

| 무엇 | 어디 |
|---|---|
| 표지 | `images/표지.png` |
| 배경 벽지 | `images/벽지.jpg` |
| 활동 44개 | `images/activities/캠핑.png` 처럼 한글 이름 그대로 |
| 캐릭터 36개 | `images/avatars/곰.png` 등 |
| 함께하는 사람 | `images/avatars/엄마.png`, `친구-여.png` 등 |
| 단추 그림 | `images/소리이모지.png` · `전체화면이모지.png` · `톱니바퀴이모지.png` · `home_icon_transparent.png` |
| 발자국 | `images/발자국.png` (256×256 · 투명 · 한 발 · 발끝 위) |
| 실내·실외 고르기 | `images/실내에서 해요.png` · `실외에서 해요.png` |
| 기분 얼굴 8개 | `images/얼굴표정/재미있었어요.png` 처럼 **끝맺은 꼴** 이름 |
| 지도 활동 표시 4개 | `images/해봤어요.png` · `좋아해요.png` · `도전하고 싶어요.png` · `아직 잘 모르겠어요.png` |
| 여가 지도 바탕 | `images/여가지도.png` (아래 6-1 참고) |

- 경로 정의는 **`js/data/activities.js` 의 `App.IMAGE_BASE` 한 곳**입니다.
- 그림이 없으면 코드로 그린 SVG 가 대신 나오므로 앱은 그대로 동작합니다.
- ★ **같은 이름으로 그림만 바꿔 넣었을 때** — 브라우저가 예전 그림을 계속 씁니다.
  그래서 그림 주소에도 `index.html` 의 `?v=...` 를 그대로 붙여 두었습니다
  (`App.imgUrl` — 스스로 `activities.js?v=...` 에서 읽어 옵니다).
  **그림을 바꾸면 `index.html` 의 숫자만 올리면 됩니다.** 학생 컴퓨터에서도 새로 받아옵니다.
- **`imageKey` 는 파일 이름과 글자 하나까지 같아야 합니다.**
  안 적으면 활동의 영문 id(`cook-bread` 등)로 찾다가 못 찾습니다.
  한글 파일 이름을 쓸 때에는 `imageKey: '식빵 요리'` 처럼 꼭 적어 주세요.
- **남녀 그림이 둘 다 있는 사람** (혼자 · 가족 · 형제자매 · 친구 · 선생님) 은
  `variants` 로 적습니다. 따로 고르지 않으면 **그 학생의 성별에 맞는 쪽**이 나옵니다
  (`App.partnerVariant`). '혼자' 는 학생 자신이라 성별이 맞아야 자기 그림으로 알아봅니다.
- ★ **사람 그림은 규격을 맞춰야 나란히 서 보입니다.**
  캔버스 크기는 상관없고 **비율**만 맞으면 됩니다 (그림이 카드 높이에 맞춰 늘어나므로).
  　**발끝 = 아래에서 6%** (모두 같게 — 같은 바닥에 서 보입니다)
  　**사람 높이 = 88%**
  이게 어긋나면 여백 차이 때문에 누구는 크고 누구는 작아 보입니다.
  기분 얼굴 8개도 마찬가지로 **높이 78% · 아래 여백 11%** 로 맞춰 주세요.
- 아직 그림이 없는 것 : `images/activities/텃밭 가꾸기.png` 하나뿐입니다.

---

## 6-1. 여가 지도 바탕 그림 (`images/여가지도.png`)

민트색 칸(`.map-wrap`) 에 깔리는 그림입니다. 파일이 없으면 지금처럼 연한 색만 깔립니다.
경로는 `App.IMAGE_BASE.mapbg`, 화면에는 `js/app.js` 가 `--mapbg` 로 넣어 줍니다.

**칸 크기는 기기마다 다릅니다** (폭은 흰 칸 1060px 에 묶여 있고, 높이만 크게 달라집니다)

| 기기 | 민트색 칸 | 가로세로 비 |
|---|---|---|
| 전자칠판 1920×1080 | 1014 × 838 | 1.21 |
| 전자칠판·노트북 1366×768 | 1040 × 600 | 1.73 |
| 태블릿 1280×800 | 1024 × 600 | 1.71 |
| 아이패드 가로 1024×768 | 988 × 568 | 1.74 |
| 아이패드 세로 820×1180 | 783 × 976 | **0.80** |

- 그림은 **늘어나지 않고 잘리며 채워집니다** (`background-size:cover`).
  늘리면 찌그러지기 때문입니다.

★ **다시 그릴 때 지켜야 할 두 가지**
1. **가로세로 비 1.5** (지금 그림 1536 × 1024)
2. **중요한 것은 가운데 가로 80% · 세로 85% 안에** — 가장자리는 잘려도 되는 것으로

> **왜 1.5인가** — 실제로 쓰는 기기의 칸 비율이 **1.21 ~ 1.74** 이고 그 가운데가 1.45쯤입니다.
> 1.5 로 두면 어느 기기에서나 **가장자리 12~19% 만** 잘립니다 (전자칠판 FHD 가로 81%,
> 전자칠판 HD·태블릿·아이패드 가로 세로 86~88%).
> **세로 아이패드는 쓰지 않습니다.** (쓰게 되면 가로가 절반 잘리니 그때 다시 의논)
> 비를 바꾸면 이 균형이 깨집니다. `contain` 으로 바꾸거나 칸 비를 고정하면
> 잘림은 없어지지만 빈 띠가 생겨 오히려 손해입니다.
- **아주 연하게** 그려야 합니다. 그 위에 흰 활동 카드 8장 · 캐릭터 · 섬 이름 바 ·
  발자국이 올라가서, 바탕이 진하면 글자가 안 읽힙니다.
  지금 그림은 평균 밝기 228/255 · 어두운 점 1% 로 알맞습니다.
- **가운데 세로로 점선 경계**가 지나가고 그 양옆에 카드가 놓이니, 가운데는 비워 두는 것이 좋습니다.
- 권장 크기 : **1600 × 1000 안팎** (지금 그림은 1586×992). 더 키워도 화면에서는 줄어듭니다.

---

## 7. 저장 방식

| 무엇 | 어디 |
|---|---|
| 학생·계획·일기·지도 표시 | `localStorage` 의 `naui-yeoga.v1` |
| 사진 | `IndexedDB` 의 `naui-yeoga-photos` (막히면 자동 대체) |

- 새로고침·재시작해도 남습니다. 다른 기기로 옮기려면 **선생님 설정 → 데이터 → 백업**.
- 밖으로 아무것도 전송하지 않습니다.
- `file://` 로 열면 브라우저에 따라 사진 저장이 막힐 수 있습니다. 그럴 때는 학교 내부
  서버나 공유폴더에 올려 `http://` 로 여세요. 글자 기록은 두 경우 모두 저장됩니다.

---

## 8. 고칠 때 조심할 것 (실제로 겪은 함정)

1. **`column-fill:auto` 는 높이가 '확정'되어야 동작합니다.**
   flex 로 정해진 높이는 크롬이 확정으로 보지 않아 좌우 쪽 나누기가 먹지 않습니다.
   `common.js` 의 `C.Stage` 에서 넘칠 때만 픽셀 높이를 직접 지정합니다.

2. **`scroll-behavior:smooth` 를 쓰면 방향키 쪽넘김이 취소됩니다.** `auto` 로 둘 것.

3. **padding 을 % 로 주면 '부모 너비' 기준입니다.** 코너 카드 그림이 0×0 이 된 적 있습니다.

4. **htm 템플릿에서 태그 짝이 맞지 않으면** `p.push is not a function` 으로 앱이 통째로 안 뜹니다.
   같은 이유로 **`html\`\`` 안에는 `/* 주석 */` 을 쓰지 마세요.** 글자로 섞여 들어가
   앱이 통째로 안 뜹니다. 주석은 템플릿 밖에 두고 값만 변수로 넘기세요.

4-1. ★★ **`html\`\`` 안에서는 백틱(\`)을 절대 쓰지 마세요** — `<!-- HTML 주석 -->`
   안에서도 안 됩니다. 백틱이 템플릿 문자열을 그 자리에서 끊어 버려
   `SyntaxError: Unexpected identifier '다음'` + React #130 이 나고
   **앱이 하얗게 빈 화면이 됩니다.**
   템플릿 **밖** 의 `/* */` 주석에서는 백틱을 써도 괜찮습니다 — 헷갈리는 지점입니다.
   템플릿 안 주석에서는 낱말을 홑따옴표로 감싸세요 : `'다음' 단추`.
   네 번 걸렸습니다 (plan.js 의 cols-8 · map.js 의 자세히 · 다음 · 찾아보기).
   고치기 전에 이것만 돌려 보세요 — 아무것도 안 나와야 정상입니다 :
   ```bash
   grep -rn '<!--' js/ | grep '`'
   ```

4-2. ★ **글자 크기의 `clamp()` 최댓값을 `rem` 으로 두면 상한이 없는 것과 같습니다.**
   이 앱은 **루트 글자 크기 자체가 화면에 따라 커집니다** (1164×1229 에서 19.95px,
   1400×1500 에서 24.25px). 그래서 `clamp(1.15rem, …, 1.9rem)` 의 1.9rem 은
   화면이 커지면 같이 커집니다.
   **폭이 px 로 고정된 칸 안의 글자**는 최댓값도 px 로 못박아야 합니다.
   코너 카드가 이것 때문에 `여가 / 계획하기` 로 두 줄이 되었습니다
   (카드는 `clamp(140px,21vh,250px)` 로 250px 에서 멈추는데 글자는 계속 컸음).
   → `.corner .desc` 는 `clamp(1.05rem, 1.7vh + .5rem, 28px)` + `white-space:nowrap`.

5. **`build-single-file.ps1` 안에서는 `Write-Output` 대신 `Write-Host`**.
   출력 스트림에 쓰면 그 글자가 HTML 에 섞여 들어갑니다.

6. 같은 스크립트는 **ASCII 로만** 쓰세요. Windows PowerShell 5.1 은 BOM 없는 .ps1 을
   ANSI 로 읽어 한글이 깨집니다. 한글은 `index.html` 과 css/js 에서만 가져옵니다.

7. **`Embed-Asset` 의 키는 `images/` 로 시작하는 값만 찾습니다.**
   그렇지 않으면 `speaker` 키가 icons.js 의 SVG 까지 잡습니다.

8. **CSS 변수(`--wallpaper`) 안의 상대 경로는 `css/` 를 기준으로 풀립니다.**
   크롬이 그 값을 *쓰는* 스타일시트를 기준으로 삼기 때문에, 그냥 두면
   `css/images/벽지.jpg` 를 찾다 못 찾아 **벽지가 안 나옵니다**.
   `js/app.js` 에서 `new URL(경로, document.baseURI)` 로 절대 주소를 만들어 넣습니다.

9. **`_sd.html` 같은 임시 빌드는 지워도 됩니다.** 정식 한 파일 판은 `single-file.html`
   하나뿐이고, `build-single-file.ps1` 이 `index.html` 을 읽어 다시 만듭니다.
   **소스를 고쳤으면 한 파일 판도 다시 만들어야** 같은 내용이 됩니다.

---

## 8-13. 읽어주기가 이상하게 들릴 때 — **낱말 하나만 주지 마세요** (2026-08-18)

`누구와 할까요?` 에서 **엄마 · 아빠** 발음이 이상하다는 이야기가 있었습니다.
읽는 글자를 가로채 보니 정확히 `"엄마"` · `"아빠"` 였고, 목소리도
한국어(Microsoft Heami, ko-KR)가 제대로 잡혀 있었습니다.

**원인은 발음이 아니라 말할 거리가 너무 짧았던 것**입니다.
윈도우 한국어 목소리는 두 글자짜리 낱말을 말끝이 올라가는 이상한 가락으로
읽습니다. `엄마` `아빠` 처럼 된소리·콧소리가 들어간 말이 특히 심합니다.

> **규칙 : 읽어 줄 때는 낱말이 아니라 짧은 문장을 주세요.**

| 곳 | 예전 | 지금 | 만드는 함수 |
|---|---|---|---|
| 계획 · 누구와 | `엄마` | `엄마와 함께 할 거예요` | `App.partnerSpeech` |
| 일기 · 누구와 | `엄마` | `엄마와 함께 했어요` | `App.partnerSpeechPast` |
| 계획 · 어느 곳 | (소리 없음) | `교실에서 할 거예요` | plan.js 안에서 바로 |
| 계획 · 준비물 | (소리 없음) | `색종이를 챙길 거예요` / `색종이를 뺐어요` | `App.eulReul` |

- `혼자` 는 `혼자 함께` 가 되면 말이 안 되므로 따로 다룹니다 (`혼자 할 거예요`).
- 준비물은 **넣을 때와 뺄 때를 다르게** 읽습니다. 같은 단추를 두 번 눌러
  빼는 화면이라, 넣은 것인지 뺀 것인지 소리로 알 수 있어야 합니다.
- 덤 : 조사(`엄마와` `교실에서` `색종이를`)까지 들려주니 말꼴도 함께 익힙니다.
- ★ `speakText` 만 고치면 **작은 스피커 아이콘만** 바뀝니다.
  카드를 눌렀을 때 나는 소리는 `onClick` 안의 `App.speakFor` 입니다.
  **둘 다 고쳐야** 합니다 (한동안 서로 다른 말을 읽고 있었습니다).

확인하는 방법 — 브라우저 콘솔에 붙여 넣고 아무 카드나 눌러 보세요 :

```js
window.__said=[];
var O=window.SpeechSynthesisUtterance;
window.SpeechSynthesisUtterance=function(t){ window.__said.push(t); return new O(t); };
window.SpeechSynthesisUtterance.prototype=O.prototype;
// 카드를 누른 뒤 : __said 를 보면 실제로 읽은 글자가 나옵니다
```

---

## 8-14. 남는 자리를 여백으로 쓰기 (`--slack`) (2026-08-18)

홈의 `나의 여가` 제목 위아래에 숨 쉴 자리를 두려다 알게 된 것입니다.

**처음에 실패한 방법** : `margin: clamp(.4rem, 2.6vh, 40px)` 처럼 화면 높이로
고정 여백을 주었습니다. 계획 카드가 없는 학생에게는 좋았지만, **오늘의 계획
카드가 있는 날에는 흰 칸이 이미 꽉 차서 홈이 두 쪽으로 갈라졌습니다.**

**지금 방법** : `C.Stage` 가 **남는 자리**를 재어 `--slack` 이라는 CSS 값으로
넣어 줍니다. 여백은 그 절반을 넘지 않으므로 **절대 넘치지 않습니다.**

```css
.home-title{
  --one-line:calc(clamp(.9rem, .55rem + .8vh, 20px) * 1.45);  /* 지운 설명 한 줄 */
  margin-top:min(var(--one-line), calc(var(--slack, 0px) / 2));
  margin-bottom:min(var(--one-line), calc(var(--slack, 0px) / 2));
}
```

재면서 걸렸던 두 가지 :

1. **`scrollHeight` 로는 남는 자리를 알 수 없습니다.** 내용이 짧아도
   `scrollHeight` 는 칸 높이보다 작아지지 않아 늘 0 이 나옵니다.
   그래서 **첫 자식과 마지막 자식의 자리**로 내용 높이를 직접 구합니다.
2. **남는 자리는 두 군데에 생깁니다.**
   - 흰 칸 **안** — 흰 칸이 화면 높이를 다 쓰는 화면 (지도처럼)
   - 흰 칸 **아래** — 흰 칸이 내용만큼만 커지는 화면 (홈처럼).
     홈은 카드 두 줄에서 흰 칸도 끝나고, 남는 자리는 흰 칸 **바깥 아래**입니다.
   둘 다 재어 큰 쪽을 씁니다 (`measureSlack` in `common.js`).
3. **재기 전에 `--slack` 을 0 으로 되돌려야** 합니다. 그러지 않으면
   '여백을 넓혔더니 남는 자리가 줄고, 줄었으니 여백이 좁아지고…' 하며
   값이 계속 흔들립니다.

**★ 여백을 준 뒤에 넘치는지 꼭 다시 봐야 합니다.**
`--slack` 을 재어서 그대로 쓰면, 그 여백이 내용을 밀어내서
**마지막 칸(오늘의 도전 같은)이 2쪽으로 밀려납니다.**
1쪽에는 큰 빈자리만 남고 학생은 왜 비었는지 알 수 없습니다.
그래서 `C.Stage` 는 **잰 값 → 절반 → 4분의 1 → 0** 으로 줄여 가며
넘치지 않는 값을 고릅니다 :

```js
el.style.setProperty('--slack', '0px');
var want = measureSlack(el);
var tries = [want, Math.floor(want/2), Math.floor(want/4), 0];
for (var ti = 0; ti < tries.length; ti++) {
  el.style.setProperty('--slack', tries[ti] + 'px');
  if (el.scrollHeight <= el.clientHeight + 2) break;
}
```

한 번에 0 으로 떨어뜨리지 않는 까닭 : 조금이라도 숨 쉴 자리를 남기려는 것입니다.

실제로 잰 값 (1116×1010) :

| 학생 | 남는 자리 | 제목 위아래 여백 | 쪽수 |
|---|---|---|---|
| 계획 카드 없음 | 247px | 25px 씩 | 1 |
| 계획 카드 있음 | 12px | 6px 씩 | 1 |

---

## 8-15. 전체화면에서 글자 사다리가 뒤집히는 문제 (2026-08-18)

전체화면으로 하면 **창 안 본문이 그 위 소제목보다 커졌습니다.**
`오늘의 도전` 보다 `캠핑하기` 가, `오늘의 여가 계획` 보다 `나는 오늘 …` 이 컸습니다.

**원인** : 소제목 `.sec h3` 는 `1.1rem` **한 값**뿐인데, 안쪽 본문은
`clamp(…, .7rem + .9vh, …)` 처럼 **`vh` 로 자랍니다.**
화면이 커질수록 본문만 빠르게 자라서 결국 소제목을 앞질렀습니다.

> **규칙 : 같은 화면에서 위아래 관계가 있는 글자는 `vh` 계수도 같은 순서로.**
> 한쪽만 `vh` 로 자라면 언젠가 반드시 뒤집힙니다.

지금의 글자 크기 사다리 (위에서 아래로) :

| 자리 | 값 | 어디 |
|---|---|---|
| 화면 제목 | `clamp(1.30rem, .85rem + 1.30vh, 1.85rem)` | `.topbar .title` |
| 질문 바 | `clamp(1.10rem, .72rem + 1.10vh, 1.57rem)` | `.q h2` |
| 창 안 소제목 | `clamp(1.00rem, .62rem + .95vh, 26px)` | `.sec h3` |
| 본문 한 줄 | `clamp(.92rem, .55rem + .80vh, 22px)` | `.card-say` · `.ch-name` |

1024×768 → 22 · 19 · 16 · 14px / 1920×1080 → 40 · 34 · 26 · 22px

**한 칸을 고치면 나머지도 같은 비로 고치세요.** 하나만 고치면 어떤 화면
크기에서는 맞고 어떤 크기에서는 뒤집힙니다.

확인하는 방법 — 브라우저 콘솔에 붙여 넣으면 뒤집힌 곳을 찾아 줍니다 :

```js
(function(){
  var panel=document.querySelector('.panel');
  var titleEl=document.querySelector('.topbar .title')||document.querySelector('.home-title h1');
  var T=titleEl?parseFloat(getComputedStyle(titleEl).fontSize):0, bad=[];
  panel.querySelectorAll('.sec').forEach(function(sec){
    var h=sec.querySelector('h3'); if(!h) return;
    var H=parseFloat(getComputedStyle(h).fontSize);
    sec.querySelectorAll('*').forEach(function(el){
      if(el===h||el.children.length) return;
      var t=(el.textContent||'').trim(); if(!t) return;
      var f=parseFloat(getComputedStyle(el).fontSize);
      if(f>H+0.5) bad.push('"'+t.slice(0,14)+'" '+Math.round(f)+' > 소제목 '+Math.round(H));
    });
  });
  return bad.length?bad:'뒤집힌 곳 없음';
})()
```

2026-08-18 에 홈 · 계획 · 지도 · 모아보기 · 일기 · 일기모음 · 포트폴리오 ·
전시 · 학생고르기와 팝업 두 개를 1920×1080 에서 훑어 **뒤집힌 곳이 없음**을
확인했습니다.

---

## 8-16. 손그림 PNG 의 투명한 여백 재기 (2026-08-18)

계획표 제목 밑줄(`images/여가 계획표 밑줄.png`)을 넣었더니, 그림 파일 둘레의
**투명한 여백까지 자리를 차지해서** 220px 을 먹고 아래 문장을 밀어냈습니다.

여백이 얼마인지는 **눈으로 짐작하지 말고 재세요.** 브라우저 콘솔에 붙여 넣으면
알파값이 있는 칸의 테두리를 찾아 줍니다 :

```js
(async function(){
  var img=await new Promise(function(res,rej){
    var i=new Image(); i.onload=function(){res(i);}; i.onerror=rej;
    i.src=App.uiImage('planLine');            // ← 재고 싶은 그림
  });
  var c=document.createElement('canvas'); c.width=img.naturalWidth; c.height=img.naturalHeight;
  var g=c.getContext('2d'); g.drawImage(img,0,0);
  var d=g.getImageData(0,0,c.width,c.height).data, top=-1, bottom=-1;
  for(var y=0;y<c.height;y++) for(var x=0;x<c.width;x++)
    if(d[(y*c.width+x)*4+3]>12){ if(top<0)top=y; bottom=y; break; }
  return '위 '+(top/c.height*100).toFixed(1)+'% · 아래 '
       + ((c.height-1-bottom)/c.height*100).toFixed(1)+'%';
})()
```

`여가 계획표 밑줄.png` (2172×724) 를 재어 보니 **위 33.8% · 아래 34.4%** 였습니다.
그래서 `--trim:0.335` 로 위아래를 잘라 냅니다 (`css/app.css` 의 `.sheet-line`).

잘라 내는 방법 — `clip-path` 로 가리고, 가린 만큼 **음수 여백으로 당깁니다.**
`clip-path` 만 쓰면 그림이 안 보일 뿐 자리는 그대로 남기 때문입니다.

```css
.sheet-line{
  --w:min(62%, 24rem);
  --trim:0.335;
  width:var(--w);
  clip-path:inset(calc(var(--trim) * 100%) 0 calc(var(--trim) * 100%) 0);
  margin-top:calc(var(--w) / 3 * -1 * var(--trim) - .1rem);
  margin-bottom:calc(var(--w) / 3 * -1 * var(--trim) + .35rem);
}
```

`/ 3` 은 그림 비율(3:1)입니다 — 높이 = 폭 ÷ 3. 그래서 **폭을 바꾸면 여백도
저절로 따라갑니다.** 그림을 다시 그려서 비율이나 여백이 달라지면
`--trim` 과 `/ 3` 을 함께 고치세요.

→ 220px 이 **77px** 로 줄었고, 밑줄 폭은 528px 로 제목 글자(228px)보다 넉넉합니다.

---

## 8-17. 고쳤는데 화면이 그대로일 때 — `index.html` 캐시 (2026-08-18)

`?v=` 숫자를 올려도 화면이 안 바뀌는 일이 여러 번 있었습니다.

**까닭** : `?v=` 는 `index.html` **안에 적혀 있는** css·js 주소를 바꿉니다.
그런데 그 숫자가 적힌 **`index.html` 자체가 브라우저 캐시에 남으면**,
예전 index.html 이 예전 숫자로 예전 파일을 계속 부릅니다.
즉 **`?v=` 는 index.html 자신은 못 바꿉니다.**

**막는 법 (2026-08-18 에 넣었습니다)** — `index.html` 의 `<head>` 에 :

```html
<meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate" />
<meta http-equiv="Pragma" content="no-cache" />
<meta http-equiv="Expires" content="0" />
```

**지금 어느 판을 보고 있는지 확인하는 법** — `학생 고르기` 화면 맨 아래,
`이름을 누르면 …` 글 오른쪽에 **판 번호**가 작은 알약으로 나옵니다
(예 `20260842j`). `index.html` 의 `?v=` 숫자와 같으면 최신 판입니다.
다르면 캐시에 예전 판이 남은 것이니 **Ctrl + F5** 로 강력 새로고침하세요.

> 만드는 쪽 : `App.buildRev` (activities.js). 지금 돌고 있는 스크립트 주소의
> `?v=` 값을 그대로 읽으므로, 따로 적어 둘 필요가 없습니다.

---

## 8-1. 기기별 스크롤 점검 (2026-08-17)

세로 스크롤 없음(규칙 10)이 지켜지는지 **모든 화면 × 5가지 화면 크기**에서 확인했습니다.
표지 · 학생 고르기 · 캐릭터 · 홈 · 계획하GO! · 알아보GO! · 기록하GO! · 모아보GO! · 그림일기.

| 기기 | 크기 | 결과 |
|---|---|---|
| 전자칠판 (FHD) | 1920 × 1080 | 스크롤 없음 |
| 전자칠판 (HD) · 노트북 | 1366 × 768 | 스크롤 없음 |
| 태블릿 (가로) | 1280 × 800 | 스크롤 없음 |
| 아이패드 (가로) | 1024 × 768 | 스크롤 없음 |
| 아이패드 (세로) | 820 × 1180 | 스크롤 없음 (※ 실제로는 쓰지 않는 크기) |

**실제로 쓰는 기기는 위 네 가지(가로)입니다.** 세로 아이패드는 쓰지 않습니다.
그래도 세로에서 깨지지는 않는지 확인해 두었습니다.

**2026-08-17 기능을 모두 넣은 뒤 다시 확인했습니다 — 5가지 크기 모두 스크롤 없음.**

- **선생님 설정만 세로로 넘깁니다.** 한 페이지에 1~13번을 순서대로 두고
  아래로 내리며 설정하도록 **일부러 그렇게 만든 것**입니다 (`.tscroll`). 학생 화면이 아닙니다.
- 내용이 넘치면 세로로 늘어나지 않고 **좌우 쪽으로 나뉩니다** (`C.Stage`, **← →** 방향키).
- 다시 확인하는 방법 : 브라우저 개발자도구(F12) → 기기 크기를 위 표대로 바꾸고
  화면을 하나씩 돌아보며 세로 스크롤 막대가 생기는지 봅니다.

---

## 9-1. 아직 없는 그림 3장 (2026-08-18 기준)

앱이 찾는 그림 **199장** 가운데 **196장이 들어와 있고 3장이 없습니다.**
없는 것은 코드로 그린 SVG 가 대신 나오므로 동작에는 지장이 없습니다.

| 어디 | 파일 이름 |
|---|---|
| 활동 | `images/activities/음악 듣기.png` |
| 시간 | `images/시간/정하지 않아요.png` |
| 준비물 | `images/준비물/스티커.png` |

### 다시 세는 방법

브라우저 콘솔에 붙여 넣으면 **그때그때 없는 것만** 알려 줍니다.
그림을 넣거나 활동을 더한 뒤에 한 번 돌려 보세요.

```js
(async function(){
  function probe(u){ return new Promise(function(res){
    if(!u) return res(false);
    var i=new Image(); i.onload=function(){res(true);}; i.onerror=function(){res(false);}; i.src=u; }); }
  var jobs=[]; function add(g,n,u){ jobs.push({g:g,n:n,u:u}); }
  (App.DATA.activities||[]).forEach(function(a){
    add('활동', a.name, App.activityImage(a));
    (a.children||[]).forEach(function(c){ add('활동(하위)', c.name, App.activityImage(c)); });
  });
  var sup={};
  (App.DATA.activities||[]).forEach(function(a){
    (a.defaultSupplies||[]).forEach(function(x){ sup[x]=1; });
    (a.children||[]).forEach(function(c){ (c.defaultSupplies||[]).forEach(function(x){ sup[x]=1; }); });
  });
  (App.DATA.supplies||[]).forEach(function(s){ sup[(typeof s==='string')?s:s.name]=1; });
  Object.keys(sup).forEach(function(n){ add('준비물', n, App.pickImage('supply', n)); });
  (App.DATA.avatars||[]).forEach(function(a){ add('캐릭터', a.name, App.avatarImage(a)); });
  (App.DATA.partners||[]).forEach(function(p){
    if(p.variants) p.variants.forEach(function(v){ add('사람', p.name+'('+v.name+')', App.partnerImage(p, v.id)); });
    else add('사람', p.name, App.partnerImage(p));
  });
  (App.DATA.moods||[]).forEach(function(m){ add('기분', m.name, App.moodImage(m)); });
  (App.DATA.agains||[]).forEach(function(a){ add('또하기', a.name, App.againImage(a)); });
  (App.DATA.mapStates||[]).forEach(function(m){ add('지도표시', m.name, App.uiImage(m.id)); });
  ['아침','낮','저녁','정하지 않아요'].forEach(function(n){ add('시간', n, App.pickImage('time', n)); });
  ['오늘','내일','날짜 고르기'].forEach(function(n){ add('언제', n, App.pickImage('when', n)); });
  ['교실','집','학교','공원','운동장'].forEach(function(n){ add('장소', n, App.pickImage('place', n)); });
  (App.DATA.corners||[]).forEach(function(c){ add('코너', c.desc, App.pickImage('corner', c.desc)); });
  Object.keys(App.IMAGE_BASE.ui).forEach(function(k){ add('단추·바탕', k, App.uiImage(k)); });
  var miss={}, ok=0;
  for (var i=0;i<jobs.length;i++){
    if(await probe(jobs[i].u)) ok++; else (miss[jobs[i].g]=miss[jobs[i].g]||[]).push(jobs[i].n);
  }
  var out={ 전체:jobs.length, 있음:ok, 없음:jobs.length-ok };
  Object.keys(miss).forEach(function(g){ out[g]=miss[g].join(' · '); });
  return out;
})()
```

⚠ **`App.IMAGE_BASE` 의 경로를 직접 이어 붙이지 마세요.** 키 이름이
`activity`(단수)·`avatar` 라서 `activities`·`avatars` 로 쓰면 전부 없다고 나옵니다.
꼭 `App.activityImage()` · `App.avatarImage()` 같은 **함수**를 쓰세요.

---

## 9. 아직 남은 것

- **준비물 그림은 61장까지 들어왔습니다** (`스티커.png` 한 장만 남음). 자세한 것은 위 9-1
- **그림은 모두 들어왔습니다.** 활동 45개 · 캐릭터 36개 · 함께하는 사람(남/여) ·
  표지 · 벽지 · 여가지도 바탕 · 발자국 · 실내/실외 · 단추 그림까지 빠진 것이 없습니다.
  (`js/data/activities.js` 의 주소로 하나씩 열어 확인했습니다)
- **규격이 조금 어긋난 그림 1개** (동작에는 지장 없습니다)
  - `avatars/형제자매 여.png` — 높이 **78%** (다른 사람은 88%). 발끝은 6%로 맞아 있어
    바닥에는 나란히 서지만 조금 작게 보입니다. 동생이라 일부러 작게 둔 것이면 그대로 두세요
  - 나머지는 모두 맞습니다 : 사람 **높이 88% · 발끝 6%**, 기분 얼굴 **높이 77~78% · 아래 11~12%**
- **일기 3단계(자유쓰기)** 는 여전히 키보드 입력이 필요합니다. 학생에 따라 1·2단계 권장

---

## 11. 2026-08-17 에 한 일

1. **문장을 직접 고쳐 쓸 수 있게** — 낱말로 만들어진 문장이 노란 칸에 뜨고,
   그 칸을 눌러 바로 고쳐 씁니다. 고치면 `고른 낱말로 되돌리기` 가 나옵니다 (`C.SentenceEdit`)
2. **어법 고침** — `재미있했어요` · `화났했어요` → `재미있었어요` · `화났어요`.
   `스티커북 만들기 을 했어요` → `…를 했어요`. `또하고 싶어요` → `또 하고 싶어요`
3. **그림일기 새로 만듦** — 보통의 그림일기 서식 · 원고지 · 따라쓰기 · A4 인쇄 (위 5-1)
4. **그림칸 세 가지 선택** — 내가 고른 그림 / 사진 / 직접 그리기 (위 5-1)
5. **우리 반 활동 더하기** — 활동 고르는 화면에서도, 선생님 설정에서도 (위 4-1)
6. **여가 지도 정리** — 가운데 경계선 · 섬 이름 바 · 발자국이 이름까지 이어짐 (위 3-11)
7. **텃밭 가꾸기** 활동을 실외에 넣음 (그림 기다리는 중)
8. **캐릭터 대신 내 얼굴 사진** — 캐릭터 고르기 화면의 `내 얼굴로 만들기`
   (`facePhotoId` · 사진은 이 기기에만 저장됩니다)
9. **벽지가 안 나오던 문제 고침** (위 8-8)
10. **기기별 스크롤 점검** (위 8-1)
11. **일기 2단계를 한 질문씩** 넘어가게 바꾸고, 낱말을 **큰 그림 카드**로 (위 2-1)
12. **제목을 학생이 고릅니다** — 고른 내용에서 후보를 만들어 줍니다 (2단계 `제목` 칸)
13. **인쇄 모양 4가지 · 3단계는 밑줄** (위 5-1)
14. **모으기 · 일기장 · 오늘의 도전 · 칭찬 반응 · 교실 TV 전시** (위 5-2)
15. **발자국 그림**을 손그림 PNG 로 (`images/발자국.png`)
16. **하위 활동 그림 14개** 를 연결 (영문 id 로 찾던 것을 한글 파일 이름으로)
17. **혼자 · 가족에 남/여 그림** — 학생 성별에 맞는 쪽이 저절로 나옵니다
18. **텃밭 가꾸기 · 실내에서 해요 · 실외에서 해요 · 엄마 · 아빠** 그림 반영
19. **여가 지도 바탕 그림** (`images/여가지도.png`) 연결 (위 6-1)
20. **그림 주소에 캐시 번호** — 같은 이름으로 그림을 바꿔 넣어도 새로 받아옵니다 (위 6)
21. **단추 파스텔 색** — 누구의 여가생활일까요?(노랑) · 내 얼굴로 만들기(파랑) ·
    이 캐릭터로 여가생활 하러 가기(빨강) · 선생님 설정의 캐릭터 고르기(빨강 + 점선)
22. **기록하GO! 위쪽 줄을 두 줄로** — 단계 표시를 아랫줄로 내려 제목이 안 잘립니다
23. **여가 지도 다듬기** — 지도 위 발자국 없앰 · 섬 이름 글자 키움(17→22px) ·
    '나' 는 네모 칸 크기는 그대로 두고 얼굴 74→102px · 이름 13→15px 로 키움
24. **실내에서 해요 / 실외에서 해요** 는 흰 카드 틀 없이 그림만 크게 (`C.Pick bare=`)
25. **얼굴표정 그림 8개** 연결 (`images/얼굴표정/재미있었어요.png` 처럼 끝맺은 꼴 이름).
    기분 고르는 자리·지도·그림일기에 나옵니다 (`C.MoodArt` · `App.moodImage`)
26. **함께하는 사람 그림 규격 통일** — 발끝(아래 여백) 6% · 사람 높이 88%.
    파일 이름이 `선생님-남` 에서 `선생님 남` 으로 바뀌어 코드도 함께 고쳤습니다
27. **제목 정리** — 홈 카드는 `여가 계획하기`(크게) + `계획하GO!`(작게),
    각 창 위쪽 줄에서는 `…GO!` 를 아예 뺐습니다 (위 4-2)
28. **사람 그림 칸을 키움** — 79 → 93px. 칸 비를 그림 원본과 같은 0.64 로 맞춰
    좌우에 빈 자리가 남지 않고 사람이 카드 한가운데에 옵니다
29. **섬 이름 바를 활동 카드 바로 위로 내림** — 예전에는 '나' 칸 맨 위와 나란히
    (화면 꼭대기) 있었는데, 집·나무 그림과 겹쳐 보여서 아래로 내렸습니다.
    `js/screens/map.js` `labelTop` — **첫 줄 카드 위 8px** 에 붙입니다.
    섬 칸 맨 위(`top`)가 아니라 **카드 자리(`placed`)를 기준**으로 잡는 것이 중요합니다.
    카드는 남는 자리에 따라 위아래로 조금씩 움직이므로, 섬 칸 맨 위에 고정하면
    큰 화면에서 카드와 멀어집니다. 위아래로 옮기려면 `LABEL_NUDGE` 숫자만 고치세요
30. **단추에 마우스를 올리면 하얗게 변하던 문제** — `계획 저장하기` · `일기 저장하기`
    처럼 **색이 있는 단추**가 마우스를 올리는 순간 흰색이 되어 글씨가 안 보였습니다.
    원인은 `css/app.css` 의 `.btn:hover{ background:var(--paper-2) }` 였습니다.
    `.panel-action .btn`(파랑)과 **가중치가 같은데 뒤에 적혀 있어서** 색을 덮어썼습니다.
    → 바탕색을 덮어쓰지 않고 `filter:brightness(.96)` 로 **아주 살짝만 어둡게** 합니다.
    어떤 색 단추든 제 색을 지킵니다.
    또 `@media (hover:hover) and (pointer:fine)` 로 감싸서 **손가락·펜(전자칠판)에서는
    아예 켜지지 않게** 했습니다. 터치에서는 한 번 누른 hover 가 눌러붙어 그대로
    하얗게 남아 있었기 때문입니다. 누른 느낌은 `:active` 가 바로 보여 줍니다
31. **섬 이름 글자가 안 보이던 문제** — 글씨가 아니라 **알약 바** 가 문제였습니다 (위 3-11).
    흰 글씨·흰 알약은 둘 다 더 나빠집니다. 글씨를 진하게 + 테두리 진하게 +
    **알약 바깥에 흰 테두리 한 겹**으로 해결했습니다
32. **섬 이름 바를 좌우 바깥쪽으로** — 바탕 그림 발자국이 보이도록 실내는 왼쪽,
    실외는 오른쪽으로 섬 폭의 14% 씩 (`LABEL_OUT`). 위 3-11 참고
33. **캐릭터를 학생 줄마다 고르게** — 예전에는 목록 아래 단추 하나뿐이라
    **맨 마지막에 추가한 학생**의 캐릭터만 골라졌습니다 (위 4번 ★ 참고)
34. **창 안 질문 글자를 제목보다 작게** — `어디에서 할까요?` 가 맨 위 제목
    `여가 계획하기` 보다 오히려 커서 무엇이 큰 제목인지 헷갈렸습니다.
    `.q h2` 를 제목의 **약 73%** 로 맞췄습니다 (1024 → 24px·18px / 1920 → 33px·24px).
    **두 번 줄였습니다** — 82% 로 줄인 뒤에도 너무 커서 오히려 읽기 어려웠습니다.
    더 줄이려면 clamp 의 세 숫자를 **같은 비로** 줄이세요 (하나만 고치면 글자가 튑니다).
    이 한 줄이 계획·일기·캐릭터 고르기·팝업의 질문을 모두 같이 바꿉니다
35. **「나의 여가 일기 — 원고지 작성 규칙」을 고정 규칙으로 정함** (위 5-1-1).
    13개 조항 + 검사 항목. `gridRows()` 를 규칙대로 다시 썼습니다.
    새로 지킨 것 : ①닫는 따옴표·괄호도 줄 첫 칸 금지 ②`?` `!` 뒤 한 칸 비우기
    ③글자 누락·중복·순서를 스스로 검사(`verifyGrid`)
36. **그림일기 3단계 구성을 고정 규칙으로 정함** (위 2-2).
    뼈대(1·2단계 칸 / 3단계 줄)는 이미 맞았고, **단계별 차이가 비어 있던** 것을 채웠습니다.
    - 인쇄 모양을 단계에 있는 것만 내놓기 (1단계 1개 · 2단계 2개 · 3단계 2개)
    - 단계에 없는 모양이 넘어오면 그 단계 기본으로 되돌리기 → 여러 학생을
      한꺼번에 인쇄해도 1단계 학생은 늘 완성된 글로 나옵니다
    - 위쪽 줄에 `2단계 · 따라 쓰거나 스스로 써요` 알약 표시
    - `첫글자만` 은 규칙과 어긋나 **없앴습니다**
37. **원고지 글자를 키움 (그림 희생 0)** — 칸은 79px 인데 글자가 46px 뿐이었습니다.
    `GLYPH_FILL` 58% → **70%** 로 올려 글자만 **55px** 로 커졌습니다.
    그림칸은 그대로입니다. 글자+마침표가 든 칸은 `.two` 로 조금 줄여 칸을 안 넘습니다
38. **최대 줄 7 → 8** — 7줄에서 넘치면 칸수가 늘어 **칸이 오히려 작아졌습니다.**
    같은 긴 글이 **12칸(66px) → 10칸(79px)** 로 커집니다.
    넘치는 줄을 **자르지 않게** 고쳤습니다 (예전에는 학생 글이 사라졌습니다)
39. **시간대를 `오전·오후` → `아침·낮·저녁`** — '오전' 은 학생에게 추상적입니다.
    해가 어디 있는지로 알 수 있는 말이 훨씬 구체적이고 그림으로도 분명합니다.
    `js/screens/plan.js` 의 `TIMES`, `js/lib/korean.js` 의 `App.timeWord`.
    **예전 기록의 `am`·`pm` 도 그대로 읽습니다** (am→아침, pm→낮).
    직접 적은 시각도 `09:30 → 아침 9시 30분` 처럼 갈라 읽습니다
40. **계획 선택지에 그림 붙일 자리를 마련함** — `언제 · 시간 · 장소 · 또하기`.
    `App.pickImage(kind, word)` + `C.PickArt` 를 새로 만들었습니다.
    **보이는 말이 그대로 파일 이름**입니다 (`낮` → `images/시간/낮.png`).
    파일이 없으면 코드로 그린 SVG 가 대신 나와서, **그림을 한 장씩 넣어도
    넣은 것부터 바로 보입니다.** 폴더마다 `README.md` 에 필요한 파일 목록을 적어 두었습니다
    - `images/` — 오늘 · 내일 · 날짜 고르기
    - `images/시간/` — 아침 · 낮 · 저녁
    - `images/장소/` — 19곳 (9곳 완료. **조리실·집이 제일 많이 쓰입니다**)
    - `images/또하기/` — 3개
41. **생각 도움말** — `스스로 쓰기`·`빈 줄` 에서만 칸 위에 나옵니다 (위 2-2)
42. **3단계 쓰는 방법 3가지** — 키보드 · 손글씨 · 종이 (위 2-2).
    키보드를 못 치는 학생도 3단계일 수 있어서 길을 셋으로 열었습니다
43. **날씨를 앱에서 고릅니다** — 그림일기의 기본 항목인데 예전에는 인쇄한 종이에
    손으로 동그라미 치는 방식이라 **기록으로 남지 않았습니다.**
    `js/data/options.js` 의 `D.weathers` 한 곳에 모아, 고르는 화면과 종이가
    **같은 그림**을 씁니다. 고른 것에 동그라미가 쳐지고 나머지는 연해집니다.
    안 골랐으면 넷 다 그대로 나와서 종이에 손으로 칠 수 있습니다
44. **`일어난 시간 · 잠드는 시간` 줄 삭제** — 보통의 그림일기 양식에는 있지만
    **여가 일기**에 기상·취침 시간은 필요하지 않습니다. 46px 을 그림칸·원고지에 돌려주었습니다
45. **저장 뒤 `일기 고치기`** — 예전에는 저장하고 나면 되돌아갈 길이 없어서,
    기분을 잘못 골라도 **선생님 설정 11번까지** 들어가야 했습니다.
    좋아해요·도전·전시 질문 창과 마지막 창에 같은 자리로 넣었습니다
46. **저장소 · 배포 주소 생김** (2026-08-17)
    - 저장소 : https://github.com/jinnie-class/my-leisure-
    - 앱 주소 : https://jinnie-class.github.io/my-leisure-/ (GitHub Pages, main / root)
    - 올리기는 **GitHub Desktop** 으로 합니다. 왼쪽 위 `Current repository` 가
      `나의 여가생활` 인지 확인 → `Commit to main` → `Push origin`
    - `single-file.html` 은 저장소에 넣지 않습니다 (`.gitignore`). 1.5MB 라서
      고칠 때마다 통째로 쌓입니다. 필요하면 `build-single-file.ps1` 로 다시 만드세요
    - 학생 백업 JSON(`나의여가_*.json`) 도 제외했습니다 — **개인정보**입니다

---

## 12. 2026-08-18 에 한 일

47. ★ **큰 화면에서 글자 정렬이 흐트러진 문제** — 코너 카드 제목이
    `여가 / 계획하기` 로 두 줄이 되었습니다. 원인은 위 **8-4-2** 에 적었습니다
    (`clamp` 최댓값을 `rem` 으로 두면 루트 글자가 커져 상한이 없는 것과 같음).
    `.corner .desc` 를 `clamp(1.05rem, 1.7vh + .5rem, 28px)` + `white-space:nowrap` 으로.
    1116×1010 · 1164×1229 · 1400×1500 세 크기에서 네 제목 모두 한 줄임을 재어 확인했습니다

48. **고르면 저절로 넘어가던 것을 없앴습니다** (계획하기 전체)
    - 예전 : 고르면 0.45초 뒤에 다음 질문으로 저절로 넘어감 (`pickAndGo`)
    - 지금 : **고르기와 넘어가기를 나눕니다** (`pick`). 골라서 확인하고
      아래 단추를 눌러야 넘어갑니다. **모든 질문이 같은 방식**이라
      학생이 규칙을 하나만 익히면 됩니다
    - `어디에서` · `무엇을` 두 질문에만 단추가 없었어서 새로 넣었습니다
    - 쪽 넘기는 단추를 `다음 활동 보기` → **`활동 더 보기`** 로.
      아래 단추도 `다음` 이라 한 화면에 `다음` 이 둘이었습니다 (하는 일은 아주 다름)

49. **계획표 짜임새** — 활동 그림이 제목 옆 오른쪽 위에 84px 로 붙어 있어
    제목을 가리고 무슨 활동인지 알아보기 어려웠습니다.
    제목(가운데) → 문장(**가운데 정렬**) → **표(왼쪽) + 큰 그림(오른쪽 200px)** 로 바꿨습니다
    (`.sheet-body` · `.sheet-art`). 좁은 화면에서는 그림이 표 아래 가운데로 내려갑니다

50. **`계획을 잘 저장했어요` 아래 작은 설명 글씨 삭제** — 작아서 학생은 읽지 않습니다

51. **질문바 글자를 제목의 73% → 85% 로** (위 34번을 다시 고쳤습니다).
    73% 는 아래 설명 글씨(1rem)와 비슷해져서 **질문으로 보이지 않았습니다.**
    지금은 제목보다 작고 설명보다 큰 **그 중간**입니다
    (1116×1010 에서 제목 28px · 질문 23px · 본문 17px)

52. **여가 지도의 선생님 도구를 아래로 내렸습니다** — `찾아보기` · `지도 도움말` 이
    지도 위에 있어서 그만큼 지도와 활동 그림이 눌려 작아졌습니다.
    맨 아래 한 줄(`.map-foot`)에 **왼쪽 찾아보기 · 가운데 모아보기 · 오른쪽 도움말** 로
    옮겼습니다. 양옆을 `1fr` 로 똑같이 나눠서 도구가 꺼져 있어도 가운데가 안 흔들립니다.
    → 도구 한 줄(약 48px)만큼 지도가 커져 **활동 카드가 한 변에 25px 늘었습니다.**
      1116×1010 에서 재어 보니 지도 높이 637 → 693, 카드 **156px → 181px**.
      카드 크기는 창 크기에 따라 달라집니다 (1400×1500 에서는 230px).
      다시 재는 방법 : 지도 위에 40px 짜리 빈 칸을 끼워 넣고 `resize` 를 일으켜
      카드 폭을 비교하면 됩니다

53. ★ **도장을 하나 채우면 폭죽을 터뜨립니다** (`C.Fireworks` in `common.js`)
    - 색종이는 그냥 작은 네모이고 `--dx` · `--dy` 로 날아갈 곳을 정합니다.
      **그림 파일도 소리 파일도 더 받지 않습니다**
    - `pointer-events:none` 이라 폭죽 위로도 단추를 누를 수 있고 1.7초 뒤 사라집니다
    - `prefers-reduced-motion` 을 켠 학생에게는 조용히 나타났다 사라집니다
    - **학생마다 한 번만** : 몇 번째 도장까지 축하했는지를 `student.stampCheered` 에
      적어 둡니다. 지도를 다시 열어도 또 터지지 않습니다
    - 축하 창에서 `어떤 활동인지 보기` 를 누르면 그 도장의 활동 목록으로 이어집니다

54. **도장 창을 한눈에** — 넓은 창(`wide`)을 빼서 폭을 반으로 줄이고,
    발자국을 84px → `clamp(120px,18vh,170px)` 로 키우고,
    `이때까지 해본 활동이에요` 를 작은 글씨에서 1.05rem 으로 올렸습니다 (`.stamp-cap`)

55. **모아보기 초록 단추가 여가 지도로 돌아갑니다** — 홈으로 나가 버리면
    지도를 다시 찾아 들어가야 했습니다. 홈은 왼쪽 위 집 단추로 갑니다

56. **홈 제목 밑 `내가 좋아하는 여가를 찾아보아요` 삭제** — 네 코너 그림만으로
    충분하고, 그만큼 코너 카드가 크게 보입니다 (읽어주기 글에는 그대로 남겼습니다)

57. **준비물 그림 62장 목록을 정리** → `images/준비물/README.md`.
    쓰이는 활동 수가 많은 것부터 1~4순위로 나눴습니다 (위 9번)

58. **엄마 · 아빠 읽어주는 소리가 이상했던 문제** (위 8-13 에 자세히)
    낱말 하나만 던져 주던 것을 짧은 문장으로 바꿨습니다
    (`App.partnerSpeech` / `App.partnerSpeechPast` in `js/lib/korean.js`).
    계획은 `엄마와 함께 할 거예요`, 일기는 `엄마와 함께 했어요`,
    `혼자` 는 `혼자 할 거예요` (혼자 함께 는 말이 안 됩니다).
    카드를 눌렀을 때와 스피커를 눌렀을 때가 서로 다른 말을 읽고 있어서
    계획 · 일기 · 뼈대 만들기 **네 곳을 모두 맞췄습니다**

59. **계획표 그림을 남은 빈 곳 가운데로** — `.sheet-body` 를 `1fr auto` 에서
    **`auto 1fr`** 로. 예전에는 그림 칸의 폭이 그림 폭과 똑같아서
    `justify-self:center` 가 가운데로 놓을 빈 자리가 없었습니다

60. **여가지도 바탕 그림을 새 그림으로** (1536×1024 — 크기가 같아서 자리 계산 그대로)

61. ★ **홈 `나의 여가` 제목의 위아래 여백** (위 8-14 에 자세히)
    지웠던 설명 한 줄(`내가 좋아하는 여가를 찾아보아요.`)이 차지했던 만큼을
    위아래로 똑같이 둡니다. 단, **남는 자리의 절반을 넘지 않습니다** —
    `C.Stage` 가 재어 넣어 주는 `--slack` 을 씁니다.
    · 계획 없는 학생(1116×1010) : 남는 자리 247px → 위아래 **25px** 씩
    · 계획 카드가 있는 학생 : 남는 자리 12px → 위아래 **6px** 씩 (안 갈라짐)

62. **여가지도 선생님 도구를 맨 위 줄로** — 52번에서 지도 아래로 내렸는데,
    학생이 쓰는 큰 단추와 섞였습니다. 이제 **맨 위 줄 스피커 옆**에 있습니다
    (집 단추 · 찾아보기 · 지도 도움말 · 읽어주기 · 이름표 — 한 줄에 다 들어갑니다).
    맨 아래에는 `내가 표시한 활동 모아보기` 하나만 가운데에 남았습니다.
    `찾아보기` 를 누르면 도구 칸이 요약 아래에 열립니다 (열려 있는 동안만
    지도가 693 → 567px 로 줄어듭니다)

63. **장소 · 준비물을 고르면 소리가 나게** (위 8-13) — 이 두 화면만
    고르는 소리가 없었습니다. `speakText` 은 있었지만 그것은 작은 스피커
    아이콘용이고, 눌렀을 때 읽어 주는 코드가 빠져 있었습니다

64. **계획표 제목을 아래 문장과 구분** — 제목도 문장도 똑같은 검은 글씨라
    어디가 제목인지 몰랐습니다. 계획하기 코너 색을 진하게 한 글자색
    (`#1c7fa8`) + 제목 아래 **짧은 하늘색 밑줄**.
    색만으로 나타내지 않는 것이 규칙이라 밑줄을 함께 둡니다 —
    색을 잘 못 가리는 학생과 흑백 인쇄에서도 제목으로 보여야 합니다

65. **모아보기에서 들어간 화면의 말 앞에 표시 그림** — `해봤어요` 앞에
    발자국, `좋아해요` 앞에 하트처럼. 네 칸 화면에서는 그림을 보고
    골랐는데 들어오면 글자만 남아서 이어지지 않았습니다 (`.q-art`)

66. **여가지도 아래 요약 흰 바 삭제** — `나는 10가지 여가활동을 해봤어요.` 바가
    바로 위 도장판(`10 / 30곳`)과 **같은 것을 두 번 말하고** 있었습니다
    (규칙 7 — 중복 금지). 요약 문장 자체는 살아 있습니다 —
    맨 위 읽어주기와 `지도 도움말` 창에서 네 줄을 다 들려주고 보여 줍니다.
    → 지도가 693 → **745px**, 활동 카드가 181 → **207px** 로 커졌습니다

67. ★ **전체화면에서 본문이 소제목보다 커지던 문제** (위 8-15) —
    소제목은 `1.1rem` 한 값인데 본문은 `vh` 로 자라서 화면이 커지면
    뒤집혔습니다. 네 칸짜리 **글자 크기 사다리**를 만들어 맞췄습니다.
    1920×1080 에서 아홉 화면과 팝업 두 개를 훑어 뒤집힌 곳이 없음을 확인

68. **홈 `나의 여가` 제목 아래 간격** — 위보다 아래를 넉넉하게 했습니다
    (아래 1 · 위 0.55). 큰 글자는 줄 상자 안에서 위쪽에 여유가 있어서
    위아래를 똑같은 숫자로 주면 아래가 더 붙어 보입니다.
    남는 자리를 넘지 않게 막아 두어(0.35 + 0.65 = 1) 쪽이 갈라지지 않습니다.
    · 1920×1080 카드 없는 학생 : 위 16px · 아래 29px · 1쪽
    · 1116×1010 카드 있는 학생 : 위 5px · 아래 10px · 1쪽 (저절로 줄어듦)
    ※ 1024×768 에서 계획·도전 카드가 다 있으면 2쪽이 되는데,
      이것은 여백과 상관없이 **예전부터 그랬습니다** (여백을 0 으로 놓고 재어 확인)

69. **계획표 제목 밑줄을 손그림으로** — 코드로 그린 짧은 하늘색 막대를
    선생님이 그린 `images/여가 계획표 밑줄.png` 로 바꿨습니다
    (왼쪽에 수첩과 연필, 오른쪽에 별). 제목 글자 228px 에 밑줄 528px.
    그림 파일이 없으면 예전 막대가 그대로 나옵니다 (`:not(:has(+ .sheet-line))`).
    그림 둘레의 투명한 여백을 잘라 내는 방법은 위 8-16 을 보세요

70. ★ **홈이 까닭 없이 2쪽으로 갈라지던 문제** (위 8-14) —
    제목 여백(`--slack`)을 잰 값 그대로 쓰다 보니, 그 여백이 내용을 밀어
    `오늘의 도전` 이 2쪽으로 넘어가고 1쪽에는 큰 빈자리만 남았습니다.
    이제 **잰 값 → 절반 → 4분의 1 → 0** 으로 줄여 가며 넘치지 않는 값을 고릅니다.
    1139×989 · 1139×760 두 크기에서, 갓 만든 학생과 계획·도전이 다 있는
    학생으로 각각 재어 확인했습니다.
    ※ 1139×760 처럼 낮은 화면에서 계획·도전이 다 있으면 여전히 2쪽입니다.
      그때는 여백이 0 이므로 **여백 탓이 아니라 내용이 정말 안 들어가는 것**입니다.

71. **홈 제목 아래 간격을 한 줄 넘게** — 아래 1.5줄 · 위 0.6줄
    (1139×989 에서 위 15px · 아래 38px)

72. **계획표 제목 밑줄을 다시 코드로 (글자 길이에 맞춰서)** — 69번에서 넣은
    손그림 PNG 를 되돌렸습니다. 그림은 글자 **아래**에만 놓을 수 있어서
    글자를 덮거나 밀어냈고, 길이도 글자와 따로 놀았습니다.
    지금은 `width:fit-content` 로 제목 칸을 글자만큼만 좁히고(가운데는
    좌우 `margin:auto`), 밑줄을 그 칸의 `width:100%` 로 긋습니다.
    → 제목이 길어지면 밑줄도 같이 길어집니다 (재어 보니 둘 다 176px).
    ※ `images/여가 계획표 밑줄.png` 은 지우지 않고 두었습니다.
      지금은 어디에서도 부르지 않습니다

73. **반려동물 활동 준비물에 `사료` 추가** — 실내·실외 두 활동 모두.
    반려동물을 돌보려면 사료가 먼저인데 빠져 있었습니다

74. **아침 · 낮 · 저녁 그림을 장면 카드로** — 새 그림이 아이콘이 아니라
    **풍경 장면**(512×341, 3:2)이라, 80×80 정사각 칸에 담으니 3분의 2만
    보이고 위아래에 빈 띠가 생겼습니다.
    `C.PickGrid` 에 `scene` 을 더해 칸을 **3:2 로 넓혔습니다**
    → 80×80 에서 **226×150** 으로 커졌습니다 (1024×768 에서도 218×145, 한 쪽).
    ※ `C.Art` 가 인라인으로 `object-fit:contain` 을 주므로,
      장면을 꽉 채우려면 CSS 에서 `object-fit:cover !important` 가 필요합니다.
    ※ `정하지 않아요` 는 코드 그림이라 `contain` 으로 두어 찌그러지지 않습니다.


75. ★ **장소가 5곳만 나오던 문제** — 코드 안에 `교실 · 집 · 학교 · 공원 · 운동장`
    다섯 곳이 박혀 있었습니다. `images/장소/` 에는 그림이 **19장** 있어서
    그려 둔 그림 14장이 화면에 나오지도 못했습니다.
    `D.places` (options.js) 에 19곳을 넣고, 활동에 어울리는 곳을 맨 앞에 두고
    한 쪽에 10곳씩 보여 줍니다 (5칸 × 2줄, 활동 고르기와 같은 쪽 넘기기).
    `직접 쓰기` 는 마지막 쪽에만 붙습니다.
    → 여기에 장소를 더하려면 **같은 이름의 그림도 함께** 넣어야 합니다.

76. **고르기 카드에서 읽어주기를 글자 오른쪽 옆으로** — 예전에는 글자 아래에
    따로 한 줄을 차지해서 카드 높이를 먹고 그림이 작아졌습니다.
    `.pick-name` 한 줄에 글자와 나란히 놓고 크기도 44 → 26px 로 줄였습니다.

77. **장소 그림 키우기는 그 화면만** (`C.PickGrid` 의 `big`) —
    처음에 `.pick .thumb` 를 통째로 키웠더니 `누구와 할까요?` 의 사람 카드
    7장이 흰 칸을 15px 넘겨 글자가 잘렸습니다.
    고르기 카드는 화면마다 개수와 줄 수가 달라서, **한 곳을 키우면 다른 곳이
    넘칩니다.** 키울 화면만 골라 `big` 을 붙이세요.
    → 장소 그림칸 96 → **115px**

78. ★ **한 쪽인데 높이를 잠가서 내용이 잘리던 버그** (`C.Stage`) —
    `measure()` 가 '넘친다' 고 보고 높이를 지금 크기로 못박은 뒤 쪽이 하나로
    나오면, 넘친 부분이 **잘린 채 넘겨 볼 방법도 없었습니다.**
    쪽이 하나면 잠금을 풀도록 고쳤습니다 (`if (n <= 1) el.style.height = ''`).

79. **사람 카드 상반신 준비** — `images/avatars/README.md` 에 규격을 적었습니다
    (512×512 정사각 · 가슴 위). 그림칸이 66 → 104px, 얼굴이 23 → 약 57px.
    ⚠ **CSS 는 아직 바꾸지 않았습니다.** 전신 그림이 든 채로
    `.thumb.portrait` 를 `aspect-ratio:1` 로 바꾸면 사람 카드가 15px 넘칩니다.
    상반신 12장을 다 넣은 뒤에 그 한 줄을 고치세요.
---

80. ★ **학생 이름이 한 글자씩 세로로 깨지던 문제** (전체화면) —
    `.stu-grid` 칸이 `220px` 로 못박혀 있었는데, 이 앱은 화면이 커지면
    루트 글자도 같이 커집니다. 글자만 커지고 칸은 그대로라 이름이 넘쳐
    `박/민/영` 처럼 한 글자씩 줄바꿈됐습니다.
    · 칸을 `minmax(min(100%,16rem),1fr)` 로 (글자를 따라 넓어집니다)
    · `.stu-card-name` 에 **`white-space:nowrap`** 을 더했습니다.
      `min-width:0` 만 있고 `nowrap` 이 없으면 한 글자씩 깨집니다
    · 이름·표시 글자도 px 로 상한을 두었습니다 (8-4-2 와 같은 까닭)

81. **판 번호를 화면에 보이게** (위 8-17) — `학생 고르기` 맨 아래에
    작은 알약으로 나옵니다. '고쳤는데 그대로' 일 때 **캐시에 예전 판이
    남은 것인지** 한눈에 알 수 있습니다.
    `index.html` 에 캐시 금지 meta 세 줄도 넣었습니다

82. ★★ **계획을 세우는 동안 '지금까지 만든 한 문장' 을 맨 위에** —
    선생님 제안입니다. `누구와 할까요?` 를 고르다 보면 '앗, 내가 뭐 하기로
    했지?' 하고 앞에서 고른 것을 잊어버립니다.
    이제 흰 칸 맨 위 한 줄에 **왼쪽 `앞 질문으로` · 오른쪽 자라나는 문장**이
    나옵니다 (`.plan-top` · `.plan-sofar`).
    · 활동 고름 → `나는 곤충을 키워 볼 거예요 …`
    · 사람 고름 → `나는 엄마와 곤충을 키워 볼 거예요 …`
    · 다 고름   → `나는 오늘 엄마와 곤충을 키워 볼 거예요.`
    ▸ **답한 것만** 넣습니다 (`passed(key)`). 날짜는 오늘로 미리 채워져 있어서,
      묻기 전에 넣으면 고르지도 않은 '오늘' 이 문장에 나옵니다.
    ▸ 한 줄로만 둡니다 (`white-space:nowrap` + `…`). 두 줄이 되면
      그만큼 질문 자리를 먹습니다.
    ▸ 이것이 생겨서 **`고른 활동 : 곤충 키우기` 줄을 없앴습니다.**
      그 화면에서만 보였고 글자도 커서 자리를 많이 먹었으며,
      `다시 선택하기` 는 다른 활동을 누르면 그만이라 필요 없었습니다.
    ▸ `계획을 확인해요` 화면에는 두지 않습니다 — 바로 아래 계획표에
      똑같은 문장이 이미 크게 나와서 같은 말을 두 번 하게 됩니다.

83. **날짜 · 시간대 카드의 읽어주기를 글자 아래에 크게** (`bigSpeak`) —
    다른 화면은 글자 옆에 작게(26px) 붙이지만, 이 두 화면은 카드가 크고
    글자 아래에 빈 자리가 남아서 작게 두면 눌러 보고 싶지 않을 만큼
    초라했습니다. 화면 맨 위 읽어주기(60px)와 비슷한 **56px** 로 두었습니다.

84. **계획표 점선 칸 안을 파스텔 노랑으로** (`#fffbe8`) —
    흰 종이에 흰 칸이라 문장이 눈에 안 들어왔습니다. 점선 안쪽만 칠합니다.


85. **학생 고르기 화면의 설명 줄 삭제** — `이름을 누르면 여가 탐험 캐릭터를
    고를 수 있어요.` 를 없앴습니다. 이름을 누르면 바로 알 수 있는 것을
    글로 또 적고 있었습니다.
    판 번호는 **선생님 설정 맨 아래**로 옮겼습니다 (학생 화면에는 안 보입니다).

86. **질문 바와 그 아래 그림 사이에 숨 쉴 자리** (`.q + .pick-grid`) —
    바짝 붙어 있어서 질문과 답이 한 덩어리로 보였습니다. 11px 띄웠습니다.

87. **실내 · 실외 카드도 읽어주기를 글자 아래 크게** (`bigSpeak`) —
    41px 로, 화면 맨 위 읽어주기(60px)와 비슷해졌습니다.

88. ★ **`한 파일 판` 이라는 표시** — 선생님 설정의 판 번호가
    `20260845j` 처럼 나오면 폴더 판(index.html)이고,
    **`한 파일 판`** 으로 나오면 `single-file.html` 을 열고 있는 것입니다.
    한 파일 판은 **따로 만들어 두는 사본**이라 고친 것이 바로 반영되지
    않습니다 — `build-single-file.ps1` 을 다시 돌리고 그 파일을 다시 열어야
    새로워집니다. '고쳤는데 그대로' 의 또 다른 원인입니다 (위 8-17).
## 10. 요구사항 20가지 점검 (2026-08-16 기준)

89. ★ **질문 알약을 가운데에서 왼쪽으로** — 어느 쪽이 나은지 재어 보고
    정했습니다 (선생님 요청).
    · 가운데 : 질문 길이가 화면마다 달라 **글 시작점이 49px 씩 움직임**
      (1024×768 에서 왼쪽 끝 341~390px)
    · 왼쪽   : 늘 **18px 고정**, 게다가 **아래 첫 카드의 왼쪽 끝과 정확히 일치**
    글자를 익히는 학생은 화면이 바뀔 때마다 '글이 어디서 시작하지?' 를
    다시 찾아야 합니다. 왼쪽이 눈이 덜 움직이고 세로 선도 하나로 정리됩니다.
    ※ 16번(가운데 배치)을 뒤집은 것입니다. 되돌리려면 `.q-bar` 의
      `justify-content` 를 `center` 로 바꾸면 됩니다.

90. **질문 알약을 파스텔 분홍으로** (`#ffeef4` · 테두리 `#f3c3d6`) —
    흰 알약이 흰 배경에 묻혀 '질문' 으로 눈에 띄지 않았습니다.
    글자는 진한 먹색 그대로라 대비가 충분하고, **색만으로 뜻을 나타내는 것이
    아니라 눈에 띄게 하려는 것**이라 색을 못 가려도 잃는 정보가 없습니다.

91. **`앞 질문으로` 를 화살표만 있는 동그란 단추로** —
    맨 위 한 줄에서 자리를 많이 먹었고, 옆의 문장 바와 글자가 두 덩어리라
    어수선했습니다. 되돌아가는 화살표는 학생도 이미 아는 모양입니다.
    ▸ 글자는 지웠지만 **뜻은 `aria-label` · `title` 에 그대로** 남깁니다
      (화면 낭독기와 마우스를 올린 선생님에게는 그대로 보입니다).
      하위 활동 화면에서는 `다른 활동 고르기` 로 바뀝니다.

92. **여가 지도 선생님 도구를 도장판 아래로** — 맨 위 줄에 두었더니
    (집 단추 · 찾아보기 · 지도 도움말 · 읽어주기 · 이름표) 그 줄이 빽빽해
    답답해 보였습니다. 도장판 아래는 원래 비어 있던 자리라
    **지도를 줄이지 않고도** 둘 곳이 생깁니다.
    왼쪽 `찾아보기` · 오른쪽 `지도 도움말` (`.map-toolrow`).
    ※ 이 도구는 62번에서 지도 아래 → 맨 위 줄로 옮겼던 것을 다시 옮긴 것입니다.
      세 번째 자리이고, 지금 자리가 가장 나은 것으로 보입니다.

93. **알림 알약이 화면 맨 아래로 뚝 떨어지던 것** — `저장했어요` 같은 알림이
    `bottom:5.2rem` 에 나왔는데, 그 자리는 **큰 단추 바가 있는 곳**이라
    단추 위로 떨어졌다 사라지는 것처럼 보였습니다.
    **맨 위 줄 아래 가운데**(`top:7.6rem`)로 옮기고, 살짝 내려오며 나타나게
    했습니다 ('움직임 줄이기' 를 켠 학생에게는 그냥 나타납니다).
    ▸ `7.6rem` 인 까닭 : 맨 위 줄이 가장 낮은 화면이 **일기**(아래끝 110px =
      6.47rem)이고, 나타날 때 10px 위에서 내려오므로 그만큼도 봐야 합니다.
      홈 · 계획 · 일기 · 지도에서 맨 위 줄·큰 단추와 안 겹침을 재어 확인했습니다.

94. **`계획을 잘 저장했어요` 바 삭제** — 저장을 누르면 알림이 뜨고 소리로도
    읽어 주며, 그 아래 계획표까지 보입니다. 바를 하나 더 두면 **같은 말을
    세 번** 하는 셈이었습니다 (규칙 7 — 중복 금지). 그만큼 계획표가 커집니다.

95. **장소를 한 쪽에 10곳 → 6곳으로** — 10곳은 한눈에 훑기에 너무 많았습니다.
    6곳이면 `무엇을 할까요?` 와 **같은 개수**라, 학생이 '한 화면에 여섯,
    더 있으면 넘긴다' 는 규칙을 하나만 익히면 됩니다.
    19곳 → 4쪽 (4곳씩이면 5쪽이라 넘기는 횟수가 늘어납니다).
    3칸 × 2줄로 놓아 카드가 넓어졌고 **그림도 115 → 159px** 로 커졌습니다.
    ⚠ 여기서 두 가지를 배웠습니다 (`.pick-grid.big .thumb` 주석 참고) :
    · 그림을 `19vh` 로 키웠더니 **두 줄이 안 들어가 좌우 3쪽으로 갈라졌습니다**
      (1쪽 질문 · 2쪽 그림 · 3쪽 쪽넘기기). `16vh` 가 한계입니다.
    · 읽어주기를 글자 아래 크게(`bigSpeak`) 두면 카드마다 54px 을 더 먹어
      역시 두 줄이 안 들어갑니다. **장소는 글자 옆 작은 읽어주기**로 둡니다.
    → 두 줄짜리 화면에서 한 줄에 쓸 수 있는 높이는
      `(흰 칸 − 질문 − 쪽넘기기) ÷ 2` 뿐입니다. 이것을 먼저 재고 키우세요.

96. **뒤로 가는 화살표를 진짜 화살표로** — 꺾쇠(`<`)만 있어서 무엇을 뜻하는지
    잘 안 보였습니다. **긴 줄(몸통) + 머리**가 함께 있어야 누가 봐도
    화살표로 읽힙니다 (`js/data/icons.js` 의 `uiIcon.back` · `next`).

97. **앞 질문으로 화살표를 선생님이 그린 그림으로** (`images/화살표.png`) —
    코드로 그린 화살표 대신 파란 화살표 그림을 씁니다.
    ▸ **동그란 틀 없이 그림만** 둡니다. 처음에는 흰 동그라미 단추 안에
      넣었는데(`uiInset`), 화살표 자체가 이미 또렷해서 동그라미가 오히려
      화살표를 작게 만들었습니다. 틀을 없애니 36 → **60px** 이 되었습니다.
    ▸ 글자는 없지만 뜻은 `aria-label` · `title` 에 그대로 있습니다
      (`앞 질문으로` / 하위 활동에서는 `다른 활동 고르기`).
    ▸ 그림 파일이 없으면 코드로 그린 화살표가 대신 나옵니다.
    ▸ 한 파일 판에도 넣도록 `build-single-file.ps1` 에 `back` 을 더했습니다.

98. ★ **계획 문장에 나눔바른펜** (`fonts/NanumBarunpenR.otf`) —
    계획 문장이 '학생이 쓴 글' 처럼 읽히게 펜 글씨로 바꾸고 크기도 키웠습니다
    (21 → **26px**, 상한 34px).
    ▸ **문장에만** 씁니다 (`.sentence` · `.plan-sofar`). 단추 · 질문 ·
      이름표에는 쓰지 않습니다 — 펜 글씨는 획이 얇아 작은 글씨나 단추
      글씨로는 또렷함이 떨어집니다. 문장은 크게 쓰므로 괜찮습니다.
    ▸ 두께를 800 → **700** 으로 낮췄습니다. 펜 글씨는 굵게 하면 뭉개집니다.
    ▸ `font-display:swap` — 글꼴을 받는 동안 기본 글꼴로 먼저 보여 줍니다.
    ⚠ **글꼴 목록 안에 `inherit` 을 쓰면 안 됩니다.**
      처음에 `font-family:'NanumBarunpen', var(--font, inherit), sans-serif`
      로 적었더니 **규칙 전체가 무효**가 되어 글꼴이 아예 안 바뀌었습니다.
      뒤에 오는 기본 글꼴은 **이름을 그대로** 적어야 합니다.
    ※ 파일이 5.1MB 입니다. 인터넷(GitHub Pages)으로 여는 첫 순간에는 기본
      글꼴로 보이다가 바뀝니다. 학교에서 폴더로 열면 곧바로 나옵니다.
      한 파일 판에는 넣지 않았습니다 — 넣으면 그 파일이 7MB 가 됩니다.
      한 파일 판에서는 기본 글꼴로 나옵니다.


99. **그림일기 양식 글자도 나눔바른펜** — `.pd-sheet` 한 곳에 글꼴을 주면
    원고지 칸 글자 · 날짜 · 날씨 · 제목 · 따라쓰기까지 자식이 모두
    물려받습니다. 손으로 쓰는 종이를 흉내 낸 곳이라 잘 어울립니다.

100. **`자석을 이용한 놀이` → `자석놀이`** (그림 파일 이름도 `자석놀이.png` 로)

101. ★★ **맨 위 줄의 뜻을 셋으로 나눔** (선생님 제안) —
     · **왼쪽 파란 화살표** → 앞 화면으로 (`onBack`)
     · **코너 제목**(`여가 계획하기` …) → 누르면 **홈**(코너 네 개) (`onTitle`)
     · **홈에서는** 제목 자리에 **`나의 여가` 글자 그림** → 누르면 **표지**
     화살표는 제목 앞 파란 세로줄보다 **더 앞**에 옵니다 — '뒤로 → 제목'
     순서로 눈이 흐릅니다. 네 코너(계획·지도·일기·포트폴리오)와
     그림일기 · 일기 고치기 · 모아보기에 모두 넣었습니다.
     → 위에 비어 있던 자리가 쓰임새를 갖고, 어디로 가는지가 분명해집니다.

102. **만드는 중 문장 바를 키움** — 화살표가 제목 앞으로 가면서 그 줄이
     통째로 비었습니다. 그 자리를 문장이 씁니다 (글자 16 → **22px**,
     활동 이름 17px 보다 큽니다).

103. **지도 도구 줄 가운데 정렬 + 배율 5%씩** — 예전에는 20%씩 뛰어서
     한 번에 너무 크게 바뀌었습니다. 도움말 창의 표시 칩 줄과 점선 안 글도
     가운데로 맞췄습니다.

104. ★ **모아보기 목록을 네모 카드 4장씩으로** (선생님 제안) —
     긴 가로 바에 그림과 글자가 왼쪽에 작게 몰려 있어 알아보기 어려웠습니다.
     카드로 만드니 **그림이 108px** 로 커지고 몇 개인지도 한눈에 보입니다.
     4장이 넘으면 `앞으로` · `더 보기` 로 넘깁니다.
교사 프로필 생성 → 일기 수준 설정 → 실내/실외 선택 → 활동 선택 → 계획 저장 →

105. **흰 칸 안의 화살표 삭제** — 앞으로 가는 화살표를 맨 위 줄로 올렸는데
     흰 칸 안에도 그대로 있어서 **화살표가 둘**이 되었습니다 (규칙 7).
     흰 칸 맨 위에는 **문장만** 남기고, 맨 위 화살표가 `앞 질문으로` ·
     `다른 활동 고르기` 를 모두 맡습니다 (`onBack=back`).
     첫 질문에서는 `나의 여가로` 가 됩니다.

106. **`나의 여가` 글자 그림을 이름 알약과 같은 키로** (52px) — 처음에는
     58px 이라 오른쪽 이름표보다 커 보였습니다.

107. ★ **`오늘의 도전` 은 오늘 계획이 없을 때에만** —
     · 뜻으로 : 오늘 할 일을 이미 정한 학생에게 다른 활동을 또 권하면
       방금 세운 계획과 서로 다툽니다. 도전은 '오늘 뭘 하지?' 하는
       학생에게 건네는 말입니다.
     · 자리로 : 도전 칸이 180px 입니다. 계획 카드까지 있으면 내용이
       811px 이 되어 흰 칸(627px)을 넘겨 **홈이 두 쪽으로 갈라졌습니다.**
     ⚠ `doneAll`(모든 활동을 해봤어요)은 **오늘 계획 때문에 도전이 안 나오는
       것과 정말로 다 해본 것을 가려야** 합니다. 그러지 않으면 계획을
       세운 날마다 '정말 대단해요' 가 잘못 나옵니다.
     → 1113×1122 · 1024×768 두 크기에서 계획 있는 학생/없는 학생 모두 1쪽 확인

---

## 13. 2026-08-19 ~ 20 에 한 일

오늘은 **길 찾기 · 3단계 통일 · 여가지도 두 걸음** 세 가지가 큰 줄기입니다.
커밋 40개. 자세한 까닭은 커밋 글과 코드 주석에 함께 적었습니다.

### 13-1. ★ 파란 화살표를 **앱이 지나온 길**로 (108번)

화면마다 `onBack` 에 `p.nav('home')` 이라고 **손으로 적어** 두었습니다.
그래서 그림일기 → 일기 고치기 에서 화살표를 누르면 그림일기가 아니라
홈으로 튀었습니다. 화면이 늘 때마다 같은 실수가 되풀이됩니다.

→ `js/app.js` 에 **지나온 길**(histRef, 20칸)을 두고 `App.navBack` / `p.back`
을 만들었습니다. 화살표는 그 길을 되짚습니다.

**새 화면을 만들 때는 `p.back()` 만 쓰세요.** 갈 곳을 적지 마세요.

⚠ 길을 쌓는 일을 `setState` 안에서 하면 React 가 그 함수를 두 번 불러
   같은 길이 두 번 쌓입니다. 밖에서 쌓습니다.

### 13-2. ★ 1·2·3단계가 **같은 일기 뼈대**를 쓰도록 (109번)

세 단계가 서로 다른 것을 물었습니다. **3단계에는 장소가 아예 없었고**,
2단계는 누구와·기분만 물어 날짜·장소가 통째로 빠져 있었습니다.

공통 뼈대 여섯 :
  **언제 · 누구와 · 어디에서 · 무엇을 · 기분 · 또 하고 싶나**

`boneBody()` 하나가 세 단계의 여섯 화면을 다 그립니다. 뒤에 붙는 것만 다릅니다.
  1단계 그림·확인 / 2단계 기억·다음에·제목·그림·확인 / 3단계 일기 쓰기·제목·그림·완성

- 장소는 세 단계 모두 **19곳**에서 고릅니다 (6곳씩 4쪽)
- 3단계는 각 뼈대 단계에서 그림도 고르고 **한 줄씩 글도** 씁니다
- **그림은 골라야** 넘어갑니다. **글 칸은 비어도** 넘어갑니다 —
  여섯 칸을 다 채워야 하면 한 칸에서 막힌 학생이 일기를 못 끝냅니다.
  대신 마지막에 **안 쓴 칸을 모아 알려 주고**, 누르면 그 질문으로 갑니다

⛔ 뼈대 차례를 바꾸지 마세요. `picsSoFar1` · `canNextBone` · 바꾸기 창이
   모두 이 차례를 기준으로 움직입니다.

### 13-3. 맨 위 띠를 **단계에 맞게** (110번)

일기와 계획하기 모두, 흰 칸 맨 위 띠가 단계마다 다릅니다.

| | 일기 | 계획하기 |
|---|---|---|
| 1단계 | 고른 것을 **그림**으로 | 그림 띠 + 아래 문장 한 줄 |
| 2단계 | 빈칸 채우기 **두 줄** | 빈칸 + 고른 낱말 **빨강·굵게** |
| 3단계 | 빈칸 채우기 (학생이 쓴 글로) | 문장만 |

- 계획하기도 **일기와 같은 단계**(`student.diaryLevel`)를 씁니다.
  `간단히/자세히`(`planLevel`)는 **묻는 항목 수**라 다른 축입니다
- 줄글로 이어 붙였더니 뒤에 기분·기억이 붙을수록 앞이 옆으로 밀려 안 보였습니다.
  **두 줄로 나누면** 2줄이 길어져도 1줄이 안 밀립니다
- 빨강은 **눈에 띄게 하려는 것**이지 뜻을 나르지 않습니다. 고른 것과 안 고른
  것은 '글자가 있나 없나' 로 갈리므로 색을 못 가려도 흑백 인쇄에도 뜻이 남습니다
- **1단계 인쇄 계획표**에도 그림 띠가 문장과 함께 나옵니다

### 13-4. ★ 여가지도를 **두 걸음**으로 (111번)

한 화면에 활동 카드 8장(섬마다 4장)이 늘 떠 있어서 섬 그림이 거의 다 덮이고,
볼 것이 한꺼번에 너무 많았습니다.

  **1층 섬 고르기** — 카드를 **아예 안 놓습니다**. 섬 두 개와 도장판만
  **2층 섬 안** — 그 섬만 폭을 통째로 쓰고 **한 쪽에 4장**. 도장판은 숨김

- 섬마다 다른 배경 (`images/지도/실내섬.jpg` · `실외섬.jpg`)
- 모아보기는 **섬 안에서만**, **그 섬 것만** 보여 주고, 나가는 단추도
  **들어온 그 섬 하나**뿐입니다
- 섬 이름은 그림 위 하늘(5~11%), 발자국 수는 각 섬 아래(87~92%).
  `들어가기` 단추는 없앴습니다 — 섬 어디를 눌러도 들어갑니다

### 13-5. 그림판에서 손끝과 그려지는 자리가 어긋나던 것 (112번)

원인이 **둘**이었습니다.
1. **테두리 3px** — `getBoundingClientRect` 는 테두리까지 잰 크기인데
   그림은 테두리 안쪽에 그려집니다
2. **빈 띠** — `width:100%` 와 `max-height:44vh` 를 함께 두어 낮은 화면에서
   칸이 그림보다 옆으로 넓어졌습니다. `object-fit:contain` 이 그림을 가운데
   놓으면서 양옆에 빈 띠가 생기는데 그 띠를 안 빼고 셈했습니다
   (1280×700 에서 칸 900px 안에 그림은 440px 뿐이었습니다)

→ `at()` 이 테두리와 빈 띠를 빼고 셈합니다. CSS 는 **높이 상한을 폭 상한으로**
  바꿔(`44vh × 10/7`) 빈 띠가 아예 안 생기게 했습니다.
  마우스·펜·손가락 모두 **어긋남 0px** 로 확인했습니다.

### 13-6. 여가지도를 학생 눈으로 다듬기 (113번)

- **말과 그림 통일** : `아직 안 해봤어요`(줄표)와 `아직 잘 모르겠어요`(물음표)는
  학생에게 같은 뜻인데 둘로 갈라져 있었습니다 → **물음표 + `잘 모르겠어요`** 하나로
- **해본 곳 / 아직 안 해본 곳** : 비슷한 파랑 두 가지로 갈라 두었더니
  사실상 구분이 안 됐습니다 (사용자 지적 — `아이들은 색감차이를 잘 못느껴`).
  → **색 · 두께 · 바탕 세 가지**를 한꺼번에 바꿉니다
      아직  테두리 3px 파랑 · 흰 바탕
      해봄  테두리 5px 주황(실내) / 하늘(실외) · 칠한 바탕
  ⚠ 밝은 주황(#E8883C)은 흑백 밝기 차이가 6 밖에 안 됩니다. 어둡게 잡으세요
- **섬마다 바탕색** : 실내 섬 배경은 파스텔 노랑이라 주황, 실외는 파랑이라 하늘빛
- **찾아보기** : 고른 것을 알리는 알약을 아래 단추와 **같은 색**으로.
  섬을 나올 때는 찾아보기를 **처음으로 되돌립니다**
- **지도 도움말** : `지도 표시 뜻과 요약` → `지도 표시 뜻`. 요약 다섯 줄을 빼고
  네 가지를 큼직한 줄 넷으로 (요약은 읽어주기로 그대로)

### 13-7. 파란 화살표 하나로 한 걸음씩 (114번)

같은 일을 하는 길이 셋씩 있었고 이름도 헷갈렸습니다
(`네 가지로 돌아가기` 가 네 코너인지 표시 넷인지 모호했습니다).

  표시 하나 보기 → 네 가지 표시 → 그 섬 → 큰 지도 → 나의 여가

화살표 이름(aria-label)도 그때그때 바뀝니다. 알약 둘과 초록 바 하나를 지웠습니다.

⚠ **코너 첫 화면에서는 `p.back`(지나온 길)을 쓰지 마세요.**
  지도 ↔ 모아보기를 오간 것이 길에 쌓여, 큰 지도에서 화살표를 누르면
  홈이 아니라 모아보기로 되돌아갔습니다. 코너 첫 화면은 늘 홈으로.

### 13-8. 실외 활동 이름을 `무엇을 하는지`로 (115번)

장소를 따로 묻는데 활동 이름에도 장소가 들어 있었습니다.

    나는 오늘 엄마와 공원에서 **공원 가기**를 할 거예요.

그리고 `○○ 가기` 는 **가는 것**이지 여가 활동이 아닙니다. 13가지를 고쳤습니다.

  놀이터 가기 → 놀이 기구 타기 · 박물관 가기 → 전시 구경하기
  미술관 가기 → 그림 감상하기 · 도서관 가기 → 책 읽기
  문구점 가기 → 문구 사기   · 노래방 가기 → 노래 부르기
  영화관 가기 → 영화 보기   · 공원 가기 → 나들이하기
  맛집 탐방하기 → 맛있는 음식 먹기 · 카페 가기 → 음료 마시기
  텃밭 가꾸기 → 식물 돌보기 · 캠핑하기(문장만) · 반려동물과 실외에서 놀기 → 반려동물과 산책하기

▸ **이름을 바꾸면 `planText` · `diaryText` 도 함께** 고쳐야 합니다 (13 × 2 = 26줄).
▸ 이름에서 장소를 뺀 덕분에 **계획·일기 문장에 장소를 넣을 수 있게** 되었습니다.
  예전에는 계획하기 노란 바에는 장소가 나오는데 계획표에는 안 나와 서로 달랐습니다.
▸ 새 활동을 더할 때 : **이름에 장소를 넣지 마세요.** 장소는 따로 묻습니다.
  `공원에서 나들이를` 처럼 이어지는지 문장으로 읽어 보고 정하세요.

### 13-9. 포트폴리오에 실내·실외를 한눈에 (116번)

여가지도는 섬마다 따로 보는 곳이라 **합쳐서 보는 자리가 없었습니다.**
포트폴리오의 `나의 여가지도` 칸을 그곳으로 만들었습니다.

- 네 가지 표시를 눌러 고르면 그 활동만 카드로
- 카드마다 **실내 / 실외** 표시 (색만이 아니라 글자로도)

---

## 14. 2026-08-21 에 한 일

### 14-1. 활동 이름 `불을 사용하지 않는 요리` → `과일 요거트`

▸ **이름만 바꾸면 그림이 사라집니다.** 그림을 찾는 열쇠(`imageKey`)도 함께
  바꿔야 `images/activities/과일 요거트.png` 를 찾습니다.
  `name` · `imageKey` · `planText` · `diaryText` 네 곳을 늘 같이 봅니다.

### 14-2. 앞 커밋에 딸려 간 그림 세 장 되살리기

커밋할 때 **작업 중이던 다른 그림 변경까지 함께 담겨** 갔습니다.

  images/여가지도.jpg          사라짐 → 새 PNG 에서 다시 만듦 (1536x864, 186KB)
  images/준비물/스티커.png      사라짐 → 되살림. 이것만 1536폭·1934KB 라 512폭(222KB)으로
  images/준비물/헬맷.png        이름 틀림 → 헬멧.png 으로. 코드가 부르는 이름과 달랐음

▸ **커밋 전에 `git status` 를 꼭 읽으세요.** 지금 고친 것만 담겼는지 봅니다.
▸ 그림 한 장만 유난히 크면 다른 것과 **같은 폭(512px)** 으로 맞춥니다.

### 14-3. 활동 고르기 여섯 장을 3열 두 줄로

한 쪽에 여섯 장인데 4열이라 둘째 줄에 두 장만 남아 오른쪽이 휑했습니다.

  계획하기 `무엇을 할까요?` · 일기 `무엇을 했나요?`   cols 6 → 3
  계획하기 세부 활동 (요리하기 6가지)                6/4 → 3/2 (일기와 같게)

⚠ 좁은 화면(900px)에서도 **3열을 지킵니다.** 2열이면 여섯 장이 세 줄이 되어
  흰 칸을 넘치고, 격자가 통째로 다음 쪽으로 밀려 1쪽이 텅 빕니다.
  620px 아래에서만 2열(2+2+2)로 내려갑니다.

▸ 계획·일기가 **서로 다른 열 수**를 쓰고 있었습니다. 같은 일을 하는 화면은
  같은 규칙으로 — 한쪽만 고치면 두 화면이 달라집니다.

### 14-4. 일기 고치기를 **완성 화면 안으로**

저장한 뒤 따로 있던 `일기 고치기` 화면으로 가지 않고, 일기가 완성된
그 자리에서 바로 고칩니다. 노란 상자 아래에 세 줄을 두었습니다.

  그림 자리 · 크기 바꾸기 (새로) / 그림 다시 그리기 / 날짜 · 사람 · 장소 바꾸기

네 번째인 `일기 내용 고치기` 는 **노란 상자 자체가** 하므로 두지 않습니다.
같은 일을 하는 단추가 둘이면 어느 것을 눌러야 할지 헷갈립니다.
머리말을 `완성된 일기 ✎ 여기를 눌러 일기를 고쳐 쓸 수 있어요` 로 바꿨습니다.

- 오른쪽에 있던 `날짜·사람·장소 바꾸기` 를 왼쪽으로 내려 **고치는 길 넷이 한 자리**에
- 자리를 옮기는 동안에는 종이를 눌러도 큰 창이 뜨지 않습니다.
  그림을 잡으려고 눌렀는데 창이 떠 버리면 옮길 수가 없습니다.
- 저장 직후 그림일기 화면의 `일기 고치기` 는 뺐습니다 (방금 다 고치고 옴).
  **어제 저장한 일기는 `나의 일기 모음` 에서 열어 고칩니다 — 그 길은 남겨 두었습니다.**
  화면을 통째로 지우면 옛 일기를 다시는 못 고칩니다.

### 14-5. 일기 제목 그림 자리 만들기 (그림은 14-10 에서 넣었습니다)

제목 고르기 칸이 코드로 그린 SVG 라 다른 화면과 화풍이 달랐습니다.
`images/제목/` 을 만들고 네 장이 들어갈 자리를 이어 두었습니다.
그림이 없으면 지금처럼 SVG 가 나옵니다. 자세한 것은 `images/제목/README.md`.

⚠ **첫째 칸이 별(★), 넷째 칸이 하트(♥)였습니다.** 지도에서 별은
  `도전하고 싶어요`, 하트는 `좋아해요` 를 뜻합니다. 앱이 스스로 적어 둔 규칙
  (`images/또하기/README.md`)을 어기고 있었습니다. 그림을 넣을 때 바로잡습니다.

### 14-6. 일기 본문을 **문장마다 줄바꿈 → 한 칸 띄워 이어 쓰기**

일기는 문장이 끝나도 줄을 바꾸지 않고 **한 칸 띄우고 이어 씁니다.**
그런데 저절로 만들어지는 문장을 `join('\n')` 으로 이어 두어서,
원고지에서 **문장 하나하나가 새 문단**이 되었습니다.
그래서 줄마다 첫 칸이 비고 줄 끝에도 빈 칸이 줄줄이 남았습니다 (규칙 2).

  js/lib/korean.js   S.diaryMade            join('\n') → join(' ')
  js/screens/diary.js 2단계 confirmStep 호출  join('\n') → join(' ')
  js/screens/picdiary.js  d.text 를 새 문단으로 떼지 않고 마지막 문단에 이어 붙임

▸ **학생이 노란 칸에서 손수 바꾼 줄(bodyEdit)은 그대로 둡니다.**
  그때는 정말로 문단을 나눈 것입니다.
▸ 줄이 줄어든 만큼 `fitGrid` 가 **더 큰 글씨**를 고를 수 있게 됩니다.
  10칸 원고지에서 7줄 → 5줄, 20칸에서 4줄 → 3줄.

검사 (App.manuscriptRows 로 다섯 경우) : 한 문단 세 문장 · 물음표 뒤 이어짐 ·
숫자와 영어 섞임 · 손수 나눈 두 문단 · 마침표가 줄 첫 칸에 올 뻔 →
글자 빠짐 없음, 줄 첫 칸 문장부호 없음, 문단 첫 칸 비움 모두 통과.

### 14-7. `혼자와 함께` 가 말이 안 되던 것 (되풀이)

`혼자` 는 사람 이름이 아니라 **혼자 했다는 말**입니다. 그런데 사람 이름처럼
`과/와 함께` 를 붙여서 이런 말이 나왔습니다.

  나는 혼자와 함께 요가하기를 했어요.   →   나는 혼자 요가하기를 했어요.
  제목 : 혼자와 함께한 요가            →   제목 : 혼자 한 요가

▸ 사람 목록에 **사람이 아닌 답**이 섞여 있으면 조사 규칙이 깨집니다.
  `혼자` 처럼 뜻이 다른 답은 문장을 만들 때 따로 갈라 주세요.

### 14-8. 칭찬 폭죽이 너무 빨리 지나가던 것

1.6초 만에 끝나서 눈 깜짝할 새였습니다. **3.3초**로 늘렸습니다.

  터지는 자리   3곳 → 6곳 (화면 위쪽에 넓게 흩어서)
  터지는 사이   0.44초 안에 다 → 1.7초에 걸쳐 차례로
  조각 나는 시간 1.15초 → 1.6초, 옅어지는 때 70% → 82%
  치우는 때     1.7초 → 3.5초

색종이 수 재어 봄 : 0초 18 → 0.9~1.8초 72(가장 화려) → 3.3초 0.
**차오르다 잦아드는** 모양이라 조각을 크게 늘리지 않고도 오래 이어집니다.

⚠ 이때 `both` 함정을 밟았습니다 — 아래 함정 (20) 을 보세요.

### 14-9. 제목이 `가족와 함께한` 으로 나오던 것

`who + '와 함께한'` 처럼 **`와` 를 못박아** 두었습니다. 받침에 따라 `과/와` 가
달라지므로 `App.waGwa` 를 씁니다 (가족**과** · 친구**와**).
혼자 한 날은 `혼자와 함께한` 이 되어 말이 되지 않아 `혼자 한 ○○` 로 바꿉니다.

▸ 조사는 **손으로 붙이지 마세요.** `App.waGwa` · `App.eulReul` · `App.josa` 가 있습니다.

### 14-10. 제목 그림 네 장 넣음 + 아직 SVG 인 곳 훑기

선생님이 그려 오신 제목 그림 네 장을 넣었습니다.
받은 그림은 **1024~1254px 흰 배경**이라 그대로 쓰면 카드 위에 흰 네모가 됩니다.
512px 투명 배경으로 바꿔 넣었습니다 (아래 14-20 참고).

그러고 나서 **앱 전체 158장**을 하나씩 불러 보고, 화면도 걸어 다니며
아직 SVG 로 나오는 자리를 찾았습니다.

  코너 네 곳 · 계획 8단계 · 일기 1·2단계 · 지도 두 층 · 포트폴리오

**결과 — 그려야 할 것은 다섯 장뿐입니다** (`images/일기 낱말/README.md`)

  만든 작품.png       가장 기억에 남는 것은?
  맛있게 먹은 것.png   가장 기억에 남는 것은?
  더 오래.png         다음에는 어떻게?
  사진 넣기.png       그림일기에 넣을 그림
  내가 그리기.png      그림일기에 넣을 그림

**그리지 않아도 되는 여섯 자리 — 있는 그림을 그대로 씁니다.**
뜻이 같은데 그림을 또 그리면 학생이 **둘을 다른 것으로** 봅니다.

  기억 `친구와 함께한 것`  ← images/제목/함께한.png
  기억 `새로 해본 것`      ← images/제목/처음 해 본.png
  다음에 `또`             ← images/또하기/또 하고 싶어요.png
  다음에 `다른 활동을`      ← images/또하기/다른 것도 하고 싶어요.png
  다음에 `친구와 같이`      ← images/제목/함께한.png
  장소 `직접 쓰기` (계획·일기) ← images/제목/직접 쓰기.png

▸ 낱말에 `use` 를 적으면 그 그림을, 없으면 `images/일기 낱말/<이름>.png` 를 찾습니다.
▸ `내가 그리기` 와 `직접 쓰기` 는 **일부러 다른 그림**입니다.
  하나는 **그림**을 그리는 것, 하나는 **글**을 쓰는 것입니다.

⚠ `새로 해본 것` 이 **별(★)** 이었습니다. 지도의 `도전하고 싶어요` 와 겹칩니다.
  병아리 그림으로 바꿨습니다. (제목 화면의 별·하트도 앞서 바로잡았습니다)

⚠ **아직 남은 겹침 하나** (14-12 에서 책갈피로 고쳤습니다) — `이 일기를 전시하고 싶어요?` 의 `전시할래요` 가
  별(★)을 씁니다. 화면 설명에도 `큰 별이 붙고` 라고 적혀 있습니다.
  그러면 별이 **도전하고 싶어요 · 전시하기** 두 뜻이 됩니다.
  고칠지는 선생님 판단이 필요해 그대로 두었습니다.

▸ 못 받는 그림 하나 : `images/여가 계획표 밑줄.png` (계획표 제목 아래 손그림 밑줄).
  없으면 코드로 그린 하늘색 막대가 나오므로 화면은 멀쩡합니다.

### 14-11. 그림을 **고른 그림으로 되돌리는 길**이 없던 것

`그림 다시 그리기` 로 내가 그린 그림으로 바꾸고 나면, 완성 화면에서
**원래 고른 그림으로 돌아갈 길이 없었습니다.** 앞 단계(`그림` 고르기)로
되돌아가야만 바꿀 수 있었는데, 완성 화면에서는 그 길이 보이지 않습니다.

  [ 고른 그림으로 되돌리기 ]  그린 그림은 지워지지 않아요.

- 노란 칸의 `고른 낱말로 되돌리기` 와 **같은 결·같은 말투**로 맞췄습니다.
- 세 줄과 같은 굵기로 두면 **네 번째 길**처럼 보입니다. 이것은 되돌리기라
  작게, 오른쪽에 붙였습니다.
- 고른 그림일 때는 되돌릴 것이 없으므로 **단추가 나오지 않습니다.**
- 완성 화면과 일기 고치기 화면 **두 곳 모두**에 넣었습니다.
  한쪽에만 있으면 저장 뒤에 다시 막힙니다.

▸ **그린 그림은 지워지지 않습니다** (`drawPhotoId` 를 건드리지 않음).
  다시 `그림 다시 그리기` 를 누르면 아까 그린 선이 그대로 나옵니다.
  그래서 학생이 마음 놓고 눌러 볼 수 있습니다 — 되돌리기가 **되돌려지는지**가
  중요합니다.

확인 : 그림 조각 0개 → 3개(활동·사람·기분 돌아옴), 단추 스스로 사라짐,
그림판 다시 열었을 때 선 그대로, `drawPhotoId` 그대로, 1280x720 에서 1쪽.

### 14-12. 전시 표시를 **별 → 책갈피** (말은 `전시` 그대로)

14-10 에서 남겨 두었던 별 겹침을 바로잡았습니다.
지도에서 별(★)은 `도전하고 싶어요` 인데, `전시할래요` 도 별을 써서
**한 모양이 두 뜻**이었습니다. 표시 그림을 **책갈피**로 바꿨습니다
(`icons.js` 의 `bookmark`). 일기에 꽂아 두는 느낌이 잘 맞고,
하트·발자국·물음표·도는 화살표 어느 것과도 겹치지 않습니다.

  이 일기를 전시하고 싶어요?
    [🔖 전시할래요]  [— 전시하지 않을래요]
  전시하기로 고른 일기에는 책갈피가 붙고, 교실에 전시해요.

**⚠ 중간에 `중요 표시` 로 바꿨다가 되돌렸습니다 — 왜 되돌렸는지 남깁니다.**

한때 이렇게 나누자고 제안했습니다.

    학생 : 중요한 일기에 표시만    선생님 : 그 표시를 보고 전시를 정함

되돌린 까닭 세 가지입니다.

  ① **`전시` 는 배울 만한 말입니다.** 학교에서 늘 쓰고, 게시판에 붙은
     제 작품을 보며 익히는 **구체적인 말**입니다. `중요` 는 눈에 보이지
     않는 추상어라 오히려 어렵습니다. 특수교육에서는 **낱말 자체가
     배울 거리**라는 점을 놓쳤습니다.
  ② **실제로 벌어지는 일이 전시입니다.** 전시판형으로 인쇄되고 교실 TV 에
     뜹니다. `중요 표시` 라 부르면 말과 일이 도리어 벌어집니다.
  ③ **무엇을 남에게 보일지 고르는 것은 학생이 배울 몫**입니다.
     선생님에게 넘기면 자기결정을 연습할 자리가 하나 사라집니다.

▸ 고칠 곳을 찾을 때 `말이 어려워 보인다` 는 것만으로 바꾸지 마세요.
  **그 말을 배우는 것이 목표일 수도** 있습니다. 먼저 선생님께 여쭙니다.

⚠ **담아 두는 값 이름(`exhibit`)은 처음부터 바꾸지 않았습니다.** 저장해 둔
  일기가 이미 그 이름을 써서, 바꿨다면 **예전 기록의 표시가 통째로
  사라졌을 것**입니다. 덕분에 말을 두 번 바꾸고 되돌리는 동안에도
  전에 표시해 둔 일기 2개가 그대로 살아 있었습니다.


### 14-13. 같은 뜻인데 **말이 둘**이던 곳 맞추기

그림은 같은 것을 쓰면서 **말만 갈려** 있던 곳이 둘 있었습니다.

  기억 `새로 해본 것`  →  `처음 해 본 것`   (제목의 `처음 해 본 ○○` 과 같은 말로)
  다음에 `친구와 같이`  →  `친구와 함께`     (기억 `친구와 함께한 것` · 제목 `아빠와 함께한` 과 같게)

**한 가지 뜻에는 한 가지 말.** 낱말 자체가 배울 거리인데, 같은 것을 두 말로
가르치면 익힐 것이 곱절이 됩니다. 앞의 함정 (25) `뜻이 같은 자리에는 그림을
또 그리지 마세요` 와 **같은 규칙이 말에도 적용**됩니다.

고른 말의 까닭

  `처음` 이 `새로` 보다 아이들이 자주 쓰고 뜻이 또렷합니다.
  `함께` 는 셋 가운데 둘이 이미 쓰고 있었습니다.

문장으로 읽어 확인했습니다.

  가장 기억에 남는 것은 처음 해 본 것이에요.
  다음에는 친구와 함께 하고 싶어요.

▸ 학생이 고르는 낱말을 **한자리에 모아 놓고** 훑으면 이런 겹침이 보입니다.
  기분 8 · 또하기 3 · 지도 표시 4 · 사람 7 · 기억 4 · 다음에 4 를 함께 보았고,
  나머지는 `또` · `다른` · `잘 모르겠어요` 가 이미 잘 맞아 있었습니다.

### 14-14. 그림칸을 **3:2 로 못박기** — 글에 따라 널뛰던 것

**증상** : 글을 이어 쓰면 그림칸이 눌려 배경 그림 위아래가 잘리고,
줄바꿈을 하면 되레 그림칸이 커졌습니다. **거꾸로**였습니다.

  이어 쓴 글  10칸 8줄 → 원고지 633px, 그림칸 320px (가로세로 2.47) ← 배경 잘림
  줄바꿈한 글 14칸 7줄 → 원고지 396px, 그림칸 557px (가로세로 1.42)

**까닭** : 그림칸이 `flex:1` 이라 **남는 자리**를 받았습니다. 원고지가
`글자를 되도록 크게` 를 앞세워 제 몫을 먼저 가져간 것입니다. 칸은 정사각형이라
`원고지 높이 = 줄 수 x (790 / 칸 수)` 입니다. 이어 쓰면 10칸이 유지되어
한 줄이 79px 씩이나 되고, 줄바꿈하면 14칸으로 떨어져 한 줄이 57px 로 줄어듭니다.
**칸 수가 적을수록(글씨가 클수록) 원고지가 두꺼워집니다.**

**고침** : 차례를 뒤집었습니다. **그림칸이 먼저, 원고지가 남은 자리에.**

  그림칸 = 속 너비 770 ÷ 1.501 = 513px  (+ 여백 20 + 밑줄 2 = 535)
  원고지가 쓸 높이 = 1119 - 64(날짜) - 535(그림) - 52(제목) - 54(알약줄) = 414

장소 그림 19장이 **모두 512x341(=1.501)** 이라, 칸을 그 비율로 두면
`background-size:cover` 로 깔아도 한 픽셀도 잘리지 않습니다.

⚠ `fitGrid` 를 **줄 수가 아니라 높이로** 재게 고쳤습니다.
  칸 수가 다르면 한 줄 높이가 달라서, `8줄` 이 10칸에서는 632px,
  14칸에서는 451px 입니다 — **줄 수만으로는 서로 견줄 수 없습니다.**

⚠ 처음에 `.pd-foot`(신나요 · 혼자 · 학교 알약 줄, 52px)을 빠뜨려 그림칸이
  42px 눌렸습니다. **종이의 모든 줄을 빠짐없이** 세어야 합니다.
  브라우저에서 `.pd-sheet` 의 자식 높이를 다 더해 확인했습니다.

잰 결과 — 다섯 경우 모두 그림칸 535px · 비율 1.501

  1단계 이어 쓰기 14칸 6줄 | 1단계 줄바꿈 14칸 7줄 | 1단계 짧은 글 10칸 5줄
  2단계 따라 쓰기 14칸 6줄 | 3단계 밑줄

글이 길어질 때 : 28자 → 10칸, 86자 → 14칸, 173자 → 20칸까지 **그림칸 그대로**.
289자(20칸으로도 안 되는 길이)에서만 그림칸이 359px 로 양보하고 콘솔에 알립니다.
글자를 자르지는 않습니다 (원고지 규칙 13).

▸ 3단계 밑줄의 `LINE_BUDGET` 도 같은 값을 쓰게 했습니다.
  640(옛 8줄 x 79px) 그대로 두면 3단계만 그림칸이 176px 작아집니다.

### 14-15. 파란 화살표가 **빈 일기 화면**으로 가던 것

그림일기에서 파란 화살표를 누르면 방금 쓴 일기가 아니라 **새 일기**가
처음부터 열렸습니다.

`p.back()` 은 지나온 길을 되짚는데, 일기를 쓸 때의 길에는
**번호 없는 `diary`** 가 쌓여 있습니다. 그리로 돌아가면 빈 일기가 열립니다.

→ 지나온 길 대신 **어디에서 왔는지(`from`)** 를 받아 그 화면으로 곧장 갑니다.

    from: 'diary'    일기를 막 저장하고 옴 → 그 일기의 **완성 화면**
    from: 'folio'    포트폴리오에서 옴     → 포트폴리오
    (없음)           → 그 일기의 완성 화면

▸ `step:'last'` 를 함께 넘겨 일기 화면을 **맨 마지막 질문**에서 엽니다.
  그러지 않으면 `언제 했나요?` 부터 다시 훑게 되어 `바로 전단계` 가 아닙니다.
▸ 단계 목록(L1/L2/L3)은 나중에 정해지므로 99 로 넣어 두고, 목록이 정해진 뒤
  실제 마지막 번호로 깎습니다.

### 14-16. 2단계 말 다듬기

  2단계 `따라 쓰거나 스스로 써요`   → `따라 쓰거나 보고 써요`
  인쇄 모양 `스스로 쓰기`          → `힌트 보고 쓰기`

`스스로 쓰기` 라고 했지만 실제로는 **힌트를 보고** 씁니다 (힌트 보기 단추도
이 모양에서만 나옵니다). 이름과 하는 일이 달랐습니다.

### 14-17. 제목 칸 그림을 **고른 것 그대로** 보여 주기

첫 칸에 나무 팻말을 넣었더니 이상해 보였습니다. 이 칸은 **활동 이름이
그대로 제목이 되는 칸**(`자전거 타기`)이라, 그 활동 그림을 보여 주는 편이
훨씬 분명합니다. 팻말은 한 번 더 생각해야 뜻을 알 수 있습니다.

  자전거 타기            → images/activities/자전거 타기.png
  아빠와 함께한 자전거 타기 → images/avatars/아빠.png
  신나는 자전거 타기       → images/얼굴표정/신났어요.png  (원래 그랬음)
  처음 해 본 자전거 타기    → images/제목/처음 해 본.png
  직접 쓰기              → images/제목/직접 쓰기.png

⚠ 사람 칸도 누구를 골랐든 `함께한.png`(손 잡은 두 아이)를 붙이고 있었습니다.
  그래서 **`혼자 한 요가` 에 두 아이가** 나왔습니다. 앞서 글은 고쳤는데
  그림이 그대로여서 말과 그림이 어긋나 있었습니다.
  → 고친 곳이 있으면 **글과 그림을 함께** 보세요.

▸ 사람 그림은 발끝까지 있는 **전신**입니다. 정사각 칸에 통째로 넣으면
  얼굴이 아주 작아집니다. CSS `.bust` 로 **상반신만** 보이게 잘랐습니다.
    cover 만 쓰면 세로의 64% (허벅지까지)
    위를 기준으로 1.25배 키워 51% (머리~허리)
  ⚠ 그림 파일은 건드리지 않습니다 — 같은 파일을 사람 고르기·그림일기에서도
    씁니다. 파일을 자르면 그쪽이 다 망가집니다.
  ⚠ `transform-origin` 을 top 으로 두어야 머리가 안 잘립니다.

▸ 팻말 그림은 `images/_원본/안 쓰는 그림/` 으로 옮겨 두었습니다.
  지우지 않은 것은 선생님이 그려 주신 것이고, 나중에 쓸 자리가 생길 수 있어서입니다.

### 14-18. 학생 고르기 화면 — 집 모양을 `나의 여가` 글자로, 안내에 톱니바퀴

두 가지가 어긋나 있었습니다.

**① 그림과 가는 곳이 달랐습니다.** 왼쪽 위가 **집 모양**인데 누르면 **표지**로
갔습니다. 집을 누르면 홈으로 갈 줄 알게 됩니다. 게다가 이 화면에서는 학생을
아직 안 골랐으니 홈으로 갈 수도 없습니다.

  → 홈 화면 왼쪽 위와 **똑같이** `나의 여가` 글자 그림으로 바꿨습니다.
    같은 자리 · 같은 그림 · 같은 곳 — 한 번 익히면 어디서나 같습니다.
    (이미 `images/나의여가 표지글자.png` 를 홈에서 쓰고 있었습니다)

**② 안내가 딴 화면을 가리켰습니다.**

    아직 등록된 학생이 없어요. 표지의 톱니바퀴 를 눌러 선생님 설정에서 …

선생님이 표지로 돌아가 톱니바퀴를 찾아야 했습니다. 갈 곳을 말로 알려 주는 대신
**그 단추를 여기에** 두었습니다.

    아직 등록된 학생이 없어요.
    선생님 설정에서 학생을 추가해 주세요.
      [⚙ 선생님 설정 열기]

▸ 누르면 `1. 우리 반 학생` 칸이 바로 열립니다 — `+ 학생 추가` 가 있는 그 자리입니다.
▸ 학생이 있으면 이 안내와 단추는 나오지 않습니다.
▸ 이것은 앱이 이미 쓰는 원칙입니다 — **안내만 하지 말고 누르면 바로 그 일이
  일어나게.** (그림 고르기 세 칸도 같은 까닭으로 고쳤습니다)

### 14-19. 선생님 설정 7번 — 끈 사람의 남녀 고르기가 남아 있던 것

`7. 함께하는 사람 선택지` 아래 `그림 고르기 (남 / 여)` 가 **위에서 껐는지와
상관없이 늘 다섯 줄 모두** 나왔습니다.

형제자매가 없는 학생은 선생님이 위에서 `형제자매` 를 끕니다. 그런데 아래에는
`형제자매 [남][여]` 가 그대로 남아, **쓰지도 않을 그림을 고르라고** 하는
셈이었습니다. 껐는지 안 껐는지도 헷갈립니다.

  → 위에서 **켠 사람만** 아래에 나옵니다. 안내도 그렇게 적었습니다.
      그림 고르기 (남 / 여) — 위에서 고른 사람만 나와요

▸ 거르는 일은 **`App.partnersFor(학생)` 이 이미** 하고 있었습니다.
  학생 화면(누구와 했나요?)에서 쓰는 바로 그 함수입니다.
  여기서 또 만들지 않고 그것을 그대로 썼습니다 — 둘로 나뉘면 언젠가 어긋납니다.

확인 : 형제자매를 끄면 아래에서 사라지고, 다시 켜면 돌아옵니다.
학생 화면의 `누구와 했나요?` 카드도 똑같이 여섯 장이 됩니다.

### 14-20. 흰 배경 걷어내기 (챗GPT 그림을 넣을 때마다 쓰는 방법)

챗GPT는 투명 배경을 잘 못 만듭니다. 흰 배경으로 받아서 **바깥쪽 흰 부분만**
걷어냅니다. 그림 안쪽의 흰색(크림색 종이 · 하이라이트)은 살려야 합니다.

  ① 512px 로 줄이기
  ② **네 모서리에서 번져 들어가며**(flood fill) 밝은 점만 투명으로
     → 안쪽 흰색은 바깥과 이어져 있지 않아 그대로 남습니다
  ③ 문턱값 238 (완전한 255 만 지우면 살짝 회색인 가장자리가 남습니다)

⚠ `GetPixel`/`SetPixel` 로 하면 26만 점이라 몇 분씩 걸립니다.
  `LockBits` 로 바이트 배열을 한 번에 읽어 다루면 몇 초입니다.

⚠ **한글이 든 .ps1 파일은 BOM 을 붙여 저장하세요.** 윈도우 파워셸 5.1 은
  BOM 이 없으면 ANSI 로 읽어서 한글 경로가 깨집니다. 그러면 파일을 하나도
  못 찾고 **아무 말 없이 그냥 끝납니다** (오류도 안 납니다).

---

## 8-18. 오늘 새로 배운 함정 (2026-08-19 ~ 20)

앞의 8-1 ~ 8-17 에 이어집니다. **고치기 전에 꼭 읽어 주세요.**

### (1) ⛔ 태그의 **속성 사이에 주석을 넣지 마세요**

```
<${C.Stage} top=${...}
  <!-- 주석 -->          ← 이러면 안 됩니다
  action=${...}>
```
htm 이 그것을 글자로 읽어서 **화면에 `action=` 이 그대로 찍힙니다.**
주석은 태그 **밖**에 둡니다.

### (2) ⛔ html 주석 안에 **백틱** (되풀이됨)

오늘만 네 번 냈습니다. 여러 줄 주석의 **둘째 줄**에 있으면 줄 단위 grep
으로는 안 잡힙니다. **`sh tools/backtick-check.sh`** 가 여러 줄까지 검사합니다.
`.claude/settings.json` 에 저장 훅으로 걸어 두어 파일을 고칠 때마다 저절로 돕니다.

### (3) ⛔ CSS `calc` 안에서는 **길이 ÷ 길이**가 안 됩니다

`min(.62, calc((100vh - 250px) / 1123))` 로 비율을 만들려 했더니 규칙 전체가
무효가 되어 종이가 794px 그대로 나왔습니다. **비율은 JS 로 재서 넣습니다.**

### (4) ⛔ CSS 고치고 **`?v=` 를 안 올리면 옛 CSS** 를 씁니다

고친 것이 안 먹는 것처럼 보여 엉뚱한 곳을 뒤졌습니다. 늘 함께 올립니다.

### (5) ⛔ 배경 비율을 **코드에 못박지 마세요**

`1536/832` 처럼 적어 두었는데 배경이 세로로 긴 그림으로 바뀌자 자리 잡기가
통째로 어긋났습니다. → 그림을 불러와 **실제 크기를 재어** 씁니다 (map.js).

### (6) ⛔ 칸 수도 못박지 마세요

섬 안을 `4칸 × 2줄` 로 두었더니 **높이에 걸려** 카드가 175 → 89px 로
오히려 작아졌습니다. 지도 칸은 옆으로 넓고 위아래로 낮습니다.
→ 몇 가지 짜임 가운데 **카드가 가장 커지는 것**을 그때그때 고릅니다.

### (7) ⛔ 배경은 **JPG**. PNG 를 줄이면 **오히려 커집니다**

`shrink-images.ps1` 로 1702 → 1536 으로 줄였더니 1086KB → **1226KB** 가
되었습니다. 사진 같은 그림은 PNG 다시 저장이 원본보다 커질 수 있습니다.

배경 세 장을 JPG 로 바꾸니 **4.6MB → 275KB** 였습니다.
학생이 주소로 앱을 열 때마다 내려받는 것이라 학교 와이파이에서 차이가 큽니다.

▸ 배경을 새로 넣을 때 순서
  1. `images/` 에 PNG 로 넣기
  2. 1536px 폭 JPG 로 바꿔 `images/여가지도.jpg` · `images/지도/*.jpg` 에
  3. **가장자리 색을 재서** 둘레 색 맞추기
     (css `.map-wrap` 의 `background-color`, map.js 의 `islandSea`)
     색이 안 맞으면 그림 둘레에 경계선이 보입니다
  4. `.gitignore` 에 PNG 이름 넣기 (원본은 `images/_원본/` 에 남습니다)

### (8) ⛔ 배경을 `contain` 으로 깔면 **그림 자리 안에** 놓아야 합니다

그림이 칸 한가운데에 놓이고 둘레가 남습니다. 섬 이름·단추·카드를 칸 전체에
놓으면 바다 위로 삐져나갑니다. → `fitBox()` 로 그림 자리를 재어 그 안에 놓습니다.

⚠ 지도 그림 **밖**(map-wrap)에 둔 것은 함께 안 움직입니다.
   쪽 넘김 줄이 252px 왼쪽으로 치우쳐 있었습니다 → 그림 안(map-canvas)으로.

### (9) ⛔ 자리(x)가 없으면 **크기도 버려지던 자리**

`posOf` 가 `자리가 적혀 있을 때만` 저장값을 썼습니다. 그래서 옮기지 않고
**크기만** 바꾸면 크기까지 버려져, 저장은 되는데 화면에는 안 나왔습니다.
→ **적어 둔 것만 하나씩** 덮어씁니다.

### (10) ⛔ 그림을 이름으로 찾을 때 — **파일 이름과 같은 말**로

계획표가 날짜 그림을 `8월 20일` 로 찾고 있었습니다. `images/시간/` 의 이름은
`오늘 · 내일 · 어제 · 날짜 고르기` 라서 못 찾고 달력 아이콘으로 떨어졌습니다.
학생은 방금 `내일` 그림을 골랐으므로 **계획표에서 그림이 바뀐 것처럼** 보입니다.
→ `App.dayWord` 로 얻습니다. 문장(`App.whenPhrase`)이 쓰는 것과 **같은 함수**라
  그림과 문장이 늘 같은 말을 씁니다.

### (11) ⛔ 크기를 바꾸는 고침 뒤에는 **두 가지 창 크기**에서 재세요

완성 화면을 "1쪽, 넘침 0" 으로 확인해 두고 그 뒤에 그림일기를 키웠더니
**다섯 쪽으로 갈라졌습니다.** 원인이 둘이었습니다.
1. 좌우로 나누는 조건에 `max-height:780px` 이 붙어 있어 **높은 화면에서는
   안 나뉘었습니다**
2. 나눈 뒤에도 오른쪽이 폭을 다 가져가 왼쪽이 **186px** 로 눌렸고,
   그러면 문장 칸이 한 줄에 한 글자씩 접혀 **900px 짜리 기둥**이 되었습니다
→ 나누는 칸에는 **가장 작은 폭**을 꼭 정해 주세요.

### (12) ⛔ 태그 **속성 사이에 주석**을 넣지 마세요

```
<${C.Stage} top=${...}
  <!-- 주석 -->        ← 이러면 안 됩니다
  action=${...}>
```
htm 이 그것을 글자로 읽어서 **화면에 속성 이름이 그대로 찍힙니다.**
실제로 모아보기 화면 맨 위에 `action=` 과 꺾쇠가 나왔습니다. 주석은 태그 **밖**에.

### (13) ⛔ 값을 **구간으로 끊어** 정하지 마세요 (되풀이된 탈)

미리보기 크기를 CSS 에서 화면 높이 900 · 780px **구간으로** 정했더니,
그 **사이 크기**(940px)에서 종이가 흰 칸을 넘겨 완성 화면이 다섯 쪽으로
갈라지고 **1쪽이 텅 비었습니다.**
→ 구간으로 끊지 말고 **화면에서 재어** 정합니다 (fitDv · fitScale).

⚠ 뺄 높이를 셀 때 **질문 바를 빠뜨리지 마세요.** 330 으로 잡았다가 또 갈라졌고,
  400(맨 위 줄 110 + 질문 바 60 + 아래 단추 66 + 여백)이 맞았습니다.

### (14) ⛔ `break-inside:avoid` 인 것이 칸보다 크면 **쪽이 통째로 밀려납니다**

포트폴리오 인쇄 미리보기가 그랬습니다 (책자형 5장 → 5쪽, 1쪽은 텅 빔).
→ 미리보기는 **제 칸 안에서 넘겨** 보게 했습니다. 선생님이 인쇄 전에
  확인하는 곳이라 넘겨 보는 것이 자연스럽습니다.

### (15) ⛔ 가운데 맞춤을 쓴 것에 `:hover` 를 걸 때는 **그 값을 함께**

`transform:translateX(-50%)` 인 것에 `transform:translateY(-3px)` 만 걸면
가운데 맞춤이 통째로 지워집니다. transform 은 덮어쓰기입니다.
마우스를 댈 때마다 섬 이름 바가 제 폭의 절반(77px)만큼 튀었습니다.
→ `transform:translate(-50%, -3px)`

### (16) ⛔ 공용 CSS 를 고칠 때는 **그것을 쓰는 곳을 다 찾아보세요**

도장 창을 좌우 2단으로 만들며 `.stamp-big` 의 `margin:0 auto` 를 지웠더니,
발자국을 **혼자 쓰는** 축하 창에서 왼쪽에 붙어 버렸습니다.

### (18) ⛔ 조사(`과/와` · `을/를`)를 **손으로 붙이지 마세요**

제목이 `가족와 함께한 요가` 로 나왔습니다. `who + '와 함께한'` 이었기 때문입니다.
`App.waGwa` · `App.eulReul` · `App.josa` 가 받침을 보고 골라 줍니다.
그리고 **`혼자` 처럼 사람이 아닌 답**도 함께 생각하세요 — `혼자와 함께한` 이 되었습니다.

### (19) ⛔ 저장 목록(payload)에 **새 값을 넣는 것을 빠뜨리지 마세요**

완성 화면에서 그림 자리를 옮길 수 있게 만들었는데, `save()` 의 payload 에
`artLayout` 을 안 넣었습니다. 화면에서는 잘 움직이는데 **저장하는 순간
처음 자리로 되돌아갑니다.** 화면만 보면 다 된 것처럼 보이니 더 위험합니다.
→ 새 값을 draft 에 담았으면 **저장 목록도 함께** 봅니다.

### (20) ⛔ 애니메이션에 `both` 를 쓰면 **기다리는 동안에도 보입니다**

폭죽을 오래 터지게 하려고 자리를 3곳 → 6곳, 사이를 0.44초 → 1.7초로 늘렸더니
**아직 안 터진 조각 90개가 처음부터 점으로 앉아** 있었습니다.
`animation-fill-mode:both` 는 기다리는 동안 **0% 칸의 모양**(opacity:1)을 씌웁니다.
자리가 셋이고 사이가 짧을 때는 안 보이던 것이, 사이를 벌리자 드러났습니다.
→ 기다리는 동안 숨기려면 `forwards` 를 씁니다 (그동안은 평소 모양 `opacity:0`).

▸ 이 탈은 **눈으로는 잡히지만 창이 접혀 있으면 못 잡습니다.** 브라우저가
  화면을 그리지 않으면 애니메이션이 아예 안 굴러갑니다.
  그럴 때는 `el.getAnimations()[0].pause()` 로 세워 두고 `currentTime` 을
  손으로 돌리며 재면 됩니다. 0.3초마다 보이는 조각 수를 세어 확인했습니다.

▸ 폭죽 시간을 고칠 때는 **셋을 함께** 셈하세요.
  자리 수 · 터지는 사이(js 의 `d`) · 조각이 나는 시간(css 의 `fw-fly`).
  하나만 고치면 끝나기 전에 사라지거나, 끝난 뒤 빈 칸이 남습니다.
  지금은 `마지막 자리 1.7초 + 조각 1.6초 = 3.3초`, js 가 3.5초에 치웁니다.

### (21) ⛔ 서식에서 **지켜야 할 칸**은 남는 자리로 두지 마세요

그림일기의 그림칸이 `flex:1` 이라 남는 자리를 받았습니다. 원고지가 제 몫을
먼저 가져가서, **글에 따라 그림 크기가 널뛰고** 배경 그림이 잘렸습니다.
→ 서식에서 모양이 정해져 있어야 하는 칸(그림칸)은 **먼저 못박고**,
  나머지가 남은 자리에 맞추게 합니다.
→ 배경을 `cover` 로 까는 칸은 **배경 그림과 같은 비율**로 두면 안 잘립니다.

⚠ 남은 높이를 셀 때 **종이의 모든 줄을 빠짐없이** 세세요.
  맨 아래 알약 줄(52px) 하나를 빠뜨려 그림칸이 42px 눌렸습니다.
  브라우저에서 부모의 자식 높이를 **다 더해** 확인하는 것이 확실합니다.

### (22) ⛔ 크기를 견줄 때 **줄 수로 재지 마세요**

`8줄` 이 10칸 원고지에서는 632px, 14칸에서는 451px 입니다. 한 칸 크기가
달라서 **줄 수만으로는 서로 견줄 수 없습니다.**
→ 언제나 **높이(줄 수 x 칸 크기)** 로 재서 정합니다.

### (23) ⛔ 단추 **그림과 가는 곳**이 같아야 합니다

학생 고르기 화면의 왼쪽 위가 **집 모양**인데 누르면 **표지**로 갔습니다.
집은 홈을 뜻하니, 그림을 믿은 사람이 엉뚱한 곳으로 갑니다.
→ 그림을 가는 곳에 맞추거나, 가는 곳을 그림에 맞추거나 **둘 중 하나**를 하세요.
→ 이미 다른 화면에 같은 일을 하는 단추가 있으면 **그것을 그대로** 씁니다
  (홈 왼쪽 위 `나의 여가` 글자 → 표지).

### (24) ⛔ 갈 곳을 **말로 알려 주지 말고 단추를 두세요**

`표지의 톱니바퀴를 눌러 …` 처럼 딴 화면을 가리키면, 읽는 사람이 되돌아가
그것을 찾아야 합니다. 안내가 있는 **그 자리에 그 단추**를 둡니다.

### (25) ⛔ 설정에서 **끈 것에 딸린 칸**도 함께 감추세요

`형제자매` 를 껐는데 그 아래 `남 / 여` 고르기는 그대로 남아 있었습니다.
쓰지도 않을 것을 고르라고 하는 셈이고, 껐는지 안 껐는지도 헷갈립니다.
→ 무언가를 켜고 끄는 설정을 만들 때는 **그것에 딸린 칸이 어디에 또 있는지**
  찾아보세요. 켬·끔이 한 곳에만 반영되면 화면이 서로 어긋납니다.
→ 거르는 함수가 이미 있으면 (`App.partnersFor` 처럼) **그것을 그대로** 쓰세요.
  같은 거르기를 두 곳에서 따로 만들면 언젠가 둘이 달라집니다.

### (26) ⛔ 말을 고쳤으면 **그림도 함께** 보세요

`혼자와 함께한` 을 `혼자 한` 으로 고쳤는데, 그 칸의 그림은 여전히
**손 잡은 두 아이**였습니다. 글만 읽으면 맞는데 화면을 보면 어긋납니다.
글을 읽지 못하는 학생일수록 **그림을 먼저** 봅니다.
→ 말을 고친 자리는 반드시 화면을 열어 그림까지 확인하세요.

### (27) ⛔ 화면 말을 바꿀 때 **담아 두는 값 이름은 바꾸지 마세요**

`전시할래요` 를 다른 말로 바꾸면서 값 이름(`exhibit`)까지 바꾸고 싶어집니다.
그러면 **예전에 저장해 둔 기록의 표시가 통째로 사라집니다.**
localStorage 에 이미 `exhibit` 로 적혀 있기 때문입니다.
→ 보이는 말만 바꾸고, 값 이름은 그대로 두고 주석으로 까닭을 남깁니다.
▸ 이 덕분에 `전시` → `중요 표시` → 다시 `전시` 로 **두 번 오가는 동안에도**
  전에 표시해 둔 일기가 그대로 살아 있었습니다.

### (28) ⛔ 학생 말이 **어려워 보인다고** 함부로 쉬운 말로 바꾸지 마세요

`전시할래요` 가 어려워 보여 `중요 표시` 로 바꿨다가 되돌렸습니다.
특수교육에서는 **낱말 자체가 배울 거리**입니다. `전시` 는 학교에서 늘 쓰고
게시판에 붙은 제 작품을 보며 익히는 **구체적인 말**인 반면, `중요` 는 눈에
보이지 않는 **추상어**라 오히려 어렵습니다.
그리고 실제로 벌어지는 일이 전시라서, 다른 말로 부르면 **말과 일이 벌어집니다.**
→ 말을 쉽게 바꾸자는 생각이 들면 **먼저 선생님께 여쭙니다.**
  그 말을 배우게 하는 것이 목표일 수 있습니다.

### (29) ⛔ **바꾸는 길**을 만들면 **되돌리는 길**도 함께 만드세요

`그림 다시 그리기` 는 있는데 돌아올 길이 없었습니다. 학생은 한 번 누르면
원래대로 못 돌린다는 것을 모르고 눌러 봅니다.
→ 무언가를 바꾸는 단추를 넣을 때는 **되돌리기도 같은 화면에** 두세요.
→ 그리고 되돌려도 **원래 것이 지워지지 않아야** 마음 놓고 눌러 봅니다.
→ 되돌릴 것이 없을 때는 단추를 **숨깁니다** (늘 보이면 무엇을 되돌리는지 모름).

### (30) ⛔ 뜻이 같은 자리에는 **그림도 말도 또 만들지 마세요**

`새로 해본 것` 과 `처음 해 본 ○○`, `친구와 같이` 와 `친구와 함께한 것` 은
각각 한 가지 뜻입니다. 자리마다 다른 말·다른 그림을 쓰면 학생은 그것을
**다른 것**으로 봅니다. 낱말 자체가 배울 거리라 익힐 것이 곱절이 됩니다.
→ 새 낱말·새 그림을 만들기 전에 **이미 있는 것에 같은 뜻이 없는지** 먼저 봅니다.
→ 학생이 고르는 낱말을 **한자리에 모아 놓고** 훑으면 겹침이 잘 보입니다.

`친구와 함께한 것` · `친구와 같이` · `○○와 함께한 제목` 은 모두 **함께**라는
한 가지 뜻입니다. 자리마다 다른 그림을 그리면 학생은 그것을 **다른 것**으로
봅니다. 있는 그림을 그대로 쓰는 편이 낫습니다 (`잘 모르겠어요` 를 한 장으로
모아 둔 것과 같은 뜻입니다).
→ 그림을 새로 시키기 전에 **이미 있는 그림에 같은 뜻이 없는지** 먼저 보세요.

### (31) ⛔ 한글이 든 .ps1 은 **BOM 을 붙여** 저장하세요

윈도우 파워셸 5.1 은 BOM 이 없으면 ANSI 로 읽습니다. 한글 경로가 깨져서
파일을 하나도 못 찾고 **오류도 없이 그냥 끝납니다.** 아무 일도 안 일어나는데
왜 그런지 알 길이 없어 한참 헤맸습니다.

### (32) ⛔ 셸 홑따옴표 안에 홑따옴표가 든 글을 넣지 마세요 (도구 쪽 함정)

`perl -pe '...'` 안에 `'--dv'` 같은 홑따옴표가 있으면 셸이 거기서 글을
끊어 버려, 찾는 글이 어긋나 **아무 데도 못 찾습니다.**
→ 바꿀 글에 따옴표가 있으면 **heredoc 으로 파일에 써서** 넘깁니다.

### (17) ⛔ 활동 이름에 **장소를 넣지 마세요**

장소는 따로 묻습니다. 이름에 또 들어가면 `공원에서 공원 가기를` 이 됩니다.
새 활동을 더할 때 `장소에서 + planText` 를 문장으로 읽어 보고 정하세요.

---
홈에 오늘의 계획 표시 → `활동을 했어요` → 일기 자동 입력 → 기분·다시하기 선택 →
일기 저장 → 발자국 자동 생성 → 좋아해요·도전 선택 → 지도에 함께 표시 →
포트폴리오 자동 추가 → 전시 기록 선택 → 기간 선택 → 전시판형·책자형 미리보기 →
인쇄/PDF → 새로고침 후 유지 → JSON 내보내기·불러오기

**모두 실제로 동작을 확인했습니다.** (19번은 위 9번 단서 참고)

### 14-21. 힌트 보고 쓰기 세 가지 + 일기 단계 설명 (2026-08-21 늦게)

**① 원고지 맨 윗칸 윗선이 잘려 보이던 것**

칸(`.pd-box`)은 **오른쪽·아래쪽 선만** 그립니다. 그래서 격자 자체에 위선·왼선을
주지 않으면 **맨 윗줄과 맨 왼쪽이 뚫린 채**로 나옵니다.

    .pd-grid{ border-top:1px solid #333; border-left:1px solid #333; }

▸ 칸마다 네 변을 다 그리면 선이 두 겹으로 굵어집니다. **격자에 두 변만** 더하는 것이 맞습니다.

**② 힌트 보기 단추를 「힌트 보고 쓰기」 바로 오른쪽으로**

줄 오른쪽 끝(`.pd-modehint`)에 멀찍이 있어서 **눌러 볼 생각을 못 한다**는 지적을
받았습니다. 그 모양을 골라야만 나타나는 단추라, 원인과 결과가 떨어져 있으면
둘이 이어진 것인 줄 모릅니다. → 10px 오른쪽에 붙였습니다 (`.pd-hintbtn`).

⚠ **흰 종이 안에는 두지 않습니다.** 종이는 인쇄되는 결과물이라 조작 단추가 섞이면 안 됩니다.

**③ 힌트 보기를 원고지 양식으로**

글자만 줄글로 보여 주면 학생이 **어느 칸에 무엇을 쓰는지** 스스로 옮겨 놓아야
합니다. 띄어쓰기 한 칸, 문장부호가 들어갈 칸까지 **눈으로 그대로 베낄 수 있어야**
보고 쓰기가 됩니다. → 아래 원고지와 **같은 칸 수·같은 줄**로 그립니다.

▸ 칸 수(`g.cols`)와 줄(`g.rows`)은 **아래 원고지가 쓰는 것을 그대로** 씁니다.
  따로 계산하면 언젠가 둘이 어긋납니다.
▸ 칸 한 변은 `HINT_INNER_W / cols` 와 `HINT_INNER_H / rows` 중 작은 쪽.
  `.pd-hintbig` 의 여백·테두리를 고치면 이 두 상수도 같이 고쳐야 합니다.
▸ 3단계(밑줄)는 원고지가 없으므로 지금처럼 문장으로 둡니다.

잰 값 : 힌트 10칸 30칸(3줄) = 아래 원고지 10칸, 노란 상자 밖으로 안 넘침.

**④ 일기 단계 설명을 질문 줄 오른쪽에**

`일기 단계 [1][2][3]` 을 골라도 **무엇이 다른지 알 길이 없었습니다.**
설명(`desc`)이 단추의 `title=` 툴팁에만 있어서, **마우스를 올려야** 보였습니다.
태블릿·전자칠판에는 올릴 마우스가 없어 **아무도 못 보는 설명**이었습니다.

    options.js  diaryLevels 에 note 추가
      1단계 · 그림을 골라 문장을 만들어요
      2단계 · 빈칸을 채워 문장을 완성해요
      3단계 · 틀 없이 내 말로 써요

▸ `누구에게 맞는지`가 아니라 **`무엇을 하는지`** 로 적었습니다. 골라 놓고도
  지금 무엇을 하는 중인지 알아야 하기 때문입니다.
▸ `C.Question` 의 `note` 슬롯으로 **모든 질문 화면 같은 자리**(오른쪽)에 나옵니다.
  질문 알약과 헷갈리지 않게 **테두리 없는 연한 글씨**로 둡니다 — 여기서 또 알약을
  쓰면 무엇이 질문인지 흐려집니다.
▸ 읽어주기에는 넣지 않습니다. 학생에게 읽어 줄 말이 아니라 지금 무엇을 하는지 알리는 안내입니다.
▸ `무엇을 했나요?`(C.ActivityChooser)는 딴 컴포넌트라 `note` 를 prop 으로 넘깁니다.

⛔ **이때 인수인계 2-3 의 함정을 그대로 밟았습니다.**
html 템플릿 안 `<!-- 주석 -->` 에 백틱을 썼더니 템플릿이 거기서 끊겨
`SyntaxError: Unexpected identifier '힌트'` 로 앱이 통째로 멈췄습니다.
**템플릿 안에서는 홑따옴표만** 씁니다. 그 자리에 경고를 적어 두었습니다.

### 14-22. 3단계 문장틀에 **같은 말이 두 번** 나오던 것 (2026-08-21 늦게)

**증상** — 3단계에서 빈칸에 어구로 적으면 틀의 고정 말과 겹쳤습니다.

    나는 [나는 어제] [가족과 함께]와 함께 [강당에서]에서 [노래부르기를 했어요.]를 했어요.
    기분이 [기분이 너무 재미있었어요.].

**까닭 — 같은 문장을 만드는 곳이 둘인데 규칙이 달랐습니다.**

| | 만드는 법 | 결과 |
|---|---|---|
| 위 문장틀 (`frameBar`) | 틀의 고정 말 **+** 학생 글 | 겹침 |
| 내가 만든 뼈대 (`sixLines`) | 학생 글만 이어붙임 | 깔끔 |

3단계는 학생이 **자기 말로** 씁니다. 안내 예시부터 `예) 친구 민수와`,
`예) 학교 놀이터에서` 처럼 **조사까지 포함**해 쓰라고 되어 있습니다.
그런데 틀은 1·2단계용 고정 말(`와 함께`·`에서`·`를 했어요.`)을 그대로 또 붙였습니다.

그래서 선생님이 이런 딜레마에 빠졌습니다.
- **낱말만** 쓰면 → 틀은 깔끔하지만 뼈대가 `어제 가족 강당 노래부르기` 단어 나열
- **어구로** 쓰면 → 뼈대는 자연스럽지만 틀이 겹침

**둘 중 하나를 고르는 문제가 아니라, 규칙이 둘로 갈린 것이 문제였습니다.**

**고침 — 이미 있는 말은 또 붙이지 않습니다** (`headWord` · `tailWord` · `dotOf`)

    나는     w1 이 `나는` 으로 시작하면 생략
    과/와 함께 w2 가 /(와|과|이랑|랑|함께)$/ 로 끝나면 생략
    에서     w3 가 /(에서|에)$/ 로 끝나면 생략
    을/를 했어요. w4 가 종결어미(RE_END)로 끝나면 생략 → **마침표만** 찍음
    기분이   how 가 `기분이` 로 시작하면 생략
    마침표   이미 . ! ? 로 끝났으면 또 찍지 않음

⚠ **끝나는 말을 하나하나 나열하면 반드시 빠집니다.**
처음에 `했어요/했다/…` 를 목록으로 두었다가 `재미있었어요` 를 놓쳐
`재미있었어요를 했어요.` 가 나왔습니다. → **종결어미 모양**(`RE_END`)으로 잡습니다.

    RE_END = /(어요|아요|여요|예요|에요|습니다|했다|았다|었다|였다|한다|이다|았어|었어|였어)$/

⚠ `을/를 했어요.` 를 빼면 **첫 줄이 마침표 없이** 끝납니다 (`…술래잡기를 했다`).
그때는 `dotOf` 로 마침표만 찍습니다.

▸ 1·2단계는 고른 낱말이 그대로 들어와 겹칠 일이 없습니다 — 지금처럼 붙습니다.
▸ **낱말만 쓰게 강제하지 않습니다.** 그러면 뼈대가 단어 나열이 됩니다.

다섯 경우 확인 (모두 한 문장으로 자연스러움)

    낱말로            나는 어제 가족과 함께 강당에서 노래부르기를 했어요.
    어구로            나는 어제 가족과 함께 강당에서 노래부르기를 했어요.
    했다로 끝         나는 어제 친구 민수와 놀이터에서 그네를 타고 술래잡기를 했다.
    섞어서            나는 어제 가족과 함께 강당에서 노래부르기를 했다.
    빈칸 그대로       나는 　　 와 함께 　　 에서 　　 를 했어요.

### 14-23. 「내가 만든 뼈대」가 내가 쓴 문장과 구분되지 않던 것

이름표와 문장이 **한 줄로 이어져** 있어서 이름표가 문장의 첫머리처럼 읽혔습니다.

    내가 만든 뼈대나는 어제 가족과 함께 강당에서 …

→ 이름표는 **알약**으로 떼어 윗줄에, 문장은 **흰 칸**에 담았습니다.

    .bones      display:grid  (이름표 한 줄 → 문장 + 단추 한 줄)
    .bones-lab  알약 (border-radius:999px, 배경 #efdb92)
    .bones-txt  흰 칸 (background:#fff, 테두리 #e6d9a8)

잰 값 : 이름표 폭 123px(글자만큼), 이름표 아래 37px → 문장 위 46px (줄 나뉨), 단추는 문장 오른쪽.

### 14-24. 조사를 손으로 붙여 둔 마지막 한 곳 (`가족와 함께`)

그림일기 아래 알약만 조사를 **손으로 못박아** 두었습니다.

    picdiary.js   ${partner.name}와 함께      →  가족와 함께  (받침이 있으면 `과`)
                                              →  혼자와 함께  (혼자는 사람 이름이 아님)

인수인계 14-7·14-9 에서 두 번이나 고친 함정인데, **이 자리만 남아 있었습니다.**
같은 일을 하는 곳이 여럿이면 한 곳을 놓칩니다.

**고침 — 규칙을 한 곳에 모았습니다** (`korean.js`)

    App.isAloneWord(name)      혼자 인지 가림 (3단계는 학생이 손으로 `혼자` 라고 씁니다)
    App.withPhrase(name)       `가족` → `가족과 함께` · `혼자` → `혼자`
    App.partnerWith(id, ids)   고른 사람(여러 명 포함)으로 만들기

▸ `S.diaryFramesLines` 도 스스로 만들던 것을 `App.withPhrase` 로 바꿨습니다.
▸ `picdiary` 알약은 `App.partnerWith(d.partnerId, d.partnerIds)` 를 씁니다.
▸ **3단계 문장틀의 `혼자` 도 함께 고쳤습니다.** 예전에는 id(`alone`)로만 가려서,
  3단계에서 **손으로 쓴 `혼자`** 를 놓쳐 `혼자와 함께` 가 나왔습니다.
  ⚠ 가리는 자리를 `w2` 가 정해진 **뒤로** 옮겨야 합니다. 앞에서 가리면 늘 빈 값을 봅니다.

확인한 값

    가족 → 가족과 함께 | 엄마 → 엄마와 함께 | 선생님 → 선생님과 함께
    형제자매 → 형제자매와 함께 | 혼자 → 혼자 | 여러 명 → 엄마와 아빠와 함께
    그림일기 알약 : 가족과 함께 · 엄마와 함께 · 혼자
    3단계 문장틀 : 혼자 강당에서… / 엄마와 함께… / 선생님과 함께…

⛔ **또 백틱 함정을 밟았습니다** (오늘 두 번째). html 템플릿 안 `<!-- 주석 -->` 에
`와` 를 백틱으로 감쌌더니 `SyntaxError: Unexpected identifier '와'` 로 앱이 멈췄습니다.
→ 전체 파일을 훑어 템플릿 안 주석의 백틱을 모두 없앴습니다. 검사하는 방법 :

```
awk '/html`/{t=1} t&&/<!--/{c=1} c&&/`/&&!/html`/{print FILENAME": "FNR} c&&/-->/{c=0}' js/**/*.js
```

### 14-25. 1단계 일기의 그림과 문장이 너무 작던 것 (2026-08-22)

**증상** — 1단계 완성 화면에서 그림도 글도 알아보기 어려울 만큼 작았습니다.

    1280x720 에서 잰 값 : 종이 226x320 (배율 0.285), 원고지 글자가 화면에서 13px

**까닭 — 인쇄용 A4 를 통째로 줄여 보여 주고 있었습니다.**

    fitDv = min( (창높이 720 − 400) ÷ 1123 = 0.285 ,   ← 이쪽이 병목
                 (창폭 1280 − 560) ÷ 794  = 0.907 )

창 높이의 **56%(400px)** 를 맨 위 줄·질문 줄·아래 단추가 먹습니다.
종이에 320px 만 남으니, **A4 는 세로로 길어서 폭까지 226px 로 눌립니다.**
가로는 490px 이나 남는데도 못 씁니다. **좌우 2단으로 나눠도 소용이 없습니다** —
세로가 모자라면 가로도 못 늘리는 구조이기 때문입니다.

**고침 — 1단계는 「그림과 문장만」 크게** (`C.BigLook`)

1단계 학생은 **글자를 못 읽습니다.** 그림이 핵심이고 문장은 읽어주기로 듣습니다.
그런데 인쇄용 종이를 통째로 줄이면 정작 봐야 할 것이 가장 작아집니다.

    종이의 그림 부분(날짜줄 64 + 그림칸 535 = 599px)까지만 크게 잘라 보여 주고
    문장은 그 옆(또는 아래)에 큰 글씨로. 인쇄 모양 전체는 `인쇄 모양 보기` 로.

▸ **종이(`C.PicDiarySheet`)를 그대로 씁니다.** 그래서 사진 · 내가 그린 그림 ·
  조립 그림이 모두 그대로 나옵니다. 따로 그리면 둘이 언젠가 어긋납니다.
▸ 2·3단계는 **그대로** 종이 미리보기를 씁니다 (글을 읽는 단계라 원고지가 중요).

**★ 문장을 옆에 둘지 아래 둘지 — 두 가지를 다 셈해 큰 쪽을 고릅니다**

넓은 화면은 세로가 병목이라 **옆**이 유리하고, 아이패드처럼 폭이 좁으면
**아래**가 유리합니다. 한쪽으로 못박으면 한 기기에서 반드시 손해를 봅니다.
(1024x768 에서 옆에 두면 407px, 아래에 두면 549px 였습니다.)

**잰 결과 — 네 기기 모두 스크롤 0**

| 기기 | 크기 | 배치 | 그림칸 | 예전 |
|---|---|---|---|---|
| 전자칠판 FHD | 1920×1080 | 옆 | **794×599** | 226×320 |
| 노트북 · 전자칠판 HD | 1366×768 | 옆 | **670×505** | |
| 태블릿 가로 | 1280×800 | 아래 | **591×446** | |
| 아이패드 가로 | 1024×768 | 아래 | **549×414** | 351×265 |

⚠ **제 칸(`.look-row`)을 재면 안 됩니다.** 그 칸은 내용만큼 커지는 칸이라,
자기 크기를 재서 자기 자식 크기를 정하는 **돌림**이 됩니다.
그렇게 했더니 그림이 처음 값 그대로 351px 에서 꿈쩍하지 않았습니다.
→ **창 크기**로 잽니다 (`innerHeight − 250`, `innerWidth − 420`).

⚠ **`.confirm-2col > .look-row` 규칙을 빠뜨리면 오른쪽이 잘립니다.**
왼쪽 칸이 `flex:1 1 0` 이라 폭을 다 가져가는데 속 그림은 그대로여서,
1920 에서 그림 오른쪽이 **162px 잘려** 나갔습니다.
`.dv-row` 에만 규칙이 있고 새 칸에는 없었습니다 — **새 칸을 만들면 이 규칙도 같이** 주세요.

### 14-26. 그림 세 장 반영 — 전시해요 · 전시하지 않아요 · 다른 활동을 (2026-08-22)

선생님이 그려 주신 그림 세 장을 넣었습니다. 셋 다 **512x512 투명 배경**으로 규격에 맞습니다.

| 어디 | 파일 | 예전 |
|---|---|---|
| 이 일기를 전시하고 싶어요? — 전시할래요 | `images/전시해요.png` | 코드로 그린 SVG(bookmark) |
| 〃 — 전시하지 않을래요 | `images/전시하지 않아요.png` | 코드로 그린 SVG(dash) |
| 다음에는 — 다른 활동을 | `images/일기 낱말/다른 활동을.png` | 또하기 그림을 빌려 씀 |

**전시 두 칸** — `IMAGE_BASE.ui` 에 `exhibitYes` · `exhibitNo` 로 등록하고
`App.uiImage()` 로 부릅니다. 파일이 없으면 예전 SVG 가 그대로 나옵니다.

▸ 새 그림도 **책갈피 모양**이라 안내 문구(`전시하기로 고른 일기에는 책갈피가 붙고…`)를
  고칠 필요가 없었습니다. 별(★)이 아니어서 여가 지도의 `도전하고 싶어요` 와도
  겹치지 않습니다 (인수인계 14-12 의 규칙 그대로).
▸ 파일 이름은 `전시해요` · `전시하지 않아요` 이고 화면에 보이는 말은
  `전시할래요` · `전시하지 않을래요` 입니다. **파일 이름은 학생에게 보이지 않으므로**
  그대로 두었습니다.

**다른 활동을** — 새 그림을 **정본으로 삼아 한 벌로 모았습니다.**

두 자리는 뜻이 같습니다 — 둘 다 `이 활동 말고 다른 것` 입니다.

    또 하고 싶나?    다른 것도 하고 싶어요
    다음에는?        다른 활동을 (하고 싶어요)

⚠ 잠깐 이 자리에 전용 그림을 따로 두었더니, 거의 같은 그림이 **두 벌**이 되어
  테두리 색만 미묘하게 다른 두 판이 생겼습니다. 학생이 두 화면에서 **다른 그림**을
  보게 되고, 나중에 한쪽만 고치면 더 벌어집니다.

**정리한 결과 — 파일 하나, 그림 하나**

    images/또하기/다른 것도 하고 싶어요.png     ← 새 그림으로 덮어씀 (정본)
    images/일기 낱말/다른 활동을.png            ← 지움
    diary.js  F4_WORDS 의 `다른 활동을` 에 use 를 다시 붙임

▸ 그림을 바꾸려면 **`images/또하기/다른 것도 하고 싶어요.png` 하나만** 바꾸면
  두 화면이 함께 바뀝니다.
▸ `images/일기 낱말/README.md` 에도 이미 그렇게 적혀 있습니다.

확인 : 세 그림 모두 512x512 로 불러와지고, 전시 창에서 SVG 가 아니라
`images/전시해요.png` · `images/전시하지 않아요.png` 가 실제로 나옵니다.
두 화면이 **같은 파일 하나**를 부르고, 지운 파일은 더 이상 불리지 않습니다.

### 14-27. 포트폴리오를 쉽고 간결하게 (2026-08-22)

**증상** — 선생님 도구를 켜면 학생 화면 위에 층이 쌓여 **좌우 3쪽**으로 갈라졌습니다.

    상단바 72 + 기간 52 + 일기장 197 + 모음 113 + 내용 213 + 한마디 125 + 돌아보기 295
    = 1366x768 의 무대(696px)를 훌쩍 넘김

**찾은 문제 넷**

    ① 학생 일과 선생님 일이 한 화면에 섞임
       인쇄·판형·기간은 선생님 일, 모음 고르기·한마디·돌아보기는 학생 일
    ② 같은 것을 두 이름으로 부름
       내가 세운 계획 / 내가 세운 여가계획들 · 나의 일기장 / 나의 여가 일기장
    ③ 단추 넷이 같은 크기로 늘어서 무엇이 중요한지 안 보임
    ④ 한마디·돌아보기가 **선생님 도구 안에만** 있어서 정작 학생이 못 씀

**고침**

    ▸ 선생님 도구를 **창 하나**로 모음 (기간 · 일기장 인쇄 3판형 · 전시판형 ·
      책자형 · 교실 TV 전시). 상단 `선생님 도구` 단추로 엽니다.
    ▸ 학생 화면에는 **지금 보는 기간 한 줄**만 남김
    ▸ 판형 탭 셋을 없애고, 판형을 보는 중일 때만 `포트폴리오로 돌아가기` 를 둠
      (탭이 늘 있으면 학생이 무엇을 눌러야 할지 헷갈립니다)
    ▸ 인쇄 단추는 **판형을 보고 있을 때만** 아래에 나옵니다
    ▸ 이름을 고르는 칸과 **똑같이** 맞춤 (내가 세운 계획 · 나의 일기장)
    ▸ `일기장 인쇄하기` 만 크게, 판형 둘은 작게
    ▸ 한마디·돌아보기를 **학생 화면으로** 옮기고 말도 바꿈 (학생의 한마디 → 나의 한마디)

**★ 한마디·돌아보기를 넷째 칸으로**

두 칸을 모음 아래에 늘 붙여 두었더니, 일기장을 고르면 일기 카드까지 더해져
**4쪽**까지 갈라졌습니다. 이 화면은 원래 **한 번에 하나만 골라 보는 곳**입니다.
같은 규칙을 적용해 `나의 한마디` 를 넷째 칸으로 넣었습니다.

    [내가 세운 계획 3장] [나의 여가지도 5가지] [나의 일기장 5장] [나의 한마디 0 / 5]

`나의 한마디` 칸의 숫자는 **채운 칸 수 / 5** 입니다 (한마디 1 + 돌아보기 4).

⚠ **칸 수가 늘면 열 수도 같이 늘려야 합니다.** 4칸을 3열(`repeat(3,...)`)에
두었더니 두 줄로 접혀 높이가 **113 → 220px** 이 되었고, 그만큼 아래가 밀려
쪽이 갈라졌습니다. → `repeat(4,...)` 로 고침 (좁은 화면은 2열).

▸ 한마디와 돌아보기는 **나란히** 둡니다. 위아래로 쌓으면 162+332=494px 라
  화면을 넘기지만, 옆으로 놓으면 큰 쪽(332px) 높이만 쓰면 됩니다.
  둘 다 학생이 글을 쓰는 곳이라 뜻으로도 맞습니다.

**잰 결과 — 세 기기 모두 스크롤 0**

| 고른 칸 | 1920×1080 | 1366×768 | 1024×768 |
|---|---|---|---|
| 내가 세운 계획 | 한 쪽 | 한 쪽 | 한 쪽 |
| 나의 여가지도 | 한 쪽 | 한 쪽 | 한 쪽 |
| 나의 한마디 | 한 쪽 | 한 쪽 | 한 쪽 |
| 나의 일기장(5장) | 3쪽 | 3쪽 | 3쪽 |

일기장만 여러 쪽인데, 이는 **일기가 5장이라 그런 것**이라 자연스럽습니다
(쪽 넘김은 이 앱이 스크롤 대신 쓰는 방식입니다).

### 14-28. 완성 화면이 빈 쪽으로 갈라지던 것 — 흰 칸을 **직접 재기** (2026-08-22)

**증상** — 일기 완성 화면이 `1 / 3` 로 갈라지고 **1쪽에는 질문 줄만** 남았습니다.

**까닭 둘**

**① 창 크기로 어림잡았습니다.**
`fitDv` 가 `창높이 − 400` 으로 종이 크기를 정했습니다. 어림값은 화면마다
맞기도 하고 안 맞기도 합니다. 1920x1080 에서 종이가 680px 이 되어
흰 칸(761px)에 **질문 줄과 함께** 들어가지 못했고, `.confirm-2col` 의
`break-inside:avoid` 때문에 본문이 **통째로 2쪽으로 밀렸습니다.**

    → 흰 칸(.stage-track)을 **직접 재고**, 형제(질문 줄)가 쓰는 높이를 뺍니다.
      C.DiaryPreview 의 fitDv · C.BigLook 의 measure 둘 다 고쳤습니다.
    ▸ 흰 칸은 처음 그릴 때 크기가 없을 수 있어 **rAF · 220ms 뒤**에 다시 잽니다.

**② 무대 안의 칸이 단(column)보다 넓었습니다.**
무대는 여러 단으로 쪽을 나눕니다. 그 안의 칸이 단보다 넓으면 **두 단을 차지**해
넘길 쪽이 늘고 빈 쪽이 생깁니다.

    .confirm-2col  offsetWidth 2062 / clientWidth 1014  ← 두 단에 걸쳐 조각남
    .look-say      아래 배치일 때 1285px 로 부풀어 무대(1257)를 넘김

    → .confirm-2col · .look-row 에 max-width:100%
    → 문장칸(.look-say)에는 폭 상한을 **픽셀로** (그림칸 폭에 맞춤)

⚠ `width:100%` 로만 두면 부모 폭을 따라가고 부모는 다시 이 칸을 따라가
**서로 부풀어 오릅니다.** 픽셀로 못박아야 멈춥니다.

⛔ **무대 안에 flex/grid 칸을 새로 놓을 때는 반드시 `max-width:100%`** 를 주세요.

**잰 결과 — 다섯 크기 × 세 단계 모두 한 쪽, 스크롤 0**

| 화면 | 1단계 | 2단계 | 3단계 |
|---|---|---|---|
| 1920×1080 | 한 쪽 | 한 쪽 | 한 쪽 |
| 1366×768 | 한 쪽 | 한 쪽 | 한 쪽 |
| 1280×800 | 한 쪽 | 한 쪽 | 한 쪽 |
| 1024×768 | 한 쪽 | 한 쪽 | 한 쪽 |
| 1257×1226 (세로 긴) | 한 쪽 | 한 쪽 | 한 쪽 |

### 14-29. 사진 고르기 창이 쓸데없이 옆으로 길던 것

속은 `사진 넣기` 단추 하나뿐인데 `wide`(1100px)로 열려 화면 폭을 다 썼습니다.
`C.Modal` 에 **narrow**(460px)를 더하고, 사진 유무로 골라 씁니다.

    사진 없음 → narrow 460px (단추 하나)
    사진 있음 → 보통  760px (격자를 펼쳐야 함)
    그림판·인쇄 모양 → wide 1100px (그대로)

### 14-30. ⛔ 백틱 함정을 **하루에 세 번** 밟았습니다

html 템플릿 안 `<!-- 주석 -->` 에 백틱을 쓰면 템플릿이 거기서 끊겨
**화면이 통째로 빕니다.** 오늘만 세 번 밟았습니다 (14-21 · 14-24 · 사진 창).

고칠 때마다 이 명령으로 전수 검사하세요.

```
awk '/html`/{t=1} t&&/<!--/{c=1} c&&/`/&&!/html`/{print FILENAME": "FNR": "$0} c&&/-->/{c=0}' js/screens/*.js js/components/*.js js/app.js
```

### 14-31. 선생님 설정 7·8번에 「더하기」

`우리 반 활동 더하기` 와 **같은 방식**입니다.

    7. 함께하는 사람  ＋ 사람 더하기   이름 + 그림
    8. 기분           ＋ 기분 더하기   고를 때 보이는 말 + **일기에 쓸 말** + 그림

▸ 더한 것은 초록 테두리(`mine`)로 보이고, 아래 `더한 사람 지우기` 로 지웁니다.
  기본 항목은 **끄기만** 됩니다 (예전 기록이 가리키던 것이 사라지면 안 됩니다).
▸ 조사는 손으로 붙이지 않습니다 — `App.waGwa` 가 정합니다 (선생님**과** 함께).
▸ 기분은 **일기 문장에 그대로** 들어갑니다. `설레요` 를 그대로 쓰면
  `기분이 설레요.` 가 되어 어색하므로 **일기에 쓸 말**(`설렜어요`)을 따로 받습니다.
  비워 두면 이름을 그대로 씁니다.

⚠ **목록을 새 배열로 갈아 끼우면 안 됩니다.**
`App.partner` · `App.mood` 는 `finder(D.partners)` 로 만들어져 **그때의 배열을
붙잡고** 있습니다. 새 배열을 대입했더니 고르는 칸에는 나오는데 `App.mood(id)` 는
못 찾아서 **문장이 통째로 비었습니다.** → `refill()` 로 **제자리에서** 비우고 다시 채웁니다.

확인 : `활동보조 선생님과 함께` · `기분이 설렜어요.`

### 14-32. ⚠ 백업에 「우리 반 활동 · 사람 · 기분」이 빠져 있던 것 (2026-08-22)

**전 화면 회귀 점검을 돌리다 찾았습니다.**

`exportData` 가 담는 것에 `customActivities` 가 **처음부터 없었습니다.**
오늘 더한 `customPartners` · `customMoods` 도 마찬가지였습니다.

**무엇이 문제인가** — 백업을 받아 다른 기기에서 불러오면
**우리 반 활동 · 더한 사람 · 더한 기분이 통째로 사라집니다.**
학생 기록(계획 · 일기)은 그것들을 **id 로 가리키므로**, 함께 옮기지 않으면
일기에 활동 이름이 안 나오고 문장도 비어 버립니다.

**고침**

    exportData   customActivities · customPartners · customMoods 를 함께 담음
    importData   replace : 그대로 넣고 syncCustom() 으로 목록에 바로 반영
                 merge   : **id 가 같으면 건너뜀**

⚠ 덧붙이기에서 **id 를 새로 매기면 안 됩니다.** 학생 기록이 가리키던 것과
끊어져서, 옮긴 뒤 일기에 활동이 사라집니다. 학생 id 는 겹치면 새로 매기지만
(기록을 함께 옮기므로) 더한 것들은 **id 를 지켜야** 합니다.

**왕복 시험 통과**

    더하기(활동·사람·기분) → 내보내기 → `모두 바꾸기` 로 불러오기 → 셋 다 그대로 ✅
    같은 파일을 `덧붙이기` 로 한 번 더 → 중복 안 생김 ✅

### 14-33. 전 화면 회귀 점검 (2026-08-22)

오늘 CSS·구조를 많이 고쳐서 **모든 화면 × 세 크기**를 다시 돌렸습니다.

| 화면 | 1920×1080 | 1366×768 | 1024×768 |
|---|---|---|---|
| 표지 · 학생 고르기 · 캐릭터 · 홈 | 1쪽 | 1쪽 | 1쪽 |
| 계획하GO! | 1/5 | 1/5 | 1/5 |
| 여가지도 · 기록하GO! · 모아보GO! | 1쪽 | 1쪽 | 1쪽 |
| 그림일기 · 선생님 설정 | 1쪽 | 1쪽 | 1쪽 |

**세로·가로 스크롤 모두 0. 단(column)보다 넓은 칸 없음.**

▸ 계획하GO! 의 1/5 는 **활동 카드가 많아서** 나뉜 것이라 정상입니다
  (쪽 넘김은 이 앱이 스크롤 대신 쓰는 방식).
▸ 점검에 쓴 잣대 : 세로/가로 스크롤 · 쪽 수 · **무대 안 칸이 단보다 넓은지**
  (마지막 것이 빈 쪽을 만드는 원인입니다 — 14-28 참고).

### 14-34. 찾아보기 · 지도 도움말을 **섬 안에서 늘 보이게** (2026-08-22)

섬 안(2층)에 들어가도 오른쪽 위 `모아보기` 만 있고 **찾아보기와 지도 도움말이
아예 없었습니다.** 선생님 설정 6번의 `지도 찾아보기·확대` 스위치를 켜야만
나오게 되어 있었기 때문입니다(`student.mapTools`).

**왜 늘 보이게 바꿨나**

  · 활동 카드가 **4쪽**까지 있습니다. 찾아보기가 없으면 원하는 활동을 찾기 어렵습니다.
  · 지도 도움말은 도장 네 가지(해봤어요 · 좋아해요 · 도전하고 싶어요 · 잘 모르겠어요)의
    **뜻을 알려 주는 유일한 자리**입니다.
  · 켜는 곳을 모르면 학생도 선생님도 이 둘을 **아예 쓸 수 없었습니다.**
    숨겨서 얻는 것이 없고, 숨겨서 잃는 것만 있었습니다.

    js/screens/map.js   L.focus && student && student.mapTools && …  →  L.focus && …
                        (도구 줄 · 찾아보기 상자 두 곳)

▸ **섬 고르기 화면(1층)에는 그대로 두지 않습니다.** 거기서 할 일은
  `어느 섬에 갈까?` 하나뿐인데 셋으로 늘면 고르기 어려워집니다.
  둘 다 활동 카드를 볼 때 쓰는 것이라 섬 안에만 둡니다.

⚠ **선생님 설정 6번의 `지도 찾아보기·확대` 스위치를 지웠습니다.**
아무 일도 하지 않는 스위치를 남겨 두면 켜고 꺼도 화면이 그대로라
**선생님이 고장인 줄 압니다.** 하는 일이 없어진 스위치는 지웁니다.
▸ 담아 두는 값(`mapTools`)은 지우지 않았습니다 — 예전 기록에 남아 있어도
  해가 없고, 지우면 되돌리기 어려워집니다 (함정 27 과 같은 까닭).

확인 (`mapTools:false` 인 학생으로) : 1층에는 안 나오고, 섬 안에서는 둘 다 나옴.
찾아보기 펼침 → 거르개 다섯(모두 보기 · 해봤어요 · 좋아해요 · 도전하고 싶어요 · 잘 모르겠어요)
→ 고르면 `해봤어요 만 보여요` 알약 → 접힘. 도움말 창에 도장 네 가지 뜻 나옴.
전 화면(홈 · 계획 · 지도 · 일기 · 설정) 스크롤 0 · 깨진 그림 0.

### 14-35. 모아보기 — 섬 이름을 **누를 수 있는 알약**으로 (2026-08-22)

분홍 질문 바 안에 이렇게 적혀 있었습니다.

    실외 여가 섬 — 내가 표시한 활동을 모아 볼까요?

**두 가지가 문제였습니다.**
  · 바가 길어져 질문이 한눈에 안 들어옵니다.
  · `실외 여가 섬` 이 **읽을 것**일 뿐 아무 일도 하지 않습니다.
    돌아가려면 화면 맨 위 끝 파란 화살표까지 가야 했습니다.

**고침 — 섬 이름을 질문 왼쪽 알약으로 떼어 내고, 누르면 그 섬으로 갑니다.**

    [◀ 실외 여가 섬]  ( 내가 표시한 활동을 모아 볼까요? )

  이름표와 돌아가는 지름길을 **한 가지가 겸합니다.**

▸ 색은 포트폴리오의 실내·실외 표시(`.folio-where`)와 **같은 색**을 씁니다.
  같은 뜻에는 같은 색 — 한 곳에서 익히면 다른 곳에서도 압니다.

    실내  밝은 주황  #ffe9cc / 테두리 #c96a1e
    실외  밝은 하늘  #d6ebf9 / 테두리 #1f6e9e

▸ 색만으로 가르지 않도록 **글자도 함께** 넣습니다 (실내/실외 여가 섬).

⚠ 예전에 `같은 일을 하는 길이 셋` 이라 위쪽 알약을 지운 적이 있습니다(규칙 7).
이번 알약은 **중복이 아닙니다** — 원래 그 자리에 있던 이름표를 누를 수 있게
바꾼 것이고, 파란 화살표는 화면 맨 끝이라 **먼 길**입니다.
표시 하나를 열어 본 화면에서는 알약이 나오지 않습니다(그때는 `네 가지 표시로`가
먼저이므로 화살표 하나면 충분합니다).

확인 : 실외/실내 알약 → 각 섬으로 정확히 이동. 파란 화살표는 그대로
`표시 열림 → 네 칸 → 섬` 한 걸음씩. 스크롤 0.

### 14-36. ⛔ 완성 화면 빈 쪽 — **세 번째 재발**, 이번엔 뿌리를 끊었습니다 (2026-08-22)

14-28 에서 고쳤는데 또 났습니다. 그래서 **왜 자꾸 되풀이되는지**부터 봤습니다.

**진짜 원인 — 다단(column)은 조금만 넘쳐도 통째로 밀어냅니다**

    흰 칸 599 − 질문 줄 60 = 자리 539
    본문(.confirm-2col) 573                    ← 단 34px 넘침
    break-inside:avoid  → 통째로 2쪽으로 밀림  → 1쪽에 질문 줄만

**왜 예전 대책이 계속 뚫렸나**
지금까지는 `내용이 자리에 들어가도록 크기를 맞추는` 방식이었습니다(fitDv).
그런데 fitDv 는 **오른쪽 그림일기만** 줄입니다. 실제 높이를 정하는 것은
**왼쪽 칸(문장 칸 + 고치는 길 세 줄)** 일 때가 많습니다.
화면 크기 · 글 길이 · 단추 줄바꿈 **어느 하나만 달라져도 다시 넘칩니다.**
즉 `이번 화면에서 맞추는` 방식은 언제든 또 뚫립니다.

**이번 해법 — 두 겹으로 막습니다**

  ① 확인 화면은 **쪽을 나누지 않습니다** (C.Stage 의 onePage → .no-page)
     한 장으로 보여 줄 화면을 다단에 맡기면, 넘칠 때마다 빈 쪽이 생깁니다.
  ② 그래도 넘치면 **통째로 줄입니다** (useFitOnePage → zoom)
     무엇이 높이를 만들든 상관없이 들어갑니다.

**밟은 함정 세 가지 — 다음 사람은 밟지 마세요**

⚠ **zoom 만으로는 못 막습니다.** 다단은 **줄이기 전 높이**로 쪽을 나눕니다.
  zoom 0.69 를 걸어 화면 높이가 247 이 되어도, 레이아웃 높이는 380 그대로라
  여전히 밀렸습니다. **나누기 자체를 꺼야(①)** 합니다.

⚠ **zoom 이 걸린 요소를 재면 안 됩니다.** 줄인 것을 또 재어 값이 겉돌다가
  0.94 에서 멈췄습니다(필요한 값은 0.52). → **껍데기(.confirm-fit)에 zoom 을 걸고
  알맹이(.confirm-2col)를 잽니다.** 알맹이의 offsetHeight 는 부모 zoom 과 무관합니다.

⚠ **상태(useState)로 넘기지 마세요.** 다시 그리는 차례와 엇갈려, 줄여야 하는데도
  zoom 이 1 인 채로 남았습니다(1366x640 에서 20px 잘림).
  → **DOM 에 바로 씁니다** (`box.style.zoom`). React 는 이 속성을 건드리지 않습니다.

▸ 여유를 넉넉히(24px) 두고 안전 몫 0.97 을 곱합니다. 딱 맞게 셈하면 재고 나서
  그림·글꼴이 마저 올 때 **몇 px 차이로 또 밀립니다** (실제로 4px 로 밀렸습니다).
▸ 창 크기 변화를 놓치지 않도록 **ResizeObserver 로 흰 칸과 알맹이를 지켜봅니다.**
  전자칠판·미리보기에서는 resize 이벤트가 안 올 때가 있습니다.
▸ 가장 작게(0.5) 줄여도 넘치면 **콘솔에 알립니다** — 조용히 1쪽이 비지 않게.

**잰 결과 — 다섯 크기 × 세 단계 모두 한 쪽**

| 창 | 1단계 | 2단계 | 3단계 |
|---|---|---|---|
| 1920×1080 | 한 쪽 (zoom 1.00) | 한 쪽 | 한 쪽 |
| 1247×1130 (선생님 창) | 한 쪽 (0.95) | 한 쪽 | 한 쪽 |
| 1280×800 | 한 쪽 (0.95) | 한 쪽 | 한 쪽 |
| 1366×640 | 한 쪽 (0.95) | 한 쪽 | 한 쪽 |
| 1024×768 | 한 쪽 (1.00) | 한 쪽 | 한 쪽 |
| 1024×480 (극단) | 한 쪽 (0.57) | 한 쪽 | 한 쪽 |

다른 일기 두 개(dy_sample1 · dy_sample3)로도 한 쪽.
전 화면 회귀(홈·계획·지도·모아보기·일기·설정·포트폴리오) 스크롤 0 · 깨진 그림 0.

⚠ **백틱 함정을 또 밟았습니다** (14-30 에 이어 네 번째).
html 템플릿 안 주석에 백틱을 써서 앱이 통째로 멈췄습니다.
이번에 **전 파일 자동 점검**을 돌려 남은 곳이 없음을 확인했습니다.

    awk '/<!--/{inc=1} inc&&/`/{print FILENAME":"NR} /-->/{inc=0}' js/screens/*.js js/components/*.js

### 14-37. 모아보기 · 포트폴리오 다듬기 (2026-08-22)

**① 표시를 열어 본 화면에 「◀ 내가 표시한 활동」 알약**

`해봤어요` 목록에 들어가면 되돌아갈 길이 **맨 위 파란 화살표뿐**이었습니다.
화면 끝이라 멀고, 여기가 어디에서 들어온 자리인지도 알기 어려웠습니다.

    [◀ 내가 표시한 활동]  ( 🦶 해봤어요 )

▸ 섬 알약(14-35)과 **같은 자리 · 같은 결**입니다. 한 번 익히면 두 곳에서 같습니다.
▸ 옆 질문 알약(.q-pill)과 **같은 분홍**이되 **작게** 둡니다(15.3px / 질문 17px).
  같은 크기면 무엇이 질문인지 흐려집니다 — **질문이 주인공**입니다.
▸ 네 칸 화면으로 돌아가면 이 알약은 사라지고 섬 알약이 나옵니다.

**② 포트폴리오 탭 개수 — `0 / 5` 를 없앴습니다**

    내가 세운 계획 1장 · 나의 여가지도 3가지 · 나의 일기장 3장 · 나의 한마디 0 / 5

「나의 한마디」만 **분수로** 세고 있었습니다. 다른 셋과 꼴이 다른 것도 문제지만,
`0 / 5` 는 학생에게 **5개 중 0개 = 0점**처럼 읽힙니다.
아직 안 쓴 것이 잘못한 것처럼 보이면 안 됩니다.

  → 쓴 만큼만 세어 `2줄`. 아무것도 없으면 **`아직 없어요`**
  → 나머지 세 탭도 0일 때 `0장`·`0가지` 대신 `아직 없어요` 로 맞췄습니다
    (모아보기가 이미 쓰는 말입니다)

**③ 포트폴리오 기간·인쇄를 기본 켬으로**

기본이 숨김이라, 켜는 곳을 모르면 **기간을 바꾸거나 인쇄할 길이 아예 없었습니다.**
포트폴리오는 인쇄해서 붙이는 것이 목적인데 그 길이 막혀 있으면
이 화면이 무엇을 하는 곳인지 알 수 없습니다.

    js/lib/store.js       folioTools: false → true
    js/screens/portfolio.js   !!(student && student.folioTools)
                            → !(student && student.folioTools === false)

⚠ **`!== false` 로 보아야 합니다.** 예전에 만든 학생 자료에는 이 값이 아예 없어서
`!!` 로 보면 **여전히 꺼진 것으로** 읽힙니다. 선생님이 **일부러 끈 경우에만** 꺼집니다.
▸ 설정 6번 안내도 `기본은 숨김이에요` → `둘 다 기본은 켜짐이에요` 로 고치고,
  두 스위치를 나눠 설명했습니다.

확인 : 값이 없는(예전) 학생으로 열어 기간 표시 · 선생님 도구 · 인쇄 단추 모두 나옴.
모아보기 알약 두 가지 모두 정확히 이동. 스크롤 0.

### 14-38. 쉬운 계획에 **「어디에서」가 아예 없던 것** (2026-08-22)

「여가 계획하기 — 쉬운 계획」에서 장소를 고르는 화면이 나오지 않았습니다.
**사라진 것이 아니라 처음부터 없었습니다.**

    detail ? ['area','what','who','when','time','place','supplies','confirm']
           : ['area','what','who','when','confirm']        ← place 없음

**왜 넣어야 하나**
계획에는 장소가 없는데 **일기에서는 장소를 묻습니다.** 그러면 학생이 같은 활동을
두 번 다르게 적게 됩니다. 계획한 것과 일기가 어긋나면 `계획 → 함 → 일기` 라는
이 앱의 흐름 자체가 끊깁니다.

▸ **일기에서 이미 같은 문제를 고쳤습니다** (14 의 BONE 원칙) —
  `3단계에는 장소가 아예 없는 등 빠진 것이 있었습니다. 이제 묻는 내용은 세 단계가
  똑같고, 다른 것은 학생이 스스로 하는 정도뿐입니다.`
  계획에는 그 원칙이 아직 적용되지 않았던 것입니다.

**고침**

    easy : ['area','what','who','when','place','confirm']    ← place 더함 (5 → 6단계)
    planLevels 설명도 고침
      쉬운 계획   활동 · 함께하는 사람 · 날짜 → **활동 · 함께하는 사람 · 날짜 · 장소**
      자세한 계획 장소 · 준비물 · 메모까지   → **시간 · 준비물 · 메모까지**

▸ 자리는 자세한 계획과 **같은 차례(언제 다음)** 에 둡니다.
  두 수준을 오가도 묻는 차례가 같아야 학생이 헷갈리지 않습니다.
▸ 두 수준의 차이는 이제 **묻는 내용이 아니라 얼마나 자세히 적는가**(시간 · 준비물 · 메모)입니다.

확인 : 쉬운 계획 1/6 → 5/6 `어느 곳에서 할까요?` → 확인 화면에 `어디에서 · 교실` 들어감.
자세한 계획은 1/8 그대로. 스크롤 0.

### 14-39. 「나의 여가로 돌아가기」를 모아보기 · 일기 모음에도 (2026-08-22)

계획하GO! 의 마지막 화면에는 있는데, **나의 여가 모아보기**와
**나의 일기 모음**에는 없었습니다. 둘 다 그 코너에서 **할 일이 끝난 자리**라
같은 단추가 있어야 합니다.

    [🏠 나의 여가로 돌아가기]      (계획하GO! 마지막 화면과 같은 모양 · 같은 자리)

▸ 맨 아래 줄(Stage 의 action)에 둡니다. 일기 모음은 인쇄 단추와 나란히.
▸ **되짚는 길이 아니라 나가는 길**입니다. 파란 화살표와 왼쪽 알약은
  한 걸음씩 되짚지만, 이 단추는 **바로 홈으로** 갑니다.
  여기까지 왔으면 한 걸음씩 되짚을 까닭이 없습니다.
▸ 세 곳이 **같은 모양(노란 pastel-yellow · 집 그림 · 같은 말)** 이라
  한 번 익히면 어디서나 같습니다.

⚠ 다른 화면에는 넣지 않았습니다. 계획·일기·지도 **만드는 도중**에는
`한 걸음씩 되짚기`가 맞습니다. 중간에 나가는 단추를 두면 쓰던 것이
날아가는 것처럼 보입니다.

확인 : 두 곳에서 눌러 홈(코너 네 개)으로 정확히 이동.
표시를 열어 본 화면에서도 되돌리기 알약과 함께 나옴.
전 화면(홈·계획·지도·모아보기·일기·일기모음·설정·포트폴리오) 스크롤 0 · 깨진 그림 0.

### 14-40. ⛔ 완성 화면은 **그림일기 양식 그대로** (2026-08-22)

14-25 에서 1단계만 `그림·문장을 크게 잘라 보여 주기`(C.BigLook)로 바꿨는데,
**완성 화면에는 맞지 않았습니다.**

**왜 되돌렸나**
완성 화면은 **편집 화면**입니다. 왼쪽 바에서 그림·문장을 고치면
**오른쪽 그림일기가 그대로 바뀌는 것을 보아야** 합니다.
그런데 크게 보기는 그림칸만 잘라 보여 주어서, 원고지에 글이 어떻게 앉는지
· 종이가 어떤 모양이 되는지를 볼 수 없었습니다.

  → **세 단계 모두** `C.DiaryPreview`(종이 미리보기)를 씁니다.
    크게 보기는 **글자를 만드는 단계**에서 쓸 것입니다 (완성 화면에서는 안 씁니다).

▸ 완성 화면 질문 줄의 **단계 설명(note)도 뺐습니다.** 단계 설명은 `지금 무엇을
  하는 중인지` 알려 주는 말인데, 완성 화면은 이미 다 만든 뒤라 알려 줄 일이 없습니다.

### 14-41. ⚠ 껍데기를 하나 씌우면 **형제를 세는 곳이 다 틀어집니다**

14-36 에서 빈 쪽을 막으려고 `.confirm-fit` 껍데기를 씌웠습니다.
그런데 크기를 재는 코드들이 **흰 칸의 직계 자식**을 훑으며 형제 높이를 뺍니다.

    var col = track.querySelector('.confirm-2col');
    [].forEach.call(track.children, function (c) {
      if (c !== col) used += c.getBoundingClientRect().height;   // ← 본문까지 빼버림
    });

`.confirm-2col` 은 이제 `.confirm-fit` **안**에 있어서 직계가 아닙니다.
그래서 본문 높이까지 `형제가 쓰는 높이`로 빼버렸고, 자리가 확 줄어
**그림이 444×200 으로 쪼그라들었습니다** (의도는 549×414).

  → `track.querySelector('.confirm-fit') || track.querySelector('.confirm-2col')`
    로 **직계를 먼저** 찾습니다. `C.BigLook` · `C.DiaryPreview` 두 곳 모두.

⛔ **무대 안에 껍데기를 씌울 때는 `track.children` 을 훑는 코드를 전부 찾아
  고치세요.** 껍데기는 눈에 안 보이지만 형제 목록은 바뀝니다.

### 14-42. 「나의 여가로 돌아가기」 자리 바로잡기

14-39 에서 **나의 여가 모아보기**에 넣었는데 빼고, **여가 지도 첫 화면**으로 옮겼습니다.

  · 모아보기에서 나가는 길은 **다시 실내섬 · 실외섬으로** 가는 것입니다.
    홈으로 곧장 나가는 단추를 함께 두면 그 흐름이 끊깁니다.
  · 여가 지도 **첫 화면(섬 고르기)** 이 이 코너의 나가는 자리입니다.
    섬 안(2층)에는 두지 않습니다 — 거기서 나가는 길은 왼쪽 위 「지도로」입니다.

**지금 있는 곳 세 군데** (모두 같은 모양 — 노란 pastel-yellow · 집 그림)

    계획하GO! 마지막 화면 · 여가 지도 첫 화면 · 나의 일기 모음

⚠ **백틱 함정을 또 밟았습니다** (다섯 번째). 주석 안에 「지도로」를 백틱으로 감쌌습니다.
**html 템플릿 안 주석에서는 홑따옴표나 낫표(「」)만** 쓰세요.

### 14-43. 완성 화면 짜임새 — 선생님이 그려 주신 모양대로 (2026-08-22)

**왼쪽은 고치는 기둥, 오른쪽은 완성된 그림일기.** 둘이 나란히 놓입니다.

    [일기가 완성되었어요] 🔊            [눌러서 크게 보기]
    ┌──┬──────────────┐   ┌────────────────┐
    │그 │ 그림 자리·크기 바꾸기 │   │  (초록 테두리)   │
    │림 │ 그림 다시 그리기    │   │   날짜 · 날씨    │
    ├──┴──────────────┤   │                │
    │ (자리 옮기기 안내가 들어올 자리) │   │     그림칸      │
    ├──┬──────────────┤   │                │
    │글 │ 완성된 일기 ✎      │   │  제목 · 원고지    │
    │  │ (노란 칸)          │   │                │
    ├──┴──────────────┤   └────────────────┘
    │ 날짜 · 사람 · 장소 바꾸기      │
    │ 일기 저장하기               │
    └───────────────────┘

**맞춘 것 다섯 가지**

  ① `일기가 완성되었어요` 줄과 그림 묶음의 **시작선을 같게**
     → 남는 폭을 가운데로 모으지 않고 **왼쪽 칸이 흡수**하게 했습니다.
       가운데로 모으면 왼쪽에 여백이 생겨 두 줄의 시작선이 어긋납니다.
  ② `눌러서 크게 보기` 를 **질문 줄과 같은 높이 · 종이 한가운데**로
     → `.dv-bar` 를 자리에서 띄웁니다(position:absolute; top:-3.2rem).
  ③ **왼쪽 그림 묶음의 윗선 = 오른쪽 종이의 초록선** (215 : 218)
     → ② 로 단추 줄이 자리를 안 먹으니 종이가 같은 높이에서 시작합니다.
  ④ 자리 옮기기 안내가 들어올 **자리를 미리 잡아 둡니다** (`.move-slot`)
  ⑤ `고른 그림으로 되돌리기` 를 **그림 묶음 바로 아래**로
     (그림에 딸린 일이라 그림 자리에 있어야 무엇을 되돌리는지 압니다)

**저장 단추는 맨 아래 띠가 아니라 왼쪽 기둥 안**에 둡니다.
오른쪽은 보는 자리, 왼쪽은 고치고 저장하는 자리로 갈라집니다.

⚠ **자리를 잡아 두려면 늘 같은 것을 그려야 합니다.**
`speakText` 를 켤 때만 주었더니 **읽어주기 단추가 그때만 생겨** 칸 높이가
달라지고 아래가 53px 밀렸습니다. 늘 그리고 `visibility` 로만 감춥니다.

**이름표는 세로로 긴 칸** (`.fix-lab`). 글자는 가로, 칸만 세로로 깁니다.
⚠ 옛 `.fix-lab` 정의가 **뒤에 남아 새 규칙을 덮고** 있었습니다. 같은 이름의
규칙을 새로 쓸 때는 **앞뒤로 남은 것이 없는지** 꼭 확인하세요.

**잰 값 (1247×1130 / 1·2·3단계 모두 같음)**

    종이 624×882   (처음 251×355 → 가로 2.5배)
    시작선 · 크게보기 중앙 · 그림위=종이위 모두 맞음, 한 쪽, 스크롤 0
    자리 옮기기를 켜고 꺼도 **한 픽셀도 안 밀림**

1024×768 에서도 세 단계 모두 한 쪽 (종이 345×488).
전 화면 회귀(홈·계획·지도·모아보기·일기모음·설정·포트폴리오) 스크롤 0 · 깨진 그림 0.

### 14-44. 안내창을 눈에 띄게 + 되돌리기 자리 확인 (2026-08-22)

**① 「고른 그림으로 되돌리기」가 안 보인다는 지적 — 자리는 맞았습니다**

코드 차례는 `그림 묶음 → 되돌리기 → 안내 자리 → 글 묶음` 으로 맞아 있었습니다.
화면에 없던 것은 **아직 그림을 그리기 전이라 되돌릴 것이 없어서**입니다.

    조건 : draft.picKind 가 'draw'(내가 그림) 또는 'photo'(사진) 일 때만

처음 상태(`app` = 고른 그림)에서는 되돌릴 것이 없으므로 나오지 않는 것이 맞습니다.
`그림 다시 그리기` 를 누른 뒤에 그림 묶음 바로 아래에 나옵니다 (재어서 확인).

**② 「그림을 끌어서 옮겨요」 안내창을 눈에 띄게**

다른 칸과 같은 옅은 테두리라 그냥 지나쳤습니다.

    테두리 색  →  **오른쪽 그림일기와 똑같은 초록** rgb(47,191,113)
    바탕      →  아주 옅은 초록 #f2fbf5
    바깥      →  옅은 초록 링 하나 더
    켤 때     →  링이 한 번 퍼졌다 잦아듦 (0.55초, 한 번만)

▸ 초록을 그림일기와 **같은 색**으로 맞춘 까닭 : 이 안내는 오른쪽 그림일기를
  만지라는 말입니다. 테두리 색이 같으면 **둘이 한 짝**이라는 것이 눈으로 이어집니다.
▸ `.reduce-motion` 을 켠 학생에게는 움직임이 나오지 않습니다.

⚠ **자리를 잡아 둔 칸에서는 크기를 바꾸는 것이 무엇이든 안 됩니다.**
두 번 밟았습니다.

    1) speakText 를 켤 때만 주었더니 읽어주기 단추가 그때만 생겨 **53px** 밀림
    2) 테두리를 2px→3px 로 굵혔더니 위아래 1px 씩 **2px** 밀림

  → 테두리 **두께는 늘 3px**, 켤 때는 **색만** 바꿉니다.
  → 강조 효과도 `transform:scale` 이 아니라 **빛 번짐(box-shadow)** 만 씁니다.
    box-shadow 는 자리를 한 픽셀도 바꾸지 않습니다.

**잰 결과 — 1247×1130 · 1024×768 × 1·2·3단계 모두**

    되돌리기가 그림 밑 · 글 위       ✅
    켜고 꺼도 그림 · 글 · 아래 안 밀림  ✅
    그림일기와 같은 초록            ✅
    한 쪽 · 스크롤 0               ✅

### 14-45. 영화관 그림을 다른 장소 그림과 같은 규격으로

`images/장소/영화관.png` 만 **1536×1024 · 2300KB** 였습니다.
나머지 18장은 모두 **512×341 · 200~420KB** 입니다.

  → 512×341 · 325KB 로 맞췄습니다 (비율 1.501 그대로).
  → 원본은 `images/_원본/장소/영화관_1536.png` 에 두었습니다.

▸ 장소 그림은 그림일기 그림칸 배경으로 `cover` 로 깔립니다. 그림칸이
  **512×341 과 똑같은 비율(1.501)** 이라, 그 규격을 지키면 한 픽셀도 안 잘립니다 (14-14).
▸ 한 장만 유난히 크면 **같은 폭으로 맞춥니다** (14-2 와 같은 사례, 두 번째).

### 14-46. CSS 중복 정의 점검

`.fix-lab` 이 두 번 정의되어 **뒤엣것이 앞엣것을 덮어** 이름표가 계속 작게
나왔습니다 (14-43). 이번에 넣은 선택자를 모두 훑어 **남은 중복이 없음**을 확인했습니다.

    .fix-lab 1곳 · .fix-part 계열 5곳(서로 다른 선택자) · .move-slot 3곳(상태별)
    .btn.fix-save 1곳 · .stage.tall 2곳(자신 · 자식)

**찾는 법**

    grep -oE '^\.[a-zA-Z][a-zA-Z0-9_-]*(\.[a-zA-Z0-9_-]+)*\{' css/app.css | sort | uniq -d

▸ 이 방법으로 앱 전체에 **예전부터 있던 중복 9개**(`.corner` · `.pd-art` ·
  `.stage-fit` 등)도 보입니다. 미디어쿼리 · 인쇄용으로 **일부러 덮어쓰는 것**일 수
  있어 손대지 않았습니다. 고칠 때는 하나씩 확인하고 지우세요.

### 14-47. 넓은 화면에서 완성 화면이 왼쪽으로 쏠리던 것 (2026-08-22)

전체화면으로 키우니 내용이 왼쪽에 몰리고 오른쪽이 넓게 비었습니다.

**까닭 — 세로를 늘리려고 준 `flex` 가 가로까지 늘렸습니다**

    .stage.tall .panel{ flex:1 1 auto; }     ← 이것이 문제였습니다

무대(`.stage`)는 **가로 방향 flex** 입니다. 그래서 `flex-grow` 는 세로가 아니라
**폭**을 늘립니다. 판이 폭 제한(`--page` 1060px)을 무시하고 1920 을 다 먹었고,
안에서는 종이가 제 크기만 쓰니 **오른쪽에 502px 이 남았습니다.**

  → 세로는 이미 `align-items:stretch` 가 늘리고 있었습니다. flex 는 지웁니다.
  → 완성 화면은 종이를 크게 보아야 하므로 **폭 제한만 조금 넓힙니다.**

    .stage.tall .panel{ width:min(100%, 1400px); }

넓은 화면에서는 이 폭으로 **가운데** 놓입니다(`.stage` 의 justify-content:center).
1920×1080 에서 좌우 여백 **260 : 260**.

**딸려 나온 것 둘**

⚠ `.confirm-2col > .dv-row{ flex:0 1 auto }` — 오른쪽이 **줄어들 수 있었습니다.**
왼쪽이 남는 폭을 가져갈 때 종이가 눌려 **624 → 502 로 122px 작아졌습니다.**
종이 크기는 `fitDv` 가 이미 자리에 맞춰 정하므로 더 줄일 까닭이 없습니다.
→ `flex:0 0 auto` (안 줄어듦). 왼쪽은 `max-width:none` 으로 남는 폭을 흡수합니다.

⚠ `fitDv` 가 **흰 칸이 커진 것을 놓쳤습니다.** 창 크기는 그대로인데 흰 칸만
커지는 때가 있습니다 — 완성 화면에서 `.stage.tall` 이 켜지는 순간입니다.
창 `resize` 만 듣고 있어서 **처음 잰 값(0.63)에 멈춰** 종이가 120px 작았습니다.
→ **ResizeObserver 로 `.stage-track` 을 지켜봅니다.** 700ms 타이머도 하나 더 두었습니다.
   (`useFitOnePage` 가 이미 같은 이유로 쓰고 있었습니다 — 두 곳 모두 필요합니다)

**잰 결과 — 세 크기 × 1·2·3단계 모두 한 쪽 · 스크롤 0**

| 창 | 판 폭 | 좌우 여백 | 종이 | 안쪽 남은 폭 |
|---|---|---|---|---|
| 1920×1080 | 1400 | 260 : 260 | 624×882 | 0 |
| 1247×1130 | 1247 | 0 : 0 | 624×882 | 0 |
| 1024×768 | 1024 | 0 : 0 | 366×517 | 0 |

▸ 미리보기에서 **창 크기만 바꾸고 새로 그리지 않으면** 예전 배율이 남습니다.
  실제 브라우저는 창을 조절할 때 `resize` 가 반드시 오므로 문제가 없습니다.
  재어 볼 때는 **새로고침한 뒤** 재세요.

### 14-48. 모아보기 머리줄 — 알약은 왼쪽 끝, 질문은 가운데

되돌아가는 알약과 질문 알약을 나란히 가운데로 모았더니, **질문이 오른쪽으로
밀려** 화면마다 질문 자리가 달라 보였습니다. 질문은 늘 한가운데 있어야 눈에 익습니다.

  → 알약을 자리에서 띄워(`position:absolute; left:0`) 왼쪽 끝에 붙이고,
    질문은 칸 한가운데 그대로 둡니다.
  → 좁은 화면(720px 이하)에서는 겹치므로 다시 한 줄로 세웁니다.

확인 : 네 칸 화면(`◀ 실외 여가 섬`) · 표시 열린 화면(`◀ 내가 표시한 활동`)
둘 다 알약 왼쪽 끝 · 질문 정중앙(1920 에서 중심 960).

### 14-49. 그림일기 1단계 이름 — `보고 써요` → `보고 따라 써요`

실제로 하는 일이 **보고 따라 쓰는 것**이라 이름과 하는 일을 맞췄습니다
(2단계 `따라 쓰거나 보고 써요` 와도 결이 이어집니다).

### 14-50. 「또 하고 싶나요?」 를 **제목 뒤**로 (2026-08-22)

`또 하고 싶나요?` 는 뼈대 여섯째 물음이었습니다. 그런데 2단계에서는 바로 앞이
`다음에는 어떻게 하고 싶나요?` 라 **같은 것을 두 번 묻는 것처럼** 보였습니다.
게다가 이것은 일기 문장을 만드는 물음이 아니라 **여가지도의 표시로 이어지는**
물음입니다. 일기를 다 쓰고 제목까지 정한 뒤에 묻는 것이 차례에 맞습니다.

  → 뼈대는 **다섯**이 됩니다 : 언제 · 누구와 · 어디에서 · 무엇을 · 기분
  → `또 하고 싶나` 는 **제목 뒤 · 그림 앞**으로 (1단계는 제목 단계가 없어 기분 뒤).

바뀐 차례 (재어서 확인)

| 단계 | 차례 |
|---|---|
| 1단계 | 언제 · 누구와 · 어디에서 · 무엇을 · 기분 · **또 하고 싶나** · 그림 · 확인 |
| 2단계 | 뼈대5 · 기억 · 다음에 · 제목 · **또 하고 싶나** · 그림 · 확인 |
| 3단계 | 뼈대5 · 일기 쓰기 · 제목 · **또 하고 싶나** · 그림 · 완성 |

**같은 고장이 다시 안 나게 — 번호로 가르던 것을 「이름」으로 바꿨습니다**

예전에는 `if (step === 6)`, `step -= 4`, `L3_OLD = {6:1, 7:4, 8:5, 9:6}` 처럼
**단계 번호**로 화면을 갈랐습니다. 그래서 차례를 한 칸만 옮겨도 엉뚱한 화면이
나왔고, 주석에 `차례를 바꾸지 마세요` 를 달아 두어야 했습니다.

    var key = L2[step];
    if (key === '제목')        return titleStep();
    if (key === '또 하고 싶나') return againBody();
    if (key === '그림')        return picStep();

  ▸ 이제 뼈대 **뒤** 단계는 L1/L2/L3 목록에서 자유롭게 옮겨도 그대로 맞습니다.
  ⛔ 뼈대 **다섯**의 차례만은 그대로 두세요. `picsSoFar1` · `diarySoFar1` ·
     `canNextBone` · `BONE_WRITE` · `META_TABS` 가 그 번호를 그대로 씁니다.

**딸려 나온 고장 하나 — 3단계가 「또 하고 싶나」 에서 못 넘어갔습니다**

    var list = (draft.level === 2) ? L2 : L1;      ← 3단계가 빠져 있었습니다

3단계가 L1(8칸) 길이로 깎여서, 8번째 칸인 `또 하고 싶나` 에서 멈췄습니다.
예전에는 그 칸이 6번째라 우연히 지나갔을 뿐입니다. → 세 단계를 모두 적었습니다.

### 14-51. 인쇄하면 힌트 원고지가 좌우로 **잘려** 나왔습니다

`힌트 보고 쓰기` 를 인쇄하니 원고지가 좌우로 한 칸씩 잘렸습니다.

**까닭 — 인쇄에서만 종이 크기를 mm 로 바꿔 잡았습니다**

    .print-area .pd-sheet{ width:186mm; height:273mm; }   ← 이것이 문제였습니다

그림일기 종이(`.pd-sheet`)는 **794×1123 px 고정 캔버스**입니다. 속의 그림칸 ·
원고지 · 힌트 상자가 **모두 px 로 못박혀** 있습니다(app.css 1532줄). 화면에서는
어디서나 `transform:scale` 로 **통째로** 줄여 보여 줍니다 — `.dv-paper` ·
`.jr-paper` · `.fix-paper` 가 모두 그렇습니다.

그런데 인쇄만 **바깥 크기만** 794 → 703px(186mm) 로 줄였습니다. 속은 그대로
794 기준이라 **91px 이 넘쳤고**, 힌트 상자는 `overflow:hidden` 이라 넘친 만큼
좌우로 잘렸습니다 (힌트 원고지 720px 이 들어갈 자리가 631px 뿐 → 한 칸씩).

  → 종이는 **794×1123 그대로** 두고 `zoom` 으로 통째로 줄입니다.

    .print-area .pd-sheet{ zoom:0.8857; width:794px; height:1123px; }

  ▸ `zoom` 은 `transform:scale` 과 달리 **차지하는 자리까지** 줄어들어서,
    종이 뒤에 빈 쪽이 생기지 않습니다.
  ▸ 794px = 210mm(96dpi) 이므로 186mm 는 186/210 = **0.8857** 배입니다.
    높이 1123 × 0.8857 = 995px = 263mm 로 273mm 안에 들어갑니다.

재어 확인 : 종이 703×995px(=186×263mm) · 속칸(그림칸 · 날짜줄 · 제목줄 ·
원고지) 모두 종이 안쪽. 넘침 0.

  ⛔ `.pd-sheet` 의 px 크기를 mm 나 % 로 바꾸지 마세요. 속이 전부 px 입니다.

### 14-52. 미리보기 port 8777 → 8791

윈도우가 8777 을 예약 범위로 잡아 서버가 뜨지 않았습니다.
`serve.ps1` 과 `.claude/launch.json` 을 **8791** 로 맞췄습니다 (둘 다 고쳐야 합니다).

## 15. 여가 포트폴리오 손보기 (2026-08-22)

### 15-1. 네 칸 그림 — **그 코너에서 쓰는 그림 그대로**

네 칸(내가 세운 계획 · 나의 여가지도 · 나의 일기장 · 나의 한마디)이 코드로 그린
SVG 를 쓰고 있어서, 홈의 코너 그림과 **달랐습니다.** 같은 곳을 가리키는데
그림이 둘이면 학생은 다른 것으로 봅니다.

    내가 세운 계획 → images/코너명/여가 계획하기.png
    나의 여가지도  → images/코너명/여가 지도.png
    나의 일기장    → images/코너명/여가 일기.png

`나의 한마디` 는 코너가 아니라 **여기에서 하는 일**이라 짝이 없습니다.
그래서 하는 일 그대로 **직접 쓰기 + 내가 그리기** 두 그림을 나란히 씁니다
(일기에서 이미 쓰는 그림이라 학생이 뜻을 압니다). css 는 `.folio-tab-art.two`.

### 15-2. 내가 세운 계획 — 한 줄에 넷 · 눌러서 계획표 보기 · 모음 인쇄

▸ 예전에는 남는 폭에 맞춰 저절로 채워서(`auto-fill`) 화면마다 줄 수가
  달라졌습니다. **한 줄에 넷**으로 못박았습니다 (`.folio-grid.four`).
▸ 계획을 누르면 **계획 만들기 화면으로 넘어갔습니다.** 보려던 학생이
  처음부터 다시 고르는 화면을 만났습니다. → **계획표 창**을 띄웁니다.
▸ `계획 모음 인쇄하기` 를 더했습니다. 이 기간의 계획표를 한 장씩 이어 냅니다
  (일기장 인쇄와 같은 방식 — `.book-page` 가 쪽을 나눕니다).

### 15-3. 나의 여가지도 — 실내 왼쪽 · 실외 오른쪽, **한 칸에 다섯씩**

예전에는 한 덩어리로 섞어 두고 카드마다 `실내`·`실외` 딱지를 붙였습니다.
어느 쪽이 몇 가지인지 **세어 보아야** 알았습니다. 좌우로 가릅니다 (`.folio-two`).

**몇 개씩 놓을지 — 재어서 정했습니다**

대표 활동은 **실내 15 · 실외 15** 가지입니다 (`App.topCards`).

| 한 줄에 | 한 칸 줄 수 | 1366x768 결과 |
|---|---|---|
| 4개 | 4줄 | **좌우 3쪽으로 갈라짐** |
| 5개 | 3줄 | **한 쪽** ✅ |

다섯씩이면 카드가 좁아지므로, **이 칸에서만** 그림·글씨를 줄이고 이름은
**두 줄까지**만 보여 줍니다 (`.folio-two .folio-name` 의 `-webkit-line-clamp:2`).
⚠ 이름을 세 줄까지 두면 긴 이름(`반려동물과 실내에서 놀기`) 한 장 때문에
  그 줄 전체가 커져서 아래가 밀리고 쪽이 갈라집니다.

`여가지도 보기` 단추는 뺐습니다. 여기는 **모아 보는 자리**이고, 지도로 가려면
홈에서 지도 코너로 들어가는 것이 제 길입니다.

### 15-4. 나의 일기장 — **떠났던 칸으로** 돌아옵니다

그림일기·일기 고치기로 갔다가 파란 화살표를 누르면 **나의 여가(홈)** 로
튀었습니다. 바로 앞 화면이 아닙니다.

  → 포트폴리오에서 갈 때 `from:'folio'` 와 함께 **`tab:'diary'`** 를 넘깁니다.
    돌아올 때 그 칸이 열립니다 (`folioTab` 이 `params.tab` 을 첫값으로 씁니다).
  → 일기 고치기는 `step:'last'` 로 **완성 화면**에서 엽니다. 그러지 않으면
    `언제 했나요?` 부터 다시 훑습니다.
  → 화면마다 **글자로 적힌 돌아가기 단추**를 두었습니다. 화살표만으로는
    어디로 가는지 몰라 학생이 누르지 못합니다.

**⚠ 돌아가기 단추를 완성 화면 왼쪽 기둥에 두지 마세요**

거기에 두었더니 기둥이 60px 커졌고, 한 쪽에 맞추는 장치(`useFitOnePage`)가
화면을 통째로 **0.85배**로 줄여서 그림일기가 **366x517 → 312x442** 가 됐습니다.
이 화면에서 가장 커야 하는 것이 그림일기입니다.
→ **맨 위 줄에 알약으로** 두었습니다 (높이를 안 먹습니다 — 351x496 로 회복).

그림일기 화면에서는 `나의 일기 모음 보기` 를 **바꿔 끼웁니다.** 돌아갈 곳이
둘이면 어디가 앞 화면인지 흐려집니다.

### 15-5. 나의 한마디 — 일기와 **같은 세 단계**

예전에는 세 단계가 모두 **글자를 쳐 넣는 칸**뿐이라, 아직 글을 못 쓰는
1단계 학생은 여기를 아예 쓸 수 없었습니다.

  1단계  그림 **두 개**(활동 · 기분)를 골라 문장이 만들어집니다
  2단계  낱말 알약으로 빈칸을 채웁니다 (직접 써도 됩니다)
  3단계  내 말로 씁니다 + `생각 도움` (이 기간의 활동·표시·일기 수)

★ 담기는 곳은 단계와 상관없이 **한 곳**입니다 (`student.word` · `student.review`).
  그래서 전시판형 · 책자형 인쇄는 **고칠 것이 없습니다.**
  1단계가 고른 것은 `student.wordPick` 에 따로 남아, 다시 열면 그대로 켜집니다.

**한마디 / 돌아보기를 한 번에 하나만**

둘을 함께 펼치면 고르는 카드가 **스물넷**(활동 8 · 기분 8 · 돌아보기 8)이라
1366x768 에서 **863px 이 필요한데 627px 뿐**이었습니다 (좌우 2쪽).
→ 단계 탭 옆에 **작은 탭 둘**을 두어 한 번에 하나만 봅니다 (`.me-parts`).
  네 코너 칸(`FOLIO_TABS`)이 이미 쓰는 방식과 같습니다.
  3단계는 둘 다 글 칸이라 낮으므로 나누지 않고 함께 둡니다.
→ 카드도 이 화면에서만 줄입니다 (`.folio-me .pick`) — 팝업 안 카드와 같은 방법.

**돌아보기 — 줄을 고르고, 넣을 것을 고릅니다** (`.me-2col`)

왼쪽에 네 문장, 오른쪽에 채울 거리. 네 줄을 한꺼번에 펼치면 그림이 네 벌이라
쪽이 갈라집니다. 줄마다 **읽어주기**를 붙였습니다 — 1단계 학생은 글을 못 읽어서
읽어 주지 않으면 어느 줄을 고르는지 알 수가 없습니다.
⚠ `C.Speak` 가 내는 class 는 `.speak` 가 아니라 **`.iconbtn has-img`** 입니다.

### 15-6. 돌아보기 문장의 **조사가 틀렸습니다** (원래부터 있던 것)

`D.reviewFrames` 가 조사를 글자로 못박아 두었습니다 (`을 좋아해요`).
받침 없는 말이 들어가면 **`나는 만들기을 좋아해요.`** 가 됩니다.

  → 조사를 `josa` 로 따로 뺐습니다. `App.reviewLine(f, v)` 하나가
    화면 · 전시판형 · 책자형에 **같은 문장**을 만들어 줍니다.

    { id:'r1', before:'나는 ', josa:'을/를', after:' 좋아해요.' }

  ▸ `에` 처럼 받침과 상관없는 조사는 한 글자만 적습니다 (`/` 가 없으면 그대로 붙임).
  ▸ 아직 안 쓴 줄은 조사도 붙이지 않습니다 — 빈칸 뒤에 을/를 이 미리 떠 있으면
    무엇이 틀에 든 말인지 헷갈립니다.

또 한마디 문장이 **`나는 수집을 할 때`** 였습니다. `App.shortName` 이 활동
이름을 줄여서(수집하기 → 수집) 하다 만 말이 됐습니다. → **이름 그대로** 씁니다.

### 15-7. 재어 확인

| 화면 | 1024x768 | 1366x768 | 1920x1080 |
|---|---|---|---|
| 내가 세운 계획 | 1쪽 | 1쪽 | 1쪽 |
| 나의 여가지도 (실내15·실외15) | 1쪽 (5열 3줄) | 1쪽 | 1쪽 |
| 나의 한마디 1·2·3단계 | 1쪽 | 1쪽 | 1쪽 |
| 나의 일기장 | 일기 수만큼 (예전 그대로) | | |

▸ 일기 1·2·3단계 차례 그대로 · 완성 화면 1쪽 · 계획 모음 인쇄 확인.

**아직 이야기할 것** : 1단계 돌아보기 네 문장을 **쉬운 문장으로** 고치기
(지금은 읽어주기로만 도와 줍니다).

### 15-8. 포트폴리오 첫 화면 — **큰 창 넷만** (2줄 x 2칸)

예전에는 작은 알약 넷을 늘 위에 띄워 두고 **그 아래에 내용을 함께** 보였습니다.
첫 화면에 고르는 것과 고른 결과가 섞여 있어, 여기가 무엇을 하는 곳인지
한눈에 들어오지 않았습니다.

  → 첫 화면은 **큰 창 넷뿐**입니다. 홈의 코너 화면(`.corner-grid`)을 **그대로**
    씁니다 — 학생이 이미 아는 모습입니다.
  → 창을 누르면 그 창 **안으로 들어가서** 거기서 활동합니다.
  → `folioTab` 의 첫값이 **null** 이면 첫 화면입니다.

**나가는 길 두 가지** (둘 다 같은 곳으로)
  · 맨 위 줄 **분홍 알약** 「여가 포트폴리오」
  · 파란 화살표 — 창 안이면 첫 화면으로, 첫 화면이면 앞 화면으로
    (예전에는 창 안에서도 곧장 홈으로 튀었습니다)

`선생님 도구` 는 **첫 화면에만** 둡니다. 창 안에서는 나가는 길이 하나라야
헷갈리지 않습니다.

### 15-9. 나의 여가지도 — 실내·실외를 **위아래**로 (그림 크게)

좌우로 놓으면 한 창이 화면의 절반뿐이라 카드가 좁아지고 그림이 작아집니다.
위아래로 놓으면 창이 **화면 폭을 다 써서** 같은 자리에 그림을 훨씬 크게 담습니다.

| | 좌우 (앞) | 위아래 (지금) |
|---|---|---|
| 한 창에 | 5개 | **8개** |
| 창마다 줄 | 3줄 | **2줄** (8 + 7) |
| 카드 폭 | 91px | **115px** |
| 그림 (1366x768) | 46px | **48px** |
| 그림 (1920x1080) | 46px | **67px** |

**재느라 두 번 걸린 곳**

⚠ 그림을 `7vh` 로 두었더니 1366x768 에서 **좌우 3쪽**으로 갈라졌습니다.
  `6.2vh` 가 딱 한 쪽입니다. 큰 화면에서는 88px 까지 커집니다.
⚠ 여덟 열을 `max-width:1150px` 에서 여섯 열로 떨어뜨렸더니 **1024x768 에서
  창마다 3줄**(6+6+3)이 되어 또 3쪽이 됐습니다. 흰 칸 폭은 **1060px 로 묶여**
  있어서 1024 든 1920 이든 창 안쪽 폭이 비슷합니다 — 여덟 열이 다 들어갑니다.
  → 780px 아래에서만 다섯 열로 줄입니다.
▸ 칸 제목도 `나의 여가지도 — 실내·실외 한눈에 보기` → **`나의 여가지도`** 로
  줄였습니다. 창에 들어와 있는 줄은 이미 알고, 길면 두 줄이 되어 높이를 먹습니다.

재어 확인 : 1024 · 1366 · 1920 모두 **8열 2줄 · 한 쪽** (실내 15 · 실외 15 기준).

### 15-10. 되돌아가는 화살표 — **안이 채워진 왼쪽 삼각형**으로 통일

`App.icon` 의 `back` 이 **흰 동그라미 안 꺾쇠(‹)** 였습니다. 그런데 맨 위 줄의
큰 파란 화살표(`images/화살표.png`)는 **채워진 화살표**라, 단추 안의 것만
속이 빈 꺾쇠여서 서로 다른 것으로 보였습니다.
「고른 낱말로 되돌리기」 · 「나의 일기장으로 돌아가기」 처럼 되돌아가는 단추가
여럿이라, 한 모양이라야 뜻이 한눈에 옵니다.

    back: '<path d="M30 11L13 24l17 13z" fill="' + L + '" stroke-width="2"/>'
    next: '<path d="M18 11l17 13-17 13z" fill="' + L + '" stroke-width="2"/>'

`next` 도 짝이 맞게 채워진 오른쪽 삼각형으로 두었습니다.

### 15-11. 「나의 일기장으로 돌아가기」 자리 — 저장 아래 **오른쪽 끝**

15-4 에서 맨 위 줄로 옮겼던 것을 **완성 화면 왼쪽 기둥 맨 아래**로 되돌렸습니다.
「고치고 → 저장하고 → 돌아가기」 가 한 기둥에 차례로 이어집니다.

  ⚠ 큰 단추(`size="big"`)로 두지 마세요. 기둥이 60px 커져서 한 쪽에 맞추는
    장치(`useFitOnePage`)가 화면을 0.85배로 줄입니다 — 그림일기가 15% 작아집니다.
  → **작은 알약을 오른쪽 끝에** 붙이고 키도 낮췄습니다 (`.fix-goback`, 35px).
    그래도 종이가 366x517 → **327x462** 로 11% 작아집니다. 자리를 바꾸는 값입니다.
  ▸ 고치는 단추들과 같은 폭으로 두지 않는 까닭도 있습니다 — 같은 폭이면
    저장만큼 중요해 보입니다. 여기서 할 일은 고치고 저장하는 것입니다.

### 15-12. 포트폴리오 첫 화면 — 왼쪽 「나」 · 오른쪽 창 넷

누구의 포트폴리오인지가 **맨 위 줄 오른쪽 끝의 작은 알약**(`.who`)에만 있었습니다.
여기는 제 기록을 모아 두고 **남에게 보여 주는** 곳이라, 표지처럼 이름과 얼굴이
크게 있어야 제 것으로 느껴집니다.

  → 첫 화면에서는 그 알약을 **화면 안(왼쪽)** 으로 옮깁니다 (`.folio-who`).
  → 창 안으로 들어가면 화면이 기록으로 채워지므로, 다른 화면과 똑같이
    맨 위 줄 오른쪽 끝에 알약으로 돌아옵니다.
  → 창 넷은 홈보다 크게 놓습니다 (`27vh`). 홈은 아래에 「오늘의 도전」 등이
    더 있지만 여기는 「나」 와 넷뿐이라 남는 자리를 창에 돌려줍니다.

### 15-13. 창 이름은 **맨 위 줄 제목**으로

창 안에서 흰 칸 제목(`h3`)을 또 두면 읽어주기까지 붙어 **60px** 을 먹습니다.
그만큼 그림이 작아집니다. 나가는 알약이 「여가 포트폴리오」 라고 적혀 있으므로,
제목은 **지금 어디에 있는지**(나의 여가지도 · 나의 한마디 …)를 알려 주는 편이 낫습니다.

  ⚠ 창 안 화면에는 `C.Sec` 에 `title` 을 주지 마세요. 읽어주기도 맨 위 줄에
    하나 있습니다 (규칙 4 — 화면마다 하나).

### 15-14. **4개씩 2줄 + 양쪽 화살표** (스크롤·넘침 막기)

이 앱은 학생 화면에 **스크롤을 만들지 않습니다.** 고를 것이 많다고 칸을 늘리면
흰 칸을 넘겨 좌우 여러 쪽으로 갈라지고, 학생은 제 것을 다 보려면 화면을 넘겨야
합니다. 그래서 **한 쪽에 여덟 개(4 x 2)** 씩만 놓고 나머지는 화살표로 넘깁니다.

    var PER_PAGE = 8;
    pageOf(list, 쪽번호)   →  { pages, page, items }
    flowBox(info, onGo, 격자class, 카드들, 이름)

  ▸ 개수가 몇이든 **화면 높이가 늘 같습니다.** 쪽이 갈라지지 않습니다.
  ▸ 화살표는 되돌아가기와 같은 채워진 삼각형입니다 (15-10).
  ▸ 쓰는 곳 : 여가지도 실내·실외 창 · 나의 한마디 고르는 칸 · 돌아보기 고르는 칸.

**여가지도 창 : 여덟씩 → 넷씩**

여덟씩 놓으니 카드가 118px 로 좁아 그림이 너무 작게 보였습니다.
넷씩이면 카드가 **213px** 로 두 배 넓어집니다.

| | 여덟씩 | **넷씩 (지금)** |
|---|---|---|
| 카드 폭 | 118px | **213px** |
| 그림 | 48px | **61px** |
| 한 창에 | 15개 다 | 8개 + 화살표 |

⚠ 세로가 빠듯합니다. 제목(60px)을 맨 위 줄로 옮기고 표시 줄을 낮춰서 얻은
  자리를 그림에 돌렸습니다. `.folio-two .folio-art` 의 `8vh` 를 더 키우지 마세요.

### 15-15. 나의 한마디 · 돌아보기 — 세 단계가 **같은 짜임**

화면을 왼쪽 기둥 · 오른쪽 마당으로 나눕니다.

    왼쪽   「나의 한마디」 알약
           무엇을 할 때      (고른 것이 알약 안에 남습니다)
           어떤 기분인가요
           [나 : 얼굴 + 이름]
    오른쪽  고르는 칸 (4개씩 2줄 + 양쪽 화살표)
           만들어지는 문장 + 읽어주기
           (2·3단계는 그 아래 직접 쓰는 칸)

세 단계가 **같은 자리**를 쓰므로 단계를 옮겨도 학생이 다시 익힐 것이 없습니다.
다른 것은 **고르는 방법**뿐입니다.

| 단계 | 나의 한마디 | 돌아보기 |
|---|---|---|
| 1 | 그림 카드를 골라 문장이 만들어짐 | 줄을 고르고 → **그림**을 고름 |
| 2 | 낱말 알약 + 직접 쓰기 | 줄을 고르고 → **낱말**을 고름 |
| 3 | 낱말은 **도움**, 글은 내 말로 | 네 줄에 **바로 씀** (위 칸은 도움) |

▸ 3단계도 한마디/돌아보기를 **나눕니다.** 함께 두면 무엇을 하는 화면인지 흐려집니다.
▸ 3단계에 「무엇을 할 때 / 어떤 기분인가요」 를 남긴 까닭 : 틀 없이 쓰라고만 하면
  막막합니다. 두 물음만 있어도 무엇을 쓸지 방향이 잡힙니다.
▸ `meActs()` 는 이제 **여덟 개로 자르지 않습니다.** 잘라 두면 뒤쪽 활동은
  아예 고를 수 없었습니다 — 화살표로 넘겨 다 고릅니다.

**아래 단추 두 개** (이 창에서만)
  · `나의 한마디 · 돌아보기 인쇄하기` — 둘은 하나의 마무리 글이라 **한 장에** 냅니다.
  · `나의 여가로 돌아가기` — 여기가 포트폴리오의 마지막 칸이라 나가는 길도 둡니다.

**재어 확인** (1024 · 1366 · 1920 — 세 단계 x 한마디·돌아보기 여섯 화면)
모두 **한 쪽**. 스크롤 없음.

### 15-16. 나의 한마디 · 돌아보기 — **바 둘**로 다시 짰습니다

    위 바   채우는 자리(왼쪽 알약)  +  고르는 칸(오른쪽, 3개씩 2줄 + 화살표)
    아래 바 나 캐릭터(왼쪽)        +  만들어지는 말(오른쪽, **가운데 정렬**)

고르는 일과 만들어진 말을 눈으로 갈라 두면 지금 무엇을 하는 중인지 분명합니다.

▸ **돌아보기의 위 바는 고르는 칸만** 있습니다 (`.me-bar.wide`). 채울 자리는
  아래 네 줄에서 고르므로 왼쪽에 알약을 둘 까닭이 없고, 왼쪽을 비워 두면
  그만큼 카드가 좁아집니다.
▸ 맨 위 줄 오른쪽 끝 **나 캐릭터 알약은 뺐습니다** — 아래 바에 크게 있어서
  같은 말이 두 번이었습니다.
▸ 흰 칸 아래 단추(`인쇄하기` · `나의 여가로 돌아가기`)도 뺐습니다.
  화면 안에서 고르고 쓰는 일에만 눈이 가야 합니다.
  → 한마디·돌아보기 **인쇄는 선생님 도구 창**으로 옮겼습니다.
▸ 세 단계가 **모두 같은 짜임**입니다. 다른 것은 고르는 방법뿐입니다.

### 15-17. 고른 것은 **늘 파랑** — 주황을 걷어냈습니다

`.me-word.on` · `.me-cap` · `.dcard.on` 이 주황(#f7b267 · #e0913c)이었습니다.
이 앱에는 이미 규칙이 둘 있습니다.

    무엇을 묻는 자리 → **분홍** (.q-pill 의 #ffeef4 / #f3c3d6)
    고른 것         → **파랑** (--sel-bg #f2f7ff / --sel-line #8fc9ee)

여기만 주황이면 학생이 익힐 규칙이 하나 더 늘어납니다. 둘로 맞췄습니다.
색만으로 가르지 않도록 고른 것은 **테두리도 두껍게** 합니다.

### 15-18. 되돌아가는 단추는 **맨 위 줄 화살표 자리**에

「나의 일기장으로 돌아가기」 를 흰 칸 안에 두었더니
  · 눈에 안 띄고
  · 그 자리만큼 한 쪽에 맞추는 장치가 화면을 줄여 **그림일기가 작아졌습니다.**

  → `C.TopBar` 에 **`backText`** 를 더했습니다. 주면 화살표 **자리에**
    글자 단추가 들어갑니다 (`.topbar-backbtn`). 흰 칸 높이를 먹지 않습니다.
  → 쓰는 곳 : 여가 일기 완성 화면 · 그림일기 화면 (둘 다 `from:'folio'` 일 때).
  ⚠ 이때 파란 화살표의 뜻도 바뀝니다. 적힌 곳과 가는 곳이 달라서는 안 되므로,
    `from:'folio'` 면 **어느 단계에 있든** 곧장 나의 일기장으로 갑니다.
    앞 질문으로 가려면 아래 단계 띠에서 그 칸을 누릅니다.

### 15-19. 나의 일기장 — 2칸 x 2줄, **넉 장씩** 넘겨보기

한 줄에 하나씩 늘어놓았더니 카드가 흰 칸 폭(1060px)을 다 써서 글줄이 너무
길었고, 일기가 쌓일수록 좌우 서너 쪽으로 갈라졌습니다.

  → **두 칸**으로 좁히고(카드 443px), **넉 장씩** 끊어 화살표로 넘깁니다.
    일기가 몇 장이든 화면 높이가 늘 같습니다.
  ⚠ 여러 쪽 나누기(stage-track 의 다단)에 맡기지 마세요. 격자는 다단에서
    제멋대로 쪼개져서, 높이가 다 들어가는데도 **4쪽**이 되었습니다.

**일기를 한꺼번에 인쇄하는 자리** — 예전에는 선생님 도구 창 안에만 있어서,
일기장을 보고 있어도 모아 인쇄하는 길이 화면에 없었습니다.
→ 나의 일기장 창 **흰 칸 아래**에 `일기장 모두 인쇄하기 (N장)` 을 두었습니다.
  `따라쓰기 판` · `빈칸 판` 은 선생님이 쓰는 학습지라 도구 창에 그대로 둡니다.

### 15-20. 고르는 칸은 **3개씩 2줄 = 여섯 개**

넷씩도 그림이 작아 보기 힘들고, 한 쪽에 여덟 개가 쏟아져 학생이 헤맸습니다.

| | 여덟씩 | 넷씩 | **셋씩 (지금)** |
|---|---|---|---|
| 카드 폭 | 118px | 213px | **285px** |
| 그림 | 48px | 61px | **69px** |
| 한 쪽에 | 8개 | 8개 | **6개** |

▸ 쪽 표시(`1 / 2`)는 여가지도에서 **창 머리줄**로 옮겼습니다. 격자 아래에 두면
  창마다 18px, 둘이면 36px 을 먹어 그 만큼 그림이 작아집니다 (`hideCount`).

### 15-21. 셋째 칸 「나의 한마디 모음」

다 쓰고 나서 **완성된 것을 함께** 보는 자리입니다.
**인쇄에 나가는 종이 그대로** 보여 줍니다 — 보는 것과 나오는 것이 같아야
학생이 무엇을 인쇄하는지 압니다.

    나의 한마디  →  돌아보기  →  나의 한마디 모음
      (쓰기)        (쓰기)        (보기 · 인쇄 · 나가기)

흰 칸 아래 단추는 **이 칸에서만** 나옵니다.
  · `나의 한마디 · 돌아보기 인쇄하기`
  · `나의 포트폴리오로 돌아가기` (창 넷이 있는 첫 화면으로)

▸ 돌아보기 안에 따로 바를 두지 않은 까닭 : 들어가는 길이 둘이 되면 학생이
  어느 쪽이 맞는지 헷갈립니다. 셋을 나란히 두면 차례가 그대로 보입니다.
▸ 선생님 도구 창에도 같은 인쇄 단추를 남겨 둡니다 (선생님이 바로 쓰는 길).

### 15-22. 1단계 그림은 **4개씩 한 줄**

낱말(2·3단계)은 3개씩 2줄 = 여섯 개인데, 1단계 **그림**은 4개씩 **한 줄**입니다.
줄이 하나뿐이라 그 높이를 그림에 다 쓸 수 있습니다.

| | 3개씩 2줄 | **4개씩 1줄** |
|---|---|---|
| 한 쪽에 | 6개 | 4개 |
| 그림 | 69px | **92px** |

1단계 학생에게는 **그림이 크고 한 번에 보이는 개수가 적어야** 합니다.
`mePicks(key, list, render, label, per, gridCls)` 의 뒤 두 값으로 정합니다.

재어 확인 : 1024 · 1366 에서 **세 단계 x 세 칸 = 아홉 화면 모두 한 쪽**.

### 15-23. 첫 화면 창 다섯에 **포트폴리오 바탕 그림**

`images/여가 포트폴리오 배경.png` 를 나 캐릭터 칸 + 코너 넷 **안쪽**에 깝니다.
첫 화면이 표지처럼 보이게 하는 그림입니다.

  · 경로는 `js/data/activities.js` 한 곳 (`App.IMAGE_BASE.ui.folioBg`)
  · `js/app.js` 의 `useIfExists` 가 CSS 변수 `--folio-bg` 에 넣습니다
    (벽지 · 지도 배경과 **같은 방법** — 파일이 없으면 조용히 흰 칸)
  · 그림 위에 그림과 글자가 올라가므로 **흰 막을 한 겹**(82%) 덮어 연하게 합니다
    → 더 진하게 보려면 `.folio-corner::before` 의 `.82` 만 내리세요

### 15-24. ⛔ 인쇄에 나와야 하는 그림은 **css 배경으로 두지 마세요**

그림일기의 **장소 배경(학교 · 교실 …)이 인쇄하면 통째로 빠졌습니다.**
css 의 `background-image` 였기 때문입니다. 브라우저 인쇄에서 「배경 그래픽」 은
기본이 꺼짐이라 css 배경은 안 나오고 `img` 만 나옵니다.
(print.css 의 `print-color-adjust:exact` 로도 막지 못했습니다)

  → **진짜 그림**으로 바꿨습니다 : `<img class="pd-art-bg">`
  → 고른 그림들은 그 위에 올라갑니다 (`z-index:1`)
  ⚠ `.pd-art-item` 에 `position` 을 주지 마세요 — 이미 absolute 로 자리를
    잡고 있어서(끌어 옮기기) relative 로 덮으면 자리가 무너집니다.

재어 확인 : 인쇄 마당(`#print-root`)에 `장소/교실.png` 가 실려 나옵니다.

### 15-25. 완성된 일기 칸이 **앞 두 줄만 보였습니다**

`C.SentenceEdit` 의 글 칸은 스크롤 대신 **저절로 늘어납니다.** 그런데 높이를
**글이 바뀔 때만** 다시 쟀습니다. 칸의 **폭**이 바뀌면 줄 수가 달라지는데,
완성 화면은 한 쪽에 맞추느라 통째로 줄였다 늘였다 하므로(`useFitOnePage` 의 zoom)
폭이 실제로 여러 번 바뀝니다. 그래서 뒷글이 잘렸습니다.

  → **ResizeObserver 로 칸 자체를 지켜보다가** 다시 잽니다.
  → 그리고 `height = scrollHeight` 만으로는 **테두리 두께(3px x 2)만큼 잘립니다.**
    box-sizing 이 border-box 라 준 높이 안에 테두리가 들어가는데 scrollHeight 에는
    테두리가 없기 때문입니다. **테두리 두께를 더해** 줍니다.
  ⛔ 둘 다 지우지 마세요 — 지우면 긴 일기의 뒷부분이 안 보입니다.

### 15-26. 고치기 화면 다듬기

▸ 포트폴리오에서 **다 쓴 일기를 고치러** 왔을 때에는 **단계 띠를 넣지 않습니다.**
  처음부터 훑는 길이 아니라 완성 화면 한 곳에서 고치고 돌아가는 길인데,
  열두 칸이 늘어서 있으면 무엇을 하는 화면인지 흐려집니다.
  → 덤으로 그림일기가 **366x517 → 400x566** 으로 커졌습니다.
▸ 「날짜 · 사람 · 장소 바꾸기」 와 「일기 저장하기」 는 **폭을 22rem 으로 줄여
  가운데**에 둡니다. 기둥 폭을 다 쓰면 위의 그림·글 칸보다 커 보여서,
  고치는 일보다 이 둘이 더 중요한 것처럼 읽힙니다.
  ⚠ `align-self` 로 가운데를 잡지 마세요. 뒤에 나오는
    `.btn.fix-go{align-self:stretch}` 가 이깁니다. **좌우 margin:auto** 를 씁니다.

### 15-27. 돌아보기 줄에 **활동 그림**

채워 넣은 활동의 그림을 문장 옆에 붙입니다. 1단계 학생은 글을 못 읽어서,
그림이 없으면 어느 줄에 무엇을 넣었는지 소리로 들어야만 알 수 있었습니다.

  · 담기는 것은 글자(이름)뿐이라 `actByName(이름)` 으로 활동을 되찾습니다.
  · 담는 것을 id 로 바꾸지 않은 까닭 : 학생이 **직접 쓴 말**도 들어오고
    (2·3단계 직접 쓰기), 인쇄 · 전시판형이 이미 글자를 그대로 씁니다.
  · 직접 쓴 말이면 짝이 되는 활동이 없으므로 그림도 없습니다.
  ⚠ 그림을 키우지 마세요 — 네 줄이라 조금만 커도 화면을 넘칩니다.

▸ 위 바의 「나의 한마디」 알약은 뺐습니다. 아래 바에 이미 있고 맨 위 줄 제목도
  같은 말이라, 한 화면에 같은 말이 셋이었습니다.

재어 확인 : 세 단계 x 세 칸 = 아홉 화면 **모두 한 쪽**.

### 15-28. ⚠ **칸을 넓게 늘이지 마세요** — 문장이 이어 읽히지 않습니다

나의 한마디 · 돌아보기의 칸들이 흰 칸 폭을 다 쓰고 있었습니다.
「나는 ____ 를 좋아해요.」 에서 앞말과 뒷말이 멀찍이 떨어져, 한 문장으로
**이어 읽히지 않았습니다.** 글자도 넓은 띠 왼쪽에 조그맣게 붙어 보였습니다.

    .me-rows            width:min(100%, 40rem)   가운데
    .me-saybar          width:min(100%, 38rem)   가운데
    .me-row-in          flex:0 1 9rem            ⚠ `flex:1 1` 로 늘이지 마세요
    .me-done > .sheet   width:min(100%, 33rem)   글자는 오히려 키움

  ▸ 세 단계가 **같은 값**을 씁니다 (reviewRows·meSayBar 를 함께 쓰므로).

### 15-29. 돌아보기 줄 — 그림은 **맨 왼쪽에 크게**

    [그림]  나는 [곤충 키우기] 를 좋아해요.   🔊

  · 왼쪽에 나란히 있으면 네 줄을 훑을 때 **그림만 따라가면** 됩니다.
  · 크기 40 → **76px** 까지 (`clamp(40px,7vh,76px)`).
  · 아직 안 채운 줄에도 **빈 자리를 남깁니다** (`.me-row-art.none{visibility:hidden}`)
    — 그러지 않으면 네 줄의 글 시작점이 어긋납니다.
  · 채운 말로 활동을 되찾는 것은 `actByName(이름)` 입니다 (15-27).

### 15-30. 포트폴리오 아래 단추 — 좁게, **한 줄에 나란히**

흰 칸 폭을 다 쓰는 큰 띠가 둘이면 시선이 그리로 쏠려, 정작 봐야 할 학생의 글에서
눈이 흩어집니다. 게다가 위아래로 쌓으면 두 줄(약 116px)을 먹어서 **모음 창이
좌우 3쪽으로 갈라졌습니다.**

    [data-corner="portfolio"] .panel-action{ flex-wrap:wrap; justify-content:center; }
    [data-corner="portfolio"] .panel-action .btn{ width:auto; min-width:14rem; max-width:26rem; }

  ▸ 다른 코너의 `다음` 단추는 **지금 할 일**이라 크게 그대로 둡니다.
  ▸ 말도 고쳤습니다 : `나의 포트폴리오로` → **`여가 포트폴리오로 돌아가기`**
    (맨 위 줄 알약 「여가 포트폴리오」 와 같은 말이라야 같은 곳으로 읽힙니다)

재어 확인 (1024x768) : 세 단계 x 세 칸 = 아홉 화면 **모두 한 쪽**.
문장바 646px · 얼굴 100px · 줄칸 680px · 줄그림 54px · 모음 종이 561px.

### 15-31. 여가지도 카드는 **정사각형**, 창도 좁게

칸을 폭에 맞춰 늘렸더니(`1fr`) **가로로 긴 칸 한가운데에 작은 그림**이 떠
있게 되어 눈에 안 들어왔습니다. 셋씩 놓아도 그림은 그대로였습니다.

  → 칸을 **그림 크기만 한 정사각형**으로 두고 **가운데로 모읍니다.**

    .folio-grid.wide{ grid-template-columns:repeat(3, auto); justify-content:center; }
    .folio-two .folio-card{ width:clamp(112px,21vh,250px); aspect-ratio:1; }
    .folio-two .folio-art { width:clamp(66px,15vh,190px); }
    .folio-half{ width:min(100%, 44rem); margin:0 auto; }

| | 여덟씩 | 넷씩 | 셋씩 2줄 | **셋씩 1줄 · 정사각형** |
|---|---|---|---|---|
| 그림 | 48px | 61px | 69px | **115px** |
| 카드 | 118 넓적 | 213 넓적 | 285 넓적 | **161 x 161 정사각** |
| 창 폭 | 1000 | 1000 | 1000 | **748** |

한 창에 **셋**, 넘치면 화살표. 창이 둘(실내·실외)이라 화면에는 두 줄입니다.
⚠ `vh` 를 더 키우지 마세요 — 1366x768 에서 아슬아슬하게 들어갑니다.

### 15-32. 나의 여가 포트폴리오 **책자** — 네 코너를 한 권으로

첫 화면 **나 칸 바로 아래**에 단추를 두었습니다 (`.folio-book`).
이 책이 누구의 것인지 바로 위에 적혀 있어서 이어집니다.

    표지 → 내가 세운 계획 → 나의 여가지도 → 나의 일기장 → 나의 한마디

  · 표지 : 나 캐릭터 · 이름 · 기간 · 담긴 것 목록 (`.bk-cover`)
  · 여가지도 쪽 : 실내 · 실외를 위아래로, 활동마다 **표시 그림**까지 (`bookMapSheet`)
  · 일기는 그림일기 A4 그대로, 한 장에 하나씩
  · 마지막에 나의 한마디 + 돌아보기 (`printMe`)
  ▸ 쪽 나누기는 `.book-page` · `.pd-page` 가 맡습니다 (print.css 의 break-after).
  ▸ **아무것도 없는 코너는 건너뜁니다** — 빈 종이가 끼면 묶을 때 헷갈립니다.

재어 확인 : 표지 1 + 계획 1 + 지도 1 + 일기 9 + 한마디 1 = **13쪽**이 이어서 나옵니다.

### 15-33. 지도에 붙이기 — 학생이 **직접 놓는** 여가 탐험 지도

여가지도 칸에 작은 탭 둘을 두었습니다.

    모아 보기      몇 가지 했는지 **세는** 곳 (지금까지의 목록)
    지도에 붙이기   그것이 내 지도 어디에 있는지 **놓아 보는** 곳

▸ 표시한 활동(해봤어요·좋아해요·도전·모르겠어요)만 붙일 수 있습니다.
  지도는 **내가 표시한 것**을 담는 곳입니다.
▸ 아래 서랍에서 활동을 누르면 지도에 붙고, **끌어서 옮기고** 눌러 골라
  **크게/작게**, **지도에서 빼기** 를 할 수 있습니다.
▸ 담는 곳 : `student.mapLayout = { 활동id: {x, y, s} }`
  자리는 **% 로 담습니다** — 지도를 줄여 보여 줘도 손끝과 그림이 함께 갑니다
  (그림일기의 `startDrag` 와 같은 방법 — picdiary.js).
▸ 섬 이름표(`실내 여가 섬` · `실외 여가 섬`)는 **하늘 자리**에 둡니다.
  섬 그림을 가리지 않으면서 어느 쪽인지 알려 줍니다.
▸ 안내는 지도 **아래**에. 한가운데에 두면 섬과 다리를 가려서 붙일 자리가
  안 보였습니다.

**테두리 색 = 대표 표시 하나**

| 표시 | 그림 | 테두리 |
|---|---|---|
| 해봤어요 | 발바닥 | 갈색 `#a9784e` |
| 좋아해요 | 하트 | 분홍 `#e58aa8` |
| 도전하고 싶어요 | 별 | 노랑 `#e3b93c` |
| 잘 모르겠어요 | 물음표 | 회색 `#9aa0b4` |

표시가 여럿이면 **카드 아래에 작은 그림**을 함께 붙입니다. 테두리를 여러 색으로
나누면 무슨 뜻인지 알아보기 어렵습니다 (색만으로 가르지 않는다는 규칙도 지킵니다).

⚠ 지도판을 **폭 기준**(`width` + `aspect-ratio`)으로 잡지 마세요. 지도가 가로로
  넓어서 흰 칸 폭을 다 쓰면 높이가 577px 이 되고, 아래 서랍까지 더해져 좌우
  3쪽으로 갈라집니다. **높이 기준**(`height:clamp(180px,38vh,520px)`)으로 잡습니다.

### 15-34. 책 표지에 **민트 벽지**

표지 바탕을 앱의 벽지(`images/벽지.jpg`)로 깔았습니다. 표지를 넘기면 나오는
화면과 같은 결이라, 이 책이 이 앱의 것임이 한눈에 보입니다.

  ⛔ css 배경으로 두지 마세요 — 인쇄에서 빠집니다 (15-24).
  ▸ 벽지는 640x640 **무늬 한 장**이라, `img` 를 3칸 x 5줄로 이어 붙여 채웁니다.
  ▸ 그 위에 흰 막(72%)을 덮어 글자가 묻히지 않게 합니다.
  ⚠ `App.uiImage` 는 `IMAGE_BASE.ui` 안만 봅니다. 벽지는 **맨 바깥**에 있으므로
    `App.imgUrl(App.IMAGE_BASE.wallpaper)` 로 주소를 만들어야 합니다.

### 15-35. 나의 한마디 — 「나」 칸을 크게, 글자를 문장과 같게

**나의 것 · 내가 했다**는 것이 이 화면의 뜻입니다. 왼쪽 칸을 넉넉히 잡고
첫 화면의 나 칸과 같은 **포트폴리오 바탕 그림**을 깔았습니다(흰 막 80%).

| | 앞 | 지금 |
|---|---|---|
| 왼쪽 칸 | 14rem | **19rem** |
| 얼굴 | 100px | **138px** |
| 문장 바 | 646px | **527px** |
| 낱말 알약 글자 | 16.8px | **17.45px** (= 문장 글자와 같음) |

▸ 고를 낱말이 문장에 그대로 들어가는 말이라, 작게 두면 무엇을 고르는지
  눈에 안 들어옵니다. **아래 노란 문장 바와 같은 크기**로 맞췄습니다.
▸ 책자의 「나의 한마디」 와 「돌아보기」 도 **글자 크기를 하나로**(19px) 맞췄습니다.
  한마디만 키워 두었더니 돌아보기 네 줄이 유난히 커 보였습니다.

### 15-36. 그림일기 — **제목 글자를 본문 글자와 같게**

인쇄해 보니 칸 안 글자는 55px 인데 제목은 20px 로 못박혀 있어, 제목이 유난히
작아 보였습니다. 날짜 줄(19px)도 작았습니다.

  → 제목 글자를 **본문 글자와 같은 값**으로 따라가게 했습니다.
    칸 글자 크기는 글 길이에 따라 달라지므로(칸 수가 바뀝니다) 제목도
    **그때 그때 따라가야** 늘 같은 크기로 보입니다.

    picdiary.js : var titleFs = useLines ? lineFit.fs
                                        : Math.round(790 / g.cols * GLYPH_FILL);
    css         : .pd-titlebar 에 font-size 를 적지 마세요 — 인라인 값이 덮입니다.

▸ 칸 글자도 조금 낮췄습니다 : `GLYPH_FILL` 0.70 → **0.55**
▸ 제목 줄 높이 52 → **60px** (`TITLE_H`), 날짜 줄 글자 19 → **26px**
▸ 「제 목:」 딱지는 `.62em` 으로 줄였습니다 — 제목이 커져서 딱지까지 크면 줄이 넘칩니다.
▸ **그림칸(513px)은 그대로**입니다 — 그림이 잘리지 않습니다. 늘어난 8px 은
  원고지 몫(GRID_BUDGET)에서 가져왔습니다.

재어 확인 : 1단계 제목 43px = 본문 43px · 3단계 제목 30px = 본문 30px · 종이 넘침 0.

### 15-37. ⛔ html 안 주석은 **여는 것과 닫는 것을 짝 맞춰**

`<!--` 로 열고 **js 주석 닫기(별표+빗금)로 닫았더니** 주석이 안 닫혀서 뒤 마크업이
통째로 먹혔고, `p.push is not a function` 과 함께 **화면이 아예 안 떴습니다.**
문법 오류가 아니라 **런타임 렌더 오류**라 `new Function()` 검사로는 안 잡힙니다.

  ▸ 검사에 **주석 닫기 짝**도 넣었습니다 (백틱 검사와 함께).
  ▸ 화면이 통째로 안 뜨면 : 콘솔 오류 → 각 화면을 따로 그려 보기 순으로 좁힙니다.

### 15-38. 라벨 규격을 **고를 수 있게**

⛔ **폼텍 LS-3104 는 40칸이 아닙니다.** 판매처가 모두 `27칸 · 62.7 x 30.1mm
(바코드용)` 으로 적습니다. **40칸은 LS-3102 (47 x 26.9mm · 4열 x 10줄)** 입니다.

가지고 계신 라벨지가 제각각이므로 **규격을 고르게** 했습니다 (`LABEL_KINDS`).
고른 것은 학생별로 저장됩니다 (`student.labelKind`).

| 규격 | 칸 | 한 장에 | 섬에 붙일 때 |
|---|---|---|---|
| **폼텍 LS-3102** 47×26.9mm | 4열×10줄 = 40칸 | 30개 다 | 3열×5줄 |
| 33×33mm | 6열×8줄 = 48칸 | 30개 다 | 넉넉히 들어감 |
| 폼텍 LS-3104 62.7×30.1mm | 3열×9줄 = 27칸 | 두 장 | 2열까지 |
| 폼텍 LS-3106 64×34mm | 3열×8줄 = 24칸 | 두 장 | 2열까지 |

▸ 칸 수가 모자라면 **여러 장으로 나누어** 냅니다 (printLabels 가 쪽을 나눕니다).
▸ 여백은 규격마다 `(210 - 열×폭)/2` · `(297 - 줄×높이)/2` 로 잡았습니다.

### 15-39. 인쇄 알약을 **맨 위 줄 한 줄에**

지도에 붙이기 화면의 인쇄 셋을 맨 위 줄 제목 오른쪽에 두었습니다.
흰 칸 안에 두면 그만큼 지도가 작아집니다 — 이 화면에서 가장 커야 하는 것은 지도입니다.

    내 지도 · A3   |   빈 지도 · A3   |   활동 라벨 · A4   |   모아 보기로

⚠ 이름을 길게 쓰거나 단추를 더 두면 **맨 위 줄이 두 줄로 접혀** 지도가 밀립니다
  (재어 보니 655x369 → 614x346, 좌우 2쪽). 그래서
  · 이름을 짧게 (「내 지도 인쇄 · A3」 → 「내 지도 · A3」)
  · 이 화면에서는 「여가 포트폴리오」 단추를 뺐습니다 (「모아 보기로」 에 다시 있습니다)
  · 서랍 머리(딱지 + 안내)를 **한 줄로** 합쳤습니다 (`.mb-tray-head`)

### 15-40. 사용 설명서 (교사용)

`사용설명서(교사용).html` — 브라우저에서 열고 <b>Ctrl+P → PDF로 저장</b> 하면
배포용 PDF가 됩니다. 「듣GO! 부르GO! 놀GO!」 설명서와 **같은 결**로 짰습니다.

  1 시작하기 · 2 화면 둘러보기 · 3 계획하GO! · 4 알아보GO! · 5 기록하GO!
  6 그림일기와 인쇄 · 7 모아보GO! · 8 나만의 여가 지도 · 9 선생님 설정
  10 자주 묻는 질문 · 11 자료 저장 및 백업

⚠ 인쇄 창에서 **「배경 그래픽」** 을 켜야 표 머리와 색 상자가 나옵니다.
⚠ 강제 쪽나눔(`break-before:page`)은 넣지 마세요 — 재어 보니 쪽마다 반 페이지씩
  빈자리가 생겼습니다. `break-inside:avoid` 만으로 충분합니다.

### 15-41. 그림일기 — **제목도 원고지 한 줄**

제목을 따로 두면 글자 크기를 손으로 맞춰야 하고, 맞춰 놓아도 칸 없이 떠 있어
본문과 달라 보였습니다. **같은 격자에 넣으니** 칸 크기도 글자 크기도 저절로 같습니다.

    [제목] [ ][ ][ ][ ][ ][ ][ ][ ]      ← 앞 두 칸은 「제목」 딱지
    [ ][ ][ ][ ][ ][ ][ ][ ][ ][ ]      ← 본문 첫 줄

  ▸ `TITLE_H` 를 없앴습니다. `fitGrid` 가 **줄 수에 1(제목 줄)을 더해** 셈합니다.
  ▸ 3단계(밑줄)도 제목은 칸입니다. 10칸을 쓰고 그 높이를 `LINE_BUDGET` 에서 뺍니다.
  ▸ 그림칸을 조금 줄여(`ART_SHRINK` 0.86) 그 자리를 원고지에 돌려주었습니다.
    ⚠ 더 줄이지 마세요 — 장소 배경이 `object-fit:cover` 라 줄인 만큼 잘립니다.
  ▸ 칸 글자 채움을 되돌렸습니다 : `GLYPH_FILL` 0.55 → **0.70**

| | 앞 | 지금 |
|---|---|---|
| 칸 크기 | 56px | **79px** |
| 칸 글자 | 43px | **55px** |
| 제목 글자 | 43px (칸 없음) | **55px (칸 안)** |
| 제목 줄 높이 | 60px | **80px** (= 한 칸) |

⚠ `.pd-box.pd-titlelab` 로 힘을 한 단계 올려야 `aspect-ratio` 가 풀립니다.
  `.pd-titlelab` 로만 두면 뒤에 나오는 `.pd-box{aspect-ratio:1}` 에 져서
  두 칸 너비만큼 **키가 두 배(159px)** 가 됩니다 — 재어서 확인했습니다.

### 15-42. 그림판 — 「다 그렸어요 · 그만두기」를 **그림판 아래로**

맨 위 도구 줄에 색·굵기 단추와 함께 두었더니, 다 그린 뒤에 어디를 눌러야
하는지 한눈에 보이지 않았습니다. **그리는 일이 끝나는 자리**(그림판 아래)에
두어야 차례가 이어집니다 (`.dp-done`).
▸ 팝업 스크롤 0 · 그림판 크기 그대로 (재어서 확인)

### 15-43. 나의 한마디 — 왼쪽 알약 칸을 좁게

`19rem` 은 **아래 바의 「나」 칸**에 맞춘 폭이라, 알약만 있는 위 바에는 넓었습니다.
위 바에만 `.me-bar.slots` 를 붙여 **12.5rem** 으로 줄이고 가운데로 모았습니다.

  · 슬롯 칸 304 → **213px** · 고르는 칸 → **752px**
  · 돌아보기 줄은 그림과 글 사이를 **15px** 띄웠습니다 (붙어 있으면 그림이
    첫 글자에 닿아 보입니다)

### 15-44. 큰 지도에 **무엇을 할 곳인지** 한 줄

처음 들어온 학생이 지도를 보고도 무엇을 눌러야 하는지 몰라 그냥 나가 버렸습니다.
맨 위 줄 제목 오른쪽에 한 줄을 두었습니다.

    여가 지도   실내 여가 섬과 실외 여가 섬을 눌러 여가 탐험을 시작해요!

  ▸ **큰 지도에서만** 나옵니다 — 섬 안에서는 이미 들어와 있어 할 말이 다릅니다.
  ▸ **읽어주기에도** 같은 말을 넣었습니다 (글을 못 읽는 학생).
  ⚠ 알약으로 만들지 마세요. 맨 위 줄의 알약은 **누르는 것**이라 배웠는데
    못 누르는 알약이 섞이면 규칙이 흐려집니다. 글자만 둡니다.
  ▸ 좁은 화면(900px 이하)에서는 숨깁니다 — 읽어주기에는 그대로 남습니다.

### 15-45. ⛔ **나가는 길을 「도구 보이기」로 가리지 마세요**

「6. 학생 화면에 도구 보이기」(`student.folioTools`)를 끈 학생은 지도에 붙이기
화면에서 **나갈 알약이 하나도 없었습니다.** 파란 화살표만 남았습니다.

  → 갈라놓았습니다.
    · **나가는 알약**(모아 보기로 · 여가 포트폴리오) — 늘 보입니다. **학생 것**입니다.
    · 판형 미리보기 · 선생님 도구 단추 — 스위치를 따릅니다. **선생님 것**입니다.

**인쇄 알약 셋도 늘 보이게** 했습니다. 스위치가 꺼진 줄 모르는 선생님이
**종이 지도를 아예 찾지 못했습니다.** 눌러도 인쇄 창이 열릴 뿐 지워지는 것이
없으니, 숨기는 것보다 찾기 쉬운 편이 낫습니다.

  ⚠ `folioTools` 는 기본이 **켬**입니다 (`!(student.folioTools === false)`).
    예전에 만든 학생 자료에 `false` 가 박혀 있으면 그 학생만 꺼져 보입니다 —
    선생님 설정 6번에서 다시 켜면 됩니다.

### 15-46. ⛔ 「활동이 하나도 안 보이는 학생」 — **네 겹으로** 막았습니다

학생을 바꿔 들어가면 홈이 **네 코너만 남고 아래가 통째로 비어** 보이는 일이
되풀이됐습니다. 까닭은 그 학생의 **보이는 활동이 0가지**였기 때문입니다.
활동이 없으면 계획·지도·일기가 모두 고를 것이 없어 어떤 칸도 그려지지 않고,
선생님은 무엇이 잘못됐는지 알 길이 없었습니다.

한 군데만 고치면 다른 길로 또 생깁니다. **네 겹**으로 막습니다.

| 층 | 어디 | 무엇을 |
|---|---|---|
| 1 | `teacher.js` `toggleAct` | **마지막 활동은 숨길 수 없습니다** (「적어도 하나는 남겨 두세요」) |
| 2 | `store.js` `init` | 켤 때 자료를 훑어 **전부 숨겨진 학생을 되돌리고 저장**합니다 |
| 3 | `activities.js` `visibleCards` | 결과가 0이면 **숨김 목록을 통째로 무시**합니다 |
| 4 | `home.js` | 그래도 없으면 **빈 화면 대신 까닭을 적은 안내** |

▸ 1은 **못 만들게**, 2는 **이미 생긴 것을 고치게**, 3은 **어떤 경로로도 0이
  되지 않게**, 4는 **미래에 다른 원인이 생겨도 빈 화면은 안 나오게**.
▸ 사람·기분(`toggleIn`)에는 이미 1층이 있었는데 **활동에만 빠져** 있었습니다.
▸ 2층이 꼭 필요합니다. 3층만 있으면 화면은 멀쩡해 보여도 **저장된 자료는
  그대로**라, 백업을 옮기면 다시 나타납니다.
▸ 숨김 목록이 배열이 아닌 이상한 값이어도 2층에서 빈 배열로 돌립니다.

재어 확인 : 30개를 모두 숨겨 저장 → 새로고침 → 숨김 0 · 보이는 활동 30 ·
홈에 「오늘의 도전」 다시 나옴.

### 15-47. 일기장 모아 인쇄 — **모양을 고를 수 있게**

`글자 그대로` 하나뿐이라, 따라쓰기 판이 필요하면 선생님 도구 창까지 찾아
들어가야 했습니다. 이제 **단계에 맞는 두 모양**이 나란히 나옵니다.

    1단계   일기장 모두 인쇄 · 글자        | · 따라 쓰기
    2단계   일기장 모두 인쇄 · 따라 쓰기    | · 힌트 보고 쓰기
    3단계   일기장 모두 인쇄 · 내가 쓴 글   | · 빈 줄

⛔ 인쇄 모양 목록을 포트폴리오에 따로 적지 마세요. `picdiary.js` 한 곳에서만
  정하고 `App.diaryPrintModes(단계)` 로 가져다 씁니다 — 둘로 나뉘면 언젠가
  어긋납니다 (단계마다 고를 수 있는 것이 다릅니다).
▸ 선생님 도구 창의 세 단추(일기장 인쇄하기 · 따라쓰기 판 · 빈칸 판)는 그대로 둡니다.

### 15-48. ⛔ 첫 화면이 텅 비는 또 다른 길 — **계획 칸이 통째로 2쪽으로**

이은우 학생만 첫 화면에 **코너 넷만 남고 아래가 텅 비고 「1 / 2」** 로 나왔습니다.
15-46의 「활동 0개」 와는 **다른 원인**입니다 (그 학생은 활동 30개가 멀쩡했습니다).

무슨 일이 일어났나 :

1. 그 학생에게 **오늘 세운 계획이 여러 개** 있었습니다
2. 계획이 있으면 「오늘의 도전」은 **일부러 숨깁니다** (15-?? · 오늘 할 일을
   이미 정한 학생에게 다른 활동을 또 권하면 방금 세운 계획과 다투므로)
3. 대신 나와야 할 「오늘의 여가 계획」 칸이 남은 자리보다 커졌는데,
   이 칸은 `break-inside:avoid` 라 **중간에 잘리지 않고 통째로 2쪽으로** 갔습니다
4. 그래서 1쪽에 **아무것도 안 남습니다** — 도전은 숨겼고 계획은 넘어갔으니

재어 본 값 (1226×1103 화면, 계획 카드 한 장 ≈ 122px) :

| 오늘 계획 | 남는 자리 | 쪽 |
|---|---|---|
| 1개 | 320px | 1쪽 |
| 2개 | 209px | 1쪽 |
| 3개 | 97px | 1쪽 |
| 6개 | 0px · **계획 칸이 2쪽으로** | 2쪽 (1쪽 텅 빔) |

**고친 방법** — 한 쪽에 들어갈 만큼만 보이고 나머지는 화살표로 넘깁니다.
포트폴리오의 계획·일기와 **같은 화살표**를 씁니다 (학생이 이미 아는 모습).

⛔ 화살표를 home.js 에 따로 만들지 마세요. `portfolio.js` 의 `flowBox`·`pageOf`
  를 `App.flowBox`·`App.pageOf` 로 내보내 **한 곳에서만** 만듭니다.

▸ 몇 장을 보일지는 **화면 높이로** 정합니다. 낮은 화면에서는 코너 넷이
  자리를 거의 다 써서 계획 칸에 남는 자리가 적습니다.

      화면 1103px … 남는 자리 456px → 두 장(244px) 넉넉
      화면  900px … 남는 자리 318px → 두 장, 여유 74px
      화면  720px … 남는 자리 198px → **한 장만** (두 장이면 밀려남)

  그래서 `window.innerHeight >= 900` 으로 가릅니다. 계획 카드 모양을 키울 일이
  있으면 이 숫자도 함께 보세요.

재어 확인 : 720px 높이 · 계획 6개 → **1쪽** (한 장씩 · 「1 / 6」 · 화살표 작동),
1103px 높이 · 계획 6개 → **1쪽** (두 장씩 · 「1 / 3」 · 남는 자리 185px),
계획 0개 → 「오늘의 도전」 정상 복귀. 포트폴리오 계획·일기 창도 그대로.

### 15-49. ⛔⛔ **1쪽이 비면 안 됩니다** — 빈 화면을 뿌리째 막았습니다

갤럭시탭에서 여러 화면이 **고를 것이 하나도 없이** 「1 / 3」 만 뜬 채 비었습니다.
계획 「무엇을 할까요?」 · 일기 「언제/기분」 · 첫 화면에서 두루 났고,
**세로 화면에서도** 났습니다. 15-48(첫 화면 계획 칸)과 **같은 병**인데,
그때는 첫 화면 한 곳만 막았습니다. 이번에는 한 곳에서 뿌리를 끊습니다.

**왜 나는가** — 이 앱은 스크롤 없이 한 화면에 담는 설계라 넘치면 좌우 쪽으로
나눕니다. 그런데 카드 묶음은 `break-inside:avoid` 라 **중간에 잘리지 않습니다.**
남은 자리보다 1px 만 커도 **통째로 다음 쪽으로** 갑니다.

재어 본 값 (1000×512 · 갤럭시탭 가로, 「무엇을 할까요?」) :

    흰 칸 314px  <  질문 46 + 카드 격자 289 + 쪽 넘김 52 = 397px
    1쪽 = 질문 줄만 · 2쪽 = 카드 · 3쪽 = 「1 / 3」

**고친 방법 — 두 겹** (`common.js` 의 `C.Stage` · `measure`)

| 겹 | 무엇을 | 언제 |
|---|---|---|
| ① `.fitting` | 통째로 **살짝 줄여** 한 쪽에 담기 (1 → 0.94 → 0.88 → 0.82 → **0.76**) | 넘칠 때 |
| ② `.flowing` | 칸 묶음을 풀어 **1쪽 남은 자리부터 이어 채우기** | 0.76 으로도 모자랄 때 |

⛔ **둘 중 하나만 두지 마세요.** ① 만으로는 아주 낮은 화면을 못 담고,
  ② 만 두면 멀쩡히 한 쪽에 들어갈 화면까지 굳이 쪼갭니다.
⛔ 0.76 아래로는 줄이지 않습니다 — 더 줄이면 학생이 못 읽습니다.
⛔ `zoom` 을 **흰 칸(.stage-track) 자체에 걸면 안 됩니다.** 담을 높이까지 같이
  줄어들어 아무 소용이 없습니다 (재어 보았습니다 : 293px → 291px).
  **안쪽 칸들에** 걸어야 내용만 작아지고 담을 높이는 그대로입니다.
⛔ 줄일 때는 폭을 `calc(100% / var(--fit))` 로 되돌려 줍니다. 안 그러면
  오른쪽이 허옇게 빕니다. 그리고 이 폭 보정은 **줄일 때에만** 걸어야 합니다
  (늘 걸면 내용만큼만 넓던 칸이 꽉 차 버립니다).
⛔ 이어 채울 때 **낱장 카드(.pick · .corner · .card)는 풀지 마세요.**
  카드가 쪽 경계에서 반으로 잘립니다. **묶음만**(.sec · .stack · *-grid) 풀면
  카드 한 장 단위로 곱게 나뉩니다.

재어 확인 :

| 화면 | 세로 600×1000 | 가로 1000×512 |
|---|---|---|
| 첫 화면 | 1쪽 · 줄임 없음 | 2쪽이나 **1쪽에 질문+카드** |
| 무엇을 할까요? | 1쪽 | 0.76 · **1쪽에 카드 6장** |
| 계획·지도·일기·일기장·포트폴리오 | 모두 1쪽 · 줄임 없음 | 모두 1쪽 |

▸ **세로에서는 줄이는 일이 아예 없습니다.** 세로가 기준인 까닭입니다.

### 15-50. 여가 지도 — 안내는 지도 안으로, 나가는 길은 맨 위로

세 가지를 함께 옮겼습니다 (2026-08-23 · 갤럭시탭에서 확인).

| 무엇 | 어디에 있었나 | 어디로 |
|---|---|---|
| 「실내 여가 섬과 실외 여가 섬을 눌러…」 | 맨 위 줄 | **지도 칸 안 맨 위** (나 카드 위) |
| 「나의 여가로 돌아가기」 | 지도 아래 큰 띠 | **맨 위 줄** |
| 「◀ 실내외 여가 섬으로」 | 없었음 | **섬 안 지도 칸 왼쪽 위** (새로) |

▸ 안내를 옮긴 까닭 : 맨 위 줄에 있을 때는 `@media (max-width:900px)` 로
  **통째로 숨겨져 있었습니다.** 세로로 세운 태블릿에서는 그 한 줄이 아예
  안 보여서, 처음 온 학생이 무엇을 눌러야 할지 몰랐습니다.
  ⛔ 다시 맨 위로 올리고 좁은 화면에서 숨기지 마세요.
▸ 나가는 길을 올린 까닭 : 아래 큰 띠가 차지하던 높이를 지도가 그대로 씁니다.
▸ 「실내외 여가 섬으로」 를 넣은 까닭 : 맨 위 파란 화살표가 같은 일을 하지만
  학생이 그것을 못 찾아 우왕좌왕했습니다. 오른쪽 위 「모아보기 ▶」 와 짝이 되어
  지도 칸 두 귀퉁이가 길이 됩니다.

재어 확인 : 세로 600×1000 · 가로 1000×512 둘 다 안내가 지도 안 맨 위(11px)에
보이고 나 카드(260px)와 겹치지 않음. 아래 큰 띠 없음. 섬 안에서는 안내가 숨고
「◀ 실내외 여가 섬으로」 와 「모아보기 ▶」 가 두 귀퉁이에.

### 15-51. 알림 알약은 **맨 아래 벽지 자리**로

「이 브라우저에서는 음성 안내를 사용할 수 없어요」 알림이 **계획을 쓰는
한가운데**에 떠서, 학생이 그것부터 보고 활동을 못 골랐습니다.
이 알림은 **선생님께 드리는 말**이지 학생이 할 일이 아닙니다.

    맨 위 줄 아래(top:7.6rem)  →  화면 맨 아래 벽지(bottom:.6rem)

⛔ 예전의 `bottom:5.2rem` 으로 되돌린 것이 **아닙니다.** 그 높이는 큰 단추 바가
  있는 자리라 알림이 단추 위로 떨어져 보였습니다. 지금은 흰 칸보다도 더 아래라
  아무것도 가리지 않습니다. 나타나는 방향도 아래에서 위로 바꿨습니다.

### 15-52. ⛔ 쪽 나누기가 **없는** 흰 칸에도 줄이기를 (`.stage-fit`)

15-49 의 두 겹은 좌우 쪽이 있는 흰 칸(`.stage-track`)만 지켜 줍니다.
**쪽이 없는 흰 칸**(`.stage-fit` — 캐릭터 고르기 · 지도 · 그림일기)은
넘칠 데가 없어서 **밖으로 삐져나오거나 겹쳤습니다.**

▸ `C.useFitBox(ref)` 로 같은 줄이기(1 → … → 0.76)를 넣었습니다.
  이어 채우기(.flowing)는 없습니다 — 넘겨 볼 쪽이 없으니까요.
⛔ `C.StageFit` 컴포넌트는 **아무도 쓰지 않습니다.** 세 화면이 `.stage-fit` 를
  직접 적어 씁니다. 그러니 그 화면에 손을 대면 `ref` 와 `C.useFitBox` 를
  **직접** 걸어 주어야 합니다.

### 15-53. 캐릭터 고르기 — 세로에서 그림이 이름을 덮던 것

세로 600×1000 에서 흰 칸을 **293px 넘겼습니다.** 캐릭터 그림이 이름 글자를
덮고, 「1 / 3 쪽」과 「다음」이 카드 위에 겹쳤습니다.

세 가지를 함께 고쳤습니다.

| 무엇 | 왜 |
|---|---|
| `.cs-pickside{ flex:1 1 auto; min-height:0 }` (폭 820 아래) | 위아래로 쌓일 때 **남는 높이를 지키라고** 일러 주어야 합니다. 없으면 격자가 제 내용만큼 커집니다 |
| 한 쪽에 **6개**(3칸 × 2줄) — 좁은 화면에서만 | 12개를 다 놓으면 카드가 손톱만 해집니다. 여섯이면 182×200px 로 큼직합니다 |
| `.cs-art{ overflow:hidden }` · `.cs-art *` | `> *` 만으로는 모자랍니다 — 그림이 한 겹 더 안에 있으면 손자는 제한을 안 받습니다 |

재어 확인 : 세로 600×1000 → 넘침 0 · 줄이지 않음 · 카드 6개 182×200 · 겹침 없음.
가로 1000×512 → 넘침 0 · 카드 12개 · 겹침 없음. 다른 화면도 그대로.

### 15-54. ⛔⛔ 이어 채우기가 **낱장 카드를 반으로 잘랐습니다**

15-49 에서 넣은 「이어 채우기」(`.flowing`)가 묶음을 풀 때, 그 안의 **카드까지**
잘렸습니다. 카드에 `break-inside:avoid` 를 준 곳이 CSS 에 **따로 없어서**
기본값(auto)이 그대로 먹힌 것입니다.

일기 제목 화면에서 「직접 쓰기」 카드가 **테두리만 남은 빈 상자**로 보였습니다.
연필도 글자도 입력칸도 다음 쪽에 있었던 것입니다.

    .stage-track.flowing .pick / .corner / .card / .cs-item / .tile / .folio-box
      { break-inside:avoid; }

⛔ 묶음을 푸는 규칙을 늘릴 때는 **낱장을 못박는 이 규칙도 함께** 보세요.

### 15-55. 일기 제목 「직접 쓰기」 — 카드 밖 한 줄로

다섯 번째 카드로 격자 안에 있었는데 두 가지가 걸렸습니다.

1. 카드가 하나 더 늘어 화면을 넘겨서, 세로로 세운 태블릿에서 **2쪽으로 밀려나
   아예 안 보였습니다** (선생님 말씀 : 「나왔다 안 나왔다 함」)
2. 카드 안의 작은 입력칸이라 손가락으로 누르기 어려웠습니다 (「직접 쓰기가 안 됨」)

▸ `.title-own` 한 줄로 뺐습니다. 연필 · 「직접 쓰기」 · 큰 입력칸(h46).
재어 확인 : 세로 600×1000 → **1쪽 · 줄이지 않음** · 직접 쓰기 줄이 1쪽에 있음.
써 넣으면 맨 위 「제목 :」 칸에 그대로 들어옴.

### 15-56. 「눌러서 크게 보기」가 「일기 저장하기」를 덮던 것

`.dv-bar` 는 `position:absolute; top:-3.2rem` 으로 **위로 빼내** 두었습니다.
좌우로 나뉜 넓은 화면에서는 종이 위 빈 자리라 괜찮았지만, 위아래로 쌓이는
좁은 화면에서는 그 자리가 **왼쪽 칸의 단추 줄**이라 저장 단추 위에 올라앉았습니다.
저장 단추 한가운데를 눌러도 「눌러서 크게 보기」가 잡혔습니다.

⛔ 띄우는 것은 **좌우로 나뉠 때(min-width:900px)뿐**입니다. 기본은 흐름 안에 둡니다.
▸ 함께, 선생님 제안대로 「날짜 · 사람 · 장소 바꾸기」와 「일기 저장하기」를
  **나란히** 놓았습니다 (`.fix-here` 를 row wrap 으로). 좁으면 저절로 내려갑니다.

재어 확인 : 세로 600×1000 → 두 단추 나란히(t592) · 「크게 보기」 t651 · **안 겹침** ·
저장 자리를 누르면 저장이 잡힘. 넓은 1280×800 → 예전대로 좌우 두 칸 · 띄운 배치 유지.

### 15-57. ⛔ 포트폴리오에 **앞으로 할 계획이 빠졌습니다**

방금 계획을 세우고 왔는데도 「내가 세운 계획 · 아직 없어요」 였습니다.

계획의 `date` 는 **활동을 할 날**입니다. 포트폴리오 기간은 「한 달 전 ~ 오늘」 이라,
**내일 이후로 잡은 계획은 통째로 빠졌습니다.** 계획은 앞날을 잡는 것이니
미래 날짜가 오히려 정상인데도요.

▸ 그래서 **세운 날**(`createdAt`)로도 한 번 더 봅니다. 이 기간에 세운 계획이면
  활동일이 언제든 내 기록입니다.

재어 확인 : 닷새 뒤(2026-08-28)로 계획을 세우니 포트폴리오에 「1장」. 지우면 「아직 없어요」.

### 15-58. 세로 화면에서 **섬 안 활동 그림이 손톱만 하던 것**

섬 그림은 가로로 깁니다(1.85 : 1). 세로로 세운 태블릿에서 그 비율을 그대로
지키니, **564×846 칸에 그림판이 564×306** 밖에 안 잡혔습니다.
위 454px · 아래 297px 이 통째로 놀고, 활동 그림은 **60×60** 으로 쪼그라들어
한가운데 모여 있었습니다.

▸ **섬 안에서** 그림판 높이가 칸의 80% 에 못 미치면 **칸을 통째로** 씁니다.
  그러면 섬 그림 좌우가 조금 잘리므로 배경도 채우기(cover)로 바꿉니다
  (`map.js` 의 `fillBg` → `.map-wrap.fill-bg`).

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 그림판 | 564×306 | **564×813** |
| 활동 그림 | 60×60 | **257×257** |
| 위·아래 남는 자리 | 454 · 297 | 214 · 44 |

⛔ 섬 **고르기** 화면(focus 없음)에는 넣지 마세요. 거기는 섬 두 개가 좌우로
  놓인 그림 자체를 보는 곳이라, 잘리면 섬이 안 보입니다.
▸ 가로(전자칠판 1280×800)에서는 채우기가 켜지지 않고 예전 그대로입니다
  (섬 안 그림판 1024×555 · 활동 그림 239×239 — 재어 확인).

### 15-59. ⛔⛔⛔ 주석을 **두 번 닫아** 줄이기가 한 번도 안 들었습니다

브라우저 글자 크기를 키운 선생님 화면에서 「어디에서 했나요?」에 장소가 **셋만**
보이고 나머지가 2쪽으로 넘어갔습니다. 아래 186px 이 남는데도요.

까닭을 재어 보니 **`--fit` 은 0.58 인데 실제 `zoom` 은 1** 이었습니다.
`css/app.css` 에서 주석을 가운데서 한 번 닫고(`*/`) 글을 두 줄 더 쓰는 바람에,
그 두 줄이 CSS 로 읽혀 **바로 아래 규칙이 통째로 무시**된 것입니다.

    ... 오른쪽이 허옇게 빕니다. */     ← 여기서 닫고
       ▸ 줄일 때에만 걸립니다(.fitting). ...   ← 이 두 줄이 CSS 로 읽힘
       ... 꽉 차 버립니다. */
    .stage-track.fitting > *{ zoom:var(--fit,1); }   ← 통째로 무시됨

즉 **15-49 의 ① 줄이기는 처음부터 한 번도 듣지 않았습니다.** 그때까지 화면이
그럭저럭 나온 것은 순전히 ② 이어 채우기 덕이었습니다.

▸ 함께 줄이기 하한을 **0.76 → 0.58** 로 낮췄습니다. 확대해서 보는 분에게는
  0.58 로 줄여도 **원래보다 큽니다** — 확대한 만큼을 되돌리는 것뿐입니다.

고친 뒤 (800×820 · 「어디에서 했나요?」) :

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 쪽 | 2쪽 (장소 셋만) | **1쪽** |
| 줄임 | 0.58 이라 적혀만 있고 안 먹음 | **0.94 로 충분** |
| 보이는 카드 | 3 / 6 | **6 / 6** |
| 이어 채우기 | 켜짐 | 필요 없음 |

⛔ **CSS 를 고친 뒤에는 주석 짝을 꼭 세어 보세요.** 문법이 깨져도 브라우저는
  아무 말 없이 그 규칙만 버립니다. 아래 한 줄이면 됩니다.

    awk '{i=1; while(i<=length($0)){t=substr($0,i,2);
      if(t=="/*"){if(d>0)print "중첩! "NR; d++; i+=2; continue}
      if(t=="*/"){if(d==0)print "짝없음! "NR; else d--; i+=2; continue} i++}}
      END{print d>0?"안 닫힘!":"OK"}' css/app.css

### 15-60. 「태블릿을 세로로 돌려 주세요」

이 앱은 **세로 화면**을 기준으로 만듭니다 (2026-08-23에 정했습니다).

    갤럭시탭 세로  … 흰 칸에 쓸 수 있는 높이 838px · 그림 250~300px
    갤럭시탭 가로  … 337~559px · 그림 140~150px (더 못 줄임)
    PC · 전자칠판  … 800px 이상 (가로여도 넉넉)

가로로 눕힌 태블릿은 위아래가 절반이라, 그림이 최소 크기까지 쪼그라들고도
모자라 화면이 좌우로 갈라집니다. 그래서 **가로로 열면 돌려 달라고 알립니다.**

▸ 나오는 조건은 **셋이 모두** 맞을 때뿐입니다 (`.turn-hint` 의 미디어 쿼리).

    (pointer: coarse)          손가락으로 쓰는 기기 — PC 마우스는 제외
    (orientation: landscape)   가로로 눕힌 상태
    (max-height: 780px)        전자칠판처럼 큰 화면은 제외

⛔ 하나라도 빼지 마세요. 빼면 전자칠판이나 PC 에서 뜹니다.
  (전자칠판도 손가락으로 쓰지만 화면이 커서 높이 조건에 안 걸립니다)
⛔ **막지는 않습니다.** 「그냥 가로로 볼래요」 를 누르면 그대로 씁니다 —
  기기 사정으로 세로가 안 되는 분을 가두면 안 됩니다.

재어 확인 : 세로 800×820 → 안 뜸. 가로 1000×512(PC 브라우저) → 손가락 조건이
false 라 안 뜸 · 나머지 두 조건은 맞음. 모양은 510×212 로 화면 안에 들어오고
닫기가 동작함.

### 15-61. 지도에 붙인 카드의 **테두리 색** — 오류가 아니라 규칙입니다

「지도에 붙이기」에서 **「만들기」만 분홍 테두리**라는 이야기가 있었습니다
(2026-08-23). 살펴보니 규칙대로 나온 것이었고, **그대로 두기로 했습니다.**

테두리 색은 그 활동에 붙은 **첫 번째 표시**를 따릅니다 (`mainMark`).

    해봤어요(👣) → 좋아해요(♥) → 도전(⭐) → 모르겠어요(?)

| 활동 | 붙은 표시 | 대표 표시 | 테두리 |
|---|---|---|---|
| 요리 · 수집 | 👣 ♥ ⭐ | 해봤어요 | 갈색 |
| **만들기** | ♥ ⭐ (👣 없음) | **좋아해요** | **분홍** |

「만들기」만 **「해봤어요」가 안 찍혀 있어서** 다음 차례인 「좋아해요」 색이 나온 것입니다.

▸ 같은 물음이 또 나오면 이 표를 보여 드리세요. 규칙이 학생 화면에 적혀 있지
  않아 선생님도 오류로 보시기 쉽습니다.
▸ 바꾸고 싶으면 `portfolio.js` 의 `MARK_ORDER` 차례만 고치면 됩니다.
  (그때 이야기한 다른 길 : 「좋아해요」를 맨 앞으로 / 테두리는 섬 색으로 통일하고
   표시는 카드 아래 아이콘으로만 구분)

### 15-62. ★ 원고지에 **손으로 따라 쓰기** (전자칠판 · 태블릿)

전자칠판이나 태블릿에서 **손가락 · 펜으로 원고지 칸을 덮어 쓰는** 길을
열었습니다. 흐린 글자가 깔려 있어 그대로 따라 쓰면 됩니다.

    일기 확인 화면 → 「글」 칸 → 「원고지에 손으로 쓰기」

▸ 다 쓰면 그림일기의 **원고지 자리를 대신**합니다. 인쇄와 포트폴리오에도
  학생 글씨 그대로 나옵니다. 「글자 원고지로 되돌리기」로 언제든 되돌립니다.
▸ 3단계에 이미 있던 「손글씨로 쓰기」(줄공책)와 **짝이 되는 기능**입니다.
  그쪽은 자유롭게 쓰는 줄공책, 이쪽은 칸을 따라 쓰는 원고지입니다.

**만든 방법** — `C.DrawPad` 에 `paper` 옵션을 더했습니다.

    paper = { cols: 10, rows: App.manuscriptRows([문장], 10), trace: true }

⛔ 줄 나누기를 새로 셈하지 마세요. `App.manuscriptRows` 한 곳(picdiary.js)에서만
  나눕니다. 따로 셈하면 화면과 인쇄가 어긋나 글씨가 칸에서 밀려납니다.
⛔ 칸과 흐린 글자는 **바탕에 함께 그립니다.** 그래야 저장한 그림에도 남아,
  인쇄했을 때 학생 글씨가 원고지 위에 앉은 것처럼 보입니다.
⛔ `paperwriting` 은 **`mode` 가 정해진 뒤에** 셈해야 합니다. 위쪽에 두었더니
  `mode` 가 아직 undefined 라, 따라쓰기 판으로 인쇄해도 손글씨가 나왔습니다.
  「글자」로 낼 때만 손글씨를 쓰고, 따라 쓰기 · 힌트 · 빈 칸으로 인쇄할 때는
  학생이 **새로 쓸 자리**를 내주어야 하므로 원래 칸을 그립니다.
⚠ 손글씨는 **그림으로** 남습니다. 글자로 알아보지는 못하므로 문장이 저절로
  만들어지는 곳에는 들어가지 않습니다.

저장 자리 : `diary.paperPhotoId` (draft 초기값과 저장 payload **둘 다**에
넣어야 합니다 — 하나라도 빠지면 저장하는 순간 글씨가 사라집니다).

재어 확인 : 판 1000×760 에 칸과 흐린 글자가 그려짐(칸선 · 글자 4만 점) →
펜으로 긋고 「다 썼어요」 → 단추가 「원고지 이어 쓰기」로 바뀌고 그림일기에
`.pd-paperimg` 가 들어감 → 저장하니 `paperPhotoId` 가 남음.

### 15-63. 일기 확인 화면 — 글자 크기를 **한 가지로**

선생님 말씀 : 「다양한 글자 크기가 있는 것도 주의 집중을 흐트린다」 (2026-08-24).

⛔ 이 화면 단추에 `font-size` 를 새로 적지 마세요. 하나만 달라도 화면이
  들쭉날쭉해 보입니다. **모두 1.05rem** 한 가지입니다 (머리말도 같습니다).
  크기로 중요도를 나타내지 않고 **색과 자리**로 나타냅니다.

| 무엇 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 그림 자리 · 크기 바꾸기 / 그림 다시 그리기 | 0.95rem (너무 작음) | **1.05rem** |
| 일기 저장하기 | 1.15rem | 1.05rem |
| 「일기가 완성되었어요」 | 1.36rem | 1.05rem |
| 날짜 · 사람 · 장소 바꾸기 | **두 줄**로 접힘 | 한 줄 (`white-space:nowrap`) |

⛔ `.fix-here .btn` 을 `flex:1 1` 로 균등하게 늘리지 마세요. 「날짜 · 사람 ·
  장소 바꾸기」가 두 줄로 접혀 알약이 뚱뚱해집니다. 폭은 글자만큼(`flex:0 1 auto`)
  이라 두 알약이 서로 달라도 됩니다.
▸ 머리말은 흰 칸 바로 아래라 `.confirm-fit` **밖**입니다. CSS 로는 여기가 확인
  화면인지 알 길이 없어, `C.Question` 에 `cls="q-confirm"` 표시를 달았습니다.

재어 확인 : 세로 600×1000 → 모든 단추와 머리말이 **17.85px 한 가지** ·
아래 두 알약 나란히 한 줄(w168 / w117) · 1쪽.

### 15-64. ⛔ 3단계 — 애써 쓴 뼈대가 완성 화면에서 사라지던 것

3단계 학생이 뼈대 여섯 줄을 다 썼는데도 완성 화면에 
**「아직 고른 내용이 없어요」** 가 뜨고 원고지도 비었습니다 (2026-08-24).

까닭 : 3단계는 뼈대를 쓴 다음 「일기 쓰기」 칸에서 살을 붙이는데, 뼈대를
그리로 옮기는 것이 **「글쓰기 칸에 넣기」 단추를 눌러야** 되는 일이었습니다.
안 누르고 넘어간 학생은 `draft.text` 가 빈 채로 끝났습니다.

▸ 글쓰기 칸이 비었으면 **뼈대를 그대로 본문으로** 씁니다 (`bodyText3`).
  뼈대만으로도 훌륭한 일기이고, 학생이 쓴 것을 버리지 않습니다.
⛔ 3단계 본문은 **반드시 `bodyText3()` 로** 꺼내세요. `draft.text` 를 그대로
  쓰면 같은 일이 다시 납니다. 지금 세 곳에서 씁니다 —
  완성 화면(`confirmStep`) · 저장 payload · 원고지 따라쓰기(`paperText`).

### 15-65. 3단계 그림일기 — 제목도 줄글, 본문 글자는 크게

**제목** : 본문이 밑줄인데 제목만 원고지 칸이면 한 장 안에 두 가지 모양이
섞여 어수선했습니다. 3단계는 「제목」 딱지 옆에 **한 줄로** 씁니다 (`.pd-titleline`).
⛔ 1·2단계는 그대로 칸입니다 — 그쪽은 본문도 칸이라 제목이 칸이어야 짝이 맞습니다.
⛔ 줄글 제목의 높이는 원고지 제목 줄과 **같은 79px** 로 둡니다. 다르면 아래
  밑줄 칸 계산이 어긋나 종이가 한 장을 넘습니다.

**본문 글자** : 34px → **46px** (선생님 말씀 — 「제목 글자만큼 조금 크게」).

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 고를 수 있는 크기 | 34 · 30 · 26 · 22 · 19 | **46 · 40** · 34 · 30 · 26 · 22 · 19 |
| 짧은 일기에 뽑히는 크기 | 30px | **46px** |
| 가장 적은 줄 수 | 5 | **4** |

⛔ `LINE_MIN_ROWS` 를 5로 되돌리지 마세요. **큰 글자가 아예 못 뽑힙니다.**
  짧은 글에도 5줄을 강제하니 46px(5×106=530) · 40px(470) 이 모두 자리(459px)를
  넘어 탈락했습니다. 4로 낮추어야 46px 이 424px 로 들어옵니다 — 재어서 정한 값입니다.

재어 확인 : 3단계 일기 → 제목 줄글(55px) · 본문 46px · 줄 간격 106px · 4줄 ·
한 장에 들어감. 「일기 쓰기」를 건너뛰어도 뼈대가 본문으로 나옴.

### 15-66. 선생님 설정 — 한눈에 들어오게 정리

(2026-08-24 · 선생님 말씀)

| 무엇 | 어떻게 |
|---|---|
| **번호가 눈에 띄게** | 이름표는 왼쪽 끝, 고를 것들은 **번호 다음 점 자리부터 들여쓰기**(1.7rem) |
| **폭 줄이기** | 「자세한 계획 · 시간 · 준비물 · 메모까지」 같은 긴 바가 화면 끝까지 늘어나 눈이 멀리 갔습니다. **34rem** 에서 끊습니다 |
| **5. 음성 안내 · 사진 첨부** | 고를 것이 둘뿐이라 **이름표 옆**에 나란히 (`.tsec.inline`) |
| **작은 머리말** (실내 / 실외, 그림 고르기) | `muted small` → **`.tsub`** (17px · 굵기 900 · 위아래 간격) |
| **그림 고르기 (남 / 여)** | 가운데 정렬 · 사람 묶음끼리 **27px 띄우고** 각 묶음은 옅은 상자로 묶음 |

⛔ 이름표(label)에는 들여쓰기를 주지 마세요 — 번호가 밀려 나 도로 묻힙니다.
⛔ 한 칸 안에서 다시 갈리는 자리를 `muted small` 로 두지 마세요. 흐린 작은
  글씨라 눈에 안 띄어 위아래가 **한 덩어리로** 보입니다. `.tsub` 를 쓰세요.

재어 확인 : 1280×800 · 600×1000 둘 다 — 이름표 x0 · 내용 x29 로 들여쓰기,
5번만 내용이 x206(이름표 옆), 작은 머리말 셋 모두 17px/900,
사람 묶음 다섯이 27px 간격으로 가운데 정렬.

### 15-67. 함께하는 사람 — 「형제자매」를 **세분화** (뒤에 삼촌·고모·이모까지)

선생님 말씀대로 「형제자매」를 빼고 나누었습니다 (2026-08-24).

    혼자 · 가족 · 엄마 · 아빠 · **할머니 · 할아버지 · 언니 · 누나 · 형 · 오빠 · 동생** · 친구 · 선생님

▸ 손위 형제는 **부르는 이름이 넷**입니다. 남학생은 누나 · 형, 여학생은 언니 ·
  오빠라 부릅니다. 하나로 묶으면 어느 쪽 학생에게든 어색해서 넷을 따로 두고,
  선생님이 그 학생에게 맞는 것만 켜 주도록 했습니다 (설정 7).
⛔ 「누나(언니)」 처럼 묶어 적지 마세요. 그대로 문장에 들어가면
  「나는 누나(언니)와 함께…」 가 됩니다.
⛔ 「형제자매」는 **지우지 않고 감춥니다**(`hidden: true`). 예전 일기·계획이
  이 이름을 가리키고 있어서, 목록에서 없애면 그 기록의 사람이 통째로 사라집니다.
  `App.partnersFor` 가 `hidden` 을 걸러 주고, `App.partner('sibling')` 은 그대로 읽힙니다.
⛔ **새로 생긴 사람은 켜 주어야 합니다.** `partnerIds` 는 「켠 것 목록」이라
  목록에 없는 사람은 안 나옵니다. 예전에 저장된 학생은 새 이름이 목록에 없어
  **하나도 나오지 않았습니다** — 선생님이 끌 기회조차 없었던 것들이라
  켜 주는 것이 맞습니다. `store.init` 에서 `partnersV2` 표시를 남겨 **한 번만**
  손봅니다 (매번 하면 선생님이 일부러 끈 것을 되살려 버립니다).

**그려야 할 그림 여덟 장** — `images/avatars/` 에 넣으면 아이콘 대신 쓰입니다.

    할머니.png · 할아버지.png · 언니.png · 누나.png · 형.png · 오빠.png
    동생 남.png · 동생 여.png

  없는 동안에는 코드로 그린 SVG 아이콘이 나옵니다 (화면은 깨지지 않습니다).
  ▸ 장소 그림은 **19곳 모두 있습니다.** 「학교 화단」은 목록에 없는 이름이라,
    선생님이 장소를 직접 써 넣으신 것입니다. 목록에 넣고 싶으면
    `options.js` 의 `D.places` 에 이름을 더하고 그림 한 장을 그려 주세요.

재어 확인 : 열세 명이 모두 나오고 한 쪽에 담김(줄이지 않음) ·
조사도 「형과 함께」 「오빠와 함께」 「할머니와 함께」 「동생과 함께」 로 맞음 ·
「형제자매」는 고르는 자리에 안 나오지만 예전 기록에서는 그대로 읽힘.

### 15-68. 장소에 「학교 화단」 더하기

`options.js` 의 `D.places` 에 넣었습니다. 학교 안 야외라 「텃밭」 다음에 두었습니다.

    교실 · 학교 · 강당 · 조리실 · 운동장 · 텃밭 · **학교 화단** · 집 · 동네 …  (모두 20곳)

▸ **그림 한 장**이 필요합니다 : `images/장소/학교 화단.png`
  없는 동안에는 코드로 그린 아이콘이 나옵니다 (화면은 깨지지 않습니다).
▸ 장소를 더할 때는 이름만 넣으면 됩니다 — 고르는 화면은 자동으로 쪽을 늘립니다
  (재어 확인 : 20곳이 되어 「1 / 4」 에서 「학교 화단」이 2쪽 첫째 자리에 나옴).

### 15-69. 나의 한마디 — **고른 그림을 문장 위에** 함께

선생님 말씀 : 「1단계 나의 한마디에서도 돌아보기처럼 문장과 그림이 같이
나왔으면」 (2026-08-24).

1단계 학생은 글을 못 읽습니다. 글자만 있으면 자기가 무엇을 골라 어떤 한마디를
만들었는지 알 길이 없었습니다. **돌아보기 네 줄에는 이미 그림이 붙어 있는데
한마디에만 없었습니다.**

▸ `student.wordPick` 에 담긴 활동 · 기분을 **문장 위에 나란히** 보여 줍니다.
▸ 고른 것이 있을 때만 나옵니다. 3단계처럼 직접 써 넣으면 짝이 되는 그림이
  없으므로 문장만 나오고 자리도 차지하지 않습니다.
⛔ 그림을 문장 **옆**에 두지 마세요. 문장 폭이 좁아져 앞말과 뒷말이 갈라집니다.
  `flex-basis:100%` 로 한 줄을 통째로 쓰고 문장을 그 아래에 둡니다.

재어 확인 : 요리 · 신나요 두 그림(72×93)이 문장 위에 나란히 · 한 쪽에 담김 ·
「나는 요리하기를 할 때 신나요.」

### 15-70. 사용설명서 다듬기 (2026-08-24)

`사용설명서(교사용).html` 에 오늘 바뀐 것을 모두 반영했습니다.
11장 · 표 24개 · 안내 상자 19개 (태그 짝 · 가로 넘침 확인).

| 장 | 무엇을 |
|---|---|
| 1. 시작하기 | 「화면 방향 — 태블릿은 세로로」 상자를 새로. 세로·가로·PC 의 높이와 그림 크기를 표로 견줌. 가장 먼저 하실 일에 **예시 자료 지우기**와 **7번 사람 고르기** 추가 |
| 3. 계획하GO! | 함께하는 사람 **13명** · 장소 **20곳** 표. 계획이 여러 개인 날의 ◀ ▶ 화살표 |
| 4. 알아보GO! | 지도 안내 문구 자리 · 「나의 여가로 돌아가기」 · 「◀ 실내외 여가 섬으로」 · 테두리 색 규칙 |
| 6. 그림일기 | 「원고지에 손으로 따라 쓰기」 상자 · 3단계 쓰는 방법 셋 · 3단계 제목은 줄글 |
| 7. 포트폴리오 | 일기장 모아 인쇄가 단계에 맞는 두 가지 · 나의 한마디에 그림 |
| 8. 라벨 | ⛔ **옛 규격을 지웠습니다** — 「5열 × 8줄 · 37 × 32mm」 는 지금 코드와 다릅니다. 고를 수 있는 네 규격(LS-3102 40칸 · 48칸 · LS-3104 27칸 · LS-3106 24칸) 표로 |
| 9. 선생님 설정 | 7번 그림 고르기 설명 · 「적어도 하나는 남겨 두세요」 상자 |
| 10. 자주 묻는 질문 | 다섯 가지 추가 — 태블릿 세로 · 확대 · 3단계 뼈대 · 음성 알림 자리 · 앞으로 할 계획 |

⛔ **앱을 고치면 설명서도 같이 보세요.** 특히 숫자가 적힌 곳은 코드와 어긋나기
  쉽습니다 — 라벨 칸 크기 · 사람 수 · 장소 수 · 인쇄 모양 이름.
▸ PDF 로 낼 때는 브라우저에서 열고 Ctrl+P → 대상 「PDF로 저장」,
  **「배경 그래픽」을 켜야** 안내 상자 색이 나옵니다.

### 15-71. 아직 그리지 않은 그림 여덟 장

`images/avatars/` 에 넣으면 아이콘 대신 그림이 쓰입니다 (없어도 화면은 멀쩡합니다).

    할머니.png · 할아버지.png · 언니.png · 누나.png
    형.png · 오빠.png · 동생 남.png · 동생 여.png

▸ 장소는 다 있습니다 — 「학교 화단.png」 도 2026-08-24 에 들어왔습니다 (20곳 완성).

### 15-72. 함께하는 사람에 **삼촌 · 고모 · 이모** (모두 16명)

    혼자 · 가족 · 엄마 · 아빠 · 할머니 · 할아버지 · **삼촌 · 고모 · 이모** ·
    언니 · 누나 · 형 · 오빠 · 동생 · 친구 · 선생님

▸ 사람이 늘면서 「새로 생긴 사람 켜 주기」(15-67)를 **판 번호**로 바꾸었습니다.
  예전에는 `partnersV2` 라는 **한 번 쓰고 마는 표시**여서, 사람을 더 넣어도
  이미 표시를 받은 학생에게는 **영영 안 보였습니다.**

    var PARTNER_ADDS = [
      { v: 2, ids: ['grandma','grandpa','sisEl','nuna','hyeong','oppa','younger'] },
      { v: 3, ids: ['uncle','gomo','imo'] }
    ];

⛔ **사람을 더할 때는 이 표에 한 줄을 더하고 `v` 를 올리세요.** 표를 안 고치고
  `options.js` 에만 넣으면 예전 학생에게는 안 보입니다.
⛔ 판 번호 없이 매번 켜면 선생님이 **일부러 끈 것을 되살려** 버립니다.
▸ `partnersV2` 는 판 번호를 쓰기 전의 표시입니다. 그것만 있는 학생은
  2판까지 받은 것으로 봅니다 (`s.partnerVer || (s.partnersV2 ? 2 : 0)`).

**그려야 할 그림이 열한 장이 되었습니다** (`images/avatars/`)

    할머니 · 할아버지 · 삼촌 · 고모 · 이모
    언니 · 누나 · 형 · 오빠 · 동생 남 · 동생 여

재어 확인 : 열여섯 명이 모두 나오고 **한 쪽에 담김**(0.82로 줄여서) ·
조사도 「삼촌과 함께」 「고모와 함께」 「이모와 함께」 로 맞음 · 판 번호 3.

### 15-73. 사람 그림 열한 장이 들어왔습니다 (2026-08-24)

    할머니 · 할아버지 · 삼촌 · 고모 · 이모
    언니 · 누나 · 형 · 오빠 · 남동생 · 여동생

⚠ **파일 이름이 「남동생 · 여동생」** 이라 `options.js` 의 `imageKey` 도 그렇게
  고쳤습니다 (처음에는 「동생 남 · 동생 여」 로 적어 두었습니다).
⛔ `imageKey` 는 **그림 파일 이름 그대로**여야 합니다 —
  `images/avatars/<imageKey>.png` 를 찾습니다. 한 글자만 달라도 그림이
  안 나오고 조용히 아이콘으로 바뀌므로, **눈으로는 알아채기 어렵습니다.**
  사람이나 장소를 더한 뒤에는 아래처럼 **그림이 실제로 실리는지** 재어 보세요.

    App.partnersFor(학생).forEach(function(p){
      var im=new Image();
      im.onerror=function(){ console.warn('그림 없음', p.name); };
      im.src = App.partnerImage(p, 학생);
    });

재어 확인 : **열여섯 명 모두 그림이 실림** (342×512 · 사람 그림은 세로형) ·
동생 남 · 여 둘 다 · 장소 「학교 화단」(512×341) 도 실림 ·
한 장에 73~100KB 로 가볍습니다.

▸ 이제 **아이콘으로 대신 나오는 사람은 없습니다.** 그림이 빠진 곳은
  「형제자매 남 · 여」 뿐인데, 그것은 감춰 둔 것이라 화면에 안 나옵니다.

### 15-74. ⛔⛔ `object-fit` 을 **인라인으로 박지 마세요** (하루를 잡아먹은 함정)

사람 카드를 **상반신으로 크게** 보이려고 `.pick .thumb.portrait img{ object-fit:cover }`
를 넣었는데 아무리 해도 `contain` 이었습니다. 규칙을 뒤로 옮겨도, 구체성을
올려도, `:not(.portrait)` 로 갈라도 안 되었습니다.

**까닭** — `C.Art` 가 `style=${{ …, objectFit:'contain' }}` 로 **인라인**에
박고 있었습니다. 인라인은 어떤 CSS 보다 셉니다.

    CSSOM 에는 cover 가 있는데 화면은 늘 contain  ←  이러면 인라인을 의심하세요.

    document.querySelector('...').getAttribute('style')

▸ 이제 `C.Art` 는 **크기만** 잡고, `object-fit` 은 CSS 가 정합니다.
⛔ 다시 인라인으로 넣지 마세요.

### 15-75. 사람 카드 — 한 쪽에 여섯, 상반신을 크게

사람이 열여섯으로 늘면서 한 화면에 다 놓으니 카드가 손톱만 해지고
**오른쪽이 잘렸습니다.**

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 한 쪽에 | 16명 (7칸) | **6명 (3칸 × 2줄)** · 「1 / 3」 |
| 그림칸 | 164×164 · 전신 | **269×201 · 상반신** |
| 화면 줄이기 | 0.7 (글 칸까지 작아짐) | **1 (줄이지 않음)** |

▸ 계획하기 · 일기 **두 곳 모두** 같은 방식입니다. 넘기는 단추 말도
  「앞 사람 보기 / 사람 더 보기」로 활동 · 장소와 같은 규칙입니다.
▸ 상반신은 `aspect-ratio:4/3` + `object-fit:cover` + `object-position:center 12%`.
  12% 는 머리 위 여백을 조금만 남기는 자리입니다 — 0% 면 답답합니다.

### 15-76. ⛔ 폭 보정이 **오른쪽을 잘랐습니다** (`zoom` 안에서 100% 의 뜻)

15-49 의 줄이기에 `width:calc(100% / var(--fit))` 를 함께 주었더니
**흰 칸 1058px 인데 격자가 2084px** 이 되어 오른쪽이 잘렸습니다.

`zoom` 이 걸린 요소에서 `100%` 는 **이미 보정된 값**(부모폭 ÷ zoom)입니다.
거기에 또 나누면 두 번 커집니다. ⛔ **폭은 건드리지 마세요.** 그냥 두면 꽉 찹니다.

### 15-77. 그림일기 · 확인 화면 — 글자 크기를 고르게

선생님 말씀 : 「보통 같은 크기로 쓰지 이렇게 들쑥날쑥하지 않지?」

| 자리 | 고치기 전 | 고친 뒤 |
|---|---|---|
| 그림일기 본문 | 46px | **34px** |
| 그림일기 제목 | 50px | **34px** (3단계는 본문과 같게) |
| 날짜 줄 | 26px | **32px** |
| 왼쪽 기둥 단추 | 1.05rem | **1.15rem** (그림 단추가 너무 작았음) |
| 아래 두 알약 | 높이 41px · 뚱뚱 | **높이 45px 로 얇게** · 글자는 위와 같은 1.15rem |
| 「일기가 완성되었어요」 | 알약 높이 34px | **49px** (옆 스피커 56px 과 어울림) |
| 「눌러서 크게 보기」 | 작음 | **높이 50px** |

⛔ 3단계 제목은 `lineFit.fs` 를 그대로 씁니다. 칸 수로 셈하면(55px) 본문보다
  훨씬 커집니다 — 3단계는 칸이 없기 때문입니다.

### 15-78. 나의 한마디 — **3단계에는 그림을 넣지 않습니다**

3단계는 낱말을 고르는 것이 아니라 **제 말로 직접 쓰는** 단계입니다.
고른 낱말 그림이 위에 남아 있으면 「이대로 쓰라」는 것처럼 보여 방해가 됩니다.

재어 확인 : 1단계 그림 2장 · 2단계 2장 · **3단계 0장**.
⚠ 확인할 때는 화면의 **「한마디 단계」 탭을 실제로 눌러야** 합니다.
  `diaryLevel` 만 바꾸면 이미 열린 화면의 단계(`meLvS`)는 그대로라 헛것을 잽니다.

### 15-79. 나의 한마디 — **단계마다 하는 일이 다릅니다**

맨 위 안내와 화면이 따로 놀았습니다. 안내는 「빈칸을 채워 완성해요」인데
문장은 저절로 완성되어 있고, 「틀 없이 내 말로 써요」인데 완성된 문장이
눈앞에 있어 그대로 베끼게 되어 있었습니다 (2026-08-24 · 선생님 말씀).

| 단계 | 맨 위 안내 | 화면 |
|---|---|---|
| **1** | 그림을 골라 문장을 만들어요 | 위에 고른 그림 둘 + **노란 바 안에도 그림** |
| **2** | 빈칸을 채워 문장을 완성해요 | 위에 고른 그림 둘 + **노란 바에 빈칸 두 개** (직접 씀) |
| **3** | 틀 없이 내 말로 써요 | **글자 알약 둘**만 + 아래 **흰 긴 바** |

⛔ **2단계에 「직접 쓰기」 칸을 따로 두지 마세요.** 노란 바가 이미 빈칸을
  채우는 자리라 같은 일을 두 번 하게 됩니다.
⛔ **3단계에 노란 문장 바를 두지 마세요.** 완성된 문장이 눈앞에 있으면
  학생이 그대로 베껴 씁니다 — 「내 말로」가 아닙니다. 그림도 넣지 않습니다.
  알약 글자만으로 충분하고, 그림까지 있으면 「이대로 쓰라」는 것처럼 보입니다.
⛔ **1단계 노란 바 안 그림을 2·3단계로 넓히지 마세요.** 1단계는 글을 못 읽는
  학생이 많아 그림과 글자가 한 줄에서 만나야 하지만, 2·3단계는 글자를
  읽고 쓰는 단계입니다.

담기는 곳 :

    1단계  wordPick(고른 id) → word(만들어진 문장)
    2단계  wordFill{act,mood}(학생이 쓴 낱말) → word(이어 붙인 문장)
    3단계  word(학생이 쓴 문장 그대로) · wordPick 은 알약 도움말로만 씀

재어 확인 (1240×1000) : 세 단계 모두 **한 쪽 · 줄이지 않음** ·
1단계 바 안 그림 2 · 2단계 빈칸 2 · 3단계 알약 2와 흰 바(614px).
2단계에 써 넣으면 `나는 놀잇감 활동을 할 때 편안해요.` 로 담기고
「나의 한마디 모음」과 인쇄에도 그대로 나옵니다.

### 15-80. 돌아보기 — 3단계는 **고른 것을 칸에 써 주지 않습니다**

3단계 돌아보기에서 위 활동을 고르면 칸에 활동 이름이 그대로 들어갔습니다.
제 말로 쓰는 단계인데 답이 미리 적혀 있던 것입니다.

▸ 이제 3단계는 고른 것을 **줄 왼쪽 그림으로만** 띄우고 칸은 비웁니다.
⛔ 그래서 고른 활동을 `student.reviewPick` 에 **따로** 담습니다.
  `student.review` 는 **학생이 쓴 글자만** 담는 자리로 그대로 둡니다.
  (인쇄 · 전시판형이 그 글자를 그대로 쓰기 때문입니다 — 15-?? 의 actByName)

### 15-81. ★ 세 단계가 **같은 것을 묻습니다** — 1단계에 빠져 있던 셋

「돌아보기」 네 줄에는 세 단계 모두 **가장 기억에 남는 것 · 다음에는**이
있는데, 정작 **일기에는 2단계에만** 있었습니다. 앞뒤가 맞지 않았습니다
(2026-08-24 · 선생님 말씀).

1단계에 **기억 · 다음에 · 제목** 셋을 넣어 2단계와 나란히 맞추었습니다.

    1단계  언제·누구와·어디에서·무엇을·기분 · 기억·다음에·제목·또 하고 싶나·그림·확인  (11)
    2단계  (같은 차례) · 확인                                                          (11)
    3단계  (같은 뼈대) · 일기 쓰기·제목·또 하고 싶나·그림·완성                          (10)

▸ 다른 것은 **학생이 스스로 하는 정도**뿐입니다 — 묻는 것은 같습니다.
▸ 제목도 그림으로 고릅니다(`titleStep`). 제 일기에 이름을 붙이는 일은
  1단계 학생에게도 값집니다.
⛔ 1단계 차례를 2단계와 **다르게 두지 마세요.** 단계를 올리면 학생이 같은
  차례를 다시 만나야 익숙합니다.

**함께 손본 것 둘**

1. `memoryBody()` · `nextBody()` 로 빼서 **1·2단계가 한 벌을 씁니다.**
   ⛔ 따로 만들면 낱말이 어긋나고 한쪽만 고치는 일이 생깁니다.
   담기는 자리도 같습니다(`frames.f3` · `f4`) — 단계를 올려도 쓴 것이 이어집니다.
2. `S.diaryAutoLines`(1단계 문장)에 그 두 줄을 더했습니다.
   ⚠ `다음에는 …` 을 골랐으면 `또 하고 싶어요`(diaryAgain)는 넣지 않습니다.
     둘 다 넣으면 같은 말이 잇달아 두 번 나옵니다. 2단계도 그렇게 합니다.

재어 확인 : 1단계로 일기를 끝까지 써서 —
「나는 어제 혼자 교실에서 요가를 했어요. 기분이 신났어요.
 **가장 기억에 남는 것은 친구와 함께한 것이에요. 다음에는 또 하고 싶어요.**」
2·3단계 흐름과 문장 틀은 그대로.

### 15-82. ★★ 세 단계를 **끝까지 맞추었습니다** (2026-08-24)

15-81 에서 1단계에 「기억 · 다음에 · 제목」을 넣었는데, 그 뒤 이야기를 나누며
**「다음에는」을 뺄지**로 한참 고민했습니다. 결론과 까닭을 남깁니다.

**「다음에는」은 남깁니다.** 「또 하고 싶나요?」와 **뜻이 다른 물음**입니다.

| 물음 | 무엇을 묻나 | 어디로 이어지나 |
|---|---|---|
| 오늘 한 것, 또 하고 싶나요? | **방금 한 그 활동**을 다시 할지 | 여가지도의 표시 |
| 다음에는 무엇을 해볼까요? | **다음에 해볼 다른 것** | 다음 계획 |

▸ 이 앱의 뼈대가 「계획 → 하기 → 일기 → 돌아보기 → 다시 계획」입니다.
  「다음에는 …」 이 그 고리를 잇는 자리입니다. 빼면 일기가 **지난 일을 적는
  데서 끝나고** 다음 계획으로 넘어가는 다리가 사라집니다.
▸ 돌아보기 네 줄도 앞 셋이 지난 일, 마지막 하나가 앞날입니다. 빼면 네 줄이
  모두 과거가 되어 마무리가 밋밋해집니다.

**두 물음이 같은 말로 들리던 것은 말이 비슷해서였습니다.** 그래서 갈랐습니다.

    또 하고 싶나요?            →  **오늘 한 것**, 또 하고 싶나요?
    다음에는 어떻게 하고 싶나요?  →  다음에는 **무엇을** 해볼까요?

⛔ 다시 비슷하게 되돌리지 마세요. 말이 겹치면 학생이 같은 것을 두 번
  묻는 줄 압니다.

**3단계 뼈대도 통일했습니다** (선생님 말씀 — 「3단계 뼈대도 통일감 있게」).

1. **묻는 말을 1·2단계와 같게** — 「언제 있었던 일인가요? · 누구와 함께했나요? ·
   어디에 갔나요? · 어떤 기분이 들었나요?」 처럼 3단계만 말이 달랐습니다.
   같은 것을 묻는데 말이 다르면 단계를 올린 학생이 **다른 것을 묻는 줄 압니다.**
   ⛔ `BONE_WRITE` 와 `SIX` **둘 다** 고쳐야 합니다 — 한쪽만 고치면
     앞 화면과 「내가 만든 뼈대」의 말이 어긋납니다.
   ▸ 「왜 그렇게 느꼈나요?」만 3단계 고유로 남깁니다.
2. **L3 에도 「기억 · 다음에」** 를 넣었습니다.
   ⛔ 「일기 쓰기」 **뒤로** 옮기지 마세요. 그 둘도 일기에 들어갈 재료라,
     글을 다 쓴 뒤에 물으면 쓸 자리가 없습니다.
3. `bodyText3()` 와 「내가 만든 뼈대」에 그 두 줄을 이어 붙였습니다.
   ⛔ 빠뜨리면 3단계에서 골라 놓고도 일기에 흔적이 없습니다.

**지금 차례**

    1단계  뼈대5 · 기억 · 다음에 · 제목 · 또 하고 싶나 · 그림 · 확인      (11)
    2단계  (한 글자도 다르지 않게 같음)                                  (11)
    3단계  뼈대5 · 기억 · 다음에 · **일기 쓰기** · 제목 · 또 하고 싶나 · 그림 · 완성  (12)

▸ 세 단계가 **같은 것을 같은 말로** 묻습니다. 다른 것은 학생이 스스로 하는
  정도와, 3단계에만 있는 「일기 쓰기」 한 단계뿐입니다.

재어 확인 : 1·2단계 물음 열두 줄이 **낱말 하나까지 같음** ·
3단계도 뼈대5 → 기억 → 「다음에는 무엇을 해볼까요?」 → 일기 쓰기 차례.

### 15-83. ★ **1단계는 뼈대 다섯만** (2026-08-24 · 마지막 결정)

15-81 → 15-82 를 거치며 「1단계에 무엇까지 물을까」를 여러 번 뒤집었습니다.
**마지막 결정**은 이것입니다 — 1단계는 **뼈대 다섯만** 묻습니다.

    1단계  언제·누구와·어디에서·무엇을·기분 · 제목 · 또 하고 싶나 · 그림 · 확인   (9)
    2단계  뼈대5 · **기억 · 다음에** · 제목 · 또 하고 싶나 · 그림 · 확인          (11)
    3단계  뼈대5 · 기억 · 다음에 · **일기 쓰기** · 제목 · 또 하고 싶나 · 그림 · 완성 (12)

▸ 그림만 고르는 학생에게는 비슷한 물음이 자꾸 이어지면 **무엇을 묻는지가
  흐려집니다.** 「기억에 남는 것」과 「무엇을 했나」도 1단계 학생에게는
  가려내기 어렵습니다.
▸ **돌아보기도 같습니다** — 1단계는 두 줄만 나옵니다.

    1단계   나는 ○○를 좋아해요 · 나는 새롭게 ○○에 도전했어요
    2·3단계 + 가장 기억에 남는 활동은 ○○ · 다음에는 ○○를 해보고 싶어요

**어떻게 갈랐나** — `options.js` 의 `reviewFrames` 에 `minLv: 2` 를 달고,
`App.reviewFramesFor(단계, 이미 쓴 것)` 한 곳에서 거릅니다.

⛔ **`rv`(이미 쓴 것)를 꼭 함께 넘기세요.** 예전에 1단계로 써 둔 줄이 있으면
  그것은 그대로 보여 줍니다. 목록에서 빼기만 하면 학생이 쓴 글이 화면과
  인쇄에서 **통째로 사라집니다.**
⛔ 부르는 곳이 **넷**입니다 — 줄 수 세기(207) · 전시판형(448) · 화면(811) ·
  인쇄(884). 한 곳만 고치면 화면과 인쇄가 어긋납니다.
▸ 화면은 **지금 고른 단계**(`meLvS[0]`), 인쇄·판형은 **학생의 단계**
  (`student.diaryLevel`)를 씁니다.

**1·2단계의 차이는 이렇게 정리됩니다**

| | 1단계 | 2단계 |
|---|---|---|
| 화면 맨 위 | 고른 **그림**이 쌓임 | 만들어지는 **문장 틀**이 자람 |
| 문장 | 고른 것으로 **저절로** | 낱말이 **빈칸에 하나씩** |
| 묻는 것 | 뼈대 다섯 | 뼈대 다섯 **+ 기억 · 다음에** |
| 돌아보기 | 두 줄 | 네 줄 |

재어 확인 : 1단계 일기가 「언제 → 누구와 → 어디에서 → 무엇을 → 기분 →
제목 → 오늘 한 것, 또 하고 싶나요? → 그림 → 완성」 ·
돌아보기 두 줄 · `reviewFramesFor` 가 단계별로 r1r2 / r1r2r3r4 를 내고
1단계라도 r3 에 쓴 것이 있으면 r1r2r3 을 냄.

### 15-84. ⛔⛔⛔ 화면이 **떨리던 것** — 다시 재는 그물을 잘못 던져서

「무엇을 했나요?」 화면에서 카드가 아래로 **93px 잘려** 있었습니다.
흰 칸 666px · 내용 827px 인데 **줄이기가 아예 안 걸렸습니다**(`--fit: 1`).

**까닭** — PNG 는 늦게 실립니다. 그림이 붙으면 카드가 커지는데 그때 다시
재지 않아 화면이 그대로 굳었습니다. 태블릿은 더 느려 잦습니다.
⛔ 흰 칸 자체를 `ResizeObserver` 로 보는 것으로는 **못 잡습니다** —
  높이가 flex 로 정해져 있어 **안쪽이 커져도 흰 칸 크기는 그대로**입니다.

**세 겹으로 고쳤습니다** (`C.Stage` · `C.useFitBox` 둘 다).

    ① 그림마다 load · error 를 듣고 그 자리에서 다시 재기
    ② 280ms · 900ms 두 번 더 재기 (load 를 놓친 그림이 있어도)
    ③ 자식 하나하나를 ResizeObserver 로 지켜보기

⛔ **900ms 를 지우지 마세요.** 태블릿은 그림 읽기가 느려 280ms 안에 다 못 싣습니다.

**그런데 ③ 을 그대로 물렸더니 화면이 떨렸습니다.**

    줄이기 → 자식이 작아짐 → 감시가 울림 → 또 재기 → …

`pass` 는 150ms 마다 풀려서 되돌이를 못 막습니다. 「일기가 완성되었어요」
화면이 계속 떨렸습니다.

⛔ **감시는 「커졌을 때만」 반응해야 합니다.**

    var lastH = el.scrollHeight;
    new ResizeObserver(function(){
      var h = box.scrollHeight;
      if (h > lastH + 2) { lastH = h; again(); }   // 커질 때만
    })

  그림이 실려 커지는 것만 잡으면 되돌이가 생기지 않습니다. 줄이면 높이가
  작아지므로 감시가 울려도 아무 일도 안 합니다.

재어 확인 : 세로 600×1000 · 가로 1280×800 · 1·2·3단계 확인 화면에서
2초 동안 `--fit` 과 `zoom` 이 **한 번도 안 바뀜**.

### 15-85. ⛔ 「식물 돌보기」가 **「식물 돌」** 로 잘리던 것

일기 제목 후보가 「고모와 함께한 **식물 돌**」 처럼 나왔습니다.
`App.shortName` 이 `보기` 를 접미사로 보고 뒤 두 글자를 떼어 낸 것입니다.
**「돌보기」는 한 낱말**인데 가운데를 자른 셈입니다.

⛔ **띄어쓰기가 있는 것만** 떼어 냅니다.

    전 : ['하기', ' 가기', '가기', ' 보기', '보기', ' 키우기', '키우기', ' 모으기']
    후 : ['하기', ' 가기', ' 보기', ' 키우기', ' 모으기', ' 그리기', ' 만들기']

▸ 「캠핑하기 → 캠핑」 만은 붙여 써도 안전합니다 — `-하기` 는 앞말에 붙는
  씨끝이라 그 앞이 늘 온전한 낱말입니다.
⛔ 붙여 쓴 꼴(`'보기'` · `'가기'`)을 다시 넣지 마세요. 「돌보기 · 알아보기 ·
  돌아보기」처럼 그렇게 끝나는 한 낱말이 많습니다.

재어 확인 : 활동 46가지 가운데 22가지가 줄고 24가지는 그대로.
「식물 돌보기 · 놀이 기구 타기 · 반려동물과 놀기」 등이 온전히 남음.

### 15-86. 선생님 설정 3·4번 — 알약을 **한 줄에 나란히**

「1단계 · 2단계」가 한 줄, 「3단계」가 다음 줄로 접혀 셋을 견주기 어려웠습니다.

⛔ 폭 제한(34rem)을 푸는 것만으로는 모자랍니다 — 좁은 화면에서는 셋을 한 줄에
  늘어놓을 자리가 아예 없습니다 (600px 화면 : 필요 691px · 자리 528px).
▸ **셋이 폭을 고르게 나눠 갖게** 합니다(`flex:1 1 0` · `flex-wrap:nowrap`).
  글자가 길면 알약 안에서 두 줄로 접히지만 **셋은 늘 나란히** 놓입니다.
▸ 「그림 고르기 (남 / 여) …」 줄은 아래 알약 줄과 맞추어 **가운데 정렬**.

재어 확인 : 600×1000 에서 3번 한 줄(170px 씩 셋) · 4번 한 줄(260px 씩 둘).

### 15-87. ⛔⛔⛔ **마지막 그물 — 밖에서 지켜보기** (가장 오래 헤맨 고장)

「무엇을 했나요?」 화면에서 카드가 **93px 잘려** 있었습니다. 흰 칸 666px ·
내용 827px 인데 줄이기가 안 걸렸습니다(`--fit: 1`). 창 크기를 건드리면
그때야 0.76 으로 줄어들어, **「가끔 되고 가끔 안 되는」** 고장으로 보였습니다.

**여러 번 헛짚었습니다.** 남겨 둡니다 — 같은 길로 다시 가지 않도록.

| 짚어 본 곳 | 결과 |
|---|---|
| 그림이 늦게 실려서 | `load` 를 들어도 그대로 |
| `zoom` 이 곧바로 반영 안 돼서 | `offsetHeight` 로 강제해도 그대로 |
| 여백(`--slack`)이 높이를 부풀려서 | 여백 전에 재도 그대로 |
| 늦게 재는 타이머가 취소돼서 | 안 지워도 그대로 |
| 부모가 준 높이로 재면 | 그래도 그대로 |

**진짜 까닭** — 계수기를 넣어 세어 보니, 그 화면에서 **재는 일 자체가 한 번도
일어나지 않았습니다.** 잰 값은 **앞 화면 것(401×401)** 그대로였고 3.5초를
기다려도 새 기록이 없었습니다. `C.Stage` 의 `useLayoutEffect` · 타이머 ·
`ResizeObserver` 가 모두 그 화면에서는 돌지 않았습니다.

**그래서 컴포넌트 밖에 그물을 하나 더 쳤습니다.**

    App.guardFit  —  0.4초마다 모든 흰 칸을 훑어 넘치면 한 단계씩 줄임

⛔ **지우지 마세요.** 안쪽 장치가 못 잡는 자리를 메우는 마지막 그물입니다.
⛔ **한 번에 한 단계씩만** 줄입니다. 한꺼번에 맞추려 하면 지나치게 줄었다
  돌아오기를 되풀이해 화면이 떨립니다.
▸ 넘치지 않으면 **아무 일도 하지 않습니다.** 그래서 늘 돌아도 가볍습니다.
▸ 되돌리는 일은 하지 않습니다 — 그것은 화면이 바뀔 때 Stage 가 합니다.
▸ 새 화면을 만들어도 자동으로 지켜집니다.

### 15-88. 기분도 **한 쪽에 여섯**

기분 열 가지를 한 화면에 다 놓으니, 3단계는 글 칸 두 줄까지 있어
**0.58 로 줄여도 자리가 모자라** 카드가 잘리고 2쪽이 되었습니다.

▸ 사람 · 장소 · 활동과 **같은 여섯 개 · 같은 말**(「앞 기분 보기 / 기분 더 보기」).
▸ 여러 개 골라도 됩니다 — 쪽을 넘겨도 고른 것은 그대로 남습니다.

재어 확인 : 3단계 기분 화면 → 카드 6개 · 「1 / 2」 · **1쪽 · 잘림 0**.

### 15-89. ★ 세 단계 **전 화면 점검** (갤럭시탭 세로 600×1000)

각 단계를 처음부터 끝까지 돌려 **잘림 · 빈 화면 · 넘침 · 작은 그림**을
모두 쟀습니다.

| 단계 | 화면 수 | 문제 |
|---|---|---|
| 1단계 | 11 | **없음** |
| 2단계 | 13 | **없음** |
| 3단계 | 14 | **없음** |

▸ 줄이기가 걸린 화면 : 누구와(0.64) · 어디에서(0.70) · 무엇을(0.76) ·
  기분(0.58~0.64) · 일기 쓰기(0.76) · 그림 고르기(0.94).
  **모두 한 쪽에 담기고 잘리지 않습니다.**
⛔ 다음에 화면을 고친 뒤에도 **이 점검을 다시 돌려** 보세요. 한 화면만 커져도
  줄이기 한계(0.58)에 걸릴 수 있습니다.

### 15-90. ⛔ 「처음 해 본 ○○」이 **아무 때나** 제목 후보로 뜨던 것

일기 제목 고르기에서 「처음 해 본 요리하기」가 **조건 없이 늘** 후보로
나왔습니다. 여러 번 해 본 활동에도 뜨니 **사실과 다른 제목**이 됩니다.
1단계 학생은 글을 못 읽어 그대로 고르기 쉽습니다 (2026-08-24 · 선생님 말씀).

▸ **정말 처음일 때만** 내놓습니다. 두 가지를 함께 봅니다.
  ① 여가지도에 **「해봤어요」 표시가 아직 없고**
  ② 그 활동으로 쓴 **일기가 이 일기 말고는 없을 때**
  둘 중 하나라도 어긋나면 이미 해 본 것이므로 내놓지 않습니다.
⛔ 조건을 지우고 늘 붙이지 마세요.

재어 확인 : 이미 해 본 4가지에는 안 나오고, 아직 안 한 26가지에만 나옴.

### 15-91. 그림일기 화면에서 **바로 손으로 따라 쓰기**

「따라 쓰기」 모양으로 보다가 **그 자리에서** 손으로 쓸 수 있습니다
(2026-08-24 · 선생님 말씀 — 「이 화면에서 바로」).
종이로 뽑지 않아도 전자칠판 · 태블릿에서 바로 씁니다.

    A4 인쇄하기 · **원고지에 손으로 쓰기** · 글자 원고지로 · 나의 일기 모음 보기

▸ 일기 확인 화면의 것과 **같은 판**입니다 (`C.DrawPad` 의 `paper`).
▸ 담기는 곳도 같습니다 — `diary.paperPhotoId`. 어느 화면에서 쓰든 이어집니다.
⛔ **1·2단계만** 내놓습니다. 3단계는 칸이 아니라 밑줄이라 원고지 판이 맞지
  않고, 그쪽에는 「손글씨로 쓰기」(줄공책)가 따로 있습니다.

### 15-92. 문장 틀 — **한 줄에 한 문장**

2단계 노란 띠에 「기분이 … 기억에 남는 것은 … 다음에는 …」 을 **한 줄에
몰아** 넣었더니, 줄이 길어져 가운데에서 접히는 바람에 「하고 싶어요.」 만
다음 줄에 덩그러니 남았습니다. 어디까지가 한 문장인지 알 수 없었습니다.

▸ **문장 하나에 줄 하나**로 나누고, 왼쪽부터 나란히 이어지게 했습니다.
  (가운데 정렬이면 접혔을 때 뒷말만 한가운데 남습니다)
▸ 줄이 늘어 자리를 더 먹지만, 넘치면 화면이 저절로 줄어듭니다
  (`App.guardFit` · Stage 의 줄이기).

재어 확인 : 2단계 「어디에서 했나요?」 → 네 줄로 나뉘고 **1쪽 · 넘침 0**
(0.64로 줄여서).

---

## 16. 2026-08-25 저녁에 한 일 — ★ 가로 게임판

**결정 셋** (선생님 말씀) : ① 아직 학생들이 써 보기 전이다 — 화면을 크게
바꿔도 된다. ② 토도수학처럼 **게임처럼 · 심플하게 · 이미지 크게**.
③ **완전 가로모드 전용**으로 쓴다.

### 16-1. 가로모드 전용 전환

| 무엇 | 전 | 후 |
|---|---|---|
| 흰 칸 폭 `--page` | 1060px | **1560px** (+ 양옆 .8rem 숨 쉴 자리) |
| `.stage.tall .panel` | 1400px | 1600px (기본보다 넓다는 뜻 유지) |
| 활동 카드 그림 `.pick .thumb` | 12.5vh (800높이 100px) | **19vh (152px)** |
| 카드 이름 `.pick .label` | 최대 1.35rem | 1.6rem · 굵기 900 |
| 고른 표시 `.pick .check` | 36px | 44px |
| 홈 코너 카드 | 21vh (168px) | **26vh (208px)** |
| 홈 코너 그림 | 8.5vh (68px) | 12vh (96px) |
| 아래 큰 단추 | 66px · 모서리 18px | 72px · **알약꼴** + 눌리는 그림자 |

- 「태블릿을 세로로 돌려 주세요」(C.TurnHint) 는 **통째로 없앴습니다**
  (common.js · app.js · app.css 세 곳). 가로 전용 결정과 정반대라서입니다.
  ⛔ 되살리려면 `_이전버전/20260825_가로게임판_이전` 을 보세요.
- 재어 확인 : 1280×800 → 활동 카드 397×215 · 그림 143px · fit 0.94 · 넘침 0 /
  1920×1080 → 흰 칸 1560(x=180) · 코너 281 · 실내외 그림 216px · 콘솔 오류 0.

### 16-2. 나가기 전 「여기까지 저장할까요?」 (계획 · 일기)

중간에 나가면 쓰던 것이 통째로 사라지던 것을 막았습니다.

- 계획·일기에서 **하나라도 고른 뒤** 홈으로 나가려 하면 묻습니다 —
  「여기까지 저장 / 저장 안 해요 / 계속 할래요」.
- 「여기까지 저장」 → `store.drafts[학생id].plan|diary` 에 `{draft, step}` 으로
  담깁니다. 다음에 그 코너에 들어오면 「쓰다 만 ○○이 있어요. 이어서
  할까요?」를 묻고, **같은 질문 · 같은 선택** 그대로 이어집니다.
- ▸ 화면으로 옮겨 온 순간 보관함은 비웁니다 (다시 나가면 또 묻습니다).
  끝까지 저장(계획 저장·일기 저장)해도 비웁니다. 학생 삭제 때도 함께 지웁니다.
- ⛔ **백업(내보내기)에는 넣지 않습니다** — 완성 전의 임시 기록입니다.
- ▸ 고치기(existing/editing) · 오늘의 도전 · 계획에서 온 일기의 첫 화면에서는
  묻지 않습니다 — 들어온 뜻이 분명한 길입니다.
- Esc·바깥 누르기는 언제나 「계속 할래요」 — 실수로 닫아도 잃는 것이 없습니다.
- 이를 위해 확인창에 **가운데 단추**가 생겼습니다 : `App.ui.confirm({altText})`
  → 'alt' 로 풀립니다. altText 를 안 주면 예전과 똑같습니다.

### 16-3. 홈 이름표 = 학생 바꾸기 팝업

홈 오른쪽 위 이름표(WhoChip)를 누르면 **화면을 떠나지 않고** 다른 학생으로
바꿉니다 (「누가 할까요?」 팝업 · 캐릭터 그림 + 이름 · 지금 학생은 파란 테두리).

- 바꾸면 알림과 함께 그 학생 목소리 설정으로 「○○, 반가워요!」 를 읽어 줍니다.
- 학생을 더하거나 고치는 일은 팝업의 「학생 화면으로 가기」 로 갑니다.
- ▸ 팝업 안 그림이 손톱만 해지는 것은 `.modal .pick .thumb`(970줄) 때문입니다 —
  격자에 `.big` 을 붙여 이깁니다 (specificity 가 같아 **뒤에 온** big 규칙이 이김).

### 16-4. 함께 올린 것

- `?v=` 20260828a (21곳) · `App.VERSION = '2026-08-25 · 가로 게임판 …'`
  (⛔ App.VERSION 은 `?v=` 올릴 때 **같이** 올리세요 — 8-17 이후 낡아 있었습니다)
- 수정 전 원본 8개 = `_이전버전/20260825_가로게임판_이전/`
- 구문 검사(node --check 6개) · 백틱/주석 짝 점검(§4 절차) 통과

---

## 17. 2026-08-25 밤에 한 일 — A급 안정성 셋

전체 점검(2026-08-25)에서 나온 A급 이슈를 고쳤습니다.

### 17-1. ★ 오류 안전망 (ErrorBoundary) — 흰 화면 금지

그리는 도중 오류가 나면 React 18 은 화면을 **통째로 내립니다.** 아무 안내
없는 흰 화면만 남았습니다 (html 주석 사고 때 실제로 겪은 일 — 작업노트 §4).

- `app.js` 에 `ErrorBoundary` 를 넣고 `<Root/>` 를 감쌌습니다.
  오류가 나면 「잠깐 쉬었다 할게요 — 선생님을 불러 주세요」 화면과
  **다시 열기** 단추가 나옵니다. 기록은 이미 저장돼 있어 잃지 않습니다.
- `index.html` 의 `#root` 에 「여는 중이에요 …」 를 미리 넣었습니다 —
  사진 보관소를 여는 최대 2.5초 동안 빈 화면이던 자리입니다 (`.boot-wait`).
- ⛔ 지우지 마세요. 평소에는 아무 일도 하지 않습니다.
- 재어 확인 : 렌더 함수 하나를 일부러 망가뜨려 → 안내 화면 + 다시 열기 동작,
  콘솔에 「나의 여가 — 화면 오류:」 기록, 새로고침 뒤 정상 복구.

### 17-2. ★ 뒤로가기가 「앞으로」 가던 것 — 길 되짚기

`nav()` 가 **되돌아가는 이동까지 길에 쌓았습니다.** 그래서
「포트폴리오 → 그림일기 → (돌아옴) 포트폴리오 → 파란 화살표」 가
홈이 아니라 **방금 나온 그림일기로 다시** 갔습니다. map.js 가 화면마다
`p.back` 을 피해 둔 것도 이 뿌리 때문이었습니다.

- 고침 : `nav()` 에서 **가려는 곳이 길 맨 위와 같은 화면이면 pop** (되짚기).
  화면 쪽 코드는 하나도 안 고쳤습니다 — 뿌리 한 곳입니다.
- ▸ 딸린 값(params)이 달라도 이름이 같으면 되짚은 것으로 봅니다.
  같은 화면을 두 겹으로 쌓으면 뒤로가기가 제자리를 돕니다.
- 재어 확인 : 그림일기 → 일기장 → 포트폴리오 → 홈 이 차례대로 (순환 없음).

### 17-3. 저장 실패 알림 + 그림판 용량

- **store.js** : 저장 실패를 `lastSaveError` 에 담기만 하고 아무도 안 읽어서,
  저장 공간이 차면 **아무 말 없이** 기록이 사라졌습니다. 이제 실패가
  시작될 때 한 번 알림을 띄웁니다 (성공하면 다시 무장 — 매번 띄우면
  알림이 화면을 덮습니다).
- **common.js DrawPad** : 그림판 출력을 PNG → **JPEG 0.85** 로. 손글씨 판이
  PNG 로는 수백 KB 라 file:// 실행의 localStorage 폴백(5MB)을 몇 장에
  채우고, 그 뒤 글자 저장까지 함께 막았습니다. 바탕은 paintBase 가 늘
  흰색으로 칠하므로 JPEG 로도 지장이 없습니다.
  (JPEG 전환은 코드 검토로 확인 — 실제 그림 저장 용량은 실기기에서 한 번 재 보세요)

### 17-4. 함께 정리한 것

- `vendor/_sd.html`(2026-08-16 구판 사본 514KB, 어디서도 안 씀) →
  `_이전버전/20260816_단일파일_구판_sd.html` 로 이동. vendor/ 에 있으면
  라이브러리로 오해합니다.
- 지도 도장 띠 잘림(세로 600px에서 우측 163px 잘리던 것)은 **가로 전용
  전환으로 해소** — 1280 가로에서 오른쪽 끝 1236px < 1280 확인.
- `?v=20260828b` · `App.VERSION '… 가로 게임판 2'` · single-file 재빌드(1.9MB) ·
  구문 검사 통과.

### 남은 B급 (다음 차례 후보)

백업 가져오기 검증·doneDiaryId 복구·중복 판정 (store.js 539~) ·
일기 삭제 시 paperPhotoId/writePhotoId 정리 (store.js 449~) ·
학생 카드 :active 피드백 · 저장 단추 연타 잠금 · 하드웨어 뒤로가기.

---

## 18. 2026-08-26 에 한 일 — B급 다섯 (17절 「남은 B급」 전부)

### 18-1. ★ 백업 「덧붙이기」 개편 — id 가 같으면 같은 것

예전 병합은 id 가 겹치면 **새 id 를 부여**해서 세 가지 탈이 났습니다 —
같은 백업 두 번이면 두 벌 · doneDiaryId(계획↔일기) 끊김 · 두 벌이 같은
사진을 가리켜 한 벌을 지우면 남은 벌 사진까지 사라짐.

- 이제 **id 가 같으면 같은 기록으로 보고 건너뜁니다** (id 는 무작위 uid 라
  다른 자료끼리 겹칠 일이 사실상 없습니다). id 를 그대로 두므로
  planId·doneDiaryId 연결이 저절로 유지됩니다.
- 지도 표시는 칸마다 **updatedAt 이 새것**을 남깁니다 — 옛 백업을 다시
  불러와도 지금 표시가 되돌아가지 않습니다.
- 파일 검증 : `app` 표시가 다르면 거부, students 가 record 꼴이 아니면 거부.
- ⛔ 「겹치면 새 id」 로 되돌리지 마세요.
- 재어 확인 : 같은 백업 2회 병합 → 학생1·계획1·일기3 그대로(중복 0) /
  다른 기기 기록(계획↔일기 연결) 병합 → 연결 유지 / 다른 앱 JSON 2종 거부.

### 18-2. 일기 삭제 때 손글씨 사진 두 장도 함께

`removeDiary` 가 photoIds·drawPhotoId 만 지워서, 가장 큰
**paperPhotoId(원고지 손글씨)·writePhotoId(손글씨 일기)** 가 보관소에
쌓였습니다. 함께 지웁니다.

### 18-3. 안드로이드·브라우저 뒤로가기

뒤로 단추를 누르면 **앱 밖으로 나가던 것**(전체화면이면 종료)을 막았습니다.

- 켜질 때 history 한 칸을 쌓고, popstate 마다 다시 쌓아 밖으로 못 나갑니다.
- 그 다음 **화면에 보이는 그대로** 움직입니다 — 팝업이 열려 있으면 팝업만
  닫고(바깥 누르기와 동일), 파란 화살표가 있으면 그것을 누른 것과 같게
  (단계 되돌리기·「여기까지 저장할까요?」 포함). 표지·홈에서는 아무 일도 안 합니다.
- ⛔ 여기서 navBack() 을 직접 부르지 마세요 — 나가기 확인을 건너뛰어
  쓰던 것을 묻지도 않고 버립니다.
- 재어 확인 : 계획 도중 history.back() → 저장 확인 팝업 · 앱 유지 /
  팝업 열린 채 또 back() → 팝업만 닫히고 화면 유지.

### 18-4. 저장 연타 잠금 (계획·일기)

저장 뒤 화면이 바뀌기 전에 두 번째 탭이 떨어지면 기록이 두 개 생길 수
있었습니다. `savedS`/`savedIdS` 가 차 있으면 그냥 돌아갑니다.

### 18-5. 학생 카드 — 누른 즉시 반응

이름을 읽어 주는 0.35초 동안 아무 변화가 없어 학생이 또 누르던 것 :
누르는 즉시 **파란 테두리(.picked)** + `:active` 눌림, 그동안 다른 카드 잠금.
재어 확인 : 누른 직후 렌더에서 picked 표시 확인.

### 18-6. 함께 올린 것

`?v=20260828c` · `App.VERSION '2026-08-26 · 가로 게임판 3'` ·
수정 전 원본 6개 = `_이전버전/20260826_B급이전/` · single-file 재빌드 ·
구문 검사 통과. 17절의 「남은 B급」은 이것으로 **모두 마쳤습니다.**

---

## 19. 2026-08-26 에 한 일 — C급 구조 정리 + ★ B안 미니멀 디자인

### 19-1. ★ B안 — 회색 바탕 · 흰 스테이지 · 파랑 하나

디자인 시안(캔버스)의 B안을 실제 적용했습니다.

| 무엇 | 전 | 후 |
|---|---|---|
| 바탕 `--bg` | #f6f8fc (거의 흰색) + 민트 벽지 | **#e9edf3 회청색 · 벽지 끔**(app.js 주석 처리) |
| 포인트 색 `--accent` | 코너마다 다름 (하늘·민트·보라·살구) | **#2b7de9 파랑 하나** — 코너별 재정의 5줄 삭제 |
| 질문 알약 `.q-pill` | 분홍 | **연한 파랑**(--sel-bg/--sel-line) — 규칙은 그대로 「묻는 자리는 늘 같은 알약색」 |
| 고른 표시 `.check` | 초록 | 파랑 |
| 홈 코너 그림 | 색 동그라미 12vh 안 | **동그라미 제거 · 그림 직접 15vh** |
| 코너 부제(계획하GO!) | 표시 | **숨김**(display:none · aria/읽어주기에는 남음) |

⛔ 「고른 것은 파랑」 규칙이 이제 앱 전체의 **유일한 포인트 색 규칙**입니다.
   새 화면에 초록·분홍·주황 포인트를 다시 들이지 마세요.
▸ 되살리기 : 코너 색 5줄·벽지 1줄은 _이전버전/20260826_C급B안이전 에 있습니다.

### 19-2. 글자 크기 토큰 (:root)

`--fs-title · --fs-q · --fs-big · --fs-card · --fs-body · --fs-sub · --fs-small`
7단을 :root 에 두고, 제목·질문·힌트·카드 이름·큰 단추·.small 을 토큰으로
바꿨습니다. ⛔ **새 글자 크기는 토큰만 쓰세요** — 숫자를 새로 지어내지 말 것
(점검에서 값 110종·clamp 공식 59종이 나온 원인). 기존 세부 화면(확인 화면
1.05rem 규칙 등)은 실측으로 맞춘 값이라 **건드리지 않았습니다**.

### 19-3. 쪽 나누기 공용화

- `App.PAGE_SIZE = 6` · `App.pageOf` · `App.flowBox` 를 **common.js** 로
  (pageOf/flowBox 는 portfolio.js 에 살아서, home.js 가 불러오는 차례 덕분에
  우연히 돌던 층위 역전이었습니다).
- plan/diary 의 `PAGE_SIZE·WHO_SIZE·WHO_PER·PLACE_PER·MOOD_PER` 다섯 이름을
  전부 `App.PAGE_SIZE` 로. 지도의 4·5개는 섬 배치용 **다른 규칙**이라 그대로.
- 일기 활동 고르기의 쪽 단추가 「앞 활동/다음 활동·작은 단추(44px)」로
  규칙(작업노트 §5)과 어긋나던 것 → 계획과 같은 「앞 활동 보기/활동 더 보기·
  기본 크기」로 통일.

### 19-4. 인쇄 가드 (app.css ↔ print.css)

app.css 는 media 없이 불려 **인쇄에도 그대로 적용**됩니다. A4 인쇄 폭이
703px 라 max-width 분기가 인쇄에서 켜질 수 있었습니다.

- app.css 의 `@media (max-…)` **34곳 전부** `screen and` 를 붙였습니다.
  ⛔ 새 미디어쿼리에도 꼭 붙이세요.
- print.css 에 방어 추가 : `#print-root .fitting>*{zoom:1}` ·
  dv/fix/jr 종이 transform 해제 · `.sheet-art` 200px 고정(vh 는 인쇄에서
  쪽 높이 기준이라 흔들립니다).
- (실제 인쇄물 확인은 실기기에서 한 번 해 보세요 — 코드 검토만 했습니다)

### 19-5. 미참조 CSS 정리

어디서도 안 쓰는 클래스 규칙 **55개**를 지웠습니다 (스크립트 42 + 수동 13 :
folio-tab/pick/sum·pd-art-main(모순 2중 정의)·mymap-list/row/thumb·
map-paths/legend/sum·keypad·scrolly·divider·plan-back·island-back 등).
- js·html 전수 대조로 미사용 확인 뒤에만 지웠습니다.
- prefers-reduced-motion 블록 4곳은 겹쳐 보여도 **뜻이 달라 남겼습니다**
  (transform:none·폭죽 대체 애니는 전역 .001ms 규칙과 다른 일을 합니다).

### 19-6. 함께 올린 것

`?v=20260828d` · `App.VERSION '2026-08-26 · 미니멀판'` · 원본 9개 =
`_이전버전/20260826_C급B안이전/` · single-file 재빌드 · 사용설명서 색 설명 갱신 ·
재어 확인 : 파랑 rgb(43,125,233) 통일 · 홈/계획/일기/포트폴리오 렌더 ·
카드 6장 · 넘침 0 · 콘솔 오류 0 · 주석 짝/구문 검사 통과.

---

## 20. 2026-08-26 에 한 일 (2) — ★ 맨 위 줄 통일 · 홈 한 줄 · 단계띠 이동 (선생님 여섯 가지)

### 20-1. ★ 맨 위 줄(TopBar) 대개편 — 어느 화면이나 같은 자리

    왼쪽  : [파란 화살표 = 앞 단계로] [나의 여가 = 홈으로] [화면 제목(글자만)]
    오른쪽: [홈] [전체화면] [선생님 설정] [학생 이름표 = 바꾸기]

- 예전에는 화살표(첫 단계) · 제목 누르기 · 「나의 여가로 돌아가기」 **셋이
  모두 홈**으로 갔습니다. 이제 화살표는 **앞 단계로만**, 제목은 그냥 이름표,
  홈은 「나의 여가」 글자와 오른쪽 홈 단추가 맡습니다.
- 「나의 여가로 돌아가기」 단추 셋(계획 저장 화면·지도·일기 모음)을 지웠습니다.
- 화면마다 따로 달던 학생 이름표(WhoChip) 9곳을 지우고 TopBar 가 **한 곳**에서
  그립니다. 이름표의 「바꾸기」 글자도 뺐습니다 — 누르면 바꾸는 것이 곧 뜻입니다.
- ★ **어디서든** 이름표를 누르면 「누가 할까요?」 팝업으로 바로 바꿉니다.
  계획·일기를 쓰는 중이면 **「여기까지 저장할까요?」를 먼저** 묻습니다.
  ▸ 그 다리가 `App.leaveGuard` 입니다 — plan/diary 가 자기 leave 함수를 걸어
    두고, 공용 단추(홈·나의 여가·설정·학생 바꾸기)는 `App.goGuarded` 로 갑니다.
  ⛔ 새로 만드는 „쓰다 마는" 화면이 있으면 같은 방식으로 leaveGuard 를 거세요.
- 홈·학생 화면의 왼쪽 위 「표지로 가기」는 없앴습니다 — 표지는 켤 때 나오는
  문이고, 그 뒤로는 갈 일이 없습니다.

### 20-2. 홈 코너 = 한 줄에 넷 (2×2 → 1×4)

가로 화면 전용이라 옆으로 펴는 편이 자리를 잘 씁니다 (`.corner-grid` repeat(4,…)).

### 20-3. 일기 단계 띠 — ✓ 칸을 누르면 그 단계로

`C.Steps` 에 onGo 가 생겼습니다. **지나온(✓) 칸만** 단추가 됩니다 —
앞 단계를 건너뛰면 답하지 않은 질문이 생기므로 막아 둡니다.
지금 단계 표시는 주황 → **진한 파랑 + 흰 글씨** (B안 색 하나 규칙).

### 20-4. 일기 제목 — 제목 칸이 곧 입력칸

「직접 쓰기」 줄을 없애고, 노란 띠의 제목 칸(input.title-input)에 바로
씁니다. 카드를 고르면 이 칸에 들어오고, 눌러서 바로 고칠 수 있습니다.
(.title-own CSS 도 지움)

### 20-5. 여가 섬 이름 · 색

「실내 여가 섬 / 실외 여가 섬」 → **「여가 섬(실내) / 여가 섬(실외)」**
(js 15곳 · 사용설명서). 섬 이름 알약을 색으로도 가릅니다 —
실내 = 주황(#ffe9cc/#c96a1e) · 실외 = 파랑(#d6ebf9/#1f6e9e),
모아보기의 .mymap-island.in/.out 과 **같은 색**입니다.
▸ 색만으로 가르지 않습니다 — 이름이 늘 함께 적혀 있습니다.

### 20-6. 함께 올린 것

`?v=20260828e` · `App.VERSION '미니멀판 2'` · 원본 10개 =
`_이전버전/20260826_상단바개편이전/` · single-file 재빌드 · 사용설명서 갱신
(어디서나 같은 규칙 · 한 줄에 넷 · 섬 이름).
재어 확인 : 홈 4열·가로 넘침 0 / 상단바 6요소 확인 / 이름표→전환 팝업→저장
확인→홈 체인 / 단계띠 ✓ 3칸 클릭 이동 / 제목 카드 선택 시 입력칸 반영 /
섬 라벨 새 이름·주황 배경 / 콘솔 오류 0 · 구문·주석 짝 검사 통과.

---

## 21. 2026-08-26 에 한 일 (3) — 선생님 여섯 가지 (도장판·이름 입력·설정 두 칸)

### 21-1. 지도 도장판 → 맨 아래

지도가 화면의 주인공이라 위쪽 자리를 지도에 줍니다.
부모(.stage-fit)가 세로 flex 라 `.stampcard{ order:99 }` 한 줄로 내렸습니다.

### 21-2. 지도 가운데 학생 카드(map-me) 삭제

맨 위 줄 오른쪽에 학생 이름표가 늘 있어 **같은 말이 두 번**이었고 지도
가운데를 가렸습니다. map.js 렌더 + CSS 5규칙 삭제.
▸ 함께 훑은 「학생 크게 표시」 자리 — 남긴 것과 까닭 :
  · 캐릭터 고르기 왼쪽 미리보기 = 고르는 대상이라 필요 (이름은 21-3 입력칸이 됨)
  · 포트폴리오 첫 화면 「나」 칸 · 나의 한마디 아래 캐릭터 = 화면 내용의 일부
  · 학생 고르기 카드 = 고르는 목록 자체
  이 셋은 **이름표와 하는 일이 달라** 남겼습니다. 빼길 원하시면 말씀 주세요.

### 21-3. 캐릭터 화면 — 이름 칸이 곧 입력칸

`.cs-name` div → input. 고치면 바로 저장됩니다(updateStudent).
재어 확인 : 입력 → store 반영.

### 21-4. 위 줄 두 길 가르기 + 홈 세로 배분

- **로고(나의 여가) = 처음 화면(표지)으로** · **집 단추 = 나의 여가(코너 넷)로**.
- 홈 제목 「나의 여가」 → **「나의 여가를 만들어볼까요?」**
- 홈 흰 칸을 tall(화면 높이 전부)로 하고, 남는 자리(--slack)를
  제목 위 .22 · 아래 .30 · 코너 아래 .28 로 나눴습니다.
  재어 확인 : 800 높이에서 흰 칸 726px · 코너 아래 여백 68px.

### 21-5. 표지 소리 단추 그림

읽어주기와 똑같은 스피커 그림이라 헷갈리던 것 → 소리 끄기는 **음표(music)**
선 아이콘으로. 꺼짐 빗금(.iconbtn.off)이 선 아이콘에도 그이게 넓혔습니다.

### 21-6. 선생님 설정 — 흰 카드 **두 칸** 배열

한 줄 목록이라 오른쪽 절반이 늘 비어 있던 것 → 항목마다 카드
(.tsec 테두리+바탕), 넓은 화면에서 두 칸 격자(.tscroll grid).
긴 항목(1 학생·7 사람·8 기분·9 활동·11 기록)은 `.wide` 로 한 줄 전체.
900px 아래에서는 한 칸으로 접힙니다.

### 21-7. 함께

`?v=20260828f` · `App.VERSION '미니멀판 3'` · 원본 7개 =
`_이전버전/20260826_지도도장판외이전/` · single-file 재빌드.
재어 확인 : 도장판 y715(지도 90 아래) · map-me 없음 · 설정 2열·wide 전체폭 ·
이름 입력 저장 · 로고→표지·집→홈 · 콘솔 새 오류 0.

---

## 22. 2026-08-26 에 한 일 (4) — 선생님 설정 짝배열 · ✓ 없는 선택

### 22-1. 고른 표시 = 색으로만 (✓ 삭제)

설정의 모든 선택 단추에서 ✓ 글자를 뺐습니다 (Choices · 7 사람 · 8 기분 ·
9 활동). 대신 켜진 알약을 **파란 테두리 3px + 연한 파랑 바탕 + 굵은 글씨**
(.tchoice.on)로 또렷하게 했습니다 — B안 「파랑 하나」 규칙 그대로.

### 22-2. 설정 박스 = 2개씩 짝배열 (고정)

    1/2 · 3/4 · 5/6 · 7/8 · 9-1/9-2 · 10/11 · 12/13   (14칸 · 7줄)

- .wide 전체폭 칸을 없애고 전부 반칸으로 — DOM 차례가 곧 짝입니다.
- **9번을 9-1. 학생 활동(실내) / 9-2. 학생 활동(실외)** 두 칸으로 갈랐습니다.
  「우리 반 활동」 목록도 실내·실외 각자 칸으로 갈라 담습니다.
- 재어 확인 : 1·2 같은 줄 · 9-1·9-2 같은 줄 · 라벨 14개 차례대로.

### 22-3. 나머지 셋

- **1. 우리 반 학생** : 캐릭터 단추에서 「캐릭터」 글자 삭제(그림만,
  aria 유지) · 학생 줄 최대 폭 36rem (카드 폭을 다 먹지 않게).
- **3·4번 두 줄 알약** : 「1단계 / 그림으로 골라 쓰기」처럼 이름과 설명이
  줄바꿈됩니다 — Choices 에 `sub` 가 생겼습니다 (.t-name / .t-sub).
- 안내 문구 「체크된 활동만」 → 「색이 켜진 활동만」.

`?v=20260828g` · `App.VERSION '미니멀판 4'` · single-file 재빌드.

---

## 23. 2026-08-26 에 한 일 (5) — 홈 85% · 음표 아이콘 · 알약 줄바꿈

1. **홈 흰 칸 높이 85%** — tall(끝까지 채움)이 답답하다는 말씀에, 홈에서만
   stretch 를 풀고 높이 85% · 세로 가운데로 (`[data-corner="home"] .stage.tall`).
   재어 확인 : 무대 대비 85%.
2. **소리 단추가 물음표로 나오던 것** — IconBtn 의 선 아이콘은 **UI 목록**에서만
   찾는데 music 을 ICONS 에만 기대고 있었습니다. UI 에 **음표+물결** 그림을
   그려 넣었습니다. ⚠ IconBtn 에 새 icon 을 쓸 때는 icons.js 의 `UI` 에
   있는지부터 보세요 — 없으면 조용히 물음표가 됩니다.
3. **3·4번 알약 설명 줄바꿈** (선생님이 정한 자리 그대로) :
   그림으로/골라 쓰기 · 문장 틀/완성하기 · 자유롭게/쓰기 ·
   활동 · 함께하는 사람/날짜 · 장소 · 시간 · 준비물/메모까지.
   sub 에 `\n` + `.t-sub{ white-space:pre-line }`.

`?v=20260828h` · `App.VERSION '미니멀판 5'` · single-file 재빌드 ·
재어 확인 : 음표 svg · 85% · pre-line 다섯 알약 전부.

---

## 24. 2026-08-26 에 한 일 (6) — 알약 옆배치 · 기록 3개+더 보기 · 큰 음표

1. **3·4번 알약** : 단계 이름 옆에 **두 칸쯤 띄고** 설명이 옵니다 —
   「1단계␣␣그림으로 / 골라 쓰기」. t-name·t-sub 를 inline-block 으로,
   설명 줄바꿈은 pre-line 그대로.
2. **5번 음성 안내** : inline(이름표 옆 스위치)을 풀어 제목이 6번과 같은
   높이(칸 맨 위)에 옵니다.
3. **11번 학생의 기록** : **최근 3개만** + 「더 보기 (모두 N개)」 →
   팝업으로 전체 목록(최근순, 고치기·지우기 그대로). 기록이 쌓여도
   옆 칸(10번)과 키가 어긋나지 않습니다.
   재어 확인 : 4개로 늘려 → 3줄+「더 보기 (모두 4개)」 → 팝업 4줄 → 정리.
4. **소리 단추(음표)** : 그림을 더 크게(56→74%) · 한가운데로, 음표도
   상자에 맞게 다시 그렸습니다 (icons.js UI.music).

`?v=20260828i` · `App.VERSION '미니멀판 6'` · single-file 재빌드.

---

## 25. 2026-08-26 에 한 일 (7) — 「모두」 켜기 · 여가 섬 화면 재구성

### 25-1. 선생님 설정 — 「모두」 단추 · ＋만 남기기

- 7 사람 · 8 기분 · 9-1 실내 · 9-2 실외 맨 앞에 **「모두」** — 누르면 그 칸의
  선택지가 전부 켜집니다 (partnerIds/moodIds 전체 채움 ·
  hiddenActivityIds 에서 그 영역 id 제거).
- 「＋ 사람 더하기」 류는 **＋ 하나만** 남기고 뜻은 aria-label·title 로.
  (「＋ 학생 추가」는 그대로 — 칸 밖에 홀로 있어 글자가 있어야 알아봅니다)
- 재어 확인 : 모두 4곳 · 사람 하나 끄고 모두 → 17/17 복원.

### 25-2. ★ 여가 섬(섬 안) 화면 재구성 (선생님 말씀 넷)

| 무엇 | 전 | 후 |
|---|---|---|
| 섬 나가기/전환 | 「◀ 실내외 여가 섬으로」 알약 | **도구 줄 왼쪽에 두 섬 알약** — 누르면 바로 건너감, 지금 섬은 파란 겹테(.now). 섬 고르기 화면은 파란 화살표로 |
| 모아보기 | 지도 오른쪽 위(absolute) | **도구 줄, 지도 도움말 왼쪽** (absolute 걷음) |
| 카드 배열 | 한 쪽 4장 (4×1/2×2) | **4칸 × 2줄 = 8장** (`F_PLANS=[[4,2]]`) — bestPlan 이 크기를 맞춰 스크롤 없음 |
| 쪽 넘김 | 아래 「◀ 이전 · 1/4 쪽 · 다음 ▶」 | **카드 양옆 flow-arrow 화살표** + 아래 **큰 점**(.isl-dots-big 15px). 「n/n 쪽」 글자 삭제 |

- ⚠ **L.islands 는 섬 안에서 들어온 섬 하나만** 담습니다 — 전환 알약은
  고정 목록으로 그립니다 (처음에 L.islands 로 그렸다가 알약이 하나만 나왔습니다).
- 재어 확인 : 알약 2 + 현재 표시 · 실외 전환 동작 · 카드 8장 전부 지도 칸 안
  (잘림 0) · 화살표로 2쪽(7장) · 점 이동 · 옛 이전/다음·쪽 글자 없음.

`?v=20260828j` · `App.VERSION '미니멀판 7'` · single-file 재빌드.

---

## 26. 2026-08-26 에 한 일 (8) — 섬 토글바 · 카드 음영 · 카드 최대화

### 26-1. 도구 줄 = 가운데 **토글바 하나** (.isl-seg)

    [ 여가 섬(실내) | 여가 섬(실외) | 모아보기 ▶ ]   … 오른쪽 끝에 지도 도움말

- 흰 알약 안 세 칸 슬라이드 메뉴. 켜진 섬 칸이 섬 색(실내 주황 · 실외 파랑)으로
  차오릅니다. 모아보기도 같은 바 안 세 번째 칸입니다.
- **찾아보기(거르기·배율 상자)는 없앴습니다** — 한 쪽 8장이 되면서 걸러 볼
  만큼 많지 않습니다. 되살리려면 _이전버전/20260826_지도도장판외이전.
- 지도 안 섬 이름 바(island-head)도 삭제 — 토글바에 같은 이름이 켜져 있어
  중복이었습니다. (섬 고르기 화면의 gate-name 은 그대로)

### 26-2. 활동 카드 — 테두리 없이 음영, 더 크게

- `.mapcard` : 테두리 3px → **0**, `0 3px 12px` 부드러운 그림자.
  해본 곳은 테두리 5px 대신 **바탕색(크림/하늘) + 조금 진한 그림자**.
  ▸ 색·두께 규칙(1050줄 언저리 주석)은 발자국 표시가 뜻을 나르므로 안전합니다.
- `LABEL_H 52→10 · NAV_H 52→36` — 이름 바·아래 줄이 사라진 자리를 카드가
  씁니다. 재어 확인 : 카드 **184×184**(1280×800) · 8장 전부 칸 안 · 잘림 0.

`?v=20260828k` · `App.VERSION '미니멀판 8'` · single-file 재빌드 ·
재어 확인 : 토글바 가운데 · 실내 켜짐 표시 · 실외 전환 · 모아보기 이동 ·
찾아보기 0 · island-head 0.

---

## 27. 2026-08-26 에 한 일 (9) — 모아보기 토글바 · 4칸 한 줄 · 연한 테두리

1. **설정 선택 테두리** : 진한 파랑 → **연한 하늘색**(--sel-line #9cc0f2).
2. **「반려동물과 실내에서 놀기」 → 「반려동물과 놀기」** (name·speechName·
   planText·diaryText). ⛔ imageKey 는 그림 파일 이름 그대로 두었습니다.
3. **모아보기(mymap) 첫 화면 재구성** :
   - 섬 화면과 **같은 토글바**([실내|실외|모아보기 켜짐]) 가운데 배치 —
     섬 칸을 누르면 그 섬으로 돌아갑니다. 「◀ 여가 섬(실외)」 알약과
     분홍 질문 바 삭제, 안내는 **박스 없는 글 한 줄**(.mymap-say).
   - 네 표시 칸(해봤어요…) **2×2 → 4×1 한 줄**, 칸 15% 크게
     (26vh→30vh · .mymap-grid.row4). 재어 확인 : 4열 · 칸 240px(전 208).
   - 모아보기 켜짐 색 = 청록 채움(.isl-seg .seg.collect.on).

`?v=20260828l` · `App.VERSION '미니멀판 9'` · single-file 재빌드 ·
재어 확인 : 테두리 rgb(156,192,242) · 새 활동 이름 · 토글바로 섬 복귀.

---

## 28. 2026-08-26 에 한 일 (10) — 섬 카드 10%↑ · 분류 토글바 · 같은 칸 크기

1. **섬 활동 카드 10% 크게 + 가운데** :
   - 여백 상수 PAD 10→4 · GAP 10→8 · LABEL_H 10→2 · NAV_H 36→10.
     재어 확인 : 카드 184 → **208px** · 8장 전부 칸 안.
   - gridFor 에 **마지막 줄 가운데 정렬** — 2쪽에 3장만 남을 때 왼쪽으로
     쏠리던 것. 재어 확인 : 마지막 줄 3장 가운데.
2. **모아보기** :
   - 안내 글(.mymap-say) **10% 확대** (fs-q × 1.1 = 23px).
   - 분류(해봤어요 …) 안에서도 **네 칸 토글바** — 지금 것 켜짐(연한 파랑),
     누르면 바로 다른 분류로. 「◀ 내가 표시한 활동」 알약·분홍 질문 바 삭제.
     네 칸 화면은 파란 화살표로.
   - 세부 활동 칸 = **네 칸 화면과 같은 크기**(clamp(170,30vh,300) · 정사각
     240px). 활동이 하나여도 같은 크기 · 줄 가운데 정렬.

`?v=20260828m` · `App.VERSION '미니멀판 10'` · single-file 재빌드 ·
재어 확인 : 분류 토글 전환(해봤어요→좋아해요) · 칸 240×240 · 가운데 정렬.

---

## 29. 2026-08-26 에 한 일 (11) — 흰 칸 높이 통일 · 계획 문장 한 줄 · 단추 통일

### 29-1. ★ 흰 칸 높이 **전 화면 통일 = 85%**

내용이 적은 화면(포트폴리오 첫 화면 등)마다 칸 키가 제각각이던 것을,
어느 화면이나 **홈과 같은 85%** 로 못박았습니다.

    .stage:not(.tall):not(.full):not(.tstage){ flex:1 1 auto; align-items:center; }
    .stage:not(.tall):not(.full):not(.tstage) > .panel{ height:85%; }

- 예외 셋 : `.tall`(완성 화면 — 종이 크게) · `.full`(지도 — 지도가 주인공) ·
  `.tstage`(선생님 설정 — 목록이 긺). 지도 stage 에 full 클래스를 새로 달았습니다.
- ⛔ 새 화면을 만들 때 흰 칸 키를 따로 정하지 마세요 — 이 규칙이 정합니다.
  꽉 채워야 할 화면이면 tall 이나 full 을 쓰세요.
- 재어 확인 : 홈 85 · 포트폴리오 85 · 계획 85.

### 29-2. 설정 4번 줄바꿈 — pre-line → **pre**

칸이 좁으면 「활동 · 함께하는 / 사람 · 날짜 · 장소」 로 제멋대로 접혔습니다.
pre 는 **적어 둔 \n 자리에서만** 줄이 바뀝니다. 재어 확인 : 정확히 2줄.

### 29-3. 홈 오늘의 계획 카드

- 문장(.card-say) **더 크게**(clamp 1.05rem·1.1vh·26px = 19px) ·
  **늘 한 줄**(nowrap + …). 전체 문장은 계획 보기·읽어주기가 보여 줍니다.
- 「계획 보기」의 small 을 빼서 세 단추(계획 보기·활동을 했어요·일기 다시
  쓰기)가 **같은 키(56px)·같은 글자(1.05rem)** 입니다. 재어 확인 : 둘 다 56px.

`?v=20260828n` · `App.VERSION '미니멀판 11'` · single-file 재빌드.

---

## 30. 2026-08-26 코드 다이어트 백로그 (점검만 — 아직 안 지움)

`?v=20260828n` · App.VERSION 「미니멀판 11」 시점에 전수 대조한
**죽은 코드·중복** 목록입니다. 기능 변화 없이 지울 수 있는 것들이며,
대략 **JS 260줄 + CSS 200줄**. 지운 뒤에는 반드시 구문·주석 짝 검사와
전 화면 훑기를 다시 하세요.

### 오늘 개편이 남긴 잔재 (우선 지울 것)
- map.js : FILTERS·matches()·filter/toolsS state(찾아보기 삭제 잔재) ·
  view zoom state+scale/offX/offY/overflowX/Y(배율 UI 삭제 — zoom 1 고정) ·
  labelBox()/labelTop/BAR_GAP/LABEL_NUDGE(island-head 삭제 잔재) ·
  COLS/ROWS/PER(섬고르기에 카드 없음) · x0(rowX0 로 대체) · CX/CY(map-me 잔재) ·
  shownCount
- app.css : .island-head/.island-label(±35줄) · .map-islands · .mymap-head ·
  .mymap-island 일족(20줄 — isl-seg 로 대체) · .map-toolrow .mymap-island ·
  .chip.filter-now · .filter-art · .map-toolbox · .map-tools ·
  .map-collect(마크업은 seg collect) · .topbar-title.as-btn ·
  .pick-grid.cols-5/.cols-7 · .q-art
- diary.js : goNext()+goTimer · metaBar() · frameLine() · moodAndAgain()
  (개편 전 잔재 — 참조 0회)
- common.js : TopBar 가 onTitle 을 더 안 읽음 — 9개 화면의 onTitle= 무효 인자

### 예전부터 죽어 있던 것
- common.js : C.BottomBar(+CSS+print.css 선택자) · C.StageFit · C.AvatarPicker ·
  App.useMemo2
- store.js : updateActivity · upcomingPlans · removePlan · seeded ·
  lastError/lastSaveError 읽기 · saveNow 노출 · App.newStudent 노출
  (mapTools 필드는 옛 데이터 호환용으로 **남김**)
- korean.js : hasBatchim · iGa · eunNeun · joinList
- options.js : D.frames · avatarGroups(죽은 AvatarPicker 전용) · App.corner ·
  App.state / activities.js : DATA.activities 노출 · isCustomActivity /
  icons.js : hasIcon · hasUiIcon · ICONS 노출 · ICONS.speaker/speakerOff/
  download/upload/eyeOff · UI.close
- plan.js pick() 항등 래퍼 · app.js .printing classList(규칙 없음) ·
  app.js App.navBack 노출 · teacher.js C.Choices 노출 ·
  portfolio.js setRange · photos.js listByStudent · speech.js sound.tap ·
  hasKorean 노출 · picdiary.js verifyManuscript/diaryLevelOf/PD_ART_H 노출

### 중복 한 벌로 합칠 것
- profiles.js 쪽넘김(.island-nav 1벌)이 CSS 23줄을 붙들고 있음 — 쪽넘김
  관용구가 앱에 5가지(pager/flow-arrow/isl-arrow/Btn+chip/island-nav) 공존.
  C.Pager 하나로 모으는 것이 다음 정리 후보.
- .mymap-grid.row4 와 .mymap-cards 는 치수가 사실상 같음(트랙 clamp 동일 ·
  정사각) — 한 벌로 합치고, 완전히 덮여 죽은 .mymap-no/.mymap-date
  옛 정의 6줄도 함께.

---

## 31. 2026-08-26 (12) — 리뷰 · 캐릭터 여덟 · 설정 잘림 · 제목 한 줄

다른 컴퓨터에서 한 §16~30 을 **이 폴더와 저장소로 가져오고**, 노트 두 편을
리뷰한 뒤 나온 것들을 고쳤습니다.

### 31-1. ★★ 열한 라운드가 **되돌릴 곳 없이** 사본 하나로만 있었습니다

§16~30 (2026-08-25~26) 은 구글드라이브 폴더에서만 진행되어, `D:\클로드\나의
여가생활` 은 `?v=20260827d`(세로판) 에 머물러 있었고 저장소에는 8-25 것까지만
있었습니다. **이틀치가 사본 하나에 달려 있었습니다.**

- 꾸러미를 작업 폴더에 풀고 커밋·푸시했습니다 (`5265de8`).
  코드·문서 22개만 바뀌었고 **그림은 한 장도 안 바뀌었습니다.**
  새로 생긴 41개는 전부 `_이전버전/` 되돌림용 사본입니다.
- `vendor/_sd.html` 은 `_이전버전/20260816_단일파일_구판_sd.html` 로 옮겨진
  것을 확인하고(MD5 동일) 원래 자리에서 지웠습니다.
- ⚠ `.gitignore` 만은 꾸러미 쪽이 **옛것**이라 덮어쓰지 않았습니다
  (`*.zip` 줄이 빠져 있었습니다).

⛔ **다른 폴더(구글드라이브 · USB · 압축 푼 자리)에서 작업했다면 그날 안에
   이 작업 폴더로 가져와 커밋하세요.** 저장소가 곧 안전망입니다.

### 31-2. 작업노트 리뷰 — 고친 것 다섯

- §1 이 「git clone 하세요」 라고만 해서, 그대로 따르면 **열한 라운드 전 판**을
  받게 되어 있었습니다. 지금 판·마지막 커밋·세 곳 맞추는 확인 명령을 넣었습니다.
- 「세 단계 전 화면 점검 통과」는 **세로 600×1000 에서 한 것**인데 그 뒤 가로로
  바뀌었습니다. ⚠ 를 달고 「가로에서 재점검」을 남은 것 1번으로 올렸습니다.
- §5 「한 쪽에 여섯」이 섬(8) · 모아보기(4) · 도장판(5) 과 어긋나 있어 표로 갈랐습니다.
- §3 「금지 사항 92개」 · §4 `sed` 예시(20260828c)가 낡아 있어 고쳤습니다.
- ▸ **세로는 안 쓴다**고 정하셨습니다 (2026-08-26). 「세로로 들었을 때」 걱정은
  접고 가로 하나로 못박습니다.

### 31-3. 캐릭터를 **여덟**으로 (선생님 : 「군더더기는 덜어내려고」)

38개를 갈래 넷으로 나눠 **네 쪽**에 보여 주었습니다. 고르기 전에 넘기는 일부터
배워야 했습니다.

- 남자아이 · 여자아이 · 동물 여섯 = **여덟**. 여기에 「사진 찍기」 한 칸을 더해
  **3칸 × 3줄 = 아홉**, 한 화면에 다 들어옵니다. 쪽 넘김 삭제.
- 동물 여섯 = 강아지 · 고양이 · 토끼 · 곰 · 사자 · 코끼리.
  집에서 보는 둘 · 작고 귀여운 둘 · 큰 동물 둘로 갈래가 고르게 섞이고,
  생김새가 비슷한 곰 · 판다 · 여우가 함께 있지 않게 골랐습니다.
- 「사진 찍기」를 **격자 안 아홉째 칸**으로 옮겼습니다. 예전에는 격자 **밖 아래**
  단추라 캐릭터와 견주기 어려웠습니다. 카메라 · 갤러리 중 고르는 방식은 그대로.
- 죽은 `C.AvatarPicker` · `D.avatarGroups` · avatar 의 `group` 필드를 함께
  걷어냈습니다 (§30 목록).
- ⛔ **캐릭터를 더하면 아홉 칸이 깨져 쪽 넘김이 되살아납니다.** 더하려면
  profiles.js 의 칸 배열도 함께 보세요.

### 31-4. 「나」가 고르는 칸보다 **작던** 것

선생님 : 「내가 더 중요하니 바뀐 캐릭터를 내가 확인하며 볼 수 있게」.
왼쪽 미리보기가 30% 칸에 `min(60%,160px)` 로 묶여 있어, 오른쪽 고르는 칸
그림(180px)보다 **작았습니다.** 칸을 36% 로 넓히고 얼굴을 `min(84%,40vh)` 로
풀었습니다. 재어 확인 : 나 320px · 고르는 칸 104px.

### 31-5. ⛔⛔ 선생님 설정 **오른쪽이 잘려 나가던 것**

`.tscroll` 이 `grid-template-columns:1fr 1fr` 이었습니다. **`1fr` 은
`minmax(auto,1fr)`** 이라 「내용보다 작아지지 않는다」는 뜻입니다. 3·4번 알약이
`flex-wrap:nowrap`(셋이 늘 나란히) 이라 한 칸의 최소 폭이 513px 로 굳었고,
격자 전체가 **1027px 아래로 안 줄어들었습니다.** 창이 그보다 좁으면 격자가
흰 칸을 삐져나가 「설정 완료」 단추와 2·4·6·8번 오른쪽이 통째로 잘렸습니다.
가로 스크롤도 안 생겨 되찾을 길이 없었습니다.

- `minmax(0,1fr)` + `.tsec{min-width:0}` · 한 칸으로 접히는 기준 900 → 1100px
- 재어 확인 : 폭 700~1280 **전부 넘침 0** (전 : 940 일 때 109px · 1024 일 때 25px)

⛔ 격자 칸을 `1fr` 로 되돌리지 마세요. 「안 줄어드는 것」이 하나라도 들어오면
   같은 고장이 그대로 납니다.

### 31-6. ⛔ 그림일기 **제목이 길면 뒤가 사라지던** 것

제목도 원고지 격자였고, 앞 두 칸을 「제목」 딱지가 쓰고 남은 `칸수-2` 개만
글자를 받았습니다. 열 칸이면 **여덟 글자**. 「가족, 친구와 함께 보드게임」 같은
제목은 뒤가 잘려 화면에도 인쇄에도 안 나왔습니다 (원고지 규칙 13 — 글자 누락 금지).

- 세 단계 모두 **칸 없는 한 줄**(`.pd-titleline`)로. 글자는 본문과 같은 크기,
  줄 높이는 그 단계의 원고지 한 줄과 같게 (fitGrid 의 `+1` 셈 유지).
- 길면 「…」로 자르지 않고 **글자를 줄여** 다 담습니다 (18px 이 바닥).
- 따라 쓰기 · 빈 줄에서 제목도 함께 흐려지고 숨도록 선택자를 옮겼습니다
  (`.pd-titlegrid .pd-ch` → `.pd-titletext`). ⛔ 이 두 줄을 빠뜨리면 「따라 쓰기」
  에서 제목만 진하게 나오고, 「빈 줄」로 뽑아도 제목만 인쇄됩니다.
- 죽은 `.pd-titlegrid` · `.pd-box.pd-titlelab` 삭제.
- 재어 확인 : 2~10자 46px(본문과 동일) · 26자 26px · 잘림 0 · 종이 넘침 0

---

## 32. 2026-08-26 (13) — ★ 그림일기 **종이 위에 바로 쓰기**

선생님 : 「그림판에 원고지를 넣어서 쓰는 것이 아니라 **이 화면에 그대로 위에**
쓸 수 있게 했으면 하는거였어」

팝업(`C.Modal` + `C.DrawPad`)을 걷어냈습니다. 「원고지에 손으로 쓰기」를 누르면
보고 있는 **그 종이의 원고지 칸 위에** 투명한 판이 얹힙니다.

- 글씨는 칸과 **합치지 않고 한 겹으로 따로** 담습니다 (`writeInkId`).
  그래서 인쇄해도 칸은 또렷한 선 그대로고 글씨만 위에 앉습니다.
  화면 · 인쇄 · 일기 모음 · 포트폴리오 어디서나 함께 나옵니다.
  ⛔ **칸과 글씨를 한 장으로 합쳐 저장하지 마세요** — 칸이 그림이 되어 인쇄가
     흐려지고, 글자 크기를 바꾸면 칸과 어긋납니다.
- 지우개는 흰색 덧칠이 아니라 `destination-out`(투명하게 도려내기)입니다.
  흰색으로 칠하면 아래 원고지 칸까지 하얗게 덮여 칸이 사라집니다.
- 쓰는 동안 인쇄 모양 줄을 접고 흰 칸을 가득 씁니다(tall) — 칸 28 → 37px.
  아래 줄은 도구 줄(펜 8색 · 굵기 3 · 지우개 · 다 지우기)로 바뀌어, 쓰다가
  인쇄를 눌러 나가는 일이 없습니다.
- 일기를 지울 때 `writeInkId` 사진도 함께 지웁니다 (§18-2 와 같은 까닭).
- 재어 확인 : 판이 칸과 딱 맞음 · 긋기 43점 · 저장 · 글씨겹 얹힘

### 32-1. ⛔ 도구 줄이 **438px** 를 먹던 것

`.panel-action .btn` 은 `width:100%` · `min-height:72px`(큰 알약)입니다.
도구 줄에 그대로 걸려 단추 일곱이 **세로로 쌓였고**, 정작 종이가 255px 로
눌렸습니다. 도구 줄 안에서만 폭·키를 되돌렸습니다 (438 → 63px).

### 32-2. ⛔⛔ **재는 일을 효과 안에 가둔** 것

`measure` 를 `useLayoutEffect` **안에** 두어 처음 한 번만 매여 있었습니다.
쓰기를 켜서 자리가 630px 로 넓어져도 종이는 480px 그대로였습니다(칸 28px).
손으로 resize 를 한 번 일으키면 그제야 630 으로 커졌습니다.

- `measureSheet` 를 컴포넌트 자리로 꺼내고, **화면 모양이 바뀔 때마다**
  다시 재게 했습니다 (`[writing, traceS[0], hintS[0]]`).
- ⛔ 이 효과를 `hintS` 선언보다 위로 옮기지 마세요 — 아직 만들어지지 않은 값을
  딸림 목록에 적게 되어 힌트를 열어도 다시 재지 않습니다.

---

## 33. 2026-08-26 (14) — ★ 세 기기 (전자칠판 · 태블릿 · 노트북)

선생님 : 「전자칠판, 태블릿, 노트북 이 3개의 기기에서 모두 구현되도록 해줘」

점검 크기를 셋으로 못박습니다.

| 기기 | 크기 |
|---|---|
| 전자칠판 | 1920 × 1080 |
| 노트북 | 1366 × 768 (셋 중 **세로가 가장 좁음** — 여기가 기준) |
| 태블릿 가로 | 1280 × 800 |

### 33-1. 처음 훑은 결과 — 잘림은 0, 그런데 **여섯 중 셋만**

열두 화면을 세 크기에서 재니 잘림 · 빈 화면 · 넘침은 **모두 0** 이었습니다.
그런데 2·3단계 고르는 화면에서 **여섯 장 중 셋만** 보이고 학생이 「활동 더
보기」를 눌러야 나머지를 봤습니다 (「한 쪽에 여섯」 규칙이 깨짐).

기기 탓이 아니었습니다 — 흰 칸의 **60%를 껍데기가** 쓰고 있었습니다
(노트북 617px 중 틀 213 + 쪽넘김 61 + 단추 91 = 365, 카드 249).
1단계는 문장 틀이 74px 뿐이라 혼자 6장이 다 보였습니다.

### 33-2. 고친 것 둘 (선생님 결정 : 둘 다)

- **쪽 넘김을 아래 단추와 한 줄로** 합쳤습니다 (`.panel-action.with-pager`).
  「‹ 1/2 ›」이 제 줄을 따로 써서 61px 를 먹고 있었습니다.
  ⛔ 다시 두 줄로 가르지 마세요. ⛔ `.panel-action .btn{width:100%}` 때문에
     합친 줄에서는 단추 폭을 `auto`+`flex:1` 로 되돌려야 합니다.
- **계획 · 일기 화면이 흰 칸을 가득** 쓰게 했습니다 (`tall`).
  85%만 쓰면 그 15%가 그대로 카드에서 빠집니다.
  ⛔ `diary.js` 의 `tall` 을 `isConfirm` 으로 되돌리지 마세요.

### 33-3. 재어 확인 — 세 기기 × 세 단계 모두 「6장 중 6장 · 한 쪽」

```
              1단계          2단계          3단계        (괄호 = 고치기 전)
  1920x1080   6/6 181px      6/6 144px      6/6 144px    (3/6 119px)
  1366x768    6/6 111px      6/6  93px      6/6  93px    (3/6  85px)
  1280x800    6/6 116px      6/6  88px      6/6  97px    (3/6  88px)
```

전 화면 다시 훑기 — **세 기기 × 열두 화면 = 36 가지 모두 통과**
(잘림 0 · 빈 화면 0 · 가로 넘침 0 · 세로 넘침 0).
※ 선생님 설정(`.tscroll`)의 세로 스크롤은 **일부러 그렇게 만든 것**입니다.
※ 쪽 넘김 판(`.stage-track`) 안에서 다음 쪽이 오른쪽으로 나가 있는 것도
  정상입니다 — 잘림으로 세지 마세요.

### 33-4. 곁들여 고친 것

- 선생님 설정 **이름 칸**이 옆 단추들에 밀려 「이름 또는 별명」이 한 글자도
  안 보였습니다. 설정이 두 칸 배열로 바뀌며 절반 폭에 들어갔는데 줄이
  `nowrap` 이었습니다. 줄바꿈을 허용하고 이름 칸에 최소 폭 11rem 을 주었습니다.
  ⛔ `min-width:0` 으로 되돌리지 마세요. 재어 확인 : 205px (필요 106px).
- 캐릭터 고르기 **사진 칸 그림** — 코드로 그린 카메라는 나머지 여덟 칸(그린
  그림)과 결이 달랐습니다. `images/사진 찍기.png` 를 쓰도록 했습니다.
  ⚠ `준비물/사진기.png`(사진 같은 노란 카메라)도 결이 달라 쓰지 않습니다.
  파일이 없으면 코드로 그린 카메라가 대신 나옵니다.

### 33-5. ⚠ node 가 없는 컴퓨터에서는 **브라우저로** 문법을 검사하세요

이 컴퓨터에는 `node` 가 없어 `node --check` 를 못 씁니다. 실제로 템플릿
literal 을 안 닫은 실수를 문법 검사로 못 잡고 **화면이 통째로 안 뜬** 일이
있었습니다 (오류 안전망이 「선생님을 불러 주세요」로 받아 주었습니다).
미리 보기를 띄운 뒤 브라우저 콘솔에서 :

```js
for (const f of ['js/screens/picdiary.js','js/components/common.js']) {
  const t = await (await fetch('/' + f + '?x=' + Date.now())).text();
  try { new Function(t); console.log('OK', f); } catch (e) { console.log('X', f, e.message); }
}
```

`?v=20260828p` · `App.VERSION 「세 기기판」`

### 33-6. ⛔ 캐릭터 단추가 **눌리는 곳으로 안 보이던** 것

선생님 : 「여기서 캐릭터 창이 너무 작아 눌러서 골라야할지 모를듯」

§22-3 에서 「캐릭터」 글자를 빼고 그림만 두었습니다 (「그림이 곧 뜻」). 그런데
고른 **뒤**에는 `.tchoice.avatar.done` 이 흰 바탕 · 실선 테두리 · 28px 그림
하나가 되어, 그냥 놓인 그림과 구별되지 않았습니다.

- 고른 뒤에도 **연한 하늘색 바탕**을 남깁니다 (`.done` 색을 흰색에서 되돌림)
- 그림 28 → **44px** · 단추 키 40 → 56px
- 「캐릭터」 **글자를 되살렸습니다**
- 재어 확인 : 단추 117×57 (1920 에서 138×59) · 세 기기 모두 오른쪽 삐짐 0

⛔ 다시 글자를 빼지 마세요. **그림은 뜻이지 손짓이 아닙니다** — 누르는 곳임을
   드러내려면 글자나 색이 함께 있어야 합니다.

### 33-7. 사진 칸 — 그림과 말 (2026-08-26)

- 그림 : `images/일기 낱말/사진 넣기.png`.
  ⛔ 코드로 그린 카메라도, `준비물/사진기.png`(사진 같은 노란 카메라)도
     쓰지 않습니다 — 나머지 여덟 칸(그린 그림)과 결이 다릅니다.
  ▸ 「일기 낱말」 폴더의 것을 쓰는 까닭 : 일기에서 사진을 넣을 때 쓰는 그림과
    **같은 그림**이라야 학생이 같은 일로 알아봅니다.
- 말 : 「사진 찍기」 → **「사진 넣기」** (선생님 말씀).
  카메라로 찍어도 되고 갤러리에서 골라도 되므로, 「찍기」는 할 수 있는 일을
  좁혀 말합니다.

### 33-8. ★ 제목 — **짧으면 칸, 길면 줄글** (2026-08-26)

선생님 : 「제목이 짧으면 원고지 안에 쓰고, 길면 줄글로. 제목의 글자크기가 칸안의
내용 글자크기와 차이가 나기도 하고 너무 붙어 있어서」

§31-6 에서 제목을 **늘 줄글**로 바꿨더니, 짧은 제목까지 줄글이 되어 본문 칸
글자와 크기가 어긋나 보였습니다. 이제 길이에 따라 가릅니다.

- 가르는 자리 : `titleFits = 글자수 <= g.cols - 2` (「제목」 딱지가 앞 두 칸)
  - 담기면 `.pd-titlegrid` — 칸 글자와 **크기가 정확히 같습니다**
  - 넘치면 `.pd-titleline` — 글자를 줄여 다 담습니다 (18px 이 바닥)
- ⛔ 3단계(밑줄)는 본문 칸이 없으므로 **늘 줄글**입니다 (`!useLines` 로 막음)
- 같은 제목이라도 **그 종이의 칸 수**에 따라 갈립니다 (본문이 길면 칸이 늘어
  한 칸이 작아지고, 제목이 담길 자리도 늘어납니다)
- 딱지와 글자가 붙어 있던 것 → `.pd-titlelab-line{margin-right:.8em}`
- 재어 확인 (본문 12칸 기준) :
  「캠핑」2자 · 「가족과 함께」6자 · 「아빠와 함께한 산책」10자 → 📗 칸 46px(본문과 동일)
  「가족과 친구와 함께 캠핑장에서 보드게임」21자 → 📄 줄글 33px · 잘림 0
  세 단계 모두 종이 넘침 0

---

## 34. 2026-08-26 (15) — 코드 다이어트 (§30 목록 실행)

§30 에 적어 둔 죽은 코드를 실제로 지웠습니다. **기능 변화 0** 이 목표였고,
지운 뒤 문법 · 주석 짝 · 전 화면 훑기로 확인했습니다.

### 34-1. 지운 것

**JS** — 참조 0회를 하나씩 세어 확인한 뒤 지웠습니다.

- `diary.js` : `goNext`+`goTimer` · `metaBar` · `frameLine` · `moodAndAgain`
  (개편 전 잔재 · 부르는 곳 0)
- `common.js` : `C.BottomBar`(+ print.css 선택자) · `C.StageFit` · `App.useMemo2`
- `store.js` : `updateActivity` · `upcomingPlans` · `removePlan` ·
  `saveNow`/`lastError` **노출** (함수 자체는 안쪽에서 씁니다) ·
  `lastSaveError` 변수 · `seeded` 깃발 · `App.newStudent` 노출
- `korean.js` : `hasBatchim` · `iGa` · `eunNeun` · `joinList`
- `options.js` : `D.frames`(2단계 옛 문장 틀) · `App.corner` · `App.state`
- `activities.js` : `App.DATA.activities` 노출 · `isCustomActivity`
- `icons.js` : `hasIcon` · `hasUiIcon` · `App.ICONS` 노출
- `photos.js` : `listByStudent` / `speech.js` : `hasKorean`
- `app.js` : `App.navBack` 노출 / `teacher.js` : `C.Choices` 노출
- `picdiary.js` : `verifyManuscript` · `diaryLevelOf` · `PD_ART_H` 노출
  ▸ `verifyGrid` 검사 자체는 `fitGrid` 안에서 그대로 돕니다 — 노출만 뺐습니다
- `portfolio.js` : `setRange`

**CSS (app.css 4579 → 4449줄, 130줄)** — 규칙 블록 34개 + 붕 뜬 주석 7덩이

- 지도 개편 잔재 : `.island-head` · `.island-label` · `.map-islands` ·
  `.mymap-head` · `.mymap-island` 일족 · `.map-toolbox` · `.map-tools` ·
  `.chip.filter-now` · `.filter-art` · `.map-collect` · `.map-toolrow .mymap-island`
- 그 밖 : `.bottombar` · `.topbar-title.as-btn` · `.pick-grid.cols-5/.cols-7` · `.q-art`
- `print.css` 의 `.bottombar` 선택자

### 34-2. ⛔ 지우다 저지른 것 — **함수 머리를 함께 베었습니다**

`setRange` 를 줄 번호로 지우다가 **바로 뒤 `setDate` 의 머리 한 줄**까지
지워서 `portfolio.js` 가 통째로 문법 오류가 났습니다.

⛔ 줄 번호(sed '495,503d')로 지우지 마세요. 앞에서 지운 만큼 번호가 밀립니다.
▸ **뒤에서 앞으로** 지우거나, 함수 이름으로 범위를 잡으세요.
▸ 지운 **직후마다** 문법을 확인하세요 (§33-5 의 브라우저 검사).

### 34-3. 확인한 것

- 문법 : 18개 파일 통과 (브라우저 `new Function`)
- 주석 짝 · 백틱 · CSS 주석 짝 · 중괄호 짝(1158/1158) 통과
- 전 화면 훑기 12개 통과 — 잘림 0 · 빈 화면 0 · 넘침 0
- 그림 · 글꼴 빠진 것 0 (코드가 부르는 경로를 파일과 전수 대조)
- ⚠ 콘솔의 404 는 **점검하느라 두드린 주소**(없는 journal.js 등)입니다.
  `performance.getEntriesByType('resource')` 로 보면 400 이상 응답 0 입니다.

### 34-4. 아직 남은 정리 (§30 의 나머지)

- 쪽 넘김 관용구가 아직 넷 공존 (pager / flow-arrow / isl-arrow / island-nav).
  `C.Pager` 하나로 모으는 것이 다음 후보
- `.mymap-grid.row4` 와 `.mymap-cards` 치수가 사실상 같음 — 한 벌로 합칠 것
- `map.js` 의 `FILTERS` · `matches()` · `labelBox()` 등 찾아보기 잔재
  (이번에는 **CSS 만** 지우고 JS 쪽은 남겨 두었습니다 — 함수끼리 얽혀 있어
   따로 시간을 두고 봐야 합니다)

`?v=20260828s`

---

## 35. 2026-08-26 (16) — 사용설명서 반영 (§31~34)

배포용 `사용설명서(교사용).html` 에 이번 것들을 넣었습니다 (634 → 690줄).

- **1장 기기** — 「전자칠판 · 태블릿 · 노트북 세 기기에서 모두 확인」과
  잰 크기 셋(1920×1080 · 1366×768 · 1280×800)을 밝혔습니다
- **2장 화면 둘러보기** — 「캐릭터 고르기」 줄을 새로 넣었습니다
  (아홉 칸 한 화면 · 왼쪽에 내 모습 크게 · 이름 칸이 곧 입력칸)
- **6장 그림일기** — ★ **종이 위에 바로 손으로 쓰기** 안내를 크게 넣고,
  제목이 「짧으면 칸, 길면 줄글」로 갈린다는 것을 적었습니다.
  단추 표에 「원고지에 손으로 쓰기」 줄을 더했습니다
- **9장 선생님 설정** — 1번 학생 줄의 「캐릭터」 단추와 빨간 점선 표시 설명
- **10장 자주 묻는 질문** — 셋을 더했습니다
  · 설정 오른쪽이 잘려 보여요 (Ctrl+0 · 좁으면 한 칸으로 접힘)
  · 학생이 캐릭터를 고를 줄 몰라요
  · 전자칠판에서 학생이 직접 써 보게 하고 싶어요

⛔ 설명서를 고칠 때는 태그 짝을 꼭 세어 보세요 (div · table · tr · ul).
   그리고 브라우저에서 `DOMParser` 로 열어 `parsererror` 가 없는지 봅니다.

---

## 36. 2026-08-26 (17) — ⛔⛔ 캐릭터 그림이 **머리만 보이던** 것 (실기기)

선생님이 **실제 갤럭시탭**에서 찍어 보내 주신 사진에서 드러났습니다.
캐릭터 고르기의 아홉 칸이 모두 **머리 윗부분만** 보였습니다.

### 36-1. 까닭 — 재어 보니 그림이 칸보다 두 배 컸습니다

```
   카드      254 × 138
   그림 칸   238 × 104      ← 여기까지만 보입니다
   그림      232 × 232      ← 이만큼 커져서 아랫부분이 잘려 나갔습니다
```

`.cs-art` 에 `overflow:hidden` 이 걸려 있어, 칸을 넘은 아랫부분이 그대로
잘렸습니다. 남은 것이 **머리와 귀**뿐이었습니다.

⛔ **`max-height:100%` 는 부모 높이가 확실할 때만 풀립니다.**
   `.cs-art` 가 `display:grid; place-items:center` 였는데, 격자 칸 안에서는
   그 백분율이 안 풀려 그림이 제 크기(232px)대로 커졌습니다.
   `C.Art` 가 인라인으로 넣는 `height:100%` 도 같은 까닭으로 안 풀립니다.

### 36-2. 고친 방법 — 그림을 **자리에서 띄웁니다**

    .cs-art{ position:relative; ... overflow:hidden; }
    .cs-art > *{ position:absolute; inset:0; width:100%; height:100%; object-fit:contain; }

띄운(absolute) 그림은 **높이가 확실한 부모**에 늘 맞춰집니다. 백분율이
풀리지 않는 자리가 없어집니다.

⛔ `display:grid; place-items:center` + `max-height:100%` 로 되돌리지 마세요.
⛔ 얼굴 사진 칸의 `object-fit:cover` 는 **사진을 넣었을 때만**(.on) 걸어야
   합니다. 카메라 그림에까지 걸면 그림이 잘립니다.

### 36-3. 곁들여 — 캐릭터 화면도 흰 칸을 가득

이 화면은 `data-corner="home"` 이라 **홈의 85% 규칙**에 걸려 있었습니다.
`.cs-stage` 표시를 붙이고 예외로 두었습니다.

- 흰 칸 617 → **726px** · 카드 138 → **174px** · 그림 104 → **141px**
- 재어 확인 (아홉 칸 모두 넘침 0 · 잘림 0) :
  1920×1080 그림 217px · 1366×768 139px · 1280×800 141px · **1280×620 89px**
  ※ 실제 태블릿은 주소창 때문에 화면이 낮으므로 **620 높이까지** 확인했습니다.

### 36-4. ★ 「누구와 할까요?」 차례 — 자주 고르는 사람이 앞

선생님 : 「첫화면에 나오는 6명은 빈도가 높은 사람이 먼저 나열되면 좋겠어.
선생님, 친구, 혼자, 가족, 엄마, 아빠」

한 쪽에 여섯이므로 **앞 여섯이 곧 첫 화면**입니다. 학교에서 쓰는 앱이라
선생님·친구가 가장 잦고, 그다음이 혼자, 그리고 집 사람입니다.

⛔ 이 여섯의 차례를 바꾸지 마세요 — 첫 화면에 보이는 사람이 달라져
   학생이 늘 넘겨서 찾아야 합니다.
▸ 선생님·친구는 목록 **끝에서 앞으로 옮긴 것**입니다. id 가 같으므로
  예전 일기·계획도 그대로 읽힙니다 (지운 것이 아닙니다).

`?v=20260828t`

### 36-5. 「어느 곳에서 할까요?」 차례 · 조리실 → **부엌** (2026-08-26)

선생님 : 「어느곳에서 할까요?도 빈도순으로」

한 쪽에 여섯이므로 **여섯씩 한 묶음**입니다. 갈래끼리 묶어 두면 학생이
「몇 쪽에 있더라」를 몸으로 익힙니다.

```
  1쪽  학교 안 : 학교 · 교실 · 운동장 · 강당 · 학교 화단 · 텃밭
  2쪽  동네    : 집 · 놀이터 · 공원 · 동네 · 노래방 · 문구점
  3쪽  나들이  : 부엌 · 음식점 · 카페 · 캠핑장 · 도서관 · 영화관
  4쪽           미술관 · 박물관
```

⛔ 차례를 바꾸면 쪽 나누기가 흐트러져 갈래가 섞입니다. 더할 곳이 있으면
   **그 갈래의 끝**에 넣으세요.

**조리실 → 부엌** (선생님 결정). 「가사실」도 견주었으나 부엌으로 정했습니다 —
학교에서만 쓰는 말보다 **학생이 집에서도 듣는 말**이 낫고, 다른 이름들
(교실 · 운동장 · 집 · 놀이터)과 결이 맞습니다.

⛔ **그림 파일 이름도 함께 바꿔야 합니다** — 장소 그림은 `images/장소/<보이는 말>.png`
   로 찾습니다. `조리실.png` → `부엌.png` 로 옮겼습니다(git mv).
▸ 함께 고친 곳 : options.js(장소 목록) · activities.js(요리 6가지의 defaultPlace ·
  경로 주석) · store.js(예시 일기) · common.js(주석) · images/장소/README.md ·
  사용설명서.
⚠ 예전 기록에 `place:'조리실'` 이 남아 있으면 그림만 안 나옵니다(코드로 그린
  지도 그림으로 대체). 글자는 그대로 읽히니 기록이 사라지지는 않습니다.

- 재어 확인 : 장소 20곳 **모두 그림 있음** · 목록과 파일이 딱 맞음(남는 파일 0) ·
  화면에서 1쪽이 「학교 · 교실 · 운동장 · 강당 · 학교 화단 · 텃밭」로 나옴

`?v=20260828u`

---

## 37. 2026-08-26 (18) — ★ 여가 계획하기 화면 다시 짜기

선생님 : 「노란색 바 안에 있는 글자들이 중요하다고 생각해. 만들때! 아래
활동들은 세개씩으로 나열하기 / 앞활동보기, 활동 더보기가 아니라 그냥 화살표
방향만 제시(투머치설명) / 노란색 상자 없고 색이 없이 나열되어도 깔끔할 것
같아. 상자없이 줄글과 그림만 보이게 중앙정렬」

### 37-1. 상자를 걷어내고 **글과 그림만**

고르는 도중 위쪽에 있던 **노란 알약**(`.plan-sofar` · `.pic-sofar` · `.plan-l1`)
의 바탕과 테두리를 없앴습니다. 알약이 눈길을 먼저 끌어, 정작 읽어야 할
문장이 뒤로 밀렸습니다. 바탕이 없으면 **글자 자체가 주인공**이 됩니다.

- 문장 글자 19.75 → **31.1px** · 문장 그림 88 → **168px**
- ⛔ `background` · `border` 를 다시 넣지 마세요.
- ⛔ **계획표(완성된 한 장)의 점선 바는 그대로 둡니다** — 선생님이 따로
  일러 주셨습니다. 인쇄해서 들고 다니는 종이라 문장의 처음과 끝이
  테두리로 뚜렷해야 합니다. 그쪽은 오히려 키웠습니다
  (`.sheet .sentence-center` 앞뒤 여백 20% · 글자 42px ·
   `.sheet-pics-item` 72 → 132px).

### 37-2. 쪽 넘김 = **화살표만**

「앞 활동 보기 / 활동 더 보기」처럼 말로 적으면, 문장과 그림보다 설명이 더
눈에 띕니다. `arrowPager()` 하나로 활동 · 사람 · 장소를 **같은 모양**으로
바꿨습니다 (◀ · 1/5 · ▶).

⛔ 어디쯤인지(1/5)는 남깁니다 — 없으면 몇 쪽이 더 있는지 몰라 끝없이 누릅니다.
⛔ 화면마다 따로 만들지 마세요. 한 번 익히면 어디서나 통해야 합니다.

### 37-3. 한 쪽에 **셋** (계획하기만)

`PLAN_PER = 3`. 활동 · 사람 · 장소 모두 셋씩입니다.
⛔ `App.PAGE_SIZE`(여섯)를 이 화면에 쓰지 마세요 — **일기 · 지도는 그대로
   여섯**입니다. 계획하기만 문장이 자리를 많이 써서 다릅니다.

### 37-4. ⛔⛔ 자리를 **%로 못박았다가** 빈 곳만 만들었습니다

처음에 「문장 2/3 : 활동 1/3」을 그대로 만들려고 `.panel-top{height:62%}` 로
못박았습니다. 결과 :

```
   문장 칸 449px  —  그림·글자가 쓰는 것은 220px, **229px 가 빈 자리**
   고르는 칸 184px —  --fit 이 0.64 까지 떨어져 카드 그림이 63~72px
```

선생님 : 「빈곳이 많구나 지금 그림과 글자 크기를 유지하고 아래 그림이
작으니 그림을 크게해줘」

▸ 높이를 못박지 않고 **제 내용만큼만** 쓰게 했습니다. 남는 높이는 모두
  고르는 칸이 가져갑니다. 문장이 커 보이는 것은 칸이 넓어서가 아니라
  **그림 168px · 글자 31px** 이 크기 때문입니다.
⛔ `%` 로 되돌리지 마세요.

### 37-5. 카드 그림도 키웠습니다

`.pick .thumb` 의 `19vh` 는 **여섯 장(3칸×2줄)** 이 800 높이에 들어가게 잰
값입니다. 셋뿐인 계획하기에서는 세로가 남아 그림이 작았습니다.

    [data-corner="plan"] .pick .thumb{ height:clamp(88px,26vh,300px); }
    [data-corner="plan"] .pick .thumb.portrait{ height:clamp(110px,26vh,300px); }

⛔ **portrait 줄을 빠뜨리지 마세요** — 사람 카드는 `.pick .thumb.portrait` 가
   따로 크기를 잡아서, 활동은 208px 인데 사람만 108px 이 됩니다.
⛔ 다른 화면(일기 · 지도)까지 키우지 마세요 — 거기는 여섯 장입니다.

### 37-6. ⚠ 같은 이름의 규칙이 **두 곳**에 있었습니다

`.plan-sofar` 가 3045줄과 3276줄 두 곳에 있어, 위만 키웠더니 화면이 그대로였습니다
(뒤에 오는 규칙이 이깁니다). 크기를 고칠 때는 **두 곳을 함께** 보세요.

### 37-7. 재어 확인 (1280×800)

```
                     전          후
  무엇을 할까요      152px      208px   3/3 · fit 1
  누구와 할까요       63px      183px   3/3 · fit 0.88
  언제 할까요         88px      171px   3/3 · fit 0.82
  어디에서           160px      160px   2/2 · fit 1
  문장 글자        19.75px     31.1px
  문장 그림           88px      168px
  빈 자리            229px         0
```
넘침 0 · 홈 · 일기 · 지도 · 모아보기 그대로(일기 카드 그림 152px 유지)

### 37-8. 사람 차례 — 형제자매를 앞으로

선생님 : 「아빠 다음으로 누나, 형, 언니, 오빠, 동생이 먼저 오고 할머니
할아버지 삼촌, 고모, 이모」

```
  1쪽  선생님 · 친구 · 혼자 · 가족 · 엄마 · 아빠
  2쪽  누나 · 형 · 언니 · 오빠 · 동생 · 할머니
  3쪽  할아버지 · 삼촌 · 고모 · 이모
```
※ 계획하기에서는 셋씩이라 쪽이 더 잘게 나뉩니다.

### 37-9. 부엌은 **집 바로 뒤**

선생님 : 「부엌은 집 뒤로보내줘」. 부엌은 집의 한 곳이라 떨어뜨려 놓으면
학생이 다른 갈래로 여깁니다.

```
  1쪽  학교 · 교실 · 운동장 · 강당 · 학교 화단 · 텃밭
  2쪽  집 · 부엌 · 놀이터 · 공원 · 동네 · 노래방
  3쪽  문구점 · 음식점 · 카페 · 캠핑장 · 도서관 · 영화관
  4쪽  미술관 · 박물관
```

`?v=20260828v`

### 37-10. 계획표(오늘의 여가 계획표) 짜임새 (2026-08-26)

선생님 : 「노란색 바의 폭을 문장길이의 앞뒤 20% 여유분이 있게 조절하고
가운데 정렬해. 그리고 무엇을 누구와 언제 어디서 준비물도 문장이 시작하는
바 바로 아래부터 / 아래는 가운데 정렬을 하면 좋겠어」

- ⛔ 앞뒤 여백을 **padding 으로 주지 마세요** — 바는 폭 그대로인 채 글자만
  가운데로 몰려 노란 바 안이 텅 빈 채 넓어 보입니다 (첫 시도에서 겪음).
  **margin** 으로 주어야 바 자체가 줄어듭니다 (`--sheet-inset:14%`).
- ⛔ `.sheet-body` 를 격자(`grid-template-columns:auto 1fr`)로 두지 마세요 —
  표가 왼쪽 끝에 붙박이고 그림만 남는 자리 가운데에 놓여, 노란 바와 축이
  **178px 어긋났습니다**. flex + `justify-content:center` 로 **표와 그림을
  한 묶음으로 가운데**에 둡니다.
- 표는 글자만큼만(`width:max-content`) 넓힙니다. 칸을 늘려 가운데 정렬하면
  「무엇을」과 답 사이가 멀어져 눈이 따라가기 어렵습니다.
- 재어 확인 : 노란 바 486px(종이의 68%) 좌우 여백 115px씩 ·
  바 가운데 640 = 아래 묶음 가운데 640 = 종이 가운데 640 · 종이 넘침 0
- ※ print.css 에 이 선택자를 덮는 규칙이 없어 **인쇄도 화면과 같습니다.**

`?v=20260828w`

### 37-11. 2·3단계도 상자 없이 · 세 단계 여백 통일 (2026-08-26)

선생님 : 「2단계, 3단계에서도 마찬가지로 노란색 상자바를 없애기」
「2단계 문장제시에 위아래 여백이 전혀 없는데 여백 좀 두면 좋겠어」

- 2단계는 `planBlankLine()` 이 그리는 **`.frame-line`** 을 씁니다.
  ⛔ `.frame-line` 을 통째로 고치지 마세요 — **일기 화면이 같은 규칙**을
     씁니다. 거기는 노란 띠를 그대로 두기로 한 자리입니다.
     반드시 `[data-corner="plan"] .plan-top` 안으로 좁혀서 씁니다.
     (재어 확인 : 일기 문장 틀은 바탕 #fff8dc · 테두리 2px 그대로)
- ⛔ 상자를 걷어내면서 **여백까지 지우지 마세요.** 처음에 `padding:0` 으로
  두었더니 글자가 위아래에 딱 붙어 답답했습니다. 상자는 없애되 숨 쉴
  자리는 남깁니다 — 세 단계 모두 **위아래 19px**.
- ✔ 같은 이름이 두 곳에 있던 `.plan-sofar` 를 **한 곳으로 합쳤습니다.**
  뒤엣것이 이겨서 위만 고치고 「화면이 그대로다」 하며 헤맸던 자리입니다.
- 재어 확인 (1280×800) — 세 단계 모두 상자 없음 · 여백 19px ·
  글자 31~33px · 카드 그림 171~208px · 넘침 0

`?v=20260828x`

---

## 38. 2026-08-26 (19) — ★ 계획표 **학습지** (인쇄하면 쓰기 칸이 함께)

선생님 : 「만든 오늘의 여가 계획표를 인쇄하면 계획표 아래에 … 이러면
인쇄하고 학습자료로도 사용가능할 것 같아」

계획표를 뽑으면 **아래 반쪽이 늘 비어** 있었습니다. 그 자리에 단계별
쓰기 칸을 넣어, 한 장이 곧 학습지가 되게 했습니다 (`C.PlanWorksheet`).

| 단계 | 제목 | 하는 일 |
|---|---|---|
| 1 | 여가 계획표 **따라 쓰기** | 흐린 글자를 따라 쓰고, 아래 빈 줄에 한 번 더 |
| 2 | 여가 계획표 **빠진 낱말 찾아 쓰기** | 빈칸 셋 + 낱말 상자 |
| 3 | 여가 계획표 **다시 스스로 쓰기** | 빈 줄 셋 |

### 38-1. ⛔ 빈칸은 **시간 · 장소 · 활동** 셋뿐입니다

선생님 말씀 : 「3가지(조사의 영향을 받지 않음)」.
「누구와」는 뒤에 오는 조사가 `와/과` 로 갈려서, 빈칸으로 두면 학생이
무엇을 쓰든 조사가 틀립니다. 그래서 **그대로 둡니다.**

    나는  [    시간    ]  친구와  [   장소   ] 에서  [   활동   ] 를 할 거예요.

⛔ 활동을 `a.planText`(슬라임 놀이를 할 거예요)로 넣지 마세요 — 빈칸이 한 줄을
   다 먹고 학생이 서술어까지 옮겨 적어야 합니다. `App.frameWord(a)` 로
   **낱말만**(슬라임 놀이) 빼고, 조사와 「할 거예요」는 빈칸 **밖**에 찍습니다.
   ※ `App.josa` 는 「낱말+조사」를 함께 돌려주므로 낱말 길이만큼 잘라
     **조사만** 남깁니다.

### 38-2. ⛔ 인쇄에서 사라지는 줄을 긋지 마세요

처음에는 따라 쓰기 글자 아래에 `repeating-linear-gradient` 로 줄을 깔았습니다.
그런데 인쇄 창의 **「배경 그래픽」을 끄면 그 줄만 사라집니다.**
줄은 **테두리(border-bottom)** 로 긋습니다 — 배경 설정과 무관하게 나옵니다.

### 38-3. 화면에는 안 나옵니다

`#print-root` 안에만 들어가고 그 칸은 화면에서 `display:none` 입니다
(app.css 1488줄). ⛔ 화면 어딘가에 또 그리지 마세요 — 계획을 다 세운
학생에게 같은 문장을 두 번 보여 주면 무엇을 하라는 것인지 흐려집니다.

### 38-4. 재어 확인

세 단계 모두 제목 · 모양이 뜻대로 나옵니다
(1단계 따라쓰기+빈 줄 2 · 2단계 빈칸 3 + 낱말 상자 3 · 3단계 빈 줄 3).

⚠ **종이에 실제로 뽑아 보는 것은 남아 있습니다.** print.css 는 `media="print"`
  라 화면에서는 잴 수 없습니다 (화면 폭에서 재면 A4 기준이 아니라 값이
  엉뚱하게 나옵니다 — 실제로 한 번 헛짚었습니다).
  계획표와 학습지가 **한 장에** 들어가는지 눈으로 보셔야 합니다.
  넘치면 `.ws-lines i` 의 높이(14mm)나 `.ws` 의 `margin-top`(10mm)을 줄이세요.

`?v=20260828y`

### 38-5. 계획표·학습지 다듬기 (2026-08-26 · 선생님 넷)

**① 오른쪽 활동 그림 — 1단계에서만 뺍니다**
선생님 : 「이제 그림이 커져서 아래에 활동그림이 안들어가도 될 것 같아 /
1단계에서만 활동그림이 없어지는 거야~ 2,3단계는 그대로」
위 `sheet-pics` 에 같은 활동 그림이 이미 크게 나와 있어 한 장에 두 번
나오는 것을 막습니다.
⛔ **아예 없애지 마세요** — 그림 줄은 1단계에만 냅니다. 2·3단계에서 없애면
   계획표에 그림이 한 장도 없게 됩니다. 조건은 `!pics.length` 입니다.

**② 「무엇을 ~ 준비물」 표를 가운데로**
표는 글자만큼만 넓히고 `margin-inline:auto`. 딱지(무엇을·누구와…)는
`text-align:right` 로 맞춰 **답이 모두 같은 세로선에서** 시작합니다.
⛔ 딱지까지 가운데로 흩뜨리지 마세요 — 답이 들쭉날쭉해집니다.
재어 확인 : 표 가운데 = 종이 가운데 · 답 왼쪽 좌표 어긋남 0px

**③ 1단계 따라 쓰기 = 위 노란 점선 바 그대로, 글자만 연하게**
선생님 : 「위의 점선 노란색 그대로를 제공하되 글자 진하기가 연하게
제공되어서 그 위에 덧쓰기를 할 수 있게」
`.sentence.sentence-center` 를 **그대로 다시 쓰고** `.ws-trace{color:#cfcfd8}`
로 색만 바꿉니다. 바탕 · 점선 · 여백 · 글자 크기가 위 계획표와 저절로 같습니다.
⛔ 줄글 밑줄로 바꾸지 마세요 — 같은 모양이라야 「저 문장을 덧쓴다」가 바로 보입니다.
⛔ `.ws-trace` 에 크기나 바탕을 다시 적지 마세요 — 위와 어긋납니다.

**④ 학습지 제목을 짧게**
「여가 계획표 따라 쓰기」 → **「따라 쓰기」**. 바로 위에 「오늘의 여가 계획표」가
이미 있어 같은 말이 두 번이었습니다. 세 단계 모두 **하는 일만** 적습니다
(따라 쓰기 · 빠진 낱말 찾아 쓰기 · 다시 스스로 쓰기).

⚠ 저지른 것 — 위 ① 주석에 **백틱을 넣어** `plan.js` 가 통째로 문법 오류가
   났습니다 (인수인계 2-3 의 그 규칙). html 주석 안에서는 낫표 「」를 씁니다.

`?v=20260828z`

### 38-6. 학습지 다듬기 둘째 판 (2026-08-26 · 선생님 셋)

**① 2단계 문장을 줄글처럼 흐르게 · 네모 칸 안에**
선생님 : 「2단계에선 아래가 이렇게 문장이 나열되도록 정렬. 위 아래
여백두고 문장은 네모칸안에 두고 아래는 여백두고 낱말상자」
⛔ `flex` 로 늘어놓지 마세요 — 낱말마다 같은 사이를 벌려서 한 줄에
   띄엄띄엄 흩어지고 서술어만 다음 줄로 떨어졌습니다.
   `display:block` + inline 낱말로 **보통 줄글처럼** 흐르게 둡니다.
⛔ 낱말 사이 **띄어쓰기를 직접 넣어야** 합니다 — inline 으로 두면 태그
   사이 줄바꿈이 공백으로 안 남아 「나는친구와」로 붙습니다.
⛔ 조사는 앞엣것에 **붙입니다**(`tight` 표시) — 「빈칸 에서」가 아니라
   「빈칸에서」. 실제 모양 : `나는 [ ] 친구와 [ ]에서 [ ]를 할 거예요.`
⛔ 빈칸 폭을 글자 수에 맞추지 마세요 — 답 길이가 드러나 학생이 세어
   맞힙니다. 어느 빈칸이나 **같은 폭(26mm)**.
▸ 제목 위아래 6mm/8mm · 낱말 상자 위 9mm 로 띄웠습니다.

**② 표는 가운데, 속은 왼쪽 일렬**
선생님 : 「무엇을 누구와~~준비물은 들쑥날쑥 아니고 왼쪽 부분은 일렬로」
⛔ 딱지를 **오른쪽 맞춤으로 두지 마세요** — 글자 수가 제각각(무엇을·
   어디에서)이라 왼쪽 끝이 들쑥날쑥해집니다. 왼쪽 맞춤 + 같은 폭(6.5em).
   재어 확인 : 딱지 왼쪽 · 답 왼쪽 모두 어긋남 0px

**③ ⛔ 따라 쓰기 바가 위 계획표보다 작았습니다**
선생님 : 「위에 제시한 노란색 바탕 상자의 글자크기 그대로 하되 글씨색만」
`.sheet .sentence-center` 만 걸려 있어서, `.sheet` **밖**에 있는 학습지 바는
그 규칙을 못 받아 25.5px → 22.1px 로 작았습니다.
▸ 선택자에 `.ws .sentence-center` 를 **함께** 넣고 `--sheet-inset` 도
  `.ws` 에 두었습니다.
재어 확인 : 위·아래 모두 25.5px · 색만 rgb(47,35,24) → rgb(220,220,228)

⚠ **print.css 는 `media="print"`** 라 화면에서 그냥 재면 인쇄 모양이 아닙니다.
   확인할 때는 `link.media='all'` 로 잠깐 바꾸고 `#print-root` 를 보이게 한 뒤
   재고, **반드시 되돌리세요.**

### 38-7. ⛔⛔ `margin:auto` 가 그림을 종이 밖으로 밀어냈습니다 (2026-08-26)

선생님 : 「3단계 그림이 오른쪽으로 치우쳐있어 / 2단계도 그림이 치우침 /
출력창에서도 양쪽 여백을 동일하게」

표를 가운데로 놓으려고 `.sheet-body .rows{margin-inline:auto}` 를 주었습니다.
그런데 **flex 칸에서 auto 여백은 남는 자리를 통째로 먹습니다.** 표가 왼쪽
자리를 다 차지하는 바람에 활동 그림이 종이 **오른쪽 끝**으로 밀려났습니다
(2·3단계 — 1단계는 그림이 없어 눈에 안 띄었습니다).

▸ auto 여백을 걷어냈습니다. 가운데 맞추기는 `.sheet-body{justify-content:center}`
  가 이미 합니다. 인쇄에서도 어긋나지 않게 print.css 에 같은 규칙을 못박고
  `.rows{margin:0}` · `.sheet-art{flex:0 0 auto}` 를 함께 두었습니다.
⛔ 어느 쪽에도 `margin:auto` 를 다시 주지 마세요.
- 재어 확인 : 1·2·3단계 모두 표 왼쪽 여백 20px = 그림 오른쪽 여백 20px

### 38-8. 3단계 학습지 제목

「다시 스스로 쓰기」는 말이 어색해서 **「나만의 문장으로 다시 쓰기」** 로
바꿨습니다 (2026-08-26 · 선생님 말씀). 3단계는 틀 없이 제 말로 쓰는
단계이므로 그 뜻을 그대로 적습니다.

⚠ **인쇄 모양을 화면에서 재는 방법** — `link.media='all'` 로 바꾸면 print.css 의
   `.app{display:none}` 까지 함께 걸려 **화면이 무너집니다**(종이 폭이 40px 로
   잡혔습니다). 치수를 잴 때는 print.css 를 걸지 말고 **화면 규칙 그대로**
   재고, 인쇄에만 있는 규칙(.sheet-art 200px 등)은 따로 읽어서 가늠하세요.

`?v=20260829b`

### 38-9. 2단계 문장 — **줄바꿈된 줄도 같은 자리에서** (2026-08-26)

선생님 : 「줄바꿈되는 문장의 첫 낱말이나 밑줄이 가운데 정렬이 아니라
위 시작 문장의 처음위치와 동일하게 배치되게」

⛔ `.ws-fill` 을 `text-align:center` 로 두지 마세요 — 두 줄째가 안쪽으로
   밀려 들어가, 어디서부터 읽어야 할지 눈이 헤맵니다. **왼쪽 맞춤**이면
   모든 줄이 같은 자리에서 시작합니다.
⛔ 빈칸(`.ws-blank`) 좌우에 여백을 주지 마세요 — 조사(에서 · 를)가 빈칸에
   **붙어야** 하는데 여백이 그 사이를 벌립니다. 낱말 사이 띄어쓰기는
   plan.js 가 글자로 직접 넣습니다 (`tight` 표시가 없는 조각 앞에만).
- 재어 확인 (조각 차례) :
  `나는 · " " · [빈칸] · " " · 친구와 · " " · [빈칸] · 에서 · " " · [빈칸] · 를 할 거예요.`

`?v=20260829c`

### 38-10. 밑줄 길이 = **들어갈 낱말 길이** (2026-08-26)

선생님 : 「밑줄의 길이가 들어갈 낱말 상자의 비율과 동일하게 밑줄구성」
「줄바꿈되어도 되고 줄바꿈되면 첫 부분이 문장의 처음과 일렬로」

- 밑줄 폭 = **글자 수 + 1 em** (아주 짧은 낱말도 쓸 자리는 되게 4em 이 바닥).
  plan.js 가 인라인으로 넣습니다.
  ⛔ print.css 의 `.ws-blank` 에 `min-width` 를 못박지 마세요 —
     「내일」과 「놀이 기구 타기」가 같은 폭이 됩니다.
- 줄바꿈은 그대로 두되 `.ws-fill{text-align:left}` 이라 **모든 줄이 같은
  자리에서** 시작합니다.
- 재어 확인 : 오늘 낮에(5자)→6em · 교실(2자)→4em · 슬라임 놀이(6자)→7em

⛔ **폭을 모두 같게 두지 마세요.** 처음에 「답 길이가 드러나면 학생이 세어
   맞힌다」는 까닭으로 같은 폭으로 두었는데, 선생님이 바로잡아 주셨습니다 —
   **「특수교육대상학생에겐 이런 힌트가 필요해」**.
   글자 수는 훔쳐보기가 아니라 **비계(scaffold)** 입니다. 긴 낱말은 칸이
   모자라고 짧은 낱말은 허전한 것도 함께 풀립니다.
   ▸ 일반 학급의 시험지 감각으로 판단하지 마세요. 이 앱의 학생에게는
     단서를 **빼는 것이 아니라 주는 것**이 맞습니다.

`?v=20260829d`

### 38-11. 따라 쓰기 바 = 위 계획표 바와 **모든 것이 같게** (2026-08-26)

선생님 : 「위의 글자 위치 크기 모양 박스 크기등 모든것 동일하되 안의
글자만 진하기가 덧쓰기 할 수 있게 연하게야」

`.sheet .sentence{background:#fffbe8}` 이 **`.sheet` 안에만** 걸려 있어서,
학습지 바(`.ws` 안)만 흰 바탕이 되어 위와 달라 보였습니다.
선택자에 `.ws .sentence` 를 함께 넣었습니다.

⛔ 계획표 바의 모양을 손볼 때는 **두 선택자를 늘 함께** 보세요
   (`.sheet .sentence-center, .ws .sentence-center` · `.sheet .sentence, .ws .sentence`).
   한쪽만 고치면 학습지가 조용히 어긋납니다.

- 재어 확인 (인쇄 규칙까지 걸고) : 글자 크기 · 바탕 · 점선 · 여백 · 모서리 ·
  굵기 · 정렬이 **모두 같음**. 다른 것은 **글자 색 하나뿐** —
  rgb(47,35,24) → rgb(220,220,228)

`?v=20260829e`

### 38-12. 학습지가 **인쇄하는 모든 곳**에 따라갑니다 (2026-08-26)

선생님 : 「여가포트폴리오에서도 내가 세운 계획이 학습지 형태로 만들어진
상태로 연결되어 출력되도록」

계획표를 **인쇄하는 곳 네 군데** 모두에 `C.PlanWorksheet` 를 붙였습니다.

| 어디 | 무엇 |
|---|---|
| 계획하기 마지막 화면 | 계획표 인쇄하기 |
| `App.printPlan` (홈 · 여기저기서 부름) | 계획표 한 장 |
| 포트폴리오 → 계획 모음 인쇄 | 계획표마다 한 장씩 |
| 포트폴리오 → 책자 | 계획 쪽마다 |

⛔ 학습지를 각 자리에 **따로 만들지 마세요** — `C.PlanWorksheet` 하나만
   씁니다. 넷이 제각각이 되면 어느 것이 맞는지 알 수 없게 됩니다.
▸ **화면에 보여 주는 곳**(홈의 계획 보기 · 계획 확인 화면 · 포트폴리오
  계획 팝업)에는 붙이지 않습니다 — 학습지는 종이에 쓰라고 있는 것입니다.
- 재어 확인 : 책자 인쇄에서 **계획 쪽에만** 학습지 1장, 여가지도·나의
  한마디 쪽은 0장 (그 쪽들도 `.sheet` 라는 같은 이름을 씁니다 — 셀 때 주의).

### 38-13. 지도 네 칸 팝업을 넓혔습니다

선생님 : 「안의 글자들이 한 줄로 나열되도록 창 폭을 넓히기」
760px 에서는 「도전하고 싶어요」·「잘 모르겠어요」가 두 줄로 접혀 칸마다
글자 줄 수가 달랐습니다. `C.Modal` 의 `wide`(1100px)를 켰습니다.
⛔ 칸 수(4)를 줄여 해결하지 마세요 — 네 표시는 **늘 한 줄에 넷**이 나란히
   있어야 서로 견주어 고릅니다.
- 재어 확인 (1280×800) : 창 1100px · 네 칸 모두 **1줄**

`?v=20260829f`

### 38-14. ⛔ 계획 모음 인쇄의 **테두리가 두 겹**이던 것 (2026-08-26)

선생님 : 「계획모음 인쇄하기가 겹두줄로 테두리가 되는데 그냥 여가계획의
여가계획표가 그대로 연달아서 출력되도록. **너무 투머치로 가지말자**」

`.print-area .sheet` 와 `.print-area .book-page` 가 **둘 다** 테두리를
그렸습니다. 계획표는 제 테두리가 있는데 쪽 테두리까지 겹쳐 두 겹이 됐습니다.

▸ 계획이 든 쪽에만 `plain` 을 붙여 **바깥 테두리만** 뺍니다.
  쪽 나누기(break-after)는 그대로라, 계획표가 한 장에 하나씩
  **계획하기에서 뽑은 것과 똑같은 모습**으로 이어집니다.
⛔ `.book-page` 의 테두리를 통째로 지우지 마세요 — 일기장 · 여가지도 ·
   나의 한마디 쪽은 그 테두리가 쪽의 테두리 노릇을 합니다.
- 재어 확인 : 쪽 테두리 0px · 계획표 테두리 2px · 학습지 계획표마다 1장

`?v=20260829g`

---

## 39. 2026-08-26 (20) — 그림 크기 · 돌아보기 그림 · 읽어주기

### 39-1. 1단계 돌아보기도 **그림과 함께** (인쇄)

선생님 : 「1단계 돌아보기도 나의 한마디처럼 같은 구성으로」

화면에서는 줄마다 활동 그림이 있었는데 **인쇄에만 빠져** 있었습니다.
집에 가져간 종이는 글자뿐이라, 글을 못 읽는 학생에게는 제 기록으로
보이지 않았습니다. `printMe()` 의 돌아보기 줄에도 `.me-print-art` 를
붙였습니다 (한마디와 같은 이름 · 같은 모양).
▸ 2·3단계는 글자만 냅니다 — 위 한마디와 같은 규칙입니다.
▸ 그림은 `actByName(쓴 글자)` 로 찾고, 3단계는 `student.reviewPick` 을 봅니다.

### 39-2. ⚠ 그림 파일마다 **속에 그려진 글자 크기**가 다릅니다

선생님 : 「오늘이라는 글자가 어제에 비해 크게 나오는데 글자 비율을 줄여서
맞출수 있니?」

두 그림 다 512×512 인데, 「어제」는 **화살표가 함께** 그려져 있어 달력이
작게 들어갑니다. 그래서 같은 칸에 넣으면 「오늘」 글자만 커 보입니다.

- CSS 로는 그림 **속** 글자를 못 줄입니다. 그 그림만 조금 작게 놓습니다.
- `C.Art` 가 `data-word` 를 붙이도록 했고, `[data-word="오늘"]{transform:scale(.82)}`.
- 재어 확인 : 「오늘」 글자 277px · 「어제」 228px → 228/277 = 0.82

⛔ **그림 파일을 다시 그리면 이 값도 다시 재세요** — 안 그러면 이번에는
   「오늘」이 작아 보입니다.

**재는 법** (브라우저 콘솔) — 그림을 캔버스에 그려 **어두운 점**의 세로 길이를
잽니다. 그 길이가 곧 글자 높이입니다.

```js
async function 글자높이(path) {
  const img = new Image();
  await new Promise(ok => { img.onload = ok; img.src = App.imgUrl(path); });
  const cv = document.createElement('canvas');
  cv.width = img.naturalWidth; cv.height = img.naturalHeight;
  const g = cv.getContext('2d'); g.drawImage(img, 0, 0);
  const d = g.getImageData(0, 0, cv.width, cv.height).data;
  let minY = cv.height, maxY = 0;
  for (let y = 0; y < cv.height; y++)
    for (let x = 0; x < cv.width; x++) {
      const i = (y*cv.width + x)*4;
      if (d[i+3] > 100 && d[i] < 150 && d[i+1] < 120 && d[i+2] < 110) {
        if (y < minY) minY = y; if (y > maxY) maxY = y;
      }
    }
  return maxY - minY;
}
```

### 39-3. 읽어주기가 화면과 어긋나던 것 (선생님 : 「모두!!!」)

⛔ **읽어주기는 지금 보이는 화면을 말해야 합니다.** 화면을 고치면서
   읽어주기를 함께 안 고치면 조용히 어긋납니다.

찾아 고친 것 둘 :
- **홈** — 「계획하GO!」를 읽는데 그 글자는 화면에서 **숨겨져** 있습니다
  (`.corner .name{display:none}` — B안에서 GO! 부제를 걷어냈습니다).
  보이는 말(`c.desc`)과 안내(`c.guide`)를 읽도록 고쳤습니다.
- **포트폴리오** — 어느 화면에서나 「전시하고 싶은 일기를 골라 보세요」를
  읽었습니다. 첫 화면은 네 칸을 고르는 곳이고 전시 고르기는 「나의 일기장」
  안이라, 학생이 시키는 대로 해도 그런 것이 화면에 없었습니다.
  이제 **화면마다** 다릅니다 (첫 화면 · 계획 · 지도 · 지도에 붙이기 ·
  일기장 · 한마디 · 책자형 · 전시판형).

훑어 확인한 것 (화면과 맞음) : 표지 · 학생 고르기 · 캐릭터 · 홈 · 지도 ·
계획하기 · 일기.

**읽어주기 훑는 법** (브라우저 콘솔) :
```js
[...document.querySelectorAll('.app button')]
  .filter(x => /^읽어주기/.test(x.getAttribute('aria-label')||''))
  .map(b => b.getAttribute('aria-label').replace('읽어주기: ',''));
```

### 39-4. ⚠ 같은 이름을 두 번 만들어 목록을 덮어썼습니다

`portfolio.js` 에 이미 `LABEL_KINDS`(라벨지 규격)가 있는데, 라벨 세 벌을
만들며 **같은 이름**을 또 선언해 규격 목록을 통째로 가렸습니다.
⛔ 이름을 짓기 전에 **그 파일에서 먼저 찾아보세요** (`grep -n "이름" 파일`).
   같은 함수 안이면 뒤엣것이 앞엣것을 덮습니다. 제 것은 `LABEL_FACES` 로 바꿨습니다.

`?v=20260829k`

---

## 40. 2026-08-28 (1) — 일기 문장 틀 · 일기장 카드 · 고치는 화면 하나로 · 블록놀이

### 40-1. ⛔⛔ **계획하기 글자 크기를 일기에 그대로 옮기지 마세요** (창이 흔들린 까닭)

선생님 : 「3단계 일기도 한번 돌려봐줘, **렉이 걸린듯 화면이 여러번 창이
흔들리면서** 다음으로 겨우 넘어감.」

여가 일기를 여가 계획하기와 똑같이 맞추면서 `.frame-top .frame-line` 의
글자를 계획하기와 같은 `clamp(1.3rem,.7rem + 2.6vh,42px)` 로 키웠습니다.
**계획하기 문장은 한 줄이지만 일기 문장 틀은 3~4줄입니다.**

```
              한 줄     문장 틀     고르는 칸(.stage-track)   --fit
  키운 뒤     86px      276px       357px                    0.58  ← 바닥
  되돌린 뒤   59px      186px       394px                    0.70
```

`--fit` 이 **바닥(0.58)** 에 붙으면 세 겹 넘침 방지(`.fitting` → `.flowing`
→ `App.guardFit`)가 「더 줄여야 하는데 더는 못 줄인다」 를 되풀이합니다.
걸음을 넘길 때마다 다시 재느라 **창이 여러 번 흔들렸습니다.**

▸ 고침 : 상자·색만 걷어내고 **크기는 원래대로**
  (`clamp(.92rem,.6rem + .85vh,1.15rem)`, 여백 `.55rem` 위아래 같게).
  가운데 정렬 · 빨간 낱말(`.blank.on.hi`)은 그대로 둡니다.
  → 120ms 로 59번 재는 동안 값이 화면 바뀔 때 **2번만** 바뀌고 되풀이 없음.

⛔ 「A 화면과 똑같이」 라는 말은 **모양이 같게** 이지 **숫자가 같게** 가
   아닙니다. 줄 수가 다른 곳에 같은 글자 크기를 넣으면 자리가 무너집니다.

### 40-2. ⛔⛔ 「똑같이」 는 **화면 안**이지 **가는 곳**이 아닙니다

선생님 : 「여가일기모음에서 하나씩 눌러보면 … **고치기**를 누르면 왼쪽 바가
더 넓게 차지하고 구성도 원래 **일기가 완성되었어요**에서 고치는 화면과
완전 달라. 그 화면과 동일하게 구성하고 크기도 동일하게 모두 똑같이.」

**제가 처음에 잘못 알아들었습니다.** 「같은 화면으로 보내면 저절로 같아진다」
싶어 일기 모음의 「고치기」 를 완성 화면(`diary` step:'last')으로 **보내
버렸습니다.** 선생님 :

> 「고치기를 눌렀더니 일기가 완성해요로 **가버렸네? 그럼 안돼지!!**
>  화면 구성을 똑같이 해달라고 했지 **위치를 이동하라는건 아니었어.**
>  고치기를 누르고 고친다음 **다시 나의 일기모음으로** 가게 되돌리고
>  안의 구성 디자인 위치는 이렇게 동일하게!!!!!」

⛔ **길(어디로 가는가)과 짜임새(무엇이 어떻게 놓이는가)는 다른 것입니다.**
   「A 처럼 해 줘」 는 거의 언제나 **짜임새** 이야기입니다. 길을 바꾸면
   학생이 하던 일의 흐름이 끊깁니다 — 고치고 나면 **온 곳으로** 돌아가야
   합니다.

▸ 지금 : 일기 모음 → `fixdiary` (그대로) → 고친 뒤 **나의 일기 모음으로**.
  `params.from === 'journal'` 이면 화살표가 「나의 일기 모음으로 돌아가기」.

**화면 안은 이렇게 같게 만들었습니다** — 껍데기 이름을 완성 화면 것으로
바꿔치기했습니다. 이름이 같으니 CSS 한 곳만 고치면 두 화면이 함께 바뀝니다.

| | 전 (fixdiary 만의 것) | 후 (완성 화면과 같은 것) |
|---|---|---|
| 좌우 2단 | `.fix-2col` / `.fix-left` / `.fix-right` | `.confirm-fit` / `.confirm-2col` / `.confirm-left` |
| 오른쪽 종이 | `.fix-paper` + 제 몫의 `fitScale()` | `C.DiaryPreview` (완성 화면과 같은 부품) |
| 묶음 이름표 | 없음 (단추 셋만 늘어섬) | `.fix-part` / `.fix-lab` — **그림 · 글** |
| 자리 옮기기 안내 | `.fix-panes` (켜면 아래가 밀림) | `.move-slot` (자리를 늘 잡아 둠) |
| 글 고치기 | 「일기 내용 수정하기」 눌러야 나옴 | `C.SentenceEdit` **늘 펴 둠** |
| 흰 칸 넘칠 때 | 장치 없음 | `App.useFitOnePage` (완성 화면과 같은 장치) |
| 무대 높이 | 내용만큼 | `tall` — 남는 높이를 다 씀 |

잰 값 (1280x800) :

```
             왼쪽 칸     종이        이름표    넘침
  고치기     866x432    361x510     그림·글    0
  완성       889x508    339x479     그림·글    0
```

⚠ 「선생님이 도와주세요.」 설명 칸은 뺐습니다 (선생님 말씀) — 완성 화면에도
   없습니다. 두 화면이 같아야 합니다.

⛔ **지운 CSS 를 되살리지 마세요** : `.fix-2col` · `.fix-left` · `.fix-right`
   · `.fix-paper` · `.fix-text` · `.fix-panes`. 특히 `.fix-paper` 는 왼쪽 칸을
   **420px 로 못박아** 두어서, 두 화면이 달라 보이던 바로 그 원인입니다.
   (`.btn.fix-go` 는 완성 화면도 쓰므로 남겨 두었습니다)

### 40-3. ★ 일기장 카드에서 **군더더기 빼기**

선생님 : 「굳이 여기서 내용을 다 써서 보여줄 필요 없음 / 활동그림, 일기제목,
년월일 요일 남기고 다 삭제 … 하나의 흰색 바 안에서 각각 선택될 수 있도록
한 줄로 깔끔해 보이도록 ~ **여긴 너무 많은 정보와 상자가 많아.**」

- 뺀 것 : 「전시할 일기」 딱지 · 「· 친구 · 교실」 · 일기 글 전문 · 사진 줄
- 단추 셋(전시할래요 · 그림일기 · 일기 고치기)을 **흰 바 하나**(`.dcard-bar`)
  안에 3등분으로 넣고 칸막이 선만 그었습니다.
- 카드 높이 **125px** (셋 다 같은 줄, 글자 넘침 0)

⛔ `.dcard-act` 에 `.btn` 을 쓰지 않았습니다 — `.panel-action .btn{width:100%}`
   같은 바깥 규칙이 끼어들어 단추가 세로로 쌓인 적이 있습니다 (§33-4).

### 40-4. ★ 자석놀이 → **블록놀이** (여섯 가지)

선생님 : 「실내여가놀이에서 자석놀이 대신에 블록놀이를 넣기, 자석놀이 삭제.
블록놀이 안에 종류를 넣어줘.」

`block` (블록놀이) 아래 여섯 : 나무 · 레고 · 자석 · 종이 · 큰 · 끼우기.
그림 7장 모두 `images/activities/` 에 있고 붙는 것을 확인했습니다.

**⛔ 활동을 지울 때는 반드시 `ALIAS` 에 한 줄 적으세요** (activities.js).

```js
var ALIAS = { magnet: 'block-magnet' };     // 자석놀이 → 자석 블록놀이
```

이미 저장된 계획·일기·지도 표시가 사라진 id 를 가리킨 채 남아, 지난 기록의
활동 칸이 **통째로 빕니다.** 학생은 자기 일기가 망가진 것으로 봅니다.
`App.act` · `App.cardIdOf` · `App.cardOf` 가 이 표를 거칩니다.

`?v=20260829n`

### 40-5. ⛔ 조사(을/를)를 **글자에 박아 두지 마세요**

선생님이 잡아 주신 오타 : 「어떤 블록놀이**을** 할까요?」

받침이 있고 없고를 코드가 알 수 없으니, 조사는 **낱말을 보고** 붙여야 합니다.
`App.eulReul(w)` 는 **낱말까지 함께** 돌려줍니다 (`블록놀이` → `블록놀이를`).

고친 곳 셋 :

| 파일 | 전 | 후 |
|---|---|---|
| `plan.js` 하위 활동 질문 | `'어떤 ' + shortName + '을 할까요?'` | `'어떤 ' + App.eulReul(shortName) + ' 할까요?'` |
| `plan.js` 계획 문장 기본값 | `a.name + '을 할 거예요'` | `App.eulReul(a.name) + ' 할 거예요'` |
| `portfolio.js` 나의 한마디 2단계 | `next.act + '을 할 때'` | `App.eulReul(next.act) + ' 할 때'` (빈칸은 「　　　을」 그대로) |

⚠ 뒤엣것 둘은 **선생님이 더한 「우리 반 활동」·학생이 직접 쓴 말**에 걸립니다.
   미리 만들어 둔 활동은 `planText` 가 있어 드러나지 않았을 뿐, 같은 고장입니다.

**찾는 법** — 조사를 박아 둔 곳은 이렇게 훑습니다 :
```
grep -rn "+ '을 \|+ '를 \|+ '은 \|+ '는 \|+ '이 \|+ '가 " js/
```

`?v=20260829r`

### 40-6. ⛔⛔ **조사·마침표는 빈칸에 붙여 씁니다** (선생님이 여러 번 말씀하신 곳)

문장 틀 줄(`.frame-line`)은 **flex** 라 칸마다 `gap:7.65px` 이 들어갑니다.
빈칸과 조사를 그냥 나란히 두면 이렇게 보입니다 :

```
  전 :  나는 [오늘] [　　　] 와 함께 [　　　] 에서 [　　　] 를 했어요.
                            ↑ 조사가 딴 낱말처럼 떨어짐
  후 :  나는 [오늘] [　　　]와 함께 [　　　]에서 [　　　]를 했어요.
```

우리말에서 조사는 앞말에 **붙여 씁니다.** 떨어져 있으면 학생이 「와」 를
하나의 낱말로 읽습니다. 마침표(`기분이 [　　　] .`)도 같습니다.

▸ 고침 : 빈칸과 조사를 **한 칸**(`.tie`)에 함께 담습니다.
  `.tie{ display:inline-flex; align-items:center; min-width:0; }`
  묶음 안에는 gap 이 없고, 낱말과 낱말 사이 간격은 그대로 남습니다.

⛔ **`gap` 을 0 으로 만들어 해결하지 마세요** — 그러면 「나는오늘」 처럼
   낱말끼리도 붙어 버립니다.

고친 곳 (같은 고장이 두 화면에 있었습니다) :

| 파일 | 자리 | 붙인 것 |
|---|---|---|
| `diary.js` `frameBar()` | 1줄 | `과/와 함께` · `에서` · `을/를 했어요.` |
| `diary.js` `frameBar()` | 기분이 줄 | 마침표 |
| `diary.js` `frameBar()` | 기억에 남는 것은 줄 | `이에요/예요.` |
| `diary.js` `frameBar()` | 3단계 왜냐하면 줄 | 마침표 |
| `plan.js` `planBlankLine()` | 2단계 문장 틀 | `과/와 함께` · `에서` · `을/를 할 거예요.` |

잰 값 — 묶음 안 0px, 낱말 사이 8px (두 화면 모두).

`?v=20260829r`

### 40-7. 활동 바꾸기 — 유튜브 보기 → **퍼즐놀이**

선생님 : 「유튜브 보기 → 퍼즐놀이 / 활동변경」

`퍼즐놀이.png` 는 이미 폴더에 있었습니다. 예전 기록은 `ALIAS` 로 잇습니다
(`youtube: 'puzzle'`) — **지울 때는 반드시 한 줄 적으세요** (§40-4).

쓰는 그림 :

| 무엇 | 파일 | 있나 |
|---|---|---|
| 활동 그림 | `images/activities/퍼즐놀이.png` | O |
| 준비물 그림 | `images/준비물/퍼즐.png` | O — 선생님이 넣어 주셨습니다 |
| 대신 아이콘 | `icons.js` 의 `blocks` | `puzzle` 아이콘이 없어 씁니다 |

⚠ `icon` 은 **그림 파일이 없을 때만** 나오는 대신 그림입니다. 퍼즐놀이.png 가
   있으므로 화면에는 늘 그 그림이 나옵니다 — `blocks` 는 보이지 않습니다.

`?v=20260830k`

### 40-8. ★ 일기 카드 — **왼쪽 그림 한 장 · 오른쪽 세 줄**

선생님 : 「왼쪽에 활동 그림만 넣고 오른쪽에 년월일요일 밑에 제목, 그 밑에
세개의 바가 배치될까? 난 **그림이 좀 눈에 띄었으면** 해서」

```
  전                              후
  ┌──────────────────────┐       ┌──────┬──────────────────┐
  │ [54] 제목  날짜       │       │      │ 2026년 8월 27일 목 │
  ├──────────────────────┤       │ 그림  │ 선생님과 함께한…   │
  │ [전시][그림일기][고치기]│       │ [96] │ [전시][그림][고치기]│
  └──────────────────────┘       └──────┴──────────────────┘
```

- 그림 **54px → 96px** (오른쪽 세 줄 높이를 통째로 씀)
- 날짜가 제목 **위** — 일기는 날짜로 찾는 것이라 먼저 보여야 합니다
- 단추 바가 오른쪽 칸 **안**으로 들어가, 그림이 세 줄 전체와 나란합니다

⚠ 그림 칸은 `auto` 가 아니라 **clamp 로 못박습니다**
  (`width:clamp(64px, 7.5vw, 96px); aspect-ratio:1`).
  auto 로 두면 일기마다 그림 크기가 달라져 카드가 들쭉날쭉해집니다.
⚠ 속 그림에 `width/height:100%; object-fit:contain` 을 걸어 둡니다 —
  안 걸면 캐릭터가 머리만 보이던 고장과 같은 일이 납니다 (§36-1).
⚠ 제목은 한 줄로 줄여 보여 줍니다 — 긴 제목이 두 줄이 되면 그 카드만
  높아져 격자가 어긋납니다.

잰 값 : 카드 591x121 (셋 다 같은 높이) · 태블릿 768 에서도 넘침 0.

`?v=20260830k`

### 40-9. ⛔⛔ 그림 크기를 **높이(vh)로만** 재면 칸이 좁은 화면에서 넘칩니다

선생님 : 「준비물이 나오는 칸의 이미지들이 **각자의 상자를 넘쳐서** 나와」

```
  준비물 (한 줄 8칸) · 창 1244px
    칸(.pick)      143px
    그림칸(.thumb) 187px   ← 좌우로 22px 씩 삐져나옴
```

**까닭** : 고르는 칸의 그림 크기는 모두 **화면 높이**로 잽니다.

```css
.pick .thumb{ width:clamp(72px,19vh,230px); … }
[data-corner="plan"] .pick .thumb{ width:clamp(88px,26vh,300px); … }
```

그런데 칸의 **넓이**는 「한 줄에 몇 칸이냐」로 정해집니다. 둘은 아무 상관이
없습니다. `26vh` 는 주석에 적혀 있듯 **「한 줄에 셋」을 보고 잰 값**인데,
`[data-corner="plan"]` 은 그 코너의 **모든** 고르는 칸에 걸립니다.
준비물은 한 줄에 여덟이라 칸이 143px 뿐이어서 그림이 뚫고 나왔습니다.

**고침 — 두 줄이 함께 있어야 합니다.**

```css
/* ① 어떤 칸에서도 넘치지 않게 (안전장치) */
.pick .thumb{ … max-width:100%; }

/* ② 준비물(8칸)은 넓이를 칸에 맞추고 높이가 따라오게 — 네모 유지 */
.pick-grid.cols-8 .pick .thumb{
  width:min(clamp(72px,19vh,230px), 100%);
  height:auto; aspect-ratio:1;
}
```

⚠ ① 만 넣으면 **넓이만 줄고 높이는 그대로**라 143x187 짜리 길쭉한 칸이
   됩니다. ② 가 있어야 네모가 유지됩니다.
⛔ ① 을 지우지 마세요 — 앞으로 어떤 칸을 새로 만들어도 이 한 줄이 막아 줍니다.

잰 값 (삐짐이 **음수**면 칸 안쪽입니다) :

```
  1244x462   칸 143 · 그림칸 120x120 · 삐짐 -23
  1280x1024  칸 300 · 그림칸 195x195 · 삐짐 -105   (높은 화면은 4칸 2줄)
  1366x768   칸 153 · 그림칸 130x130 · 삐짐 -23
```

세 크기 모두 쪽 넘침 0. 셋·여섯 칸 화면(계획 활동 · 일기)은 그대로입니다.

`?v=20260830k`

### 40-10. 학습지 1단계 따라 쓰기 — 흰 바탕 · 30pt

선생님 : 「아래 따라쓰기의 **노란색 바탕색을 삭제**하고 글자 크기는
**폰트 30정도** 되게 맞춰서 위에 제시된 문장보다 조금 크게」

```css
.print-area .ws-trace{ color:#dcdce4; background:none; font-size:30pt; }
```

| | 바탕 | 글자 | 하는 일 |
|---|---|---|---|
| 위 계획표 바 | 파스텔 노랑 | 28.3pt | **읽는** 칸 |
| 아래 따라 쓰기 | **흰색** | **30.0pt** (1.06배) | **연필로 덧쓰는** 칸 |

두 칸은 하는 일이 다릅니다 — 덧쓰는 칸은 흰 바탕이라야 학생 글씨가
또렷하고, 획을 따라 그려야 하므로 읽는 칸보다 조금 커야 손이 편합니다.
⚠ 점선 테두리는 **남겨 둡니다** — 어디에 쓰는지 알려 주는 표시입니다.

※ 2026-08-26 에는 「위의 글자 위치 크기 모양 박스 크기등 **모든것 동일하되**
   안의 글자만 연하게」 였습니다. 실제로 뽑아 보시고 바꾸신 것이니,
   app.css 의 옛 주석(`.sheet .sentence, .ws .sentence`)을 보고 되돌리지 마세요.

**⛔⛔ print.css 는 `media="print"` 라 화면에서 그냥 재면 틀립니다.**
`clamp(…, .9rem + 2vh, …)` 의 `vh` 가 **화면 높이**로 풀려, 짧은 창에서는
위 문장이 25.5px 로 나옵니다 (실제 인쇄는 37.7px). 재는 법 :

1. 창을 **A4 크기(794x1122)** 로 맞춥니다
2. `print.css` 를 `media="all"` 로 한 벌 더 붙입니다
3. `.sheet .sentence` 와 `.ws-trace` 의 `fontSize` 를 잽니다 (pt = px x 72/96)

`?v=20260830k`

### 40-11. 학습지 2단계 밑줄 — **낱말 길이 x 1.2**

선생님 : 「밑줄을 글자크기 고려해서 하되 지금보다 **각각 20%정도 더** 연장
하거나, 「할 거예요.」가 아래에 위치하도록 … **원래보다는 조금씩만 더 길게**」

```js
minWidth: (Math.round(Math.max(4, x.t.length + 1) * 1.2 * 10) / 10) + 'em'
```

| 들어갈 낱말 | 전 | 후 |
|---|---|---|
| 내일 (2자) | 4em | 4.8em |
| 놀이터 (3자) | 4em | 4.8em |
| 종이 블록놀이 (7자) | 8em | 9.6em |
| 놀이 기구 타기 (8자) | 9em | 10.8em |

문장 오른쪽 끝 **680 → 733px** (A4 폭 794 안). 넘치지 않습니다.

▸ 학생 글씨는 인쇄 글자보다 크고 고르지 않아, 글자 수에 딱 맞게 잡으면
  쓸 자리가 모자랍니다. 길어진 만큼 서술어가 아래로 내려가기도 합니다.
⚠ 단위가 `em` 이라 **글자 크기를 키우면 밑줄도 함께 늘어납니다** —
  선생님이 말씀하신 「글자크기 고려해서」가 저절로 지켜집니다.
⚠ `.ws-blank` 에 `max-width:100%` 가 있어 아주 긴 낱말이어도 칸을 넘지 않습니다.

`?v=20260830k`

### 40-12. 계획 수준 기본값 — **자세한 계획**

선생님 : 「설정에서 자세한 계획을 기본설정으로!」

`easy` → `detail` 로 바꾼 곳 **세 군데** (하나라도 빠지면 어긋납니다) :

| 파일 | 자리 | 무엇 |
|---|---|---|
| `store.js` | `newStudent()` | 새로 만드는 학생의 기본값 |
| `store.js` | `seed()` | 예시 학생 |
| `plan.js` | `level: (student && student.planLevel) \|\| 'detail'` | 값이 없을 때의 대비값 |

확인 : 새 학생을 만들면 계획이 **1 / 8** 걸음으로 시작합니다
(쉬운 계획은 6걸음 — `area · what · who · when · place · confirm`,
 자세한 계획은 8걸음 — 거기에 `time · supplies` 가 붙습니다).

**⚠⚠ 이미 만들어 둔 학생은 그대로입니다.** 기본값은 **새로 만들 때** 쓰는
값이고, 학생마다 고른 값은 이미 저장돼 있습니다. 옛 학생을 바꾸려면
선생님 설정에서 학생마다 「자세한 계획」을 누르세요.
⛔ 저장된 학생 값을 코드로 한꺼번에 덮어쓰지 마세요 — 선생님이 그 학생만
   일부러 쉬운 계획으로 두었을 수 있습니다.

`?v=20260830k`

### 40-13. 책 읽기 · 노래 부르기를 **실내에도**

선생님 : 「노래 부르기와 책 읽기는 실내활동에도 넣어줘」

도서관·노래방에 가야만 하는 일이 아니라 교실에서도 늘 합니다.

| id | 섬 | 장소 | 준비물 |
|---|---|---|---|
| `library` | 실외 | 도서관 | 가방 · 대출증 |
| **`library-in`** | **실내** | **(비움)** | **책** |
| `karaoke` | 실외 | 노래방 | 용돈 |
| **`karaoke-in`** | **실내** | **(비움)** | (없음) |

▸ 실외 짝은 **그대로 둡니다.** 지우면 그 활동으로 쓴 지난 기록이 실외
  섬에서 사라집니다. 두 섬에 하나씩 두는 것은 `pet` / `pet-in`
  (반려동물과 놀기)에서 이미 쓰던 방식입니다.
▸ 이름은 실외 짝과 **똑같이** 둡니다 — 장소만 다를 뿐 같은 일이라,
  이름을 다르게 하면 학생이 다른 활동으로 봅니다 (규칙 8-18).

확인 : 실내 15 → **17개**, 실외 15개 그대로, id 겹침 없음, 이름 겹침 없음.

**⚠⚠ 그림이 아직 실내 것이 아닙니다.**
지금은 실외 짝의 그림(`도서관.png` · `노래방.png`)을 그대로 씁니다. 그림에
도서관과 노래방이 그려져 있어, 교실에서 하는 일로는 결이 어긋납니다.
**글자를 못 읽는 학생은 그림으로 고릅니다.** 아래 두 장을 넣어 주시면
`imageKey` 만 바꾸면 됩니다 :

```
images/activities/책 읽기.png        (교실·집에서 책 읽는 그림)
images/activities/노래 부르기.png    (교실에서 노래 부르는 그림)
```

`?v=20260830k`

### 40-14. ⛔⛔ `defaultPlace` 는 **메모가 아니라 미리 고르기**입니다

선생님 : 「도서관, 노래방 장소는 바꾸지말고 **활동만** 넣어줘. 왜냐하면
**교실에서 책읽기, 집에서 책읽기, 교실에서 노래하기가 가능하도록**」
「실내활동에서 장소는 바꾸지 말고 활동만 2개 추가해달라는 것」

처음에 실내 짝에 `defaultPlace: '교실'` 을 적었습니다. 그러면 안 됩니다.
`defaultPlace` 는 적어 두기만 하는 값이 아니라 **두 가지 일**을 합니다 :

```js
// ① 활동을 고르는 순간 장소 칸을 미리 채웁니다
patch({ …, place: draft.place || card.defaultPlace })      // plan.js 337·344
// ② 장소 고르는 칸에서 그곳을 맨 앞으로 끌어옵니다
places = [act.defaultPlace].concat(places.filter(…))       // plan.js 722 · diary.js 596
```

`교실` 을 적어 두면 교실이 **먼저 정해져 버려**, 집에서 읽는 날에도 학생이
굳이 지우고 다시 골라야 합니다. 비워 두어야 교실이든 집이든 똑같이 고릅니다.

| id | 섬 | defaultPlace | 장소 칸 첫 줄 |
|---|---|---|---|
| `library` | 실외 | 도서관 | 도서관 · 학교 · 교실 … |
| `library-in` | 실내 | **(비움)** | 학교 · 교실 · 운동장 … (원래 차례) |
| `karaoke` | 실외 | 노래방 | 노래방 · 학교 · 교실 … |
| `karaoke-in` | 실내 | **(비움)** | 학교 · 교실 · 운동장 … (원래 차례) |

▸ **활동만 둘 늘었습니다** — 장소 목록은 한 줄도 건드리지 않았습니다.
  실내 15 → 17개, 실외 15개 그대로.
⛔ 「이 활동은 보통 여기서 해요」 라는 뜻으로 `defaultPlace` 를 적지 마세요.
   한 곳에서만 하는 활동(도서관·노래방·미술관…)에만 씁니다.

`?v=20260830k`

### 40-15. ⛔ 장소 **미리 정하기 없앰**

선생님 : 「장소 미리 정하기 기능 삭제」

활동을 고르는 순간 그 활동의 `defaultPlace` 가 장소 칸에 **저절로**
들어갔습니다. 그러면 「어느 곳에서 할까요?」 는 답이 이미 정해진 채로 열려,
학생이 **고르는** 것이 아니라 **지우고 다시 고르는** 화면이 됩니다.
집에서 책을 읽는 날, 부엌이 아닌 곳에서 요리하는 날마다 그렇습니다.

뺀 곳 **네 군데** :

| 파일 | 자리 |
|---|---|
| `plan.js` | `chooseCard()` — 대표 활동을 고를 때 |
| `plan.js` | `chooseChild()` — 하위 활동을 고를 때 |
| `plan.js` | 첫 밑그림 (오늘의 도전으로 들어올 때) |
| `diary.js` | 3걸음 활동 고르기 |

▸ **준비물은 그대로 채워 줍니다** — 여러 개를 더하고 빼는 칸이라
  밑그림이 있는 편이 낫습니다. 장소는 하나만 고르는 칸이라 다릅니다.
▸ `defaultPlace` 값 자체는 남깁니다. 이제는 **장소 칸에서 그곳을 맨 앞에
  놓아 주는** 일만 합니다 — 고르기 쉽게 하는 **단서**이지, 정하는 것이
  아닙니다 (「단서는 비계다」 — 빼지 말고 줍니다).

확인 : 장소 화면에 **골라진 칸이 하나도 없음**, 「교실」은 맨 앞에 있을 뿐.

### 40-16. 실내 활동 그림은 실외 짝의 것을 그대로

선생님 : 「그림은 그대로 써도 되지 않을까?」

`library-in` → `도서관.png`, `karaoke-in` → `노래방.png`.
같은 일(책 읽기 · 노래 부르기)이라 그림도 같은 것이 맞습니다.
새 그림을 따로 그리지 않습니다.

`?v=20260830k`

### 40-17. ★ 활동 차례를 **계열성 있게** — 차례표를 한 곳에

선생님 : 「활동들이 조금 **계열성 있게** 나열되어 있으면 좋겠는데 지금 보니
중구난방 같아」 하시며 실내 17 · 실외 15 차례를 그대로 적어 주셨습니다.

**⛔ 활동 덩어리를 오려 붙여 차례를 바꾸지 마세요.** 한 활동이 여러 줄이라
옮기다 통째로 날리기 쉽습니다 (실제로 §34 에서 겪었습니다).
▸ `activities.js` 에 **차례표(`ORDER`)** 를 두고 `App.topCards()` 가 그대로
  정렬해 내어 줍니다. 목록은 **적어 두는 곳**, 차례는 **표**가 정합니다.

```js
var ORDER = {
  indoor: [
    'make','block','puzzle','toy','marble',      // 손으로 만들고 조작하기
    'music','karaoke-in','library-in',           // 듣고 부르고 읽기
    'collect','cook','insect','pet-in',          // 모으고 기르고 만들어 먹기
    'tv','game','boardgame',                     // 보고 겨루기
    'yoga','gymnastics'                          // 몸 움직이기
  ],
  outdoor: [
    'playground','stationery','karaoke',         // 가까운 곳에서
    'park','bike','walk','garden','pet-out',     // 바깥에서 몸 쓰고 돌보기
    'restaurant','cafe','camping',               // 먹고 쉬기
    'library','cinema','artmuseum','museum'      // 문화 시설에서
  ]
};
```

▸ 계획하기 · 일기 · 여가지도 · 선생님 설정이 모두 `App.topCards()` 를
  거치므로 **네 화면의 차례가 한 번에 같아집니다.**
▸ 표에 없는 것(우리 반 활동, 적기를 빠뜨린 것)은 **맨 뒤로** 갑니다.
  ⛔ 빼 버리지 않습니다 — 사라지면 그 활동으로 쓴 지난 기록을 못 찾습니다.
⚠ 활동을 새로 더하면 **표에도 id 를 적으세요.**

확인 : 실내 17 · 실외 15 모두 말씀하신 차례와 **한 칸도 어긋나지 않음**.
       우리 반 활동을 더해 보니 맨 뒤(18번째)에 붙음.

`?v=20260830k`

### 40-18. 그림일기 제목 **자간** — 왜 붙어 보였나

선생님 : 「제목에 글자가 너무 다닥다닥 붙어있는것 같은데 이건 글자체
때문인거지? 이 글자체로 좀 **자간을 넓히는건** 어렵나?」

맞습니다. 까닭이 **둘**입니다.

1. 글자체(나눔바른펜)가 획이 얇고 폭이 좁습니다.
2. 더 큰 까닭 — **본문은 원고지 칸마다 글자가 한 칸씩 벌어져 있습니다.**
   그 옆에서 제목만 붙어 있으니 한 장 안에서 유난히 빽빽해 보입니다.

▸ 고침 : `.pd-titletext{ letter-spacing:.1em }`
  (한 글자 폭의 10분의 1. 칸 글자만큼 벌리면 되레 낱말이 흩어져 보입니다.)

**⛔⛔ 자간을 고치면 `titleFsFit` 의 나눗셈도 함께 고쳐야 합니다.**
자간을 주면 한 글자가 1em 이 아니라 **1.1em** 을 씁니다. 쓸 수 있는 폭을
그만큼 나눠 두지 않으면 긴 제목의 뒤가 「…」로 잘립니다
(원고지 규칙 13 — 글자 누락 금지).

```js
// picdiary.js
Math.floor(636 / titleText.length)      // 700 / 1.1 ≒ 636
```

잰 값 (종이 폭 794 · 글자 폭을 Range 로 직접 잼) :

```
  15자  42px  글폭 509 / 자리 654   남음 145
  16자  39px  글폭 466 / 자리 663   남음 197
  23자  27px  글폭 486 / 자리 701   남음 215
  32자  19px  글폭 465 / 자리 726   남음 261
```

⚠ `scrollWidth - clientWidth` 로 재면 안 됩니다 — 이 칸은 `flex:1 1 auto`
   라 자리가 늘 꽉 차서 **넘쳐도 0 이 나옵니다.** `document.createRange()`
   로 글자 자체를 재세요.
※ 짧은 제목(`.pd-titlegrid`)은 칸마다 한 글자라 자간을 줄 곳이 없습니다.

`?v=20260830k`

### 40-19. ⛔⛔ **우리 반 활동 그림 — 폴더가 아니라 「내 그림 넣기」로**

선생님 : 「다른 사람의 컴에서 이게 쓰일텐데 **파일을 넣어두는 장소를 저렇게
명시**했는데… 다른 컴에서도 **일반화**해서 쓸 수 있도록 해줘. 컴 안에
저장하면 그 사진을 넣으면 되는 식으로, **사진 넣기 처럼**」

활동 더하기 창에 이렇게 적혀 있었습니다 :

> **images/activities/활동이름.png** 파일을 넣어 두면 그 그림이 대신 나와요.

⛔ 이건 **앱 폴더를 고칠 수 있는 사람만** 할 수 있는 일입니다. 다른 선생님
   컴퓨터에서는 아무 소용이 없고, 안내만 보고 따라 하려다 막힙니다.

▸ 이제 **기기 안 사진 보관소(App.photos)** 에 담습니다 — 학생 사진과
  같은 곳, 같은 방식입니다.

고친 곳 :

| 파일 | 무엇 |
|---|---|
| `common.js` `C.AddActivityModal` | 「내 그림 넣기」 파일 고르기 · 미리보기 · 빼기 |
| `store.js` `addActivity` | `photoId` 를 함께 저장 |
| `store.js` `removeActivity` | 활동을 지울 때 **그림도 함께** 지움 |
| `activities.js` `A()` · `setCustomActivities` | `photoId` 를 실어 나름 |
| `activities.js` `App.activityImage` | **넣은 그림이 먼저**, 없으면 파일 → 코드 그림 |
| `photos.js` `exportRecords` | 학급 그림(studentId 없음)은 **늘** 백업에 담음 |
| `teacher.js` | 우리 반 활동 목록에 그림을 함께 보여 줌 |

**⛔ `photos.js` 의 `!r.studentId` 조건을 빼지 마세요.**
우리 반 활동 그림은 어느 한 학생 것이 아니라 `studentId` 가 비어 있습니다.
학생으로만 걸러 내면 **학생별 백업에서 그림만 빠져**, 다른 컴퓨터에서
불러왔을 때 활동은 있는데 그림이 없습니다 (활동 자체는 `customActivities`
로 늘 함께 갑니다).

⚠ 창을 **그만두기**로 닫으면 방금 넣은 그림을 버립니다. 안 그러면 아무도
  안 쓰는 사진이 보관소에 쌓여 백업 파일만 커집니다.

**시험한 것** (실제로 돌려서 확인) :

```
  그림을 넣어 활동 만들기      → 활동 그림이 넣은 그림으로 나옴 ✓
  전체 백업                     → 활동 + 그림(7KB) 담김 ✓
  학생별 백업                   → 활동 + 그림(7KB) 담김 ✓   ← 여기가 빠지기 쉬움
  활동 지우기                   → 그림도 함께 지워짐 (사진 0장) ✓
  지운 뒤 백업으로 되살리기      → 그림까지 그대로 돌아옴 ✓
```

`?v=20260830k`

### 40-20. 「내 그림 넣기」를 **사람 · 기분에도** — 칸은 한 벌만

선생님 : 「같이 해줘」 (§40-19 를 사람·기분에도)

⛔ 세 창에 같은 칸을 **베껴 쓰지 않았습니다.** `common.js` 에 `usePicPick()`
   하나를 두고 셋이 함께 씁니다. 베껴 두면 한 곳만 고치고 나머지 둘이
   어긋납니다 (같은 이름을 두 번 만들어 겪은 §39-4 와 같은 결).

```js
var pic = usePicPick();
…
${pic.ui(PARTNER_ICONS, iconS)}          // 그리기
App.store.addPartner({ …, photoId: pic.id || null });   // 저장
function cancel() { pic.drop(); p.onClose(); }          // 그만두기
```

그림을 찾는 차례는 **셋 다 같습니다** — 넣은 그림 → 폴더 파일 → 코드 그림.

| 함수 | 파일 |
|---|---|
| `App.activityImage` | `activities.js` |
| `App.partnerImage` | `activities.js` |
| `App.moodImage` | `activities.js` |

⚠ 사람은 남녀 그림 고르기(`variants`)가 있지만 **기본 목록에만** 있습니다.
  직접 넣은 그림은 한 장이므로, 있으면 그것을 그대로 씁니다.

**시험한 것** : 사람 「사촌」 · 기분 「설레요」 에 그림을 넣어 —

```
  화면에 넣은 그림이 나옴                    사람 ✓ 기분 ✓
  전체 백업에 담김                           사람 ✓ 기분 ✓
  학생별 백업에도 담김                        사람 ✓ 기분 ✓
  지우면 그림도 함께 지워짐 (사진 0장)         사람 ✓ 기분 ✓
```

### 40-21. 일기 모음 책 인쇄를 **두 가지**로

선생님 : 「일기 모음 책 인쇄하기를 두가지로 하나는 **글자 있는 그대로**
하나는 **따라쓰기**로」

```
  책으로 인쇄 · 내가 쓴 글   → trace 'text'   (읽고 보관하는 책)
  책으로 인쇄 · 따라 쓰기    → trace 'trace'  (덧쓰는 학습지 책)
```

포트폴리오의 「일기장 모두 인쇄」와 **같은 방식**입니다 — 두 곳이 다르면
어디서 무엇을 뽑았는지 헷갈립니다.

⛔ `onClick=${printBook}` 처럼 **함수를 그대로 넘기지 마세요.** 클릭 이벤트가
   첫 인자로 들어가 `mode` 자리에 앉습니다. `function () { printBook('text'); }`.

확인 : 인쇄 종이가 `t-text` 3장 / `t-trace` 3장으로 나뉨.

### 40-22. 두 곳에서 설명 줄 빼기

선생님 : 「설명삭제」 · 「표시부분 삭제 간단히하기!」

| 화면 | 뺀 것 | 까닭 |
|---|---|---|
| 「이 일기를 전시하고 싶어요?」 | 「전시하기로 고른 일기에는 책갈피가 붙고, 교실에 전시해요.」 | 아래 두 칸에 그림과 말이 다 있어 같은 말이 두 번 |
| 「모두 마쳤어요」 | 「○○의 지금 표시예요」 + 표시 알약 셋 | 일기를 다 쓴 자리에서 지도 표시를 알려 줄 까닭이 없음 |

▸ 「모두 마쳤어요」 는 이제 **다음에 할 일 하나**(완성한 그림일기 보기)만
  남습니다. `stNow`(지도 표시를 셈하던 줄)도 함께 걷어냈습니다.

`?v=20260830k`

### 40-23. 힌트 보고 쓰기 — **제목 힌트가 빠져 있었습니다**

선생님 : 「제목 힌트가 없어.」

2단계 「힌트 보고 쓰기」에서 노란 힌트 상자에 **본문만** 있었습니다.
아래 종이에는 제목 칸이 **비어 있는데** 볼 것이 없으니, 학생은 제목에
무엇을 쓸지 알 길이 없었습니다. **보고 쓰기는 볼 것이 다 있어야 합니다.**

▸ 힌트 상자 맨 위에 아래 종이와 **같은 차림**(제목 딱지 + 글)으로 넣었습니다.

**⛔ 제목 줄이 자리를 먹으므로 칸 크기 셈에서 빼야 합니다.**

```js
var HINT_TITLE_H = 48;                                   // 제목 줄이 쓰는 높이
var 남는높이 = HINT_INNER_H - (titleText ? HINT_TITLE_H : 0);
var cell = Math.min(HINT_INNER_W / g.cols, 남는높이 / rows.length);
```

안 빼면 힌트 원고지가 상자를 넘쳐 **아래 줄이 잘립니다** — 그러면 보고
쓰기의 뒷부분이 통째로 안 보입니다.
⚠ css 의 `.pd-hintbig-title` 크기를 고치면 `HINT_TITLE_H` 도 같이 고치세요.

확인 : 짧은 제목 · **긴 제목(21자) + 긴 글(128칸)** 둘 다 넘침 0, 제목 다 보임.

`?v=20260830k`

### 40-24. 「모두 마쳤어요」 창을 **좁게 · 가운데로**

선생님 : 「가운데 정렬하면서 창 폭도 줄이기」

§40-22 에서 표시 줄을 걷어내고 나니, 760px 짜리 넓은 창에 단추 하나만
덩그러니 남았습니다.

```
  전 : 760px · 제목 왼쪽 붙음
  후 : 460px · 제목 가운데      (narrow + center)
```

`narrow` 는 이미 있던 갈래입니다(단추 한둘뿐인 창). `center` 를 새로 넣었습니다.

```css
.modal.center .q > h2{ flex:1 1 auto; text-align:center; }
.modal.center > *{ text-align:center; }
```

⚠ 읽어주기 단추는 제목 **오른쪽 끝**에 그대로 둡니다 — 자리가 늘 같아야
  학생이 눈으로 찾습니다 (규칙 4). 그래서 글자만 가운데로 보냅니다.
⛔ 목록·격자가 든 창에는 `center` 를 주지 마세요 — 줄마다 시작 자리가
  달라져 훑기 어려워집니다.

**⛔⛔ 팝업은 미리보기 창이 가려져 있으면 못 잽니다.**
`position:fixed` 라 화면 크기를 따르는데, 창이 숨겨져 있으면
`innerWidth` 가 **0** 이라 모두 45px 로 나옵니다. 재기 전에 크기를
정해 주세요 (`resize_window` 1280x800 등).

`?v=20260830k`

### 40-25. ⛔⛔ 인쇄 모양은 **단계마다 다릅니다** (제가 못박아 두어 틀렸습니다)

선생님 : 「이게 단계별로 가능한가? 1단계는 내가 쓴 글, 따라쓰기가 맞지만,
**2단계는 따라 쓰기, 힌트 보고 쓰기**」

§40-21 에서 일기 모음 책 인쇄를 `text` · `trace` 로 **못박아** 두었습니다.
2단계 학생에게 맞지 않는 두 가지가 나왔습니다. `modesFor(lv)` 를 쓰도록
고쳤습니다 — 목록은 `MODES_BY_LEVEL` **한 곳**에서만 정합니다.

```
  1단계  글자 · 따라 쓰기
  2단계  따라 쓰기 · 힌트 보고 쓰기
  3단계  내가 쓴 글 · 빈 줄
```

**⛔⛔ 그러고 나서 더 깊은 고장이 드러났습니다.**
`C.PicDiarySheet` 가 넘겨받은 모양을 **그 일기의 단계로 다시 걸러내고**
있었습니다.

```
  학생은 지금 2단계 → 단추는 「힌트 보고 쓰기」
  지난 일기는 level 1 로 저장돼 있음
  → 1단계에 empty 가 없으니 **말없이 text 로 바뀌어** 인쇄됨
     (고른 것과 정반대로, 글이 다 채워진 종이가 나왔습니다)
```

▸ **무엇을 내놓을지는 화면이 정하고**(단계별 단추 — `modesFor`),
  **종이는 시킨 대로 그립니다.** 세 모양은 어느 단계에서나 그릴 수 있습니다
  (칸이든 줄이든 `t-text·t-trace·t-empty` 규칙이 다 있습니다).
⚠ 아는 모양이 아닐 때만 기본값으로 되돌립니다 (안전장치).

```js
// 전 : if (!modesFor(lv).some(m => m.id === mode)) mode = defaultModeFor(lv);
// 후 :
if (['text','trace','empty'].indexOf(mode) < 0) mode = defaultModeFor(lv);
```

확인 (여섯 가지 모두 시킨 대로) :

```
  1단계  글자 → t-text        따라 쓰기      → t-trace
  2단계  따라 쓰기 → t-trace   힌트 보고 쓰기 → t-empty
  3단계  내가 쓴 글 → t-text   빈 줄          → t-empty
```

그림일기 한 장 화면은 그대로입니다 (1단계 일기 → 「글자 · 따라 쓰기」만 보임).

`?v=20260830k`

### 40-26. ★ 하위 활동도 **한 쪽에 셋**

선생님 : 「6개가 나오네? 이것도 3개씩 나오게」

블록놀이(6가지)를 넣으면서 드러났습니다. 하위 활동만 `cols = kids.length > 4
? 3 : 2` 로 **한꺼번에** 늘어놓고 있었습니다. 여섯이면 두 줄이 되어 카드가
눌리고 위 문장 틀까지 밀립니다.

▸ 대표 활동과 **같은 방식**으로 고쳤습니다 — 한 쪽에 셋 + 화살표.
  `plan.js`(PLAN_PER) · `diary.js`(PAGE_SIZE) 두 곳 모두.

⛔ **하위로 들어갈 때 쪽 번호를 0 으로 되돌리세요.**
   대표 활동과 하위 활동이 **같은 `pageS`** 를 씁니다. 3쪽에서 블록놀이를
   누르면 하위가 2쪽뿐이라 **빈 쪽**이 보입니다.
   되돌리는 자리 넷 : 들어갈 때 · 골랐을 때 · 「다른 활동 고르기」 (두 화면).

확인 : 「어떤 블록놀이를 할까요?」 1/2쪽 (나무·레고·자석) → 2/2쪽 (종이·큰·끼우기).

### 40-27. ★ 문장이 **구(句)마다** 끊기게

선생님 : 「나는 내일 낮에 선생님과 친구와 함께 / 교실에서 보드게임을 할
거예요. 이렇게 깔끔하게 **구와 어절이 끊어지게** 표시되게 나오면 좋겠어」

한 덩어리 글로 두면 자리가 모자랄 때 **아무 데서나** 끊깁니다.

```
  전 : 나는 내일 낮에 / 선생님과 친구와 함께 / 교실에서 보드게임을 / 할 거예요.
                                                              ↑ 한 구가 갈라짐
  후 : 나는 내일 낮에 / 선생님과 친구와 함께 / 교실에서 / 보드게임을 할 거예요.
```

▸ `S.planParts(plan)` 가 **같은 문장을 구마다 잘라** 돌려줍니다. 화면은 그
  조각을 `<span class="ph">` 로 하나씩 묶습니다. 묶음이 통째로 넘어가므로
  **구 사이에서만** 끊깁니다.
  구는 원래 `S.plan` 이 쓰던 `bits` 그대로입니다 —
  `나는 / 내일 낮에 / 선생님과 친구와 함께 / 교실에서 / 보드게임을 할 거예요.`

**⛔ 글자에 손대지 마세요.** 줄바꿈 없는 빈칸(NBSP)을 끼워 넣는 방법도
   있지만, 그러면 **읽어주기 · 원고지 · 검색이 모두 그 글자에 걸립니다.**
   `S.planParts(p).join(' ') === S.plan(p)` 임을 확인했습니다.

```css
.sentence .ph{ white-space:nowrap; }
@media screen and (max-width: 560px){ .sentence .ph{ white-space:normal; } }
```

⚠ 아주 좁은 화면에서는 묶음을 풉니다 — 안 풀면 긴 구가 칸을 넘칩니다.
⚠ `inline-block` 을 쓰지 마세요 — 줄 높이와 따라 쓰기 밑줄이 구마다 따로
  놀아 들쭉날쭉해집니다. `nowrap` 만으로 충분합니다.

▸ 학습지 1단계 따라 쓰기도 **같이** 묶었습니다 — 덧쓰는 종이라 줄이
  갈라지는 자리까지 위 계획표와 같아야 학생이 그대로 베낍니다.

`?v=20260830k`

### 40-28. 빠진 낱말 찾아 쓰기도 **구마다** 끊기게

선생님 : 「빠진낱말 찾아쓰기에서도 구어절 끊기하면 좋겠다」

```
  전 : 나는 ____ 혼자 ____에서 ______를 할
       거예요.                    ↑ 한 구가 갈라짐
  후 : 나는 ____ 혼자 ____에서
       ______를 할 거예요.        ↑ 통째로 넘어감
```

▸ 한 묶음 = **빈칸(또는 낱말) + 뒤에 붙는 조사들**(`tight`).
  토막을 그릴 때 `tight` 인 것을 앞엣것에 모아 `<span class="ws-ph">` 하나로
  묶습니다. 위 계획표 문장(`.sentence .ph`)과 **같은 규칙**입니다.

```css
.print-area .ws-ph{ white-space:nowrap; }
```

확인 : 상자를 **430px 까지 좁혀도** 다섯 묶음이 모두 한 줄 조각
(`getClientRects().length === 1`) — 갈라지지 않습니다.

⚠ 재는 법 : `top` 으로 묶어 세면 안 됩니다. 빈칸은 `vertical-align` 때문에
  낱말과 `top` 이 달라서, 갈라지지 않았는데도 다른 줄로 세어집니다.
  **`getClientRects().length`** 를 보세요 — 줄이 갈라지면 2 이상이 됩니다.

`?v=20260830k`

### 40-29. ★ 3단계 「일기 쓰기」 — 같은 글이 **세 번** 있었습니다

선생님 : 「너무 퍼져있어서 쓴 내용이 눈에 들어오지 않아」 →
「**뼈대 칸 없애고 종이에 쓰기 없애자**」

```
  ① 맨 위 빨간 문장 틀    나는 어제 친구 강윤이와 …
  ② 내가 만든 뼈대        나는 어제 친구 강윤이와 …   ← 같은 글
  ③ 쓰기 칸               나는 어제 친구 강윤이와 …   ← 또 같은 글
```

그 사이에 「글쓰기 칸에 넣기」 단추까지 끼어, 정작 **학생이 쓰는 칸**이
화면 아래로 밀렸습니다.

▸ **② 를 없앴습니다.** 대신 뼈대를 **처음부터 쓰기 칸에 넣어 둡니다**
  (`bonesFilledS` 로 지키는 useEffect). 「글쓰기 칸에 넣기」 단추가 하던
  일을 학생이 누르지 않아도 되게 한 것입니다.
⚠ **이 칸에 처음 들어올 때 한 번만** 넣습니다. 학생이 지운 뒤 다시 들어와도
  또 채우지 않습니다 — 지운 것은 지운 대로 두어야 합니다.
⚠ 이미 쓴 글이 있으면 건드리지 않습니다.

**「종이에 쓰기」도 없앴습니다.**
그것은 **여기서 하는 일이 아니었습니다** — 누르면 「저장한 뒤 그림일기
보기 → 빈 줄 로 인쇄하세요」 라는 **안내만** 떴습니다. 화면에서는 아무 일도
일어나지 않는데 자리만 차지했고, 학생은 눌러 놓고 무엇을 해야 할지 몰랐습니다.
▸ 종이에 쓰는 길은 그대로 있습니다 — 그림일기에서 **빈 줄**로 인쇄합니다.
  그 길은 **인쇄하는 자리**에 있는 것이 맞습니다.

⚠ 예전에 `paper` 로 저장해 둔 일기가 **빈 화면**이 되지 않게 이어 줍니다 :
  `draft.writeWay !== 'hand'` 이면 키보드로 봅니다 (활동의 ALIAS 와 같은 결).

**⛔⛔ 또 걸렸습니다 — html 주석 안 백틱 (인수인계 2-3).**
`<!-- 없어진 `paper` 로 … -->` 라고 적었다가 템플릿이 끊겨 화면이 통째로
안 떴습니다. **낫표(「」)만 쓰세요.** 고친 뒤 파일 전체를 훑어 확인했습니다.

확인 : 뼈대 칸 ✓ 없어짐 · 넣기 단추 ✓ 없어짐 · 종이에 쓰기 ✓ 없어짐 ·
       쓰기 칸에 뼈대 ✓ 미리 들어감 · 키보드↔손글씨 ✓ 둘 다 열림.

`?v=20260830k`
